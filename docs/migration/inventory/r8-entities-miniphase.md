# R8 Entities Mini-Phase

> Generated: 2026-05-07
> Rule: every Entities claim is contrasted against `/home/server/woltk-trinity-legacy/src/server/game/Entities/`.

## Pending Review Tasks

## Closed Tasks
- [x] **#NEXT.R8.ENTITIES.546** GameEvent NPCFlag metadata + GetNPCFlag foundation.
  Status: represented-partial foundation only; dependency `#NEXT.R8.ENTITIES.545`; not manual-test-ready. Rust adds exact C++-shape `SELECT guid, eventEntry, npcflag FROM game_event_npcflag`, stores ordered `u64` NPC flag rows by event id under `CanonicalSpawnMetadataLikeCpp`, reports rows/loaded/out-of-range/events touched, and exposes a pure `GetNPCFlag` equivalent helper that ORs matching spawn-id flags over supplied active event ids with missing/invalid events returning `0`/no-op.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:730-761,920-935`; context-only future live `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1069-1082,1093-1112,1115-1147`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event_npc_flag` tests cover row-order preservation, out-of-range skip, high-bit `u64` preservation, OR across multiple active events for one spawn id, ignored other spawn ids/missing events, and zero mask with no active events. Review `APROBADO`; CI `CI_OK`: `cargo fmt --check`, focused world-server bin test, `cargo check -p world-server`, `git diff --check`, and TSV 9-column check all OK.
  Boundaries: no live `UpdateEventNPCFlags`, no `CreatureTemplate::npcflag` base, no `ReplaceAllNpcFlags`/`ReplaceAllNpcFlags2`, no gossip reset, no quest/vendor/worldstate/SAI/seasonal side effects, no MapObjectRecord/session/client fanout mutation, no push/install/restart.
- [x] **#NEXT.R8.ENTITIES.545** GameEvent ChangeEquipOrModel live represented consumption.
  Status: represented-partial bounded live consumption only; review `APROBADO` and CI `CI_OK`; local commit is this slice closeout; not manual-test-ready. Rust extends live GameEvent scheduler side-effect actions so ApplyNewEvent consumes Spawn(event), Unspawn(-event), ChangeEquipOrModel(event,true), and UnApplyEvent consumes Unspawn(event), Spawn(-event), ChangeEquipOrModel(event,false); negative-only Update evidence does not imply ChangeEquipOrModel.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1064-1113,1330-1384`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:478-531`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:147-162`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs`, `crates/wow-map/src/map.rs`, `crates/wow-entities/src/creature.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused tests cover scheduler action order, baseline activate/deactivate previous-value save/restore, C++ display reset semantics for zero model ids, missing row/bucket no-panic/no-dummy behavior with single missing-bucket evidence, and map-owned live spawn-id facade mutating multiple live creatures plus missing/wrong-kind/model-validation-unavailable outcomes.
  Boundaries: no full Creature::LoadEquipment, no visible item/value/session fanout, no real DB2 GetCreatureModelInfo validation, no ObjectMgr, no quests/worldstates/NPC flags/vendors/SAI/seasonal reset, no ForceGameEventUpdate/manual-test-ready runtime, no push/install/restart.
- [x] **#NEXT.R8.ENTITIES.544** GameEvent model/equipment metadata foundation.
  Status: represented-partial metadata/dependency foundation only; not a live `ChangeEquipOrModel` implementation and not manual-test-ready. Rust adds C++-shaped startup metadata for `game_event_model_equip` under `CanonicalSpawnMetadataLikeCpp`, loads minimal positive `(CreatureID, ID)` existence from `creature_equip_template`, skips/counts out-of-range event ids and missing positive equipment templates, accepts `equipment_id == 0`, initializes previous model/equipment ids to 0, preserves per-event row order, and exposes read-only slices for a later live consumer. Review `APROBADO`; CI `CI_OK`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:478-531,1064-1113,1330-1384`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:80-86,147-149,162`; `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:1478-1502,1508-1542`; `/home/server/woltk-trinity-legacy/sql/base/dev/world_database.sql:671-684`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Boundaries: no live `ChangeEquipOrModel`, no `Creature::LoadEquipment`, no `Creature::SetDisplayId`, no ObjectMgr baseline mutation, no values/session fanout, no DB2 creature model validation, no announcements, quests, worldstates, NPC flags, vendors, SAI, seasonal reset, ForceGameEventUpdate, push/install/restart.
- [x] **#NEXT.R8.ENTITIES.543** live GameEventMgr::StartSystem/WUPDATE_EVENTS represented scheduler wiring.
  Status: represented-complete only for bounded world-server composition; not manual-test-ready. Rust changes `world-server` composition to keep one mutable canonical `Arc<Mutex<CanonicalSpawnMetadataLikeCpp>>` for live GameEvent state, represents C++ `GameEventMgr::StartSystem()` by clearing active events, running first `Update()` with `is_system_init=false`, installing a dynamic WUPDATE_EVENTS-like scheduler from the returned delay, and consuming only existing represented GameEventSpawn/Unspawn map helpers in C++ ApplyNewEvent/UnApplyEvent order. The live map-update loop ticks this world-level scheduler, runs subsequent `Update()` with `is_system_init=true`, installs the next dynamic delay, and avoids awaits under metadata locks.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp:2435-2441,2816-2824,2873-2879`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:135-213,961-967,994-1061,1064-1113`.
  Boundaries: represented `world_conditions_met=false` only; no real GameEvent DB saves/deletes, no real ConditionMgr world-event rows/completion, no announcements, quests/vendors/worldstates/NPC flags/model/equip/SAI/seasonal reset, no full ForceGameEventUpdate command caller, no manual-test-ready GameEvent runtime, no push/install/restart.
- [x] **#NEXT.R8.ENTITIES.542** GameEventMgr::Update represented local scheduler seam.
  Status: represented-complete only for bounded local GameEventMgr::Update scheduler/metadata seam; not manual-test-ready. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Rust adds `CanonicalSpawnMetadataLikeCpp::update_game_events_like_cpp` over local `game_events` + `game_event_active_set`, scanning ids `1..mGameEvent.size()`, queueing sorted/deduped activations/deactivations, processing starts before stops, representing WORLD_NEXTPHASE finish save evidence with C++ `continue`, WORLD_CONDITIONS save evidence via caller predicate, negative spawn request evidence after init, min-delay/returned millis evidence, and explicit invalid helper outcomes. Boundaries: no real ApplyNewEvent/UnApplyEvent, no real GameEventSpawn/Unspawn invocation, no DB writes/deletes, no real ForceGameEventUpdate, no MapManager/session/ObjectAccessor/fanout, no quests/vendors/worldstates/SAI/scripts/model/equipment.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:994-1061`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1573-1588`.
- [x] **#NEXT.R8.ENTITIES.541** GameEvent StartEvent/StopEvent represented active-set/state seam.
  Status: represented-complete only for the bounded local metadata-runtime seam of C++ `GameEventMgr::StartEvent` / `StopEvent` over `CanonicalSpawnMetadataLikeCpp::game_events` plus `game_event_active_set`. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Rust mutates only raw GameEvent state/timers and active-set membership, returns explicit start/stop outcomes for missing events and represented side-effect requests, and uses caller-provided `world_conditions_met` for the not-yet-real `CheckOneGameEventConditions` decision.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:135-173,175-213,1573-1588`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:122-125`.
  Rust targets: `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event` tests cover normal/internal start active/apply-only behavior, normal overwrite start/end repair without minutes, world inactive -> conditions with conditions false, serverwide conditions true -> nextphase/next_start and force flag, unknown raw serverwide no-panic, normal stop overwrite active removal/unapply and end repair without minutes, serverwide non-finished reset/delete evidence, world finished no-overwrite no-reset, and missing event no mutation. Boundaries: no GameEventMgr::Update, scheduler, real ApplyNewEvent/UnApplyEvent side effects, GameEventSpawn/Unspawn invocation, quests/vendors/worldstates/SAI, condition rows, DB writes/deletes, ForceGameEventUpdate runtime, MapManager/session/ObjectAccessor/fanout, install/restart/push/manual-test-ready claim.

- [x] **#NEXT.R8.ENTITIES.540** GameEvent active-event set / IsEventActive / IsHolidayActive foundation.
  Status: represented-complete only for the local metadata-runtime foundation of C++ `GameEventMgr::m_ActiveEvents` and read-only helpers. Rust adds `GameEventActiveSetLikeCpp` backed by `BTreeSet<u16>` for `std::set` order/dedupe, local add/remove/clear/membership/id iteration helpers, and a pure `is_holiday_active_like_cpp` over `GameEventDataStoreLikeCpp` that returns inactive for holiday id `0` and reports missing active event ids explicitly instead of panicking. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:102-110,122-123,169`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1763-1782`.
  Rust targets: `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused unit tests cover insert/dedupe/order, idempotent remove, clear, `is_active_event_like_cpp`, holiday 0 inactive, matching holiday active, mismatched holiday inactive, missing active-event outcome, and the local active set stored alongside canonical GameEvent metadata. Boundaries: no `GameEventMgr::Update`, no scheduler, no StartEvent/StopEvent runtime, no ApplyNewEvent/UnApplyEvent, no spawn/unspawn, no MapManager/Map/session/condition mutation, no DB read/write for active state, no DB2 holiday validation, no manual-test-ready claim.

- [x] **#NEXT.R8.ENTITIES.539** GameEvent prerequisite metadata foundation.
  Status: represented-complete metadata-only for C++-shaped `game_event_prerequisite` startup metadata stored on `GameEventDataLikeCpp::prerequisite_events` inside the same `GameEventDataStoreLikeCpp`/local `mGameEvent` representation. Rust adds the exact C++ prerequisite query, loads rows immediately after master `game_event` metadata, validates event range, rejects `GAMEEVENT_NORMAL`/`GAMEEVENT_INTERNAL` non-world events, validates prerequisite range, deduplicates/orders with `BTreeSet<u16>`, reports rows/loaded/duplicate and the three C++ skip reasons, and makes `check_one_game_event_like_cpp` consume stored prerequisites instead of an external slice. Review `APROBADO`; CI `CI_OK`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:44-80`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:331-374`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:33-78`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event` tests cover stored-prerequisite `CheckOneGameEvent` empty/valid/future/normal/missing cases plus loader/applier acceptance of world-event states, normal/internal rejection, out-of-range event/prerequisite skips, duplicate ignore, and sorted set order. Checks: `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event -- --nocapture` OK (47 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK; `git diff --check` OK; TSV 9-column check OK. Boundaries: no `GameEventMgr::Update`, no active-event set, no `StartEvent`/`StopEvent`, no `CheckOneGameEventConditions`, no `SaveWorldEventState`, no characters DB writes, no holiday DB2, no `ApplyNewEvent`/`UnApplyEvent`, no NPC/vendor/model/equipment/worldstate/SmartAI/script hooks, no live scheduling, no push/install/restart, and no manual-test-ready runtime claim.
- [x] **#NEXT.R8.ENTITIES.538** GameEvent `CheckOneGameEvent` / `NextCheck` pure timing helpers.
  Status: represented-complete dependency-only for pure, metadata-read GameEvent timing decisions on `GameEventDataStoreLikeCpp`; Review `APROBADO`; CI `CI_OK`. Rust mirrors C++ state branches for `CheckOneGameEvent(uint16)` and `NextCheck(uint16)`, uses explicit seconds APIs, preserves unknown raw states through the C++ default/normal periodic branch, returns explicit missing-event/missing-prerequisite and zero-occurrence invalid-timing outcomes instead of panicking, and models `max_ge_check_delay` as C++ `DAY`/86400 seconds. Prerequisites are passed explicitly to the helper because DB prerequisite metadata is not loaded yet; this is a documented future scheduler dependency boundary, not runtime activation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:44-80`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:82-119`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:33-78`; context-only `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:994-1062`.
  Rust targets: `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event` unit tests cover normal in/out-of-window and strict start/end bounds, unknown raw state default branch, world conditions/nextphase/finished/internal, inactive prerequisites including empty, nextphase/finished, normal, future `next_start`, and missing prerequisite, NextCheck nextphase/finished, conditions length/zero, outdated end, never-started, in-window, out-of-window, end clamp, missing event, and zero occurrence. Checks: `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event -- --nocapture` OK (46 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with existing warnings; `git diff --check` OK; TSV 9-column check OK. Boundaries: no scheduler/active-event set, no StartEvent/StopEvent, no ApplyNewEvent/UnApplyEvent, no MapManager/DB writes/characters DB/save state, no DB2 holiday, no spawn/unspawn runtime, and no manual-test-ready GameEvent runtime claim.
- [x] **#NEXT.R8.ENTITIES.537** GameEvent master metadata foundation.
  Status: represented-complete metadata-only for C++-shaped `mGameEvent` master rows in `CanonicalSpawnMetadataLikeCpp`: exact `game_event` startup query, `MAX(eventEntry)` master sizing with slot 0 reserved/default, read-only `GameEventDataStoreLikeCpp`, raw scheduling fields, raw holiday/stage preservation, `GameEventState` raw/known representation, and `is_valid_like_cpp()` matching `length > 0 || state > GAMEEVENT_NORMAL`. Reserved `eventEntry == 0` and out-of-range IDs are skipped/reported; normal zero-length rows are retained as evidence and reported without scheduler-valid claims; holiday DB2 validation/`SetHolidayEventTime` is explicitly deferred. Review `APROBADO`; CI `CI_OK`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.h:33-78`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:215-285`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:937-958`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused pure unit tests cover C++ master sizing/indexing, reserved zero skip, field order and `next_start=0`, normal zero-length invalid report, world/internal zero-length validity, holiday value preservation/deferred validation, out-of-range skip, and metadata accessor. Boundaries: no scheduler/active-event set, no `CheckOneGameEvent`, no `StartEvent`/`StopEvent`, no `ApplyNewEvent`/`UnApplyEvent`, no `SaveWorldEventState`, no characters DB writes, no DB2 holiday lookup or time rewrite, no spawn/unspawn execution beyond existing #535/#536 callers, no push/install/restart.
- [x] **#NEXT.R8.ENTITIES.536** GameEventUnspawn top-level non-pool + pool caller composition.
  Status: represented-complete only for the bounded `world-server` top-level composition seam over existing `GameEventUnspawn` non-pool Creature/GameObject GUID and pool-despawn helpers. Source-of-truth: C++ `GameEventMgr::GameEventUnspawn` computes `internal_event_id = mGameEvent.size() + event_id - 1`, returns before GameObjects/pools on missing Creature GUID bucket, returns before pools on missing GameObject GUID bucket, and only then resolves pool ids/templates and calls `DespawnPool(..., true)` for matching loaded maps. Rust preserves that call/early-return order with explicit `pool_skipped_due_to_non_pool_bucket` and `missing_event_pool_ids` summary evidence.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1246-1327`.
  Rust targets: `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event_unspawn` world-server tests cover happy-path top-level composition applying non-pool timer/live remove-list effects plus pool despawn metadata in one call, missing Creature bucket skip of GameObjects and pool with pre-existing timers/pool state untouched, and missing pool bucket preserving non-pool Creature/GameObject effects while reporting `missing_event_pool_ids`. Checks: review `APROBADO`; CI `CI_OK`; `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event_unspawn -- --nocapture` OK (7 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with existing represented-helper warnings; `git diff --check` OK; TSV 9-column check OK. Boundaries: no real ObjectMgr grid mutation, no DB persistence, no full GameEvent scheduler/start/stop runtime, no sessions/ObjectAccessor fanout, no scripts/AI/quests/vendors/world-states/model/equipment, no push/install/restart, and non-pool/pool effects remain existing represented/bounded safe map-local actions.
- [x] **#NEXT.R8.ENTITIES.535** GameEventSpawn non-pool Creature/GameObject GUID caller bridge.
  Status: represented-complete only for the bounded `world-server` caller bridge for C++-shaped GameEvent non-pool Creature/GameObject GUID metadata over maps that are already loaded. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. It resolves `mGameEventCreatureGuids`/`mGameEventGameobjectGuids` for the requested event id, preserves the C++ early-return order before the pool branch (missing/out-of-range Creature bucket skips GameObject and pool; missing/out-of-range GameObject bucket skips pool after Creature work), records represented ObjectMgr grid-add evidence from immutable canonical spawn metadata, removes map-owned respawn timers before grid/load checks, skips unloaded grids without fabrication, delegates Creature/GameObject materialization to existing DB-backed loaded-grid record builders, and inserts through map-owned AddToMap only on successful load; GameObjects preserve `CreateGameObjectFromDB(..., addToMap=false)` plus `isSpawnedByDefault()` before AddToMap.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1163-1224`; Creature loader refs `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1190`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815`; GameObject loader refs `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1203-1212`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs` (`pub(crate)` test/helper seam only), `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event_spawn` world-server tests cover loaded-grid Creature spawn timer removal plus typed Creature insertion with post-remove `respawn_time == 0`, loaded-grid spawned-by-default GameObject insertion, unloaded-grid timer removal without fabricated record, missing/out-of-range creature bucket early return before GameObjects and pool/no mutation, missing/out-of-range GameObject bucket early return before pool after Creature work, and non-spawned-by-default GameObject load attempt skipped before AddToMap. Checks: `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event_spawn` OK (12 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with existing warnings/dead_code for represented helper seams; `git diff --check` OK; TSV 9-column check OK. Boundaries: no real ObjectMgr grid-cell mutation beyond represented metadata evidence, no full GameEventMgr scheduler/start/stop runtime, no non-pool DB event activation beyond metadata, no ObjectAccessor/session fanout, no scripts/AI/quests/vendors/world-states/model/equipment, no full Creature duplicate/dead-instance lifecycle beyond existing loader/AddToMap guards, no DB persistence, no install/restart/push, and no manual-test-ready GameEvent runtime claim.
- [x] **#NEXT.R8.ENTITIES.534** GameEventUnspawn non-pool Creature/GameObject GUID caller bridge.
  Status: represented-complete only for the bounded `world-server` caller bridge for C++-shaped GameEvent non-pool Creature/GameObject GUID metadata over maps that are already loaded. Review `APROBADO`; CI `CI_OK`; final local SHA is reported in the closeout chat. It resolves `mGameEventCreatureGuids`/`mGameEventGameobjectGuids` for the requested event id, skips GUIDs still active in another active event, consumes map-owned respawn timers, and delegates loaded objects to the existing map remove-list enqueue path. ObjectMgr grid-removal mutation is represented as evidence only.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1246-1327`; supporting metadata refs `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:379-475`, `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:937-956`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs` (`pub(crate)` test/helper seam only), `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: focused `game_event` world-server tests cover positive non-pool Creature/GameObject unspawn over loaded maps, active-other-event skip, negative-event no-skip behavior, out-of-range creature-list early return, represented respawn-timer removal counters, and explicit test-only physical drain behavior. The implementation reports missing spawn metadata and missing loaded-map counters, but those remain summary evidence paths rather than separately focused coverage in this slice. Checks: `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event -- --nocapture` OK (23 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK; `git diff --check` OK; TSV 9-column check OK. Boundaries: no real ObjectMgr grid mutation, no DB persistence, no full GameEvent scheduler/start/stop, no sessions/ObjectAccessor fanout, no scripts/AI/quests/vendors/world-states/model/equipment, no GameEventSpawn non-pool runtime, and no physical remove-list drain outside the explicit test seam.
- [x] **#NEXT.R8.ENTITIES.533** GameEvent creature/gameobject GUID metadata foundation.
  Status: represented-complete only for C++-shaped `mGameEventCreatureGuids` and `mGameEventGameobjectGuids` in `CanonicalSpawnMetadataLikeCpp`: shared `MAX(eventEntry)` sizing, signed internal event-id mapping, ordered creature/gameobject GUID vectors, missing spawn metadata and out-of-range skips, and pooled rows counted but still loaded. Review `APROBADO`; CI `CI_OK`; final local SHA is reported in the closeout chat.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:379-475`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:937-956`; future-consumer context only `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1163-1328`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds DB statements for `game_event_creature`/`game_event_gameobject`, load-report counters for rows/loaded/missing/out-of-range/pooled-still-loaded, focused tests for signed mapping, ordering, missing metadata, pooled-load behavior, and valid empty slices. Checks: `cargo fmt --check` OK; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server game_event -- --nocapture` OK (19 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with warnings/dead_code for unconnected represented helpers; `git diff --check` OK; TSV 9-column check OK. Boundaries: no live `GameEventSpawn`/`GameEventUnspawn` non-pool creature/gameobject branches, no `AddCreatureToGrid`/`AddGameobjectToGrid`, no `RemoveRespawnTime`, no loaded-grid creation, no `AddObjectToRemoveList`, no active-event guards, no ObjectAccessor/session fanout, no scripts/AI, no DB runtime writes, no push/install/restart.
- [x] **#NEXT.R8.ENTITIES.532** GameEvent pool metadata caller bridge.
  Status: represented-complete only for the bounded `world-server` caller bridge from C++-shaped `CanonicalSpawnMetadataLikeCpp::game_event_pool_ids_like_cpp(event_id)` into the existing #529/#530 explicit pool-id helpers for `GameEventSpawn`/`GameEventUnspawn` pool branches. Review `APROBADO`; CI `CI_OK`; final local SHA is reported in the closeout chat. Source-of-truth: C++ `GameEventMgr::GameEventSpawn`/`GameEventUnspawn` compute `internal_event_id = mGameEvent.size() + event_id - 1`, guard `mGameEventPoolIds` bounds, then iterate the resolved pool ids in stored order and delegate to `SpawnPool`/`DespawnPool` for maps matching `PoolTemplateData::MapId`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:874-916`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1163-1243`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1246-1327`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs` (test-only metadata builder), `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds spawn/unspawn event-id bridge summaries with explicit `missing_event_pool_ids`; valid event ids delegate only the metadata slice to #529/#530 helpers; missing/out-of-range event ids are no-op and do not mutate maps; valid empty lists are OK/no-op; focused tests cover positive spawn/unspawn, missing id no-op, empty-list no-op, and keep explicit-list helpers intact. Boundaries remain: no full GameEventMgr scheduler/start/stop live, no non-pool Creature/GameObject branches, no ObjectMgr grid metadata mutation, no quests/model/equipment/world-state/vendor/scripts/AI, no exact live RNG/session ObjectAccessor/fanout, no DB persistence/manual-test-ready runtime.
- [x] **#NEXT.R8.ENTITIES.531** GameEvent `game_event_pool` DB metadata resolver.
  Status: represented-complete only for C++-shaped `mGameEventPoolIds` metadata inside `CanonicalSpawnMetadataLikeCpp`; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth: C++ `GameEventMgr::LoadFromDB` loads `pool_template.entry` + signed `game_event_pool.eventEntry`, computes `mGameEvent.size() + event_id - 1`, skips out-of-range and `CheckPool` failures, and preserves row-order `push_back`; C++ `Initialize()` sizes from `MAX(eventEntry)`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:874-916`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:937-956`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1163-1243`; `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1246-1327`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: dedicated DB statements for `SELECT MAX(eventEntry) FROM game_event` and the exact C++ `game_event_pool` join; loader runs after `PoolMgrLikeCpp` validation, stores ordered pool ids by signed event id, exposes `CanonicalSpawnMetadataLikeCpp::game_event_pool_ids_like_cpp(event_id) -> Option<&[u32]>`, and reports rows/loaded/out-of-range/broken-pool skips. Checks: `cargo fmt --check`; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo test -p world-server --bin world-server game_event_pool -- --nocapture` (9 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK; `git diff --check` OK; TSV 9-column check OK. Boundaries: no GameEventMgr start/stop scheduler, no live spawn/despawn invocation, no Creature/GameObject non-pool branches, no ObjectMgr grid metadata mutation, no scripts/AI/quests/vendors/world-states/model-equipment, no exact live RNG, no sessions/ObjectAccessor/fanout, no DB persistence, no install/restart/manual-test-ready runtime.
- [x] **#NEXT.R8.ENTITIES.530** GameEventSpawn pool `SpawnPool` loaded-map consumption seam.
  Status: represented-complete only for the bounded `GameEventSpawn` pool branch over existing canonical maps. Source-of-truth: C++ `GameEventMgr::GameEventSpawn` pool loop calls `sPoolMgr->SpawnPool(map->GetPoolData(), pool_id)` for maps matching `PoolTemplateData::MapId`; C++ `PoolGroup<T>::SpawnObject` mutates spawned-pool state before loaded-grid Creature/GameObject creation attempts. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1163-1244`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-351`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:353-379`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:382-387`.
  Acceptance: `Map::spawn_pool_loaded_grid_records_like_cpp` delegates direct `PoolMgrLikeCpp::spawn_pool_plan_like_cpp(&mut Map.pool_data, ...)` and applies loaded-grid plan records using caller-supplied `SpawnStore`/loader only; `world-server` helper counts seen/missing/invalid/no-loaded-map/planner cases, filters existing maps by template map id, reuses canonical spawn metadata/caches and DB-backed Creature/GameObject builders, and keeps AreaTrigger as `None`. Boundaries: no full GameEventMgr start/stop, no Creature/GameObject non-pool branches, no ObjectMgr grid metadata mutation, no scripts/AI/quests/vendors/world-states/model-equipment, no exact live RNG, no sessions/ObjectAccessor/fanout, no DB persistence, no manual-test-ready runtime.
- [x] **#NEXT.R8.ENTITIES.529** GameEventUnspawn pool `DespawnPool` world-server/map-manager consumption seam.
  Status: represented-complete only for a bounded `world-server` helper that consumes explicit event pool ids and dispatches existing canonical maps by `PoolTemplateDataLikeCpp::MapId` into `Map::despawn_pool_safe_map_actions_like_cpp(..., true)`. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Events/GameEventMgr.cpp:1312-1327`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:813-831`; reviewer additionally verified `PoolMgr.h:34-38` and `MapManager.h:168-179`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/wow-map/src/pool.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: helper reads `CanonicalSpawnMetadataLikeCpp.pool_mgr_like_cpp()`, counts missing templates/no loaded canonical maps/planner errors, filters existing maps by template map id, calls the #528 `Map` facade with `always_delete_respawn_time=true`, and aggregates safe map-local removal/timer summaries. Tests cover map-id filtering, missing-template no-op, and always-delete removal of a non-spawned member timer. Boundaries: no full GameEventMgr, no spawn path, no quest/model/equipment/world-state/vendor/scripts/AI, no ObjectMgr grid metadata mutation, no DB save/delete bridge, no sessions/fanout, no push/install/restart, and no manual-test-ready GameEvent/PoolMgr runtime claim.

- [x] **#NEXT.R8.ENTITIES.528** PoolMgr `DespawnPool` top-level map-owned safe-action facade.
  Status: represented-complete only for the bounded `wow-map` facade `Map::despawn_pool_safe_map_actions_like_cpp(...)` over map-owned `SpawnedPoolDataLikeCpp`; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:183-218`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:220-257`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:259-264`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:421-422`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:813-831`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: public `Map` facade delegates direct `PoolMgrLikeCpp::despawn_pool_plan_like_cpp(&mut Map.pool_data, pool_id, always_delete_respawn_time)` then applies existing safe map-local Creature/GameObject removals and represented respawn-timer deletions. Tests cover top-level live Creature/GameObject removal, non-spawned Creature/GameObject timer deletion with Pool no-op, and parent-to-child Pool recursion. Boundaries: no DB/template/live record fabrication, no AreaTrigger creation, no ObjectAccessor/session fanout, no scripts/AI/dynamic-tree/collision, no broader DB save-delete runtime, no push/install/restart, and no manual-test-ready PoolMgr claim.

- [x] **#NEXT.R8.ENTITIES.527** PoolMgr child-pool recursive `Spawn1Object`/`Despawn1Object` consumption seam.
  Status: represented-complete only for bounded `wow-map` recursive child-pool plan/effect consumption inside existing PoolMgr seams. Source-of-truth: C++ `PoolGroup<Pool>::Spawn1Object` recurses to `SpawnPool(child)`, `PoolGroup<Pool>::ReSpawn1Object` is no-op, `PoolGroup<Pool>::Despawn1Object` recurses to `DespawnPool(child, alwaysDeleteRespawnTime)`, and Pool respawn-time DB removal is no-op. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:183-218`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:260-264`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-351`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:382-387`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:405-407`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:421-422`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:806-829`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `PoolSpawnObjectPlanLikeCpp` carries nested child-pool spawn/despawn plans; `PoolMgrLikeCpp` generates real nested `SpawnPool(child)` plans for `SpawnOne Pool`, keeps `ReSpawnOne Pool` no-op, guards child-pool overflow/cycles, and `Map` recursively consumes nested child plans through the existing safe-side-effect/loaded-grid loader path. Child Creature/GameObject work still requires `SpawnStore` metadata, loaded-grid checks and caller-owned records; missing child-plan mismatches count unsupported rather than silently succeeding. Boundaries: no DB/template fabrication in `wow-map`, no full live PoolMgr/entity runtime, no AreaTrigger live builder, no ObjectAccessor/session fanout, scripts/AI, dynamic tree, DB save/delete broader runtime, push/install/restart, or manual-test-ready claim.

- [x] **#NEXT.R8.ENTITIES.526** PoolMgr `Spawn1Object`/`ReSpawn1Object` loaded-grid caller-loader execution seam.
  Status: represented-complete only for the bounded `wow-map` map-owned execution seam that consumes caller-supplied `LoadedGridRespawnRecordsLikeCpp` records for loaded-grid pooled Creature/GameObject `SpawnOne`/`RespawnOne` actions produced by `Map::ProcessRespawns` -> `PoolMgrLikeCpp::UpdatePool`. Source-of-truth: C++ pooled timers update pool state before timer removal, `PoolGroup<T>::SpawnObject` adds selected spawns before `Spawn1Object`, Creature/GameObject `Spawn1Object` only creates on loaded grids, and `ReSpawn1Object` despawns then calls `Spawn1Object`; child-pool runtime remains unsupported/no-op. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-351`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:353-379`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:389-403`; `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:405-407`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `process_due_respawns_composite_loaded_grid_respawns_like_cpp` passes the existing explicit caller loader into pool `SpawnOne`/`RespawnOne`; loader `Some(records)` pre-adds records best-effort and inserts the primary typed record through map-owned `AddToMap`; loader `None` or returned `None` preserves blocked/load-plan evidence; unloaded grids skip without calling the loader; AddToMap failure counts failure, removes the pooled trigger due timer and does not revert `pool_data`. Boundaries: no DB/template fabrication in `wow-map`, no full child-pool runtime, no AreaTrigger live builder, no scripts/AI/ObjectAccessor/session fanout/dynamic-tree/collision/DB save-delete broader runtime/manual-test-ready claim.

- [x] **#NEXT.R8.ENTITIES.525** Map `SpawnGroupSpawn` AreaTrigger no-respawn-map skip parity.
  Status: represented-complete only for the bounded `wow-map` correction that makes `spawn_group_spawn_loaded_grid_records_like_cpp` skip `SpawnObjectType::AreaTrigger` at the C++ `GetRespawnMapForType(...) == nullptr` guard before timers, `TypeHasData`/live checks, difficulty, grid, load planning, or caller loader invocation. Source-of-truth remains C++ `Map::SpawnGroupSpawn`: `SetSpawnGroupActive(groupId, true)` runs before member iteration, then AreaTrigger metadata continues because `Map::GetRespawnMapForType(SPAWN_TYPE_AREATRIGGER)` returns null. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2315-2401`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:751-763`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:765-777`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `SpawnGroupSpawnOutcomeLikeCpp` now reports `skipped_no_respawn_map` for AreaTrigger members instead of fake unsupported/loader-missing counters; focused test asserts the group is activated, loaded compatible grid still produces no load plan, no loader call, no inserted map object, `unsupported_spawn_types == 0`, and `blocked_loaded_grid_spawn_loads == 0`. Boundaries: no `AreaTrigger::LoadFromDB`, no DB-backed AreaTrigger store/create properties, no `AddToMap` AT live branch, no ObjectAccessor/session fanout, no scripts/AI/dynamic tree, no manual-test-ready runtime.

- [x] **#NEXT.R8.ENTITIES.524** `UpdateSpawnGroupConditions` consumes loaded-grid `SpawnGroupSpawn` caller loader.
  Status: represented-complete only for the bounded live `world-server` condition-update caller path: C++ `Map::UpdateSpawnGroupConditions()` planned `SpawnGroupSpawn(groupId)` now enters the explicit `wow-map` loaded-grid loader seam and may materialize caller-supplied Creature/GameObject `LoadedGridRespawnRecordsLikeCpp` through map-owned `AddToMap`. Review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Creature condition loads use no-timer/`respawn_time=0` after the map-owned timer filter, while ProcessRespawns DoRespawn still requires the due timer.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2315-2401`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow-map` preserves the compatibility wrapper as loader `None`, adds a public condition-update helper that accepts a caller loader, and `world-server` reuses existing DB-backed Creature/GameObject builders from canonical spawn metadata/caches under the current map lock. Boundaries: no AreaTrigger loader, no ObjectAccessor/session fanout, no scripts/AI/dynamic tree, no DB persistence beyond existing respawn timer diff side effects, no full live SpawnGroup runtime, and no manual-test-ready claim.

- [x] **#NEXT.R8.ENTITIES.523** Map `SpawnGroupSpawn` loaded-grid caller-supplied loader seam.
  Status: represented-complete for the bounded `wow-map` map-owned `SpawnGroupSpawn` loaded-grid Creature/GameObject execution seam; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains C++ `Map::SpawnGroupSpawn`: group validation/active-state, respawn timer removal before timer check, live/difficulty/grid filters, then Creature/GameObject DB-backed `LoadFromDB` attempts. Rust now preserves the compatibility planner wrapper while adding an explicit caller loader -> map-owned `AddToMap` direction for typed `LoadedGridRespawnRecordsLikeCpp`; loader `None` remains planned/blocked and AreaTrigger remains unsupported.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2315-2395`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `spawn_group_spawn_loaded_grid_records_like_cpp` keeps load-plan evidence for loaded-grid Creature/GameObject members after C++ filters, passes `force` to the caller loader, best-effort adds pre-add records, counts only primary `AddToMap` success as executed, and reports loader-missing vs primary-add failures separately without fabricating DB/templates, DB saves/deletes, AreaTrigger, ObjectAccessor/global fanout, scripts/AI/session packets, or manual-test-ready runtime.

- [x] **#NEXT.R8.ENTITIES.522** GameObject `SendGameObjectDespawn` shared-vision target in-world gate.
  Status: represented-complete only for the bounded session-side canonical typed Player/Creature shared-vision target `in_world` guard while consuming map-owned GameObject visual-despawn evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains C++ `GameObject::SendGameObjectDespawn()` -> `SendMessageToSet(..., true)` -> `Cell::VisitWorldObjects`/`PlayerMapType`/`CreatureMapType`: out-of-world canonical typed Player/Creature targets must not participate in represented shared-vision fanout even if stale position/phase/shared_vision_guids remain.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-83`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:156-192`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused tests `gameobject_visual_despawn_player_shared_vision_out_of_world_target_no_send_like_cpp` and `gameobject_visual_despawn_creature_shared_vision_out_of_world_target_no_send_like_cpp` plus existing `gameobject_visual_despawn` coverage.

- [x] **#NEXT.R8.ENTITIES.521** GameObject `SendGameObjectDespawn` direct Player in-world receiver gate.
  Status: represented-complete only for the bounded session-side canonical typed Player `in_world` receiver guard while consuming map-owned GameObject visual-despawn evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains C++ `GameObject::SendGameObjectDespawn()` -> `SendMessageToSet(..., true)` -> `Cell::VisitWorldObjects`/`PlayerMapType`: a canonical typed Player no longer in-world must not be visited and must not receive `SMSG_GAMEOBJECT_DESPAWN`, even if session-local `HaveAtClient` still contains the GameObject GUID.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-60`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:156-192`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused test `gameobject_visual_despawn_not_in_world_player_no_send_like_cpp` plus existing `gameobject_visual_despawn` coverage.
  Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` returns `0` immediately after resolving the canonical typed Player when `player.unit().world().object().is_in_world()` is false, before position/phase/range/shared-vision/dynamic-object target derivation, before packet send, before duplicate-delivery insertion, and without mutating `client_visible_guids_like_cpp`. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (20 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries remain explicit: no full `SendMessageToSet`, exact cell traversal, ObjectAccessor/global fanout, DB/PoolMgr/scripts/AI, skipped/team receivers, GameObjectMapType, or manual-test-ready GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.520** GameObject `DestroyForNearbyPlayers` direct Player in-world receiver gate.
  Status: represented-complete only for the bounded session-side direct Player in-world receiver guard while consuming map-owned GameObject `UpdateObjectVisibilityOnDestroy`/DestroyForNearbyPlayers evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains C++ `WorldObject::DestroyForNearbyPlayers()` through `Cell::VisitWorldObjects`/`PlayerListSearcher`: a target Player no longer in-world would not be visited, so this session must not send a represented destroy or erase session-local `client_visible_guids_like_cpp` even if the GUID is still visible locally.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:535-545,564-573`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:277-295`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused test `gameobject_visibility_on_destroy_not_in_world_player_no_send_like_cpp` plus existing `gameobject_visibility_on_destroy` coverage.
  Acceptance: `WorldSession::send_represented_gameobject_visibility_on_destroy_from_last_update_like_cpp` now returns `0` immediately after resolving the canonical typed Player when `player.unit().world().object().is_in_world()` is false, before phase/range evaluation and before any session-local `client_visible_guids_like_cpp` removal. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visibility_on_destroy --lib -- --nocapture` (7 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries remain explicit: no full cell traversal, ObjectAccessor/global fanout, Unit charmer/puppet branch, shared-vision fanout, VisibleNotifier, DB/scripts/AI/PoolMgr, or manual-test-ready runtime.

- [x] **#NEXT.R8.ENTITIES.519** DynamicObject VALUES direct Player in-world receiver gate.
  Status: represented-complete only for the bounded session-side direct Player receiver guard while consuming map-owned stable DynamicObject VALUES snapshots; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains C++ `WorldObjectChangeAccumulator::Visit(PlayerMapType&)` reached through `Cell::VisitWorldObjects`, so a Rust canonical typed Player record that is no longer `in_world` must not act as a receiver even when session-local `client_visible_guids_like_cpp` still contains the updated DynamicObject GUID. Ownership remains one-way: canonical `ManagedMap` snapshot + canonical typed in-world Player/DynamicObject records -> this session outbound VALUES packet/dedupe state only; no session-to-map mutation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3652-3717`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3728`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused test `dynamic_object_values_snapshot_not_in_world_player_no_send_like_cpp` plus existing `dynamic_object_values_snapshot` coverage.
  Acceptance: `WorldSession::send_represented_dynamic_object_values_updates_from_last_map_send_object_updates_like_cpp` now returns `0` before phase/range/source branch evaluation if the session Player's canonical `MapObjectRecord::Player` is not in-world; this preserves `client_visible_guids_like_cpp` and sends no packet. Boundaries remain explicit: no exact cell traversal, ObjectAccessor/global fanout, full `UpdateDataMapType`, full visibility/farsight, DB/scripts/AI, or manual-test-ready runtime.

- [x] **#NEXT.R8.ENTITIES.518** DynamicObject VALUES shared-vision Player/Creature receiver fanout.
  Status: represented-complete only for bounded session-side PlayerMapType/CreatureMapType shared-vision receiver eligibility while consuming map-owned stable DynamicObject VALUES snapshots. Canonical typed Player/Creature sources in the same managed map may fan out to this session when their Unit `shared_vision_guids` contains the session Player GUID, the source is in-world, same phase-visible to the updated DynamicObject, and within 3D `map.visibility_range()` of the updated DynamicObject. Direct Player and DynamicObjectMapType seer behavior from #517 remains unchanged; final `client_visible_guids_like_cpp`/HaveAtClient and dedupe gates remain unchanged; no session-to-map mutation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3652-3717`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3728`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused tests `dynamic_object_values_snapshot_player_shared_vision_no_seer_gate_sends_like_cpp`, `dynamic_object_values_snapshot_creature_shared_vision_sends_like_cpp`, `dynamic_object_values_snapshot_shared_vision_requires_source_lists_viewer_like_cpp`, `dynamic_object_values_snapshot_shared_vision_phase_mismatch_no_send_like_cpp`.
  Acceptance: snapshot delivery now models the C++ `WorldObjectChangeAccumulator::Visit(PlayerMapType&)` and `Visit(CreatureMapType&)` shared-vision `BuildPacket` receiver branches without requiring `represented_seer_guid_like_cpp == source`; delivery still requires the updated DynamicObject GUID to be already visible at the session and remains one packet per dedupe fingerprint. Boundaries remain explicit: no exact cell traversal, ObjectAccessor/global fanout, full `UpdateDataMapType`, shared-vision create/destroy/far visibility completion beyond this VALUES snapshot consumption, or manual-test-ready farsight/GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.517** DynamicObject VALUES session fanout direct/seer range gate.
  Status: represented-complete only for bounded session-side receiver-source/range correction while consuming map-owned stable DynamicObject VALUES snapshots; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Direct canonical Player branch requires same managed map, phase visibility and 3D map visibility range to the updated DynamicObject; DynamicObjectMapType represented seer branch requires canonical in-world DynamicObject seer in the same map, caster/bound caster resolving to this session Player GUID and Player high-guid, same phase and 3D map visibility range. `client_visible_guids_like_cpp` remains final `HaveAtClient` gate; no session-to-map writes.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3652-3717`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3728`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`.
  Rust refs: `/home/server/rustycore/crates/wow-world/src/session.rs`; focused tests `dynamic_object_values_snapshot_direct_player_out_of_range_keeps_visible_no_send_like_cpp`, `dynamic_object_values_snapshot_dynamic_object_seer_near_same_phase_sends_like_cpp`, `dynamic_object_values_snapshot_dynamic_object_seer_wrong_caster_no_send_like_cpp`.
  Acceptance: snapshot delivery remains deduped by map/generation/guid/fingerprint and keeps packet map id behavior, but each DynamicObject VALUES update is collected only if the represented session is eligible under the modeled direct Player or DynamicObjectMapType C++ visitor branch, then sent only if `HaveAtClient` is true. Boundaries remain explicit: no Player/Creature shared-vision VALUES fanout, exact cell traversal, ObjectAccessor/global fanout, full `UpdateDataMapType`, or manual-test-ready farsight/GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.516** GameObject `DestroyForNearbyPlayers` reqAlive=false dead-player inclusion regression.
  Status: represented-complete guard/test only for the bounded session-side direct target DestroyForNearbyPlayers dead-player inclusion while consuming map-owned GameObject `UpdateObjectVisibilityOnDestroy` GUID evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. This adds no new runtime/fanout and does not claim manual-test-ready. Source-of-truth remains one-way: canonical `ManagedMap` last GameObject update summary + canonical typed Player/GameObject records + session-local `HaveAtClient` -> this session outbound `UpdateObject` destroy packet and `client_visible_guids_like_cpp` removal; session/player alive state must not filter this path because C++ uses `AnyPlayerInObjectRangeCheck(..., false)`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:1519-1533`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:535-545,564-573`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:277-295`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `gameobject_visibility_on_destroy_includes_dead_player_like_cpp` sets both represented session dead state and canonical Player health to zero, then verifies `WorldSession::process_pending()` still consumes map-owned visibility-on-destroy evidence, emits exactly one `UpdateObject`, and removes the GameObject GUID from session-local `client_visible_guids_like_cpp` when phase/range/HaveAtClient pass. Boundaries remain explicit: no full `DestroyForNearbyPlayers`, exact cell traversal, ObjectAccessor/global/session fanout, Unit charmer/puppet branch, shared-vision fanout, scripts/AI/DB/PoolMgr, or manual-test-ready runtime.

- [x] **#NEXT.R8.ENTITIES.515** GameObject `DestroyForNearbyPlayers` represented phase gate.
  Status: represented-complete only for the bounded session-side direct target phase filter while consuming map-owned GameObject `UpdateObjectVisibilityOnDestroy` GUID evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains one-way: canonical `ManagedMap` last GameObject update summary + canonical typed Player/GameObject records + explicit represented GameObject phase fallback -> this session outbound `UpdateObject` destroy packet and `client_visible_guids_like_cpp` removal only after all C++ gates pass.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:535-545,564-573`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:277-295`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:1519-1533`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::send_represented_gameobject_visibility_on_destroy_from_last_update_like_cpp` now gates the canonical typed Player by C++ `PlayerListSearcher` same-phase semantics before the existing `WorldObject::is_within_dist(..., true, true, true)` check and before session-local `HaveAtClient` removal. The source GameObject phase uses the same explicit represented seam as existing visual-despawn consumption, `represented_gameobject_phase_shifts.get(guid).unwrap_or_else(|| gameobject.world().phase_shift())`; Player phase comes from canonical typed Player `unit().world().phase_shift()` in the same managed map/instance. Boundaries: no shared-vision fanout for `DestroyForNearbyPlayers`, no `GameObjectMapType`, no charmer/unit puppet because the represented path is GameObject not Unit, no exact cell traversal, no ObjectAccessor/global/session fanout, no VisibleNotifier/manual-test-ready runtime, no DB/scripts/AI/PoolMgr.

- [x] **#NEXT.R8.ENTITIES.514** GameObject `DestroyForNearbyPlayers` represented combat-reach range gate.
  Status: represented-complete only for the bounded session-side combat-reach correction while consuming map-owned GameObject `UpdateObjectVisibilityOnDestroy` GUID evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. No full `DestroyForNearbyPlayers`, exact cell traversal, ObjectAccessor/global fanout, phase filtering, shared-vision/DynamicObject/GameObjectMapType fanout, VisibleNotifier, Unit charmer handling, scripts/AI/DB/PoolMgr, or manual-test-ready GameObject runtime claim.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:1519-1533`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1062-1082,1148-1150`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:555`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::send_represented_gameobject_visibility_on_destroy_from_last_update_like_cpp` resolves the canonical typed Player world object and canonical typed in-world GameObject, then gates destroy consumption with `WorldObject::is_within_dist(..., true, true, true)` so C++ own/target combat reach radii extend the 3D visibility range; existing no-send guards and one-way sync direction are preserved.

- [x] **#NEXT.R8.ENTITIES.513** GameObject `SendGameObjectDespawn` represented DynamicObjectMapType shared-vision fanout consumption.
  Status: represented-complete only for the bounded session-side DynamicObjectMapType branch while consuming map-owned GameObject visual-despawn summary evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains one-way: canonical `ManagedMap` last GameObject update summary + canonical typed DynamicObject/GameObject records + session-local represented `m_seer`/`HaveAtClient` -> this session outbound `GameObjectDespawn` packet only; no session-to-map, DynamicObject, seer, caster, or `client_visible_guids_like_cpp` mutation. C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1738-1772`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:150-192`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:86-119`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`. Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` now admits represented DynamicObject delivery only when this session's represented seer equals a canonical DynamicObject in the same managed map, the DynamicObject is in-world, its `bound_caster().unwrap_or(caster_guid())` is this session Player GUID and a Player GUID, its phase can see the represented/canonical GameObject phase, the GameObject is in 2D visibility range of the DynamicObject, and session-local `HaveAtClient` contains the GameObject GUID. Existing direct-target, PlayerMapType shared-vision, CreatureMapType shared-vision, duplicate guard, and no-remove visual-despawn semantics remain intact. Boundaries: no skipped/team receivers, GameObjectMapType, exact cell traversal, ObjectAccessor/global fanout, full `SendMessageToSet`, external packet fanout, DB/PoolMgr/scripts/AI, or manual-test-ready GameObject runtime. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (19 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.512** GameObject `SendGameObjectDespawn` represented CreatureMapType shared-vision fanout consumption.
  Status: represented-complete only for the bounded session-side CreatureMapType shared-vision fanout branch while consuming map-owned GameObject visual-despawn summary evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains one-way: canonical `ManagedMap` last GameObject update summary + canonical typed Player/GameObject/Creature records + session-local represented `m_seer`/`HaveAtClient` -> session outbound `GameObjectDespawn` packet only; no session-to-map mutation. C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:156-192`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-84`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`. Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` now admits represented shared-vision delivery when a canonical typed target Creature in the same map/phase/2D range lists the session Player in `shared_vision_guids`, the session represented seer equals that target Creature, and the session-local `HaveAtClient` set contains the GameObject GUID. Existing direct-target Player and bounded PlayerMapType shared-vision branches remain intact; duplicate-send guards still suppress repeated delivery for the same map-update generation. Boundaries: no generic WorldObject fallback, no GameObject/Transport/AreaTrigger/DynamicObject seer target in this branch, no exact C++ cell traversal, no skipped/team receivers, no full `SendMessageToSet`, no ObjectAccessor/global fanout, no multi-session broadcast, no DB/PoolMgr/scripts/AI, and no manual-test-ready GameObject runtime. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (15 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.511** GameObject `SendGameObjectDespawn` represented PlayerMapType shared-vision fanout consumption.
  Status: represented-complete only for the bounded session-side PlayerMapType shared-vision fanout branch while consuming map-owned GameObject visual-despawn summary evidence; review `APROBADO`; CI `CI_OK`; local commit is this slice closeout. Source-of-truth remains one-way: canonical `ManagedMap` last GameObject update summary + canonical typed Player/GameObject state + session-local represented `m_seer`/`HaveAtClient` -> session outbound `GameObjectDespawn` packet only; no session-to-map mutation. C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1738-1772`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:150-192`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-60`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`. Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` no longer returns early solely on a failing direct-target seer gate; it also admits represented shared-vision delivery when a canonical typed target Player in the same map/phase/range lists the session Player in `shared_vision_guids`, and the session represented seer equals that target Player. Session-local `HaveAtClient` still requires `client_visible_guids_like_cpp` to contain the GameObject GUID; duplicate-send guards remain. Boundaries: no CreatureMapType shared-vision fanout, exact C++ cell traversal, skipped/team receivers, full `SendMessageToSet`, ObjectAccessor/global fanout, packet broadcast to multiple sessions, DB/PoolMgr/scripts/AI, or manual-test-ready GameObject runtime. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (11 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.510** GameObject `SendGameObjectDespawn` represented session `m_seer` direct-target gate.
  Status: represented-complete only for the #507-#509 session-side visual-despawn direct target correction: C++ `MessageDistDeliverer::Visit(PlayerMapType)` sends directly to a target Player only when `target->m_seer == target || target->GetVehicle()` after phase/range/shared-vision handling and before `SendPacket`'s `HaveAtClient` guard; shared-vision list/fanout remains open and is not implemented here. C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-60`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:182-189`. Rust targets: `crates/wow-world/src/session.rs` plus this inventory/handoff note. Ownership remains one-way: canonical `ManagedMap` summary plus typed canonical Player/GameObject state plus session-local represented `m_seer`/vehicle evidence -> session outbound packet only; no session-to-map mutation, no `client_visible_guids_like_cpp` removal, and no duplicate-delivery guard consumption when direct-target `m_seer` blocks send. Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` treats represented seer `None`, empty, or player GUID as `m_seer == target`; a distinct non-empty seer blocks direct send unless the existing represented player vehicle kit is active (`VehicleStatus::Installed`). Focused wow-world coverage includes self/default seer positive, mismatched DynamicObject seer no-send/preserve-visible/no-guard-consume then restored self send, and mismatched seer plus represented vehicle send. Review `APROBADO`; CI `CI_OK`; checks: `cargo +stable fmt --check`; `cargo +stable test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (6 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo +stable check -p world-server`; `git diff --check`; TSV 9-column check. No push/install/restart. Boundaries remain no shared-vision list/fanout, exact cell traversal, full `SendMessageToSet`, ObjectAccessor/global fanout, DB/PoolMgr/scripts/AI, or manual-test-ready GameObject runtime. Progress remains conservative at ~94.3%.

- [x] **#NEXT.R8.ENTITIES.509** GameObject `SendGameObjectDespawn` represented session phase gate.
-  Status: represented-complete only for the #507/#508 session-side visual-despawn target phase correction: C++ `MessageDistDeliverer` skips each target Player unless `target->InSamePhase(src->GetPhaseShift())` before range and `HaveAtClient`; review `APROBADO`; CI `CI_OK`; no full `SendMessageToSet`, shared-vision/m_seer fanout, exact cell traversal, ObjectAccessor/global fanout, DB/PoolMgr/scripts/AI, phase ownership beyond represented/canonical phase values consumed here, or manual-test-ready GameObject runtime claim.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:156-192`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-60`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:505-515`.
  Rust targets: `crates/wow-world/src/session.rs` plus this inventory/handoff note. Ownership remains one-way: canonical `ManagedMap` summary plus typed canonical Player/GameObject state and explicit represented GameObject phase evidence -> session outbound packet only; no session-to-map mutation. Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` now gates despawn candidates by canonical typed Player phase against either explicit represented GameObject phase fallback or canonical `GameObject::world().phase_shift()` before the existing 2D range, `client_visible_guids_like_cpp`, and duplicate-delivery guards. Focused wow-world coverage includes incompatible represented phase no-send that preserves `client_visible_guids_like_cpp` and compatible represented phase delivery. Checks: `cargo +stable fmt --check`; `cargo +stable test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (4 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo +stable check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.508** GameObject `SendGameObjectDespawn` represented session 2D range parity.
-  Status: represented-complete only for the #507 session-side visual-despawn range correction from 3D to C++ `MessageDistDeliverer` default 2D distance; review `APROBADO`; CI `CI_OK`; locally committed at current #508 HEAD; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1746-1756`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:156-192`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiersImpl.h:37-60`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::send_represented_gameobject_visual_despawn_from_last_update_like_cpp` keeps the existing same-map typed Player/GameObject, in-world, `client_visible_guids_like_cpp` and duplicate-delivery guards, but gates visual despawn with `Position::is_within_dist_2d` to represent `SendMessageToSetInRange`/`MessageDistDeliverer(required3dDist=false)`. Vertical Z-only separation no longer suppresses represented `GameObjectDespawn`; XY out-of-range remains a no-send path. Checks: `cargo fmt --check`; `cargo test -p wow-world gameobject_visual_despawn --lib -- --nocapture` (3 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries remain no full `SendMessageToSet`, phase filtering, shared-vision/m_seer fanout, exact cell traversal, ObjectAccessor/global fanout, DB/PoolMgr/scripts/AI, or manual-test-ready GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.507** GameObject `SendGameObjectDespawn` represented session consumption.
-  Status: represented-complete only for session-side consumption of map-owned GameObject visual-despawn GUID evidence; review `APROBADO`; CI `CI_OK`; no full `SendMessageToSet`, exact cell traversal, ObjectAccessor/global fanout, scripts/AI/DB/PoolMgr, respawn/despawn runtime, or manual-test-ready GameObject runtime claim.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1639-1648`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `GameObjectsUpdateSummaryLikeCpp` preserves the existing visual-despawn count and carries explicit non-truncating `generic_visual_despawn_guids`; logged-in `WorldSession::process_pending()` consumes only the canonical Player map/instance summary and sends represented `GameObjectDespawn` only when the typed canonical Player/typed in-world GameObject are in the same map instance, the Player is within `Map::visibility_range()`, and the GUID is already in `client_visible_guids_like_cpp`. Unlike `DestroyForNearbyPlayers`, this slice preserves session-local visibility state after visual despawn and dedupes repeated consumption for the same map update generation.

- [x] **#NEXT.R8.ENTITIES.506** GameObject `DestroyForNearbyPlayers` represented session range/in-world gate.
-  Status: represented-complete only for the session-side range and in-world gate while consuming map-owned GameObject `UpdateObjectVisibilityOnDestroy` GUID evidence; review `APROBADO`; CI `CI_OK`; no full `DestroyForNearbyPlayers`, exact cell traversal, ObjectAccessor/global fanout, UNIT charmer handling, scripts/AI/DB/PoolMgr, or manual-test-ready GameObject runtime claim.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.h:1519-1533`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1679`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::process_pending()` still consumes the canonical Player map/instance summary, but now reads the typed canonical Player position and exact typed canonical GameObject under the map lock, requires the GameObject to remain in-world, and requires the Player target to be within `Map::visibility_range()` of the GameObject before erasing `client_visible_guids_like_cpp` and sending represented `UpdateObject::destroy_objects`; missing map/player/object, non-GameObject GUIDs, not-in-world objects, and out-of-range Players do not send and do not erase session-local visibility state.

- [x] **#NEXT.R8.ENTITIES.505** represented GameObject `UpdateObjectVisibilityOnDestroy` session destroy consumption.
  Status: represented-complete only for session consumption of map-owned represented GameObject `UpdateObjectVisibilityOnDestroy` GUIDs; review `APROBADO` after one bounded correction to remove fixed-size GUID truncation; CI `CI_OK`; validation OK; no push/install/restart. `GameObjectsUpdateSummaryLikeCpp` preserves the existing count and carries an explicit non-truncating GUID list for represented `UpdateObjectVisibilityOnDestroy` outcomes. Logged-in `WorldSession::process_pending()` reads only the canonical Player map/instance summary, drops the map-manager lock before packet send, sends represented `UpdateObject::destroy_objects` only for GUIDs currently in `client_visible_guids_like_cpp`, and erases that session-local visibility state. Checks: `cargo fmt --check`; `cargo test -p wow-map gameobject_visibility_on_destroy --lib -- --nocapture` (1 passed); `cargo test -p wow-world gameobject_visibility_on_destroy --lib -- --nocapture` (1 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1679`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:701-704`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3617-3641`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: sync direction is canonical map summary -> session-local packet/state only; no session-to-map mutation, no fallback to session-requested map without canonical Player, no sends for non-visible GUIDs, and repeated consumption after removal does not duplicate sends. Boundaries: no full `DestroyForNearbyPlayers`, exact cell traversal, full `VisibleNotifier`, ObjectAccessor, session/global fanout, GameObject respawn/despawn, DB/PoolMgr, scripts/AI, packets beyond this represented destroy packet, or manual-test-ready full GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.504** DynamicObject VALUES stable snapshot session fanout consumption.
  Status: represented-complete; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at current #504 HEAD; no push/install/restart. `WorldSession::process_pending()` now consumes the map-owned stable `ManagedMap::last_send_object_updates_summary_like_cpp().dynamic_object_values_updates` snapshot captured before `Map::send_object_updates_like_cpp` clears masks, sends represented DynamicObject VALUES `SMSG_UPDATE_OBJECT` packets only for GUIDs already in `client_visible_guids_like_cpp`, uses the canonical Player map/instance and canonical map id for packet construction, and suppresses only duplicate delivery within the same map-update generation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:790-806`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3728`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:265-278`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: sync direction is map-owned stable summary -> visible session outbound represented VALUES packet only; no live mask reads after map clear, no session-to-map mutation, no same-map cross-instance fallback, no stale legacy session-map packet id, and no duplicate resend for the same update generation. Focused tests cover visible snapshot single delivery after map clear, invisible/no-op, wrong-instance no-op, same-bytes resend on later generation, canonical map-id packet construction, and missing canonical map no-op. Checks: `cargo fmt --check`; `cargo test -p wow-world dynamic_object_values_snapshot --lib` (6 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no full `UpdateDataMapType`, exact VisibleNotifier, ObjectAccessor/global fanout, generalized `Map::SendObjectUpdates`, DynamicObject lifecycle completion, aura lifecycle, DB/scripts/AI, or manual-test-ready farsight.

- [x] **#NEXT.R8.ENTITIES.503** Creature template lifecycle test fixture `creature_type` unblock.
  Status: test-fixture maintenance; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #503 HEAD; no push/install/restart. This does not claim new runtime parity or migration percentage: it only updates `CreatureTemplateLifecycleRecordLikeCpp` unit-test fixtures after the already-landed loader field `creature_type` so broad workspace tests are no longer blocked by stale initializers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:349-482`; `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:575-620+`.
  Rust targets: `crates/wow-data/src/creature_template.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: fixture `creature_template_lifecycle_store_preserves_cpp_field_mapping_and_vehicle_id` now uses `creature_type: 7` and asserts the preserved field; the other lifecycle fixtures use `creature_type: 0` where irrelevant. Runtime loader/schema behavior is unchanged.

- [x] **#NEXT.R8.ENTITIES.502** DynamicObject VALUES stable snapshot before represented SendObjectUpdates clear.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #502 HEAD`; no push/install/restart. `Map::send_object_updates_like_cpp` now captures `DynamicObject::values_update()` from canonical `Map::map_objects` while the typed mask is still live, stores it as represented summary evidence with the DynamicObject GUID only when data exists, then clears `DynamicObjectData` changes and `Object::m_objectUpdated` in the existing represented `BuildUpdate` clear seam. No session fanout is claimed.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:760-794`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3729`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3731-3740`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.cpp:4475-4512`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.cpp:4514-4522`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.

- [x] **#NEXT.R8.ENTITIES.501** DynamicObject update-field changes enqueue represented object update.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; committed locally at current #501 HEAD. `DynamicObject::mark_dynamic_object_data` now mirrors C++ `SetUpdateFieldValue` enqueue semantics by marking the embedded `EntityObject` with the represented object-update flag only through value-changing setters and only after the object is in-world; `Map::send_object_updates_like_cpp` clears the DynamicObjectData change mask as part of the represented object-update clear.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:271-276`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:292-310`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:100-108`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1939`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:777-779`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3728`.
  Rust targets: `crates/wow-entities/src/object.rs`, `crates/wow-entities/src/dynamic_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: source-of-truth remains the embedded `EntityObject` in canonical `DynamicObject` records and `Map::map_objects`; sync direction is DynamicObjectData setter -> same object's data mask/object update flag -> represented `Map::send_object_updates_like_cpp` consumption/clear. Unit tests cover post-`add_to_world` enqueue, pre-world no-op, and same-value no requeue; map test covers queued_before=1, processed=1, cleared_update_masks=1, final `is_object_updated()==false`, and cleared DynamicObjectData change mask for a canonical DynamicObject updated by `set_radius`. Checks: `cargo fmt --check`; `cargo test -p wow-entities dynamic_object --lib -- --nocapture` (9 passed); `cargo test -p wow-map send_object_updates --lib -- --nocapture` (5 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no `UpdateDataMapType`, visible players, session/ObjectAccessor fanout, exact `_updateObjects` pointer set, packet sends, `WorldObjectChangeAccumulator`, or generalized setter coverage for other entity types.

- [x] **#NEXT.R8.ENTITIES.500** represented Player::SetViewpoint apply=true immediate target visibility consumption.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; committed locally at current #500 HEAD; no push/install/restart. `WorldSession::consume_add_farsight_set_seer_outcome_like_cpp` now consumes the C++ `Player::SetViewpoint(target, true)` direct `UpdateVisibilityOf(target)` ordering for DynamicObject targets after the represented `FarsightObject` VALUES update and before represented `m_seer` assignment. The helper reads only the canonical map-owned typed `MapObjectRecord::DynamicObject` from the same `MapManager` instance containing the typed Player, serializes the existing represented DynamicObject create block, sends it to this session, and inserts the GUID into `client_visible_guids_like_cpp` only when not already visible.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25365`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:23193-23230`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:34-41`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:254-263`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-only target visibility consumption for successful AddFarsight/SetViewpoint DynamicObject outcomes. Source-of-truth is canonical typed DynamicObject -> session represented packet/client-visible GUID; missing canonical Player/map/DynamicObject, wrong instance/map, lock failure, or already-visible GUID are no-op and do not fabricate packets. Focused tests cover far target create+client-visible insertion, already-visible duplicate suppression, and wrong-instance no-fabrication. Checks: `cargo fmt --check`; `cargo test -p wow-world set_viewpoint_target_visibility --lib -- --nocapture` (3 passed); `cargo test -p wow-world add_farsight --lib -- --nocapture` (6 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no canonical map mutation, no generic fallback/dummy DynamicObject, no spell-destination fabrication, no full `CanSeeOrDetect`, no ObjectAccessor/VisibleNotifier/session fanout, no Unit shared-vision, no full `SetSeer`, no DB/scripts/AI/runtime install claim.

- [x] **#NEXT.R8.ENTITIES.499** live session-tick farsight clear consumption after canonical DynamicObject removal.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::process_pending()` now consumes the existing `sync_represented_farsight_clear_from_canonical_like_cpp()` helper once during logged-in session ticks after represented GameObject/spell ticks, so an empty canonical map-owned typed Player `ActivePlayerData::FarsightObject` can mirror into session-local represented `m_seer = self` and a represented VALUES-empty update without requiring movement or `update_visibility()`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:63-80`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:218-225`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h:2438`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-only live session-tick consumption seam; source-of-truth remains canonical map-owned typed Player `FarsightObject`; sync direction is canonical empty -> represented `m_seer = self` plus represented VALUES-empty only. Focused tests cover logged-in `process_pending()` canonical clear without `update_visibility()`, non-logged-in no-op, and non-empty canonical `FarsightObject` no-op. Checks: `cargo fmt --check`; `cargo test -p wow-world far_sight_process_pending --lib -- --nocapture` (3 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no canonical map mutation, no ObjectAccessor/global lookup, no full `SetSeer`, no VisibleNotifier/session fanout, no DynamicObject lifecycle/aura completion, no DB/scripts/AI, and no manual-test-ready farsight claim.

- [x] **#NEXT.R8.ENTITIES.498** represented farsight clear/session reset after canonical DynamicObject viewpoint removal.
  Status: reviewed-validated, committed locally at current #498 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::sync_represented_farsight_clear_from_canonical_like_cpp` now consumes only the canonical-clear side of map-owned DynamicObject viewpoint removal by mirroring an empty canonical Player `ActivePlayerData::FarsightObject` into session-local represented `m_seer = self` and a represented empty `ActivePlayerData::FarsightObject` VALUES update at the start of `update_visibility()`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:63-80`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:218-225`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25381-25382`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h:2438`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.cpp:3158-3266`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-only caller consumption seam for canonical clear: if the session has a non-self represented seer and the typed canonical map-owned Player resolves with empty `FarsightObject`, reset represented seer to the player GUID, send represented VALUES-empty update, and invalidate visibility throttle before resolving the next visibility source. Missing session player/canonical map/canonical Player, self/empty represented seer, and non-empty canonical `FarsightObject` are no-op fallbacks. Focused tests cover canonical clear, non-empty canonical keep, and missing canonical Player no-op. Review `APROBADO`; CI `CI_OK`; checks: `cargo fmt --check`; `cargo test -p wow-world far_sight_update_visibility --lib -- --nocapture` (5 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no canonical map mutation, ObjectAccessor, DynamicObject lifecycle, full `SetSeer`, VisibleNotifier/fanout, DB, install/restart, or manual-test-ready farsight.

- [x] **#NEXT.R8.ENTITIES.497** live AddFarsight `ActivePlayerData::FarsightObject` VALUES update seam.
  Status: reviewed-validated, committed locally at current #497 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::consume_add_farsight_set_seer_outcome_like_cpp` now consumes the already-successful map-owned `Player::SetViewpoint(..., true)` outcome by sending a represented `SMSG_UPDATE_OBJECT` VALUES update for `ActivePlayerData::FarsightObject` to the casting player.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:209-225`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.cpp:3158-3266`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-only packet consumption for the live AddFarsight success path. The sender is gated by the same successful outcome evidence used for session `m_seer` (`Created`, dynamic GUID present, caster viewpoint applied to that GUID, `Player::SetViewpoint` applied to that target, `set_seer_requested`, status `Applied`, and a valid session player GUID). It serializes `UpdateObject::full_active_player_values_update` with `ActivePlayerDataValuesUpdate.active_player_data_mask` bits `0` and `26` set and `farsight_object` equal to the created DynamicObject GUID. Focused tests/checks: `cargo fmt --check`; `cargo test -p wow-world add_farsight_live_spell_sets_session_seer_to_created_dynamic_object_like_cpp`; `cargo test -p wow-world add_farsight_live_spell_without_destination_keeps_session_seer_like_cpp`; `cargo test -p wow-world add_farsight_live_spell_already_has_viewpoint_keeps_session_seer_like_cpp`; `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no full `SetSeer`, real `UpdateVisibilityOf`/`VisibleNotifier` packet fanout, ObjectAccessor/session fanout to other players, aura runtime, arbitrary far-spell callbacks, DynamicObject lifecycle completion, or manual-test-ready farsight.

- [x] **#NEXT.R8.ENTITIES.496** canonical map instance ownership for represented visibility scans.
  Status: reviewed-validated, committed locally at current #496 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::visible_gameobjects_from_canonical_map_like_cpp` and `WorldSession::visible_dynamic_objects_from_canonical_map_like_cpp` now select the canonical `wow_map::MapManager` map instance by resolving the typed canonical Player's current `MapKey` when present, instead of always scanning `(map_id, instance 0)`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25364`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25389-25395`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:29-60`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:113-119`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-only canonical map-instance selection for represented GameObject/DynamicObject visibility scans. `current_canonical_player_map_key_like_cpp()` searches the whole canonical `MapManager` for the typed Player GUID. If the typed canonical Player key exists and matches the requested map id, the helpers scan exactly that map instance; if the key is for another map, the helpers do not silently fall back; instance `0` fallback remains only for helper/test contexts without a typed canonical Player key anywhere. The DynamicObject scanner consumes GUIDs from both represented `nearby.world.dynamic_objects` and `nearby.grid.dynamic_objects`, resolving each through the typed canonical map record before packet data is built. Focused tests/checks: `cargo fmt --check`; `cargo test -p wow-world canonical_visibility_uses_player_instance --lib -- --nocapture` (2 passed); `cargo test -p wow-world dynamic_object_visibility_uses_player_instance --lib -- --nocapture` (1 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: does not close full `UpdateVisibilityOf`, full `SetSeer`, ObjectAccessor/session fanout, dynamic tree/collision, visibility packet parity beyond represented create/OOR state already in scope, scripts, aura runtime, exact grid traversal, or manual-test-ready farsight.

- [x] **#NEXT.R8.ENTITIES.494** live Spell::EffectAddFarsight SetSeer/session-seer consumption seam.
  Status: reviewed-validated, committed locally at current #494 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::execute_spell_with_visual_and_target_data` now consumes represented `Map::create_farsight_dynamic_object_like_cpp` outcome evidence after the map mutation returns and mirrors successful C++ `Player::SetViewpoint(..., true)`/`SetSeer(target)` into the session-local represented `m_seer` GUID only when the target is the created map-owned DynamicObject.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:209-225`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h:2432,2438`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:23343-23349`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: consumes only explicit successful `player_set_viewpoint.set_seer_requested` evidence for the created DynamicObject GUID, updates represented session seer after the canonical map lock is dropped, and forces the existing visibility refresh only when the outcome requested C++ `UpdateVisibilityOf(target)`. Missing destination, no created DynamicObject, no caster-viewpoint evidence, false SetSeer flag, mismatched target, and AlreadyHasViewpoint leave the session seer unchanged. Checks: `cargo fmt --check`; `cargo test -p wow-world add_farsight --lib -- --nocapture` (5 passed, 0 failed, 620 filtered); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no ObjectAccessor fallback, no dummy DynamicObjects, no GameObject/AreaTrigger seers, no wrong-map fallback, no packet fanout/full VisibleNotifier/real SetSeer claim, no server restart/install/push.

- [x] **#NEXT.R8.ENTITIES.493** Player::RemoveFromWorld viewpoint cleanup seam.
  Status: reviewed-validated, committed locally at current #493 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `Map::remove_from_map_like_cpp` now consumes the late C++ `Player::RemoveFromWorld()` viewpoint cleanup before extracting an exact typed in-world Player from canonical `Map::map_objects`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:1567-1585`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25389-25395`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed in-world Player records with non-empty `ActivePlayerData::FarsightObject` resolve only same-map represented `TYPEMASK_SEER` targets before player extraction. Creature/Pet targets reuse `apply_player_set_viewpoint_unit_like_cpp(..., false, None)`, DynamicObject targets clear only the removing Player directly when `FarsightObject == target GUID` and do not depend on `DynamicObject::bound_caster()` or toggle DynamicObject caster-viewpoint state, and Player targets clear the removing Player's FarsightObject directly with represented `SetSeer(this)` and no `UpdateVisibilityOf`. Missing/not-in-world/unsupported targets do not mutate as cleanup success. Focused tests cover DynamicObject direct cleanup and bound-caster independence, Creature shared-vision/SetWorldObject removal, missing/unsupported targets, and not-in-world/empty-farsight no evidence. Checks: `cargo fmt --check`; `cargo test -p wow-map player_remove_from_world --lib -- --nocapture` (5 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no ObjectAccessor/session fanout, no packets, no real SetSeer, no full Player::RemoveFromWorld/Unit::RemoveFromWorld/item lifecycle, no DynamicObject RemoveFromWorld caster-viewpoint lifecycle, no vehicle-base runtime skip beyond documented `None`, no DB.

- [x] **#NEXT.R8.ENTITIES.492** CMSG_FAR_SIGHT / Player::UpdateVisibilityForPlayer represented seer-position consumption.
  Status: reviewed-validated, committed locally at current #492 HEAD; review `APROBADO`; CI `CI_OK`; no push/install/restart. `WorldSession::represented_visibility_source_position_like_cpp()` now consumes the existing session-local represented `m_seer` GUID (`represented_seer_guid_like_cpp`) as the source position for the session visibility refresh, resolving non-self seers only in the canonical `ManagedMap` instance that contains the typed Player and accepting only represented `TYPEMASK_SEER` families (`Player`, `Creature`, `Pet`, `DynamicObject`). Invalid, empty, self, unsupported, missing, or cross-instance seers fall back to the logged-in player's position.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MiscHandler.cpp:863-880`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h:2432,2438,2477`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:298-300,23343-23349,25344-25395`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `update_visibility()` now scans map-owned creatures/gameobjects using the effective seer position and stores `last_visibility_pos` for that effective source position; focused tests cover a far same-instance Creature seer making far map-owned Creature/GO visible and an unsupported GameObject seer falling back to player position. Checks: `cargo fmt --check`; `cargo test -p wow-world far_sight --lib -- --nocapture` (8 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: this only consumes the represented seer position for the existing session visibility path; it does not implement canonical `Player::SetSeer`, real `UpdateVisibilityOf(target)`, `VisibleNotifier`/`UpdateDataMapType` fanout parity, ObjectAccessor global lookup, dynamic-tree/cell traversal parity, session fanout to other players, DB fallback parity, or manual-test-ready farsight.

- [x] **#NEXT.R8.ENTITIES.491** CMSG_FAR_SIGHT same-map/instance GetViewpoint resolution seam.
  Status: represented-complete bounded handler resolution seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #491 HEAD; no push/install/restart. `WorldSession::current_canonical_farsight_object_like_cpp()` and `canonical_map_has_seer_like_object_like_cpp()` now anchor represented `GetViewpoint`/`TYPEMASK_SEER` lookup to the canonical `ManagedMap` instance that contains the Player, avoiding same-map-id cross-instance fallback.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MiscHandler.cpp:863-880`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25389-25395`; `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:120-169,176-188,206-224,227-239`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.h:69`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: CMSG_FAR_SIGHT enable reads `Player.active_data().farsight_object` only from the canonical map/instance containing the Player and verifies represented `TYPEMASK_SEER` targets only in that same instance (`Player`, `Creature`, `Pet`, `DynamicObject`); focused tests cover cross-instance rejection and same non-zero-instance acceptance. Checks: `cargo fmt --check`; `cargo test -p wow-world far_sight --lib -- --nocapture` (6 passed, 0 failed, 614 filtered); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no real SetSeer, ObjectAccessor, session fanout, visibility packet parity, DynamicObject lifecycle completion, or full spell runtime is claimed.

- [x] **#NEXT.R8.ENTITIES.490** live Spell::EffectAddFarsight session caller consumption seam.
  Status: represented-complete bounded live caller seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #490 HEAD; no push/install/restart. WorldSession now preserves full `SpellTargetData` through instant/delayed execution, detects `SPELL_EFFECT_ADD_FARSIGHT`, computes represented radius/duration from wired DB2 stores, and delegates actual DynamicObject mutation to the existing map-owned `Map::create_farsight_dynamic_object_like_cpp` seam over canonical `Map::map_objects`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellInfo.cpp:653-692`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellInfo.cpp:3894-3910`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/Spell.cpp:133-205`.
  Rust targets: `crates/wow-data/src/spell_db2.rs`, `crates/wow-data/src/spell.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `SpellCastState` and `execute_spell_with_visual_and_target_data` retain `SpellTargetData::dst_location` so `Spell::EffectAddFarsight` can create a canonical map-owned farsight `DynamicObject` at the client destination; `SpellDurationStore`/`SpellRadiusStore` are loaded/wired into sessions; helper fallbacks mirror represented C++ radius/duration behavior; focused session tests verify destination, radius, duration, caster, spell visual and cast time on the canonical DynamicObject plus no-destination no-mutation. Checks: `cargo fmt --check`; `cargo test -p wow-world add_farsight --lib -- --nocapture` (2 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; CI `CI_OK`. Boundaries: no full Spell runtime/effect pipeline, player spell mods, random destination radius, SetSeer/session fanout, ObjectAccessor visibility, update packets, transport offsets, DB/scripts/AI, aura lifecycle, or full map/session sync; direct `cargo test -p wow-data ...` remains blocked by out-of-scope creature_template test fixtures missing `creature_type`.

- [x] **#NEXT.R8.ENTITIES.489** SpellTargetData optional location preservation seam.
  Status: represented-complete bounded packet/data dependency; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #489 HEAD; no push/install/restart. Packet seam complete only for preserving Trinity `SpellTargetData` optional source/destination target locations, orientation, map id, and name through `wow-packet` read/write; it does not implement live spell effect 72, DynamicObject creation, `Player::SetViewpoint`, SetSeer, ObjectAccessor/session fanout, or visibility packets.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/SpellPackets.h:195-211`; `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/SpellPackets.cpp:153-188`; `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/SpellPackets.cpp:288-323`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/Spell.cpp:152-164`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`.
  Rust targets: `crates/wow-packet/src/packets/spell.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `TargetLocation` now carries transport GUID plus XYZ, `SpellTargetData` preserves optional `src_location`, `dst_location`, `orientation`, `map_id`, and `name`, and read/write order mirrors Trinity. Existing minimal unit/item targets use `..SpellTargetData::default()` and still emit no optional payload. Checks: `cargo fmt --check`; `cargo test -p wow-packet spell_target_data --lib -- --nocapture` (2 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; CI `CI_OK`. Boundaries: no live `SPELL_EFFECT_ADD_FARSIGHT`, no dynamic-object/aura runtime, no map/session sync, no SetSeer/visibility fanout, no DB/scripts/AI.

- [x] **#NEXT.R8.ENTITIES.488** ActivePlayerData::FarsightObject create-packet serialization seam.
  Status: represented-complete bounded packet serialization seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #488 HEAD; no push/install/restart. Packet-create seam complete only for `PlayerCreateData::write_active_player_data` carrying a non-empty `ActivePlayerData::FarsightObject` immediately after `InvSlots[141]`, while `UpdateObject::create_player(...)` remains compatibility-defaulted to `ObjectGuid::EMPTY`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h:643-646`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.cpp:2869-2877`, `:3554-3560`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`.
  Rust targets: `crates/wow-packet/src/packets/update.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `PlayerCreateData` now exposes `farsight_object`, the ActivePlayerData create path serializes it with packed GUID encoding immediately after the 141 inventory slots, and focused wow-packet tests cover non-empty direct create serialization plus empty `UpdateObject::create_player(...)` fallback. Checks: `cargo fmt --check`; `cargo test -p wow-packet farsight --lib -- --nocapture` (2 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column/no-duplicate check. Boundaries: no full `Player::SetViewpoint`, `CMSG_FAR_SIGHT`, `SetSeer`, ObjectAccessor/session fanout, dynamic-object/aura runtime, visibility packet parity, or new map/session sync is claimed.

- [x] **#NEXT.R8.ENTITIES.487** live `Map::DelayedUpdate` far-spell callback drain seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #487 HEAD; no push/install/restart. Runtime-seam complete only for the C++ `Map::DelayedUpdate` first block ordering: drain map-owned represented far-spell callback actions before remove-list and grid-state update.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2514-2517`, `:2519-2530`, `:2532-2544`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns a FIFO represented `_farSpellCallbacks` queue; callers enqueue explicit closed actions and only `Map::drain_far_spell_callbacks_like_cpp()` drains them. `ManagedMap::delayed_update(diff_ms)` now records delayed-update instrumentation, drains far-spell callbacks, then drains remove-list, then updates/skips grid states. `QueueObjectRemove { guid }` delegates to `add_object_to_remove_list_like_cpp(guid)` so an object can be removed in the same tick by the following remove-list drain; missing/stale/duplicate/unsupported outcomes are summarized without panic. Checks after review/CI: `cargo fmt --check`; `cargo test -p wow-map far_spell --lib -- --nocapture` (3 passed, 0 failed, 578 filtered); `cargo test -p wow-map map_manager_update --lib -- --nocapture` (26 passed, 0 failed, 555 filtered); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with pre-existing warnings; `git diff --check`; TSV 9-column check (485 rows, no duplicates). Boundaries: no real Spell/FarSpellCallback arbitrary closures, `Spell::CallScript*`, Aura/caster lookup, ObjectAccessor/session fanout, packets, DB, scripts/AI, exact threaded callback queue, BG special runtime, or full `Map::DelayedUpdate` parity.

- [x] **#NEXT.R8.ENTITIES.486** live `Map::DelayedUpdate` grid-state update consumption seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #486 HEAD; no push/install/restart. Runtime-seam complete only for live `Map::DelayedUpdate` grid-state update consumption after remove-list.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2519-2544`, `:1561-1624`, `:2687-2697`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/GridStates.cpp:28-64`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_loaded_grid_states_like_cpp(diff_ms)` snapshots currently loaded NGrid slots in deterministic `x * MAX_NUMBER_OF_GRIDS + y` order, re-checks each slot before update, then consumes the existing pure grid-state machine without recreating missing grids. `ManagedMap::delayed_update(diff_ms)` now preserves C++ order by running represented remove-list drain first, then grid-state updates only for non-BG/arena maps; BG/arena records an explicit skip summary. Checks: `cargo fmt --check`; `cargo test -p wow-map grid_state --lib -- --nocapture` (6 passed, 0 failed, 572 filtered); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with pre-existing warnings; `git diff --check`; TSV 9-column check (484 rows, no duplicates). Represents only live `Map::DelayedUpdate` grid-state update consumption after remove-list; far spell callbacks, BG special runtime beyond skip, full unload terrain/dynamic-tree side effects, ObjectAccessor/session fanout, scripts/AI, packets, exact `GridRefManager` ordering beyond Rust snapshot, and full unload parity remain open.

- [x] **#NEXT.R8.ENTITIES.485** Map-owned active non-player list + active unload-lock consumption seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at current #485 HEAD; no push/install/restart. Runtime-seam complete only for the bounded map-owned `m_activeNonPlayers` equivalent and active-unload-lock consumption.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`, `:933-951`, `:2700-2776`, `:758-767,804-805`, `:2687-2697`; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:617-619,727-745`.
  Rust targets: `crates/wow-map/src/grid.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` now owns `active_non_players_like_cpp: HashSet<ObjectGuid>` derived only from AddToMap/RemoveFromMap over canonical `map_objects`; `AddToActive`/`RemoveFromActive` insert/remove active non-player GUIDs and inc/dec existing respawn-grid unload active locks for exact typed Creature/GameObject spawn IDs without creating missing grids; spawn-id-zero active objects mutate only the set. `ProcessRelocationNotifies` and `ActiveObjectsNearGrid` consume the active set rather than scanning all `map_objects` to reconstruct active non-player sources. Checks after review/CI: `cargo fmt --check`; `cargo test -p wow-map active_non_player --lib -- --nocapture` (6 passed, 0 failed, 568 filtered); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` (OK with pre-existing warnings); `git diff --check`; TSV 9-column check (483 lines, bad=0). Boundaries: no `IsAlwaysActive()` BG/arena hook, no C++ `Map::Update` mutating iterator adjustment, no ObjectAccessor/session/packet fanout, no scripts/AI/DB or full active-object update runtime.

- [x] **#NEXT.R8.ENTITIES.484** Map::RemoveFromMap generic tail personal-phase unregister + visibility-on-destroy evidence seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #484 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`, `/home/server/woltk-trinity-legacy/src/server/game/Phasing/PersonalPhaseTracker.cpp:35-39,171-175`.
  Rust targets: `crates/wow-map/src/personal_phase.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-complete only for the generic `Map::RemoveFromMap<T>` tail after per-type RemoveFromWorld helpers. `Map::remove_from_map_like_cpp` now records the C++ `inWorld` boolean separately from raw `was_in_world`, unregisters the removed object from map-owned `MultiPersonalPhaseTracker` by the object's own `PhaseShift::personal_guid`, and records represented `UpdateObjectVisibilityOnDestroy` evidence only when that C++ `inWorld` boolean is false. Empty personal owner, missing owner tracker, missing object-in-tracker, unsupported non-Unit/GameObject type paths, and not-in-world eligible records are explicit no-panic outcomes. Boundaries: no real visibility/fanout packets, ObjectAccessor/session runtime, full personal-phase visibility runtime, exact active-list runtime, DeleteFromWorld, DB, scripts/AI, or full object removal parity. Checks: `cargo fmt --check`; `cargo test -p wow-map remove_from_map --lib -- --nocapture` (26 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.483** GameObject::RemoveFromWorld linked-trap ordering/evidence correction.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #483 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed in-world `MapObjectRecord::GameObject` in `Map::remove_from_map_like_cpp` now snapshots and consumes bounded linked-trap cleanup after represented ZoneScript remove, represented `RemoveFromOwner()`, and represented model removal, but before owner `remove_map_object(guid)`, represented `WorldObject::RemoveFromWorld()`, spawn-id unindex/store extraction and grid/reset/delete tail. Outcome evidence reports owner GUID, optional linked-trap GUID, whether the owner was still present in canonical `Map::map_objects` at the seam, whether the trap was removed, explicit empty/self/missing no-op handling, and an in-progress cycle guard for malformed linked-trap cycles. Generic kind-only GameObjects and exact typed not-in-world GameObjects emit no linked-trap RemoveFromWorld evidence. Boundaries: no real `DespawnOrUnsummon` scheduler, ObjectAccessor, sessions/fanout/packets, DB, scripts/AI, or full GameObject runtime. Checks: `cargo fmt --check`; `cargo test -p wow-map gameobject_linked_trap_remove --lib -- --nocapture` (4 passed); `cargo test -p wow-map remove_from_map --lib -- --nocapture` (21 passed); `cargo test -p wow-map process_respawns_loaded_grid_pre_add_records_are_best_effort_like_cpp --lib -- --nocapture` (1 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.482** GameObject::RemoveFromWorld RemoveFromOwner represented seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #482 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:880-897`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:5213-5250`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.h:227-237`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed in-world `MapObjectRecord::GameObject` in `Map::remove_from_map_like_cpp` now records a represented `RemoveFromOwner()` outcome immediately after represented ZoneScript remove and before model removal/linked-trap/WorldObject/store removal. `GameObject::set_owner_guid_like_cpp` writes `CreatedBy` and forces `spawned_by_default=false` like C++ `SetOwnerGUID`; non-empty owners are cleared even when no map-local Unit-like record exists, matching the lost-owner fallback. Owner presence is evidence-only from canonical map-local `Player`, `Creature`, or `Pet` records. Generic GameObject-kind-only records and not-in-world typed GameObjects emit no RemoveFromOwner evidence. Boundaries: no full `Unit::RemoveGameObject` side effects (owned-GO list, object slots, auras, cooldown event, Creature AI callback/delete), no real ObjectAccessor/global lookup, no session/fanout/packets, DB writes, real ZoneScript/model/linked-trap runtime or full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.481** GameObject RemoveFromWorld ZoneScript remove callback evidence seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #481 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`, plus add-side symmetry/context `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923` and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed in-world `MapObjectRecord::GameObject` in `Map::remove_from_map_like_cpp` snapshots represented ZoneScript remove callback boundary evidence before represented model removal, linked-trap despawn, represented `WorldObject::RemoveFromWorld`, spawn-id unindex and record extraction. Evidence is map-owned from canonical `Map::map_objects`, includes pre-callback model-remove-pending and spawn-index presence, and keeps `script_dispatch_represented=false`. Missing records, non-GameObject records, generic kind-only GameObject/WorldObject paths, and exact typed GameObject not-in-world emit no GameObject ZoneScript remove evidence. Boundaries: no real ZoneScript object/trait/dispatch, no ObjectAccessor/session fanout, packets, DB writes, real model geometry/collision expansion, linked-trap scheduler/fanout, or full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.480** GameObject AddToWorld ZoneScript create callback ordering evidence seam.
  Status: represented-complete bounded slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #480 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948` (remove-side context only).
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed `MapObjectRecord::GameObject` on the normal not-already-in-world AddToMap path records represented ZoneScript create callback boundary evidence immediately after AddToGrid/current cell and before `insert_map_object_record`, including pre-callback map store and by-spawn-index presence checks. Already-in-world, generic kind-only GameObject and non-GameObject paths emit no GameObject ZoneScript create evidence. This remains represented-only with `script_dispatch_represented=false`; no real ZoneScript object/trait/dispatch, ObjectAccessor/session fanout, packets, DB writes, or full GameObject runtime parity is claimed. Checks: `cargo fmt --check`; `cargo test -p wow-map gameobject_zone_script_create --lib -- --nocapture` (2 passed); `cargo test -p wow-map add_to_map --lib -- --nocapture` (26 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check.

- [x] **#NEXT.R8.ENTITIES.479** GameObject AddToWorld map-owned ObjectsStore/by-spawn pre-WorldObject insertion and canonical model/collision mutation seam.
  Status: represented-complete bounded slice; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #479 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed `MapObjectRecord::GameObject` on the normal not-already-in-world AddToMap path now inserts/replaces into canonical `Map::map_objects` through `insert_map_object_record` after AddToGrid/current cell but before model insertion, collision enable and represented `WorldObject::AddToWorld`; `gameobjects_by_spawn_id` remains derived only from `insert_map_object_record`. Subsequent represented model/collision/AddToWorld side effects read/mutate the canonical map-owned record, not a stale local copy. Already-in-world, generic kind-only GameObject, non-GameObject and wrong-kind paths emit no typed GameObject pre-insert evidence. GameObject::AddToWorld ZoneScript create ordering/callback remains OPEN/BLOCKED/FUTURE because C++ `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923` executes `m_zoneScript->OnGameObjectCreate(this)` before ObjectsStore/by-spawn insertion and this slice does not represent that earlier callback order. Checks: `cargo fmt --check`; `cargo test -p wow-map add_to_map --lib -- --nocapture` (26 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no real ObjectAccessor global registration, no GameObject ZoneScript callback/order/dispatch representation, real GameObjectModel geometry/BIH/collision, transport delayed-add model runtime, packets/session fanout, DB writes or full GameObject runtime.

- [x] **#NEXT.R8.ENTITIES.478** Map::AddToMap InitializeObject + create-visibility represented tail.
  Status: represented-complete bounded only for exact typed Creature/GameObject `Map::AddToMap<T>` post-`AddToWorld` tail; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #478 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:517-527`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapObject.h:28-32,41-56`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed Creature AddToMap keeps `Map::map_objects` canonical after #477 pre-Unit insertion and now runs represented tail only after Unit/SearchFormation/AIM/Vehicle/ZoneScript evidence: typed `InitializeObject(Creature*)` consumes map-owned pending Creature move state and removes the queued delayed relocation, represented `AddToActive` remains evidence/runtime-gap only, and `SetIsNewObject(true)` / represented `UpdateObjectVisibilityOnCreate` / `SetIsNewObject(false)` leave canonical `is_new_object=false`. Exact typed GameObject AddToMap consumes pending GameObject move state the same way and records the same honest tail evidence. Generic kind-only Creature/GameObject and already-in-world paths emit no typed InitializeObject/tail overclaim. Checks: `cargo fmt`; `cargo test -p wow-map add_to_map --lib -- --nocapture` (23 passed); `cargo test -p wow-map creature_add_to_world --lib -- --nocapture` (2 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no ObjectAccessor global, real active-list runtime, session fanout, packets, dynamic tree expansion beyond existing represented GameObject seams, AI/scripts/session/DB writes, real visibility/fanout, or full Creature/GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.477** Creature AddToWorld map-owned ObjectsStore/by-spawn pre-Unit insertion seam.
  Status: represented-complete bounded only for exact typed Creature AddToWorld canonical pre-Unit insertion; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #477 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:9471-9477`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1017-1021`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-576`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: exact typed `MapObjectRecord::Creature` on the normal `Map::add_map_object_record_to_map_like_cpp` path now inserts/replaces into canonical `Map::map_objects` and updates `creatures_by_spawn_id` after AddToGrid/current cell but before local `Unit::AddToWorld`, `SearchFormation`, AIM, vehicle reset/install and ZoneScript evidence. Subsequent represented Creature AddToWorld side effects read/mutate the canonical map-owned record (`Map::map_objects[guid]`) plus derived by-spawn index, not a later local copy write-back; outcome evidence reports the exact typed store and spawn-index visibility before the Unit seam. Already-in-world, generic kind-only Creature and non-Creature paths emit no pre-Unit store/index evidence. Checks: `cargo fmt --check`; `cargo test -p wow-map creature_add_to_world --lib -- --nocapture` (1 passed); `cargo test -p wow-map creature_aim --lib -- --nocapture` (1 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no ObjectAccessor global, session fanout, packets, AI runtime/factory, real ZoneScript dispatch, DB writes, dynamic tree/visibility, or full Creature runtime.


- [x] **#NEXT.R8.ENTITIES.476** Creature AddToWorld `AIM_Create`/`AIM_Initialize` represented seam.
  Status: represented-complete only for local `Creature::AIM_Initialize()` / `AIM_Create()` evidence consumed during canonical exact typed Creature AddToMap; review `APROBADO` after one bounded documentation-state correction; CI `CI_OK`; validation OK; committed locally at `current #476 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1026-1044`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1046-1060`.
  Rust targets: `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Creature::aim_initialize_like_cpp()` records only explicit local evidence for the C++ order after `SearchFormation()`: represented `AIM_Create`, represented `Motion_Initialize`, formation-present/leader flags, no represented `MoveIdle()` because real `CreatureGroup::IsFormed()` is absent, represented `MotionMaster::Initialize` only when there is no formation or the creature is the formation leader; for non-leader creatures with formation, `CreatureGroup::IsFormed()`/MoveIdle vs Initialize remains explicitly unknown/unrepresented via `motion_initialize_requires_formed_state`. It records represented AI selection and represented `AI()->InitializeAI()`, and reports whether a local VehicleKit exists so the following existing vehicle reset seam represents `if (GetVehicleKit()) Reset()` without duplicating mutation. `Map::add_map_object_record_to_map_like_cpp` emits `creature_aim_initialize` only for canonical exact typed Creature records on the normal not-already-in-world path after SearchFormation and before vehicle reset/install; already-in-world, generic kind-only Creature records and non-Creature paths emit no AIM evidence. No real AI factory/InitializeAI, SmartAI/script AI, MotionMaster movement, FormationReset, MoveIdle, ObjectAccessor/session fanout, packets, DB writes, install/restart/push, or full Creature runtime parity is claimed.

- [x] **#NEXT.R8.ENTITIES.475** Creature AddToWorld `Unit::AddToWorld()` motion/EnterWorld aura evidence seam.
  Status: represented-complete only for the bounded local `Unit::AddToWorld()` seam consumed by canonical exact typed Creature AddToMap; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #475 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:9471-9477`, `/home/server/woltk-trinity-legacy/src/server/game/Movement/MotionMaster.cpp:120-132`, `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellDefines.h:70-96`.
  Rust targets: `crates/wow-entities/src/unit.rs`, `crates/wow-entities/src/unit_subsystems.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Unit::add_to_world_like_cpp()` preserves C++ local order: represented `WorldObject::AddToWorld()`, `MotionSubsystem::add_to_world_like_cpp()` with the `INITIALIZATION_PENDING` early return and local `DirectInitialize`/delayed-action evidence, then `RemoveAurasWithInterruptFlags(EnterWorld)` using `0x0040_0000`. `Map::add_map_object_record_to_map_like_cpp` consumes this only from canonical exact typed `MapObjectRecord::Creature` at the old `WorldObject::add_to_world()` point before SearchFormation, AIM/vehicle reset/install and ZoneScript create; already-in-world, non-Creature and generic kind-only Creature paths emit no Unit evidence. Checks: `cargo fmt --check`; `cargo test -p wow-entities unit_add_to_world --lib -- --nocapture` (1 passed); `cargo test -p wow-map creature_add_to_world --lib -- --nocapture` (1 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; `git diff --check`; TSV 9-column check. Boundaries: no real movement-generator runtime/pathing, aura scripts/procs/update masks/packets, ObjectAccessor/session fanout, AI, ZoneScript dispatch, DB writes, or full Unit/Creature runtime parity.

- [x] **#NEXT.R8.ENTITIES.474** Creature RemoveFromWorld Unit cleanup/order evidence seam.
  Status: represented-complete only for local `Creature::RemoveFromWorld()` ordering/evidence in canonical typed Creature remove-from-map; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #474 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:353-368`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:9479-9533`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`.
  Rust targets: `crates/wow-entities/src/unit.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Unit::remove_from_world_like_cpp()` preserves the C++ `IsInWorld()` guard, records bounded during-remove/world-object evidence, calls local `VehicleSubsystem::remove_vehicle_kit_like_cpp(true)` at the Unit point, and leaves AI/aura/control/owned-object/follower/totem cleanup explicitly unrepresented. `Map::remove_from_map_like_cpp` snapshots `was_in_world` before the Unit helper, keeps ZoneScript then formation then Unit ordering for exact typed in-world Creatures, derives legacy vehicle evidence from the Unit outcome, and emits no Unit/vehicle evidence for missing, non-Creature, generic kind-only Creature, or not-in-world Creature records. Boundaries: no real AI `OnDespawn`, aura/control/owned-object cleanup, ExitVehicle, totems, ObjectAccessor/session/fanout, packets, DB writes, or full Unit runtime cleanup.

- [x] **#NEXT.R8.ENTITIES.473** Creature AddToWorld/RemoveFromWorld ZoneScript callback evidence seam.
  Status: represented-complete only for map-owned evidence of `Creature::AddToWorld -> GetZoneScript()->OnCreatureCreate(this)` and `Creature::RemoveFromWorld -> GetZoneScript()->OnCreatureRemove(this)` callback positions; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #473 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:353-368`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-complete only for bounded outcome evidence over canonical `Map::map_objects` / exact typed `MapObjectRecord::Creature`: AddToMap emits `creature_zone_script_create` only on the normal not-already-in-world exact typed Creature path after `SearchFormation`, AIM/vehicle reset and `creature_vehicle_install`; already-in-world, generic kind-only Creature records and non-Creature paths emit no create evidence. RemoveFromMap computes `creature_zone_script_remove` from canonical in-world exact typed Creature state before vehicle-kit removal, formation cleanup and record extraction; missing, non-Creature and not-in-world paths emit no remove evidence. Boundaries: no real `ZoneScript`, no script manager/trait/dispatch, no ObjectAccessor/session cache write-back, no fanout/scripts/packets/DB/full Creature runtime.

- [x] **#NEXT.R8.ENTITIES.472** DB-backed Creature formation metadata -> loaded-grid SearchFormation consumption seam.
  Status: represented-complete only for DB `creature_formations` startup metadata feeding loaded-grid Creature `FormationInfo` into the existing bounded `SearchFormation`/map-owned holder seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #472 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.h:39-46`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.cpp:96-172`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:379-389`, `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp:2288-2292`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`, `crates/world-server/src/creature_loaded_grid.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: DB `creature_formations` rows are loaded after creature spawn metadata using C++ query/semantics: leader/member existence validation from `SpawnStore`, leader self-row dist/angle zero, member angle degrees->radians, waypoint IDs/groupAI preserved, duplicate members keep first valid row, and formations without a valid leader self-row are pruned. Loaded-grid Creature resolver carries optional formation info into `Creature` before `MapObjectRecord::Creature`, so `Map::add_map_object_record_to_map_like_cpp` can consume existing bounded `SearchFormation` behavior. Boundaries: no global `sFormationMgr`, no movement formation/`LeaderStartedMoving`/`FormationMovementGenerator`, no combat assist, no scripts/AI/fanout/ObjectAccessor/dynamic tree/packets/DB writes/full CreatureGroup runtime.
- [x] **#NEXT.R8.ENTITIES.471** Creature RemoveFromWorld formation group-holder cleanup seam.
  Status: represented-complete only for the bounded `Creature::RemoveFromWorld()` formation cleanup seam; review `APROBADO` after one bounded test-coverage correction; CI `CI_OK`; validation OK; committed locally at `current #471 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:353-368`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.cpp:79-94`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-complete only for map-owned cleanup of the represented `CreatureGroupHolder` during `Map::remove_from_map_like_cpp` before canonical record extraction and before `Object::RemoveFromWorld()`: exact typed in-world Creature records with explicit formation info remove their GUID from the holder keyed by leader spawn id, erase the holder if it becomes empty, and expose bounded outcome evidence. Missing formation info, absent holder, GUID not a member, wrong-kind and not-in-world paths do not fabricate holders or scan external mirrors. Boundaries: no real ZoneScript, no global `sFormationMgr`, no real `CreatureGroup` movement/AI/following semantics, no scripts/fanout/ObjectAccessor/dynamic tree/packets/DB/full CreatureGroup runtime.
- [x] **#NEXT.R8.ENTITIES.470** Creature AddToWorld `SearchFormation()` represented group-holder seam.
  Status: represented-complete only for the bounded `Creature::SearchFormation()` AddToWorld consumption seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #470 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:379-389`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.h:39-46`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.cpp:45-77`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureGroups.cpp:96-176`.
  Rust targets: `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-complete only for explicit caller-provided `FormationInfo` evidence consumed during canonical `Map::add_map_object_record_to_map_like_cpp` for exact typed Creature records: summon and spawn-id-zero C++ early returns are explicit no-ops, missing formation info does not fabricate a group, and successful search creates/uses a map-owned represented `CreatureGroupHolder` keyed by leader spawn id while removing stale same-spawn member GUIDs from that holder through the typed spawn-id index. Ordering remains after object AddToWorld/cell insertion and before the existing vehicle reset/install evidence. Boundaries: no DB `creature_formations` loader, no global `sFormationMgr`, no real `CreatureGroup` movement/combat/leader semantics, RemoveFromWorld cleanup is now represented by #471 only for holder membership, no scripts/AI/fanout/ObjectAccessor/dynamic tree/packets/DB/ZoneScript/full Creature runtime.
- [x] **#NEXT.R8.ENTITIES.469** Creature AddToWorld `AIM_Initialize -> Vehicle::Reset(false)` reset/accessory evidence seam.
  Status: represented-complete only for local reset evidence before existing AddToMap install evidence; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #469 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:2469-2474`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureData.h:54`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:4572-4582`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:84-107`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:193-257`.
  Rust targets: `crates/wow-data/src/creature_template.rs`, `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-entities/src/unit_subsystems.rs`, `crates/wow-map/src/map.rs`, `crates/world-server/src/creature_loaded_grid.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented-complete only for local reset evidence before existing AddToMap install evidence; reset records knockback immunity, mechanical non-world-boss immunity gating, accessory-plan DTOs, and spawn-specific accessory preference over entry rows. Boundaries: no real accessory Creature summon/AddToMap, no scripts/AI/fanout/ObjectAccessor/dynamic tree/packets/DB/ZoneScript/formation.
- [x] **#NEXT.R8.ENTITIES.468** loaded-grid Creature `CreateVehicleKit` DB2 seat-backed local Vehicle object seam.
  Status: represented-complete for DB2 seat-backed local `Vehicle` object creation during loaded-grid Creature `CreateVehicleKit(loading=true)` only; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #468 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1812`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:11298-11312`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:37-59`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:78-83`.
  Rust targets: `crates/wow-data/src/vehicle.rs`, `crates/wow-entities/src/unit_subsystems.rs`, `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`, `crates/world-server/src/creature_loaded_grid.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: loaded-grid DB-backed Creature templates with a `vehicle_id` receive pure DTO `VehicleKitCreateInputLikeCpp` from startup `VehicleStore`/`VehicleSeatStore` when the `VehicleEntry` exists; `wow-entities` stays independent of `wow-data` and constructs the existing local `Vehicle` with C++ seat-index order, explicit `SeatID == 0` skip, skipped missing seat rows, usable-seat count, status `None`, `installed=false`, and structured evidence for represented update flag/type mask/NPC flag/movement-info/update-display-power effects. Missing `VehicleEntry` preserves the vehicle template metadata and `HighGuid::Vehicle` identity but creates no local kit and reports false/no-kit evidence. Loading=true keeps `SendSetVehicleRecId` evidence false and does not send packets. Remaining gaps: no live packets/fanout/scripts/passenger runtime, no real Unit update-field/mask/NPC-flag integration beyond evidence, no AddToMap install beyond #466, no RemoveFromMap uninstall beyond #467, no ZoneScript/formation/AI/ObjectAccessor/dynamic tree/DB persistence, and no full Creature/Vehicle runtime parity.
- [x] **#NEXT.R8.ENTITIES.467** Creature RemoveFromMap vehicle-kit `RemoveVehicleKit(true)` / `Uninstall` seam.
  Status: represented-complete only for the bounded RemoveFromMap vehicle-kit removal evidence seam; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #467 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:353-369`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:9479-9534`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:11314-11328`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:109-125`.
  Rust targets: `crates/wow-entities/src/unit_subsystems.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: local `VehicleSubsystem::remove_vehicle_kit_like_cpp(true)` clears only the local kit and records evidence that `SendSetVehicleRecId(0)` is skipped for `onRemoveFromWorld=true`, represented `Uninstall()`, represented `RemoveAllPassengers()`, represented script-hook evidence, and previous install state; no-kit returns before send evidence even when `on_remove_from_world=false`. `Map::remove_from_map_like_cpp` consumes this only from canonical map-owned Creature records before removal and only when the object is in world; missing, non-Creature, no-kit and not-in-world paths report no evidence. Remaining gaps: real Vehicle object status/seats/passengers, scripts, Unit flags/masks/NPC flags, ZoneScript/formation/AI, ObjectAccessor/fanout, dynamic tree/collision, packets, DB, full Creature/Vehicle runtime.

- [x] **#NEXT.R8.ENTITIES.466** Creature AddToMap vehicle-kit Install seam.
  Status: represented-complete only for the local AddToMap vehicle-kit install evidence seam; review `APROBADO` after bounded fixture corrections; CI `CI_OK`; validation OK; committed locally at `current #466 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.cpp:78-83`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:11298-11312`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1808-1810`.
  Rust targets: `crates/wow-entities/src/unit_subsystems.rs`, `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `crates/world-server/src/creature_loaded_grid.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `VehicleKitState` distinguishes local kit creation from install via `installed`; `set_vehicle_kit` initializes kits as active but not installed. `Map::add_map_object_record_to_map_like_cpp` consumes only canonical `MapObjectRecord::Creature` records on the normal not-already-in-world path after `Object::AddToWorld()` and marks an existing local vehicle kit installed with structured evidence, including represented script hook evidence only. Existing #465 world-server tests/fixtures assert `installed: false` before AddToMap. Creature without a kit, non-Creature records, and already-in-world early returns do not install or report install evidence. Remaining gaps: real VehicleKit seat data/accessories, scripts `sScriptMgr->OnInstall`, ZoneScript, SearchFormation, AIM, ObjectAccessor/fanout, dynamic tree/collision, packets, DB persistence, RemoveFromWorld/Uninstall, full Creature/Vehicle runtime.

- [x] **#NEXT.R8.ENTITIES.465** loaded-grid Creature DB-backed vehicle-template identity/local kit seam.
-  Status: represented-complete only for loaded-grid Creature DB-backed vehicle-template identity/local kit seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #465 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1812`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1922`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`.
  Rust targets: `crates/world-server/src/creature_loaded_grid.rs`, `crates/world-server/src/main.rs`, `crates/wow-entities/src/creature.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: DB-backed loaded-grid Creature templates with resolved `vehicle_id` no longer stop at `UnsupportedVehicle`; the resolver preserves `vehicle_id`, requires `HighGuid::Vehicle` for vehicle-template map object GUIDs while still producing typed `Creature`/`MapObjectRecord::Creature`, and rejects `HighGuid::Creature` for vehicle templates. The live respawn helper keeps `map.generate_low_guid_like_cpp(HighGuid::Creature)` for the low GUID per C++ `LoadFromDB`, then builds the world object GUID as `HighGuid::Vehicle` when the resolved template has `vehicle_id`. `Creature::create_from_lifecycle` represents C++ `CreateVehicleKit(vehId, entry, true)` locally with `VehicleSubsystem::set_vehicle_kit(veh_id, true)` and preserves lifecycle metadata. Remaining gaps: real VehicleKit seat data/install/uninstall, `AddToWorld`, ZoneScript, AI, ObjectAccessor/fanout completo, dynamic tree/collision, packets, DB persistence, full Creature/Vehicle runtime.

- [x] **#NEXT.R8.ENTITIES.464** Map-owned `GameObject::Update` positive-delay respawn scheduling / `SaveRespawnTime` tail seam.
  Status: represented-complete for this bounded scheduling seam; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #464 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1653-1662`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:2095-2111`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3516-3546`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1740-1764`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after #458-#463 ordering branches and the zero-delay early return, canonical exact typed `Map::map_objects` GameObjects with positive `respawn_delay_time` consume the bounded C++ tail. Spawned-by-default objects set canonical `respawn_time = game_time_secs + respawn_delay_time`; `SaveRespawnTime` evidence is gated by explicit represented `m_goData`/GameObjectData presence plus nonzero `spawn_id` and future time. Non-compat mode inserts a GameObject `RespawnInfoLikeCpp` into the map-owned respawn store only with that evidence; compatibility mode records DB-only `SaveRespawnInfoDB` evidence and represented `UpdateObjectVisibilityOnDestroy()` without mutating the respawn store or remove-list. Missing represented GameObjectData is explicit evidence and no respawn timer is inserted. Temporary/not-spawned-by-default objects set canonical `respawn_time = 0`; `spawn_id != 0` records represented visibility-on-destroy and does not enqueue remove-list, while `spawn_id == 0` represents bounded `Delete()`/remove-list. Zero delay still returns before scheduling/persistence/delete. Acceptance does not claim real DB writes, PoolMgr, ZoneScript, ObjectAccessor/session fanout, dynamic tree, scripts/AI, remove-list drain, install/restart/push, or full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.463** Map-owned `GameObject::Update` generic `GO_NOT_READY` visual-despawn/no-respawn-delay seam.
  Status: represented-complete for this bounded seam; review `APROBADO` after docs-only correction; validation OK; committed locally at `current #463 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1651`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:843-850` (`IsDespawnAtAction()` CHEST/GOOBER consumable/default false source), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3683-3709` (`SetLootState` side-effect anchor), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:951-1200`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after #458-#462 ordering branches and only if the canonical exact typed `Map::map_objects` GameObject still has `LootState::JustDeactivated`, represented `Map::update_game_object_like_cpp` sets `LootState::NotReady`, computes visual despawn from explicit CHEST/GOOBER despawn-at-action source evidence or represented `go_anim_progress > 0`, records represented `SendGameObjectDespawn`/baseline flag restore evidence when available, and returns at the represented seam when `respawn_delay_time == 0` without scheduling, SaveRespawnTime, PoolMgr, ZoneScript or DB effects. Missing CHEST/GOOBER source is reported and never assumed. Checks: reviewer `APROBADO`; `cargo fmt --check`; `cargo test -p wow-map gameobject_update --lib -- --nocapture` (32 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` (warnings only); `git diff --check`; TSV 9-column check; `git status --short --branch`. Remaining gaps: `spawnedByDefault`, respawn timer/scheduling, `SaveRespawnTime`, PoolMgr, ZoneScript, DB persistence, real packet/fanout/ObjectAccessor, override/template baseline completeness, remove-list drain, install/restart/push, and full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.462** Map-owned `GameObject::Update` owner/spell-created expired delete seam.
  Status: represented-complete for this bounded owner/spell-created expired delete seam; review `APROBADO` after docs-only correction; CI `CI_OK`; validation OK; committed locally at `current #462 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1636`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1740-1764`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:506-569`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after represented `ClearLoot()` and the #461 non-consumed CHEST/GOOBER guard, canonical exact typed GameObjects with `(owner_guid != EMPTY || spell_id != 0) && respawn_time == 0` consume represented `SetRespawnTime(0)`/`Delete()`: `LootState::NotReady`, non-transport `GoState::Ready`, represented despawn evidence, and map-owned remove-list enqueue. `GAMEOBJECT_TYPE_NEW_FLAG_DROP` resolves owner only from canonical `Map::map_objects`; exact typed `GAMEOBJECT_TYPE_NEW_FLAG` owner records `SetNewFlagState(InBase, nullptr)` as represented evidence, while missing/empty/wrong-kind/not-NEW_FLAG owners are explicit no-op outcomes. No generic visual despawn, SaveRespawnTime, PoolMgr, DB, ZoneScript, real packets/fanout, ObjectAccessor, or full GameObject runtime parity is claimed.

- [x] **#NEXT.R8.ENTITIES.461** Map-owned `GameObject::Update` non-consumed chest/goober post-`ClearLoot()` early return.
  Status: represented-complete for this bounded post-`ClearLoot()` early-return seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #461 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1607`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1609-1623`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1740-1764`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3683-3709`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:843-849`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after represented `ClearLoot()` in the canonical exact typed `Map::map_objects` GameObject update path, CHEST/GOOBER objects consume only explicit represented template source evidence for C++ `IsDespawnAtAction()`. Missing source reports `non_consumed_source_missing` and never assumes non-consumable. Consumable/despawn-at-action objects and the represented spell-created expired case (`spell_id != 0 && respawn_time == 0`) skip the branch. Non-consumed CHEST with positive restock writes `restock_time = game_time_secs + chest_restock_time_secs`, sets `LootState::NotReady`, records represented dynamic-flags evidence, and returns without remove-list; non-restock CHEST and GOOBER set `LootState::Ready` and record represented visibility evidence. Remaining gaps stay explicit: owner-guid half of `isSummonedAndExpired`, owner `NEW_FLAG_DROP`, generic `GO_NOT_READY` visual despawn/respawn/SaveRespawnTime/PoolMgr, DB writes, scripts/AI, packets/fanout/ObjectAccessor, remove-list drain, install/restart/push, and full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.460** Map-owned `GameObject::Update` GOOBER pre-`ClearLoot()` represented branch / `GO_FLAG_NODESPAWN` early return.
  Status: represented-complete for this bounded GOOBER ordering seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #460 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1581-1607`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3711-3721`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:2902`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after canonical exact typed GameObject validation and entity-level update, GOOBER objects still in `LootState::JustDeactivated` consume only represented source evidence for spell/user cleanup and lock/autoclose state reset before `ClearLoot()`. The C++ `GO_FLAG_NODESPAWN` early return is checked from `game_object.data().flags` for every GOOBER in the branch, including when `represented_goober_use_source_like_cpp()` is `None`; that outcome reports `goober_nodespawn_return`, skips `ClearLoot()`, preserves loot/use state, and does not enqueue remove-list. Non-NODESPAWN GOOBER records reach represented `ClearLoot()` after the pre-branch. Remaining gaps stay explicit: real spell casts, ObjectAccessor/player lookup/session fanout, DB/PoolMgr/scripts/AI, go-type implementation ownership, install/restart/push, and full GameObject runtime parity.

- [x] **#NEXT.R8.ENTITIES.459** Map-owned `GameObject::Update` non-goober `GO_JUST_DEACTIVATED` `ClearLoot()` seam.
  Status: represented-complete only for this bounded non-goober `ClearLoot()` consumption seam; review `APROBADO` after one docs-only correction; CI `CI_OK`; validation OK; committed locally at `current #459 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1607`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3711-3721`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs` (test summary struct fallout only), `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: after canonical exact typed GameObject validation and entity-level `update_like_cpp(diff_ms)`, `Map::update_game_object_like_cpp` preserves represented C++ order for the safe subset: if entity-level `m_despawnDelay` requests despawn, it skips the `GO_JUST_DEACTIVATED` branch and keeps the existing remove-list path; otherwise, while the canonical non-goober GameObject still has `LootState::JustDeactivated`, it consumes linked-trap cleanup first and then calls the existing canonical `GameObject::clear_loot_like_cpp()` helper to clear shared loot, personal loot, unique users and use times. `GAMEOBJECT_TYPE_GOOBER` in `GO_JUST_DEACTIVATED` must preserve shared/personal loot, unique users and use-times and report `loot_cleared=false`, because C++ lines 1581-1605 run the goober spell/user/state/NODESPAWN branch before line 1607 `ClearLoot()` and that branch is out of scope. Outcome/summary evidence reports `loot_cleared` only for actual non-goober cleanup; no loot state, go state, respawn time, DB, remove-list, cache/session mirror or fanout side effects are added. Remaining gaps: goober spell/user cast and state reset, `GO_FLAG_NODESPAWN` early return, goober `ClearLoot()` after that pre-branch, chest/goober non-consumed branches, owner/spell-created delete, generic `GO_NOT_READY`, visual despawn, respawn persistence/remove-list drain, DB/PoolMgr/scripts/AI/ObjectAccessor/dynamic-tree/session fanout/packets, install/restart/push, and full `#NEXT.R8.ENTITIES.021`/GameObject runtime parity remain open.

- [x] **#NEXT.R8.ENTITIES.458** Map-owned `GameObject::Update` linked-trap cleanup for `GO_JUST_DEACTIVATED`.
  Status: represented-complete for this bounded linked-trap update seam; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #458 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1580`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_game_object_like_cpp` now consumes only the first C++ `GO_JUST_DEACTIVATED` side effect after canonical exact typed GameObject validation and entity-level update: it snapshots canonical `linked_trap_guid_like_cpp()`, removes an existing non-empty/non-self exact typed linked trap through `remove_from_map_like_cpp`, reports linked-trap GUID/removal/empty-self-missing evidence, and skips that branch when the same tick's `m_despawnDelay` outcome already represents `DespawnOrUnsummon -> Delete -> GO_NOT_READY`. `Map::update_game_objects_like_cpp` summarizes linked trap removals. Remaining gaps: no `ClearLoot()`, no goober spell/unique-user cleanup, no chest restock/delete/respawn branches, no DB/PoolMgr/scripts/AI/ObjectAccessor/dynamic-tree/session fanout/packets, install/restart/push, or full `GO_JUST_DEACTIVATED` runtime parity.
- [x] **#NEXT.R8.ENTITIES.457** live GameObject loot-release caller consumes canonical `SetLootState` seam.
  Status: represented-complete for this bounded caller-consumption seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #457 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp:262-320`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3683-3709`.
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: represented `WorldSession::DoLootRelease`/`handle_loot_release` GameObject release now delegates to canonical `Map::set_gameobject_loot_state_like_cpp(...)` when the current-map typed GameObject exists, and synchronizes the represented session mirror only after canonical success. FishingHole increments canonical `GameObject::use_times` when available and compares that canonical count with represented `FishingHole.MaxOpens`; missing canonical manager/map/GameObject preserves the prior represented fallback including non-consumable chest restock evidence. Remaining gaps: no new real ObjectAccessor/session fanout, GO AI `OnLootRelease`, distance/owner filter completion, live loot storage ownership, scripts/DB, install/restart/push, or full GameObject loot runtime parity.
- [x] **#NEXT.R8.ENTITIES.456** represented `GameObject::SetLootState` collision/restock map-owned caller seam.
  Status: represented-complete for this bounded facade/local evidence slice; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #456 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3683-3709`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `GameObject::set_loot_state` now mirrors C++ local GUID storage for any state when caller supplies unit evidence and clears on `None`; `Map::set_gameobject_loot_state_like_cpp` consumes only canonical exact typed `MapObjectRecord::GameObject` records, writes loot state/unit GUID before later represented effects, exposes the unimplemented AI hook as evidence, arms restock only from explicit chest/restock/GameTime/`Loot::IsChanged()` caller evidence and previous zero restock time, returns before collision for doors, and records represented `EnableCollision` only from explicit model evidence using the C++ condition over current GO state and requested loot state. Missing, wrong-kind, and generic/untyped records are explicit no-mutation outcomes. Remaining gaps: no real AI `OnLootStateChanged`, no real `m_goTypeImpl`, no real `GameObjectModel`/BIH/collision geometry, no live `GameTime` source beyond caller input, no real `Loot::IsChanged` unless supplied by caller, no ObjectAccessor/session/fanout/scripts/DB/live callers, install/restart/push, or full runtime loot-state parity.
- [x] **#NEXT.R8.ENTITIES.455** represented `GameObject::SetGoState` -> `EnableCollision` map-owned caller seam.
  Status: represented-complete; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #455 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3771-3793`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:2065-2072`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::set_gameobject_go_state_like_cpp` consumes only canonical exact typed `MapObjectRecord::GameObject` records, writes represented `GameObjectData::State` through `GameObject::set_go_state` before C++ early-return points, and records represented `EnableCollision(state == GO_STATE_READY)` evidence only when explicit model evidence exists, the object is in-world, and C++ `IsTransport()` is false (`GAMEOBJECT_TYPE_TRANSPORT` and `GAMEOBJECT_TYPE_MAP_OBJ_TRANSPORT` both skip collision). Missing, wrong-kind, and generic/untyped GameObject records are explicit no-mutation outcomes; not-in-world typed records still receive the State write while preserving prior collision evidence. Remaining gaps: no AI `OnStateChanged`, no `m_goTypeImpl` hooks, no real `GameObjectModel`, geometry/model-store hydration, collision/BIH/LOS/intersection/height queries, live callers outside this map-owned seam, Transport delayed-add runtime, ObjectAccessor/session/fanout/scripts/AI/zone hooks, install/restart/push, or full runtime state-change parity.
- [x] **#NEXT.R8.ENTITIES.454** represented `GameObject::SetDisplayId` -> `UpdateModel` map-owned caller seam.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #454 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3817-3820`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3867-3880`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:4394-4399`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::set_gameobject_display_id_like_cpp` consumes only canonical exact typed `MapObjectRecord::GameObject` records, writes represented `GameObjectData::DisplayID`/update mask through `GameObject::set_display_id(display_id)` before delegating to `Map::update_gameobject_model_like_cpp`, and returns explicit no-mutation outcomes for missing, wrong-kind, and generic/untyped GameObject records. For not-in-world typed GameObjects the DisplayID write is still preserved before nested `UpdateModel()` returns `NotInWorld`, leaving represented old model key/collision evidence unchanged. Model existence/map-object status remains explicit caller-provided `CreateModel()` evidence; no display/template/DB/model-store inference and no `EnableCollision()` call are added. Remaining gaps: no real `GameObjectModel`, geometry/model-store hydration, BIH/collision/LOS/intersections/heights, ObjectAccessor/session/fanout/scripts/AI/zone hooks, Transport delayed-add runtime, live callers outside this map-owned seam, install/restart/push, or full runtime model refresh parity.
- [x] **#NEXT.R8.ENTITIES.453** represented `GameObject::UpdateModel` map-owned refresh seam.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #453 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3818-3820`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3867-3880`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:4394-4399`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:72-82`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:90-101`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:115-127`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `GameObject::apply_represented_gameobject_model_creation_like_cpp` now treats represented `CreateModel()` as fresh model assignment and clears prior collision evidence on recreation/replacement while preserving `GO_FLAG_MAP_OBJECT` only for explicit `has_model && is_map_object`. `Map::update_gameobject_model_like_cpp` consumes only canonical exact typed `MapObjectRecord::GameObject` state, returns explicit missing/wrong-kind/not-in-world outcomes without mutation, removes an old represented DynamicMapTree key only when the old model evidence exists and the key is registered, clears old model/map-object/collision evidence, applies explicit new `CreateModel()` evidence, and inserts the new represented model key whenever `new_has_model` is true without calling `EnableCollision()`. Remaining gaps: no real `GameObjectModel`, geometry/model-store hydration, BIH/collision/LOS/intersections/heights, display/template/DB inference, live `SetDisplayId` caller wiring, ObjectAccessor/session/fanout/scripts/AI/zone hooks, Transport delayed-add runtime, install/restart/push, or full runtime model refresh parity.
- [x] **#NEXT.R8.ENTITIES.452** represented `GameObject::CreateModel` map-object flag and `AddToWorld` `EnableCollision` seam.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #452 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:911-922`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-943`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3856-3864`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3867-3879`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:4394-4399`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:2914`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `GO_FLAG_MAP_OBJECT` is defined with the C++ value and `GameObject` now owns explicit local represented evidence for `m_model && m_model->isMapObject()` plus the last represented `m_model->enableCollision(enable)` value. Existing `m_model` evidence remains explicit and non-inferred; disabling it clears map-object flag/evidence and collision evidence. `Map::add_map_object_record_to_map_like_cpp` consumes exact typed GameObject model evidence to keep represented DynamicMapTree insertion and record `EnableCollision(toggledState)` evidence using C++ chest loot-state vs non-chest GO-state logic. Transport records remain excluded from immediate insertion/collision and are still a delayed-add gap. Remaining gaps: no real `GameObjectModel`, model geometry/model-store hydration, true collision/BIH/LOS/intersection/height queries, `UpdateModel()` runtime, Transport delayed-add model runtime, ObjectAccessor/session/fanout/scripts/AI/DB, install/restart/push, or full AddToWorld side effects.
- [x] **#NEXT.R8.ENTITIES.451** represented `GameObject::AddToWorld`/`RemoveFromWorld` DynamicMapTree consumption seam.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #451 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:530-576`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3856-3865`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3867-3880`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:4394-4398`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:72-82`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:115-127`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:457-460`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `GameObject` now owns only explicit local represented evidence for C++ `m_model != nullptr`, defaulting false and set only by callers/tests. `Map::add_map_object_record_to_map_like_cpp` consumes that evidence for exact typed `GameObject` records in the normal add path and exposes `AddToMapOutcome::gameobject_model_insert`; already-in-world and no-model paths do not mutate the map-owned DynamicMapTree. `Map::remove_from_map_like_cpp` checks the canonical record before final removal, removes a contained represented key only for in-world exact typed `GameObject` records with explicit evidence, and exposes `RemoveFromMapOutcome::gameobject_model_remove`; missing keys are guarded no-ops. Transport records remain excluded from immediate GameObjectModel insert, matching the C++ delayed-add split. Remaining gaps: no real `GameObjectModel`, model geometry, BIH/LOS/collision, `EnableCollision`, `GO_FLAG_MAP_OBJECT`, DB/model-store hydration, ObjectAccessor/session/fanout/scripts/AI/runtime collision, install/restart/push, or full AddToWorld/RemoveFromWorld side effects.
- [x] **#NEXT.R8.ENTITIES.450** represented `DynamicMapTree` insert/remove/contains model-key seam.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #450 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:72-82`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:90-101`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:115-127`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:457-460`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:926-948`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3867-3880`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:4394-4398`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns represented dynamic-tree model keys by owner `ObjectGuid` and exposes `contains_gameobject_model_like_cpp`, `insert_gameobject_model_like_cpp`, and `remove_gameobject_model_like_cpp` facades. Actual insert/remove increments represented `unbalanced_times` once like C++ `DynTreeImpl::{insert,remove}`; duplicate insert and missing remove are deterministic guarded no-ops to avoid represented key-count drift. `update_dynamic_tree_like_cpp` now derives emptiness from the key set. No real `GameObjectModel`, RegularGrid/BIH, LOS/intersection/height, AddToWorld/RemoveFromWorld wiring, transport delayed-add, `GO_FLAG_MAP_OBJECT`, `EnableCollision`, ObjectAccessor/session/fanout/scripts/AI/DB, install/restart/push, or broader collision runtime is claimed.

- [x] **#NEXT.R8.ENTITIES.449** represented DynamicMapTree update seam at `Map::Update` head.
  Status: represented-complete; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #449 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-668`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:34-38`, `/home/server/woltk-trinity-legacy/src/common/Collision/DynamicTree.cpp:66-101`, `/home/server/woltk-trinity-legacy/src/common/Time/Timer.h:121-143`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns a narrow represented `_dynamicTree` update seam: represented tree emptiness, TimeTracker-like 200ms rebalance remaining timer, and `unbalanced_times`; `update_dynamic_tree_like_cpp(diff_ms)` returns before timer/unbalanced mutation when empty, consumes/resets timer when non-empty, and clears unbalanced count only when timer passes with pending unbalance. `ManagedMap::update(diff_ms)` stores the summary before `update_calls` instrumentation and all later represented phases. #449 did not represent insert/remove/contains, real `GameObjectModel`, RegularGrid/BIH balance, LOS/intersections/heights, AddToWorld/RemoveFromWorld registration, ObjectAccessor/session/fanout/scripts/AI/DB side effects, install/restart/push, or broader collision runtime.

- [x] **#NEXT.R8.ENTITIES.448** represented `Map::Update` tail `OnMapUpdate`/metrics seam.
  Status: represented-complete only for the C++ `Map::Update` tail boundary after `ProcessRelocationNotifies(t_diff)`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #448 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:804-815`, `/home/server/woltk-trinity-legacy/src/server/game/Scripting/ScriptMgr.cpp:1598-1613`, `/home/server/woltk-trinity-legacy/src/server/game/Scripting/ScriptMgr.cpp:1509-1524`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ManagedMap::update(diff_ms)` records a represented `sScriptMgr->OnMapUpdate(this, diff)` boundary invocation immediately after live `process_live_relocation_notifies_like_cpp(...)`, preserving map id, instance id, kind, diff, and `script_dispatch_represented=false`; it then records represented metric counts for canonical typed `Creature` and exact `GameObject` records in `Map::map_objects`. Generic `WorldObject` records, `Player`, `Pet`, `Transport`, `DynamicObject`, and `AreaTrigger` do not count as these metrics. No real ScriptMgr registry/dispatch, `WorldMapScript`/`InstanceMapScript`/`BattlegroundMapScript` callbacks, Prometheus/telemetry backend, ObjectAccessor/session/fanout, DB, or install/restart/push is claimed.
- [x] **#NEXT.R8.ENTITIES.447** represented Map weather update timer/default-weather seam.
  Status: represented-complete only for map-owned `_weatherUpdateTimer` / `_zoneDynamicInfo.DefaultWeather` update/reset seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #447 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:72-73`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:163`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:777-798`, `/home/server/woltk-trinity-legacy/src/common/Time/Timer.h:62-87`, `/home/server/woltk-trinity-legacy/src/server/game/Weather/Weather.cpp:44-65`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns represented zone dynamic info plus an `IntervalTimer`-like 1000ms weather timer; `Map::update_weather_like_cpp(diff_ms)` accumulates current, no-ops without iteration when not passed, calls represented `DefaultWeather::Update(interval)` with 1000ms when passed, removes only the optional represented `DefaultWeather` when update returns false, and resets current with modulo to preserve overshoot. `ManagedMap::update` stores `last_weather_update_summary_like_cpp()` immediately after script schedule processing and before personal phase. No `Weather::ReGenerate`, `UpdateWeather` packet/player fanout, `sWorld->SendZoneMessage`, `sScriptMgr` hooks, DB/WeatherMgr creation, or real zone-player count is claimed.
- [x] **#NEXT.R8.ENTITIES.446** represented Map script-schedule processing seam.
  Status: represented-complete only for map-owned `m_scriptSchedule`/`i_scriptLock` queue/drain at the C++ `Map::Update` order point; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #446 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:777-795`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapScripts.cpp:33-98`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapScripts.cpp:311-321`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapScripts.cpp:895-897`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:108-109`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns a represented sorted absolute-time script schedule plus lock, schedule helper reports represented global-count increment evidence, due processing drains only the sorted due prefix and records represented-executed actions without real command side effects, zero-delay scheduling processes immediately only when unlocked, and `ManagedMap::update` stores script processing summary immediately after `send_object_updates_like_cpp()` and before personal phase/move-list drains. No real `ScriptInfo`, script manager commands, chat/emote/movement/teleport/quest/gossip/item/weather/session/ObjectAccessor/DB/fanout side effects, destruction accounting, or full script runtime is claimed.
- [x] **#NEXT.R8.ENTITIES.445** represented `Map::SendObjectUpdates` consumption seam.
  Status: represented-complete only for map-owned `m_objectUpdated`/changed-mask consumption at the C++ `Map::Update` order point; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #445 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1929-1948`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:791-806`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3722-3739`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::send_object_updates_like_cpp()` snapshots canonical `Map::map_objects` records whose embedded object has `is_object_updated()`, re-resolves each GUID mutably, defensively counts missing/stale and not-in-world states, leaves not-in-world records uncleared because C++ asserts, and represents `WorldObject::BuildUpdate -> ClearUpdateMask(false)` by clearing update masks on in-world records. `ManagedMap::update` records `last_send_object_updates_summary_like_cpp()` after represented ObjectUpdater/Transport/SceneObject visitation and before personal phase. No exact `_updateObjects` set, `UpdateDataMapType`, visible-player iteration, sessions, packets, `SendDirectMessage`, ObjectAccessor/fanout, dynamic-tree, script, weather, or DB side effect is claimed.
- [x] **#NEXT.R8.ENTITIES.444** represented `DelayedUnitRelocation` visibility-plan consumption.
  Status: represented-complete only for map-owned consumption/exposure of `DelayedUnitRelocation` visibility/AI plans before notify reset; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #444 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:830-905`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:137-168`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:171-196`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:199-232`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ProcessRelocationNotifiesOutcome` includes represented `visibility_plans` built after delayed relocation selection and before reset, live contexts come only from canonical `Map::map_objects`, Player delayed relocation selection uses typed `farsight_object`/self as C++ `player->m_seer`, non-self invalid viewpoints are skipped with evidence, typed Creature `is_alive()` drives represented creature AI checks, and `previous_client_guids` stays empty as a documented fanout gap. No real visibility mutation, packet send, session fanout, ObjectAccessor bridge, dynamic-tree, script, or DB side effect is claimed.
- [x] **#NEXT.R8.ENTITIES.443** live `ProcessRelocationNotifies` consumption seam.
  Status: represented-complete only for live `ManagedMap::update -> Map::ProcessRelocationNotifies` source-gating/marked-cell/timer/reset consumption; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #443 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:692-717`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:758-768`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:797-805`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:830-905`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ManagedMap::update(diff_ms)` calls `Map::process_live_relocation_notifies_like_cpp(diff_ms, DEFAULT_VISIBILITY_NOTIFY_PERIOD)` immediately after the #442 Creature/GameObject/AreaTrigger live move-list drains. The map-owned helper derives represented sources only from canonical `Map::map_objects`: in-world typed Players (plus represented `FarsightObject` when present) and in-world active non-Players. It then uses existing `map_update_visit_plan_like_cpp`, `visit_nearby_cells_of_like_cpp`, and `process_relocation_notifies_like_cpp` helpers to consume marked cells, update/reset relocation timers, record delayed relocation evidence, and reset visibility notify flags. With no represented player or active non-player source it returns the default/no-op outcome and preserves notify flags/timers. This does not claim real session/MapRef source ownership, far combat/aura/summon source hydration, exact TypeContainerVisitor order/cell traversal, real delayed relocation notifier side effects, `UpdateObjectVisibility`, `SendObjectUpdates`, ObjectAccessor/session/fanout/packets/dynamic-tree/AI/scripts/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.442** live `Map::Update` delayed move-list drain wiring.
  Status: represented-complete only for live `ManagedMap::update -> Map::MoveAll{Creature,GameObject,AreaTrigger}InMoveList` consumption; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #442 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:797-805`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1239-1416`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1418-1481`.
  Rust targets: `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ManagedMap::update(diff_ms)` stores a narrow live move-list drain summary and, immediately after `Map::update_personal_phase_tracker_like_cpp(diff_ms)`, drains the existing map-owned Creature, GameObject, and AreaTrigger delayed move-lists in C++ order. The live tick intentionally does not call the DynamicObject move-list drain because C++ `Map::Update` does not. Coverage goes through `MapManager::update`, proves queued in-world typed Creature/GameObject/AreaTrigger delayed moves are drained/relocated with summary counts, and proves a queued DynamicObject move remains queued after the live manager tick. Remaining gaps: `ProcessRelocationNotifies` live wiring, visibility/fanout/UpdatePositionData/AfterRelocation/UpdateShape/dynamic-tree/vehicle passenger/script/AI/DB/session runtime, full movement runtime parity.
- [x] **#NEXT.R8.ENTITIES.441** Map-owned delayed cell/grid move-list seam.
  Status: represented-complete only for map-owned delayed Creature/GameObject/DynamicObject/AreaTrigger cell move-list queues and drains; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #441 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapObject.h:28-33`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapObject.h:41-57`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:566-579`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1163-1237`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1239-1416`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1418-1481`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1561-1627`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1646-1651`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` owns per-family move-list vectors, pending `_moveState`/`_newPosition`-like state, add/remove facades, and family drains over canonical `map_objects`; duplicate Add updates position without duplicate queue entries, Remove marks active entries inactive, drains tolerate missing/wrong-kind/not-in-world/stale cases without dummy records or panic, active in-world records delegate to `relocate_map_object_like_cpp`, and `run_unload_lifecycle` consumes Creature/GameObject/AreaTrigger drains only when `!unload_all`, before/after the evacuator. `unload_grid_at(..., true)` does not drain or relocate move-lists; full-unload semantics are limited to `clear_unload_all_delayed_moves_like_cpp()` clearing the Creature/GameObject delayed queues like `Map::UnloadAll` without clearing AreaTrigger/DynamicObject or faking relocation. DynamicObject is intentionally not drained by unload. Remaining gaps: real respawn relocation/Pet::Remove, UpdatePositionData, visibility/fanout/AfterRelocation/UpdateShape, vehicle passenger relocation, dynamic tree, ObjectAccessor/session/DB/scripts/AI, live `Map::UnloadAll` entrypoint and live movement callsites.
- [x] **#NEXT.R8.ENTITIES.440** live MultiPersonalPhaseTracker update consumption seam.
  Status: represented-complete only for live map-owned `MultiPersonalPhaseTracker::Update` expiry consumption through `ManagedMap::update`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #440 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:797-798`, `/home/server/woltk-trinity-legacy/src/server/game/Phasing/PersonalPhaseTracker.h:32-40,68-80`, `/home/server/woltk-trinity-legacy/src/server/game/Phasing/PersonalPhaseTracker.cpp:62-78`, `/home/server/woltk-trinity-legacy/src/server/game/Phasing/PersonalPhaseTracker.cpp:106-113`, `/home/server/woltk-trinity-legacy/src/server/game/Phasing/PersonalPhaseTracker.cpp:192-202`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_personal_phase_tracker_like_cpp(diff_ms)` consumes only `Map.personal_phase_tracker.update(diff_ms)` as the source of expired GUIDs, delegates each GUID to the existing canonical `add_object_to_remove_list_like_cpp` over `Map::map_objects`, reports queued/duplicate/missing/unsupported outcomes, and avoids dummy records, ObjectAccessor/session/DB/fanout/scripts/dynamic-tree and local remove-list drain. `ManagedMap::update` stores the summary and the existing delayed update drains afterward in the same effective manager tick.
- [x] **#NEXT.R8.ENTITIES.439** Transport ObjectUpdater visitation seam.
  Status: represented-complete only for map-owned typed Transport visitation and local `Transport::Update` timer/path-progress/stop evidence through live `ManagedMap::update`; review `APROBADO` after one bounded correction; CI `CI_OK`; validation OK; committed locally at `current #439 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Transport/Transport.cpp:179-251`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Transport/Transport.cpp:520-612`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Transport/Transport.cpp:700-775`.
  Rust targets: `crates/wow-entities/src/transport.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Transport::update_like_cpp(diff_ms, now_ms, current_map_id)` safely rejects period zero without division/path-progress mutation, represents local path-progress advancement for allow-stopping and non-allow-stopping transports, handles stop requests inside the tick with justStopped GO state/dynflag evidence, resets cycle event evidence, updates client path-progress, and records 200ms expected-map gated position-update due evidence without real `ComputePosition`. `Map::update_transports_like_cpp(diff_ms, now_ms)` snapshots only canonical typed Transport records from `Map::map_objects`, validates missing/non-Transport/untyped outcomes without mutation, delegates typed Transport records even when their embedded WorldObject is not in-world like C++ `Map::_transports`, and `ManagedMap::update(diff_ms)` consumes the summary after GameObject and before later represented families. Remaining gaps: exact TypeContainerVisitor/`_transports` order, AI/script hooks, GameEvents, real spline `ComputePosition`, path-leg map transfer, `TeleportTransport`, grid/model movement, static passenger DB spawning/removal, passenger relocation/fall info, packet fanout/`BuildUpdate`/`SendObjectUpdates`, full `TransportMgr`, ObjectAccessor/session/fanout/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.438** GameObject ObjectUpdater visitation seam.
  Status: represented-complete only for map-owned GameObject ObjectUpdater visitation and local `m_despawnDelay` countdown/remove-list enqueue through live `ManagedMap::update`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #438 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1215-1233`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1235-1274`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1276+`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1711-1729`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1740-1764`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_game_objects_like_cpp(diff_ms)` snapshots only canonical typed GameObject GUIDs from `Map::map_objects`, then calls `update_game_object_like_cpp` per GUID and reports a narrow summary. Missing/non-GameObject/not-in-world outcomes do not mutate or enqueue. In-world typed GameObjects consume `GameObject::update_like_cpp(diff_ms)` for the represented `m_despawnDelay` branch only; non-expiry decrements the delay, expiry represents `DespawnOrUnsummon(0ms, m_despawnRespawnTime)` by setting `GO_NOT_READY` and enqueueing through the existing remove-list without draining. `ManagedMap::update(diff_ms)` consumes the summary and delayed update drains after the family visits. Remaining gaps: exact TypeContainerVisitor cell traversal/order, real GameObject AI/AIM, `m_goTypeImpl`, per-player state/visibility packets, full loot-state machine, `SaveRespawnTime`, PoolMgr, `SendGameObjectDespawn`, Transport full update, ObjectAccessor/session/fanout/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.437** SceneObject ObjectUpdater visitation seam.
  Status: represented-complete only for map-owned SceneObject ObjectUpdater visitation and conditional remove-list enqueue through live `ManagedMap::update`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #437 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/SceneObject/SceneObject.cpp:58-90`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_scene_objects_like_cpp(diff_ms, context_resolver)` snapshots only canonical typed SceneObject GUIDs from `Map::map_objects`, resolves an explicit represented ObjectAccessor/Aura context before per-object update, and delegates to `update_scene_object_like_cpp`. Missing/non-SceneObject/not-in-world outcomes do not mutate or enqueue. In-world typed SceneObjects represent `WorldObject::Update(diff)` as evidence, call existing `SceneObject::should_be_removed(creator_exists, linked_aura_exists)`, and enqueue through the existing remove-list when the represented creator/aura context says removal is required while preserving records until delayed drain. `ManagedMap::update(diff_ms)` consumes the summary after Conversation with a conservative default context (`creator_exists=true`, `linked_aura_exists=true`) so lack of real ObjectAccessor/Aura runtime does not delete SceneObjects by default. Remaining gaps: real `ObjectAccessor::GetUnit`, Aura lookup by spell/cast id, exact TypeContainerVisitor order/cell traversal, players/sessions, GameObject/Transport update, `SendObjectUpdates`, scripts, fanout, DB, packets/visibility, remove-list drain beyond existing delayed seam.
- [x] **#NEXT.R8.ENTITIES.436** live `MapManager`/`ManagedMap` Conversation update visitation seam.
  Status: represented-complete only for map-owned Conversation ObjectUpdater visitation/duration-expiry -> remove-list enqueue through live `ManagedMap::update`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #436 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Conversation/Conversation.cpp:67-87`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_conversations_like_cpp(diff_ms)` snapshots only canonical typed Conversation GUIDs from `Map::map_objects`, then calls `update_conversation_like_cpp` per GUID and reports a narrow summary. Missing/non-Conversation/not-in-world outcomes do not mutate or enqueue. In-world typed Conversations represent `sScriptMgr->OnConversationUpdate` before duration handling; non-expiry decrements `Conversation::duration_ms` and reports represented `WorldObject::Update`, while expiry uses `Conversation::update_duration(diff_ms)` then delegates to the existing remove-list without draining. `ManagedMap::update(diff_ms)` consumes the summary after AreaTrigger; exact TypeContainerVisitor/cell traversal ordering remains a gap. Remaining gaps: GameObject/SceneObject update visitation, full `Map::Update` nearby-cell traversal/player/session update, real scripts/fanout/ObjectAccessor/visibility/DB/push/install/restart.
- [x] **#NEXT.R8.ENTITIES.435** live `MapManager`/`ManagedMap` Creature update visitation seam.
  Status: represented/complete only for map-owned Creature ObjectUpdater visitation through existing `Creature::runtime_update_plan`; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #435 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:696-903`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_creatures_like_cpp(diff_ms, now_secs, context_resolver)` snapshots only canonical typed Creature GUIDs from `Map::map_objects`, resolves a represented `CreatureRuntimeUpdateContext` before mutable access, and calls `update_creature_like_cpp` per GUID. Missing/non-Creature/not-in-world outcomes are counted without mutation; in-world typed Creature records consume `Creature::runtime_update_plan(diff_ms, now_secs, context)`, and `ManagedMap::update(diff_ms)` calls this after DynamicObject and before AreaTrigger using `GameTime::now().as_secs()` converted saturating to `i64` plus default context. Checks: `cargo fmt --check`; `cargo test -p wow-map creature_update -- --nocapture` (4 passed); `cargo test -p wow-map map_manager_update -- --nocapture` (11 passed); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server`; TSV 9-column verification; `git diff --check`. Limits: no full `Map::Update`, no nearby-cell traversal, no GameObject/SceneObject/Conversation update, no Pet forcing, no real AI/scripts/Unit::Update/threat/focus/combat pulse fanout/regen math beyond represented plan, no ObjectAccessor/session/fanout/visibility/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.434** live `MapManager`/`ManagedMap` AreaTrigger update visitation seam.
  Status: represented/complete only for map-owned AreaTrigger ObjectUpdater visitation/duration expiry -> remove-list enqueue; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #434 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/AreaTrigger/AreaTrigger.cpp:297-372`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_area_triggers_like_cpp(diff_ms)` snapshots only canonical typed AreaTrigger GUIDs from `Map::map_objects`, then calls `update_area_trigger_like_cpp` per GUID and reports a narrow summary. The helper returns explicit missing/non-AreaTrigger/not-in-world outcomes without dummy records, increments represented time/duration through `AreaTrigger::update_time_and_duration`, and on expiry enqueues the existing remove-list while preserving the record until delayed drain. `ManagedMap::update(diff_ms)` calls this after DynamicObject; `ManagedMap::delayed_update(diff_ms)` drains afterward in the same effective tick. Remaining gaps: full `Map::Update` nearby-cell traversal/player/session update, other ObjectUpdater families, real non-static AreaTrigger movement/orbit/attached/spline/stationary/shape, AI, target-list runtime, ObjectAccessor/session/fanout/visibility/dynamic-tree/scripts/packets/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.433** live `MapManager`/`ManagedMap` DynamicObject update visitation seam.
  Status: represented/complete only for live `MapManager -> ManagedMap -> Map` DynamicObject update visitation seam; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #433 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-785`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264,296-301`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:136-171`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_dynamic_objects_like_cpp(diff_ms)` snapshots only canonical typed DynamicObject GUIDs (`AccessorObjectKind::DynamicObject` plus preserved typed body) from `Map::map_objects`, then calls `update_dynamic_object_like_cpp` per GUID and reports a narrow summary. `ManagedMap::update(diff_ms)` preserves update-call instrumentation and calls that map-owned method; DynamicObject expiry enqueues the existing remove-list and `ManagedMap::delayed_update(diff_ms)` drains afterward in the same effective tick. Remaining gaps: full `Map::Update`/nearby-cell traversal, player/session update, non-DynamicObject ObjectUpdater families, dynamic tree/collision, SendObjectUpdates, scripts/AI, ObjectAccessor/session/fanout/visibility, real Aura runtime, DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.432** live `Map::DelayedUpdate` remove-list drain in `MapManager` tick.
  Status: complete for the bounded live delayed-update drain only; review `APROBADO`; CI `CI_OK`; validation OK; committed locally at `current #432 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2520-2545`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2646`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:136-171`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:258-264`.
  Rust targets: `crates/wow-map/src/manager.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ManagedMap::delayed_update(diff_ms)` preserves existing instrumentation and calls the canonical map-owned `Map::remove_all_objects_in_remove_list_like_cpp()` drain from the live `MapManager::update` delayed-update loop. Focused coverage proves a queued typed DynamicObject is present before the manager tick, `delayed_update_calls == &[1]`, and the canonical `Map::map_objects` record plus remove-list queue are gone after the tick. Complete only for live `MapManager::update -> ManagedMap::delayed_update -> remove_all_objects_in_remove_list_like_cpp` drain. Remaining gaps: `Map::Update` live object visitation, far spell callbacks, grid state update side effects beyond existing no-op lifecycle, ObjectAccessor/session/fanout/dynamic-tree/scripts/AI/DB persistence, full Aura runtime, install/restart/push.
- [x] **#NEXT.R8.ENTITIES.431** DynamicObject `Update` aura-bound represented branch.
  Status: represented/complete for the bounded map-owned aura-bound `DynamicObject::Update` branch only; review `APROBADO`; validation OK; committed locally at `current #431 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:136-165`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:167-171`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:200-207`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:695-717`.
  Rust targets: `crates/wow-entities/src/dynamic_object.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `DynamicObject` now carries represented active-aura removed/expired flags plus `Aura::UpdateOwner(p_time,this)` call evidence; `set_aura_bound()` resets active-aura flags and pending-delete state. `Map::update_dynamic_object_like_cpp` consumes in-world aura-bound typed records through the represented aura helper, reports `Updated` with represented script-update evidence on non-expiry, and delegates expiry to the existing remove-list facade while preserving the record until drain/#430 cleanup. Complete only for this represented branch: no real Aura runtime, no real `Aura::UpdateOwner`, no real `_Remove`, no target applications, no script manager execution, no ObjectAccessor/fanout/DynamicTree/packets/DB/install/restart/push.
- [x] **#NEXT.R8.ENTITIES.430** DynamicObject `RemoveFromWorld` aura/unbind cleanup seam.
  Status: represented/complete for the bounded map-owned DynamicObject `RemoveFromWorld` local `RemoveAura()` + `UnbindFromCaster()` state cleanup during `Map::remove_from_map_like_cpp` only; review `APROBADO`; validation OK; committed locally at `current #430 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:63-80`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:200-207`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:218-225`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:242-247`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::remove_from_map_like_cpp` keeps the existing in-world DynamicObject caster-viewpoint cleanup first, then while the canonical typed `MapObjectRecord::DynamicObject` is still in `Map::map_objects` represents `RemoveAura()` by moving local aura-bound state to pending-delete evidence and represents `UnbindFromCaster()` by clearing the represented bound caster and reporting the previous caster GUID. Non-DynamicObject, missing, and not-in-world records skip this special cleanup; in-world DynamicObjects without aura/caster report explicit false/no-op evidence. Remaining gaps: aura-bound `DynamicObject::Update`, real `Aura::_Remove`, target maps/applications, ObjectAccessor/session/fanout/scripts/DB/DynamicTree and full DynamicObject lifecycle.

- [x] **#NEXT.R8.ENTITIES.429** `DynamicObject::Update` non-aura expiry -> remove-list enqueue seam.
  Status: represented/complete for the bounded map-owned non-aura Update expiry -> remove-list enqueue seam only; review `APROBADO`; validation OK; committed locally at `current #429 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:136-165`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:167-171`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2547-2555`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::update_dynamic_object_like_cpp(dynamic_object_guid, elapsed_ms)` reads and mutates only canonical typed `MapObjectRecord::DynamicObject` state. Missing, non-DynamicObject/untyped, not-in-world, and aura-bound records return explicit no-mutation/no-queue outcomes. In-world non-aura records consume `DynamicObject::update_non_aura_duration(elapsed_ms)`; non-expiry reports represented script-update evidence only, while expiry drops the mutable borrow and delegates to `add_object_to_remove_list_like_cpp` without draining. Complete only for “Map-owned DynamicObject non-aura Update expiry -> remove-list enqueue seam only”. Not aura update/removal, not `_Remove`, not `RemoveAura`, not `UnbindFromCaster`, not ObjectAccessor store removal, not scripts `OnDynamicObjectUpdate`, not DynamicTree/fanout/visibility/SetSeer, not packet/session mirrors, not Spell handler wiring, not full Map::Update visitor, not DB/install/restart/push.

- [x] **#NEXT.R8.ENTITIES.428** `Spell::EffectAddFarsight` DynamicObject creation seam.
  Status: complete for the bounded map-owned `Map::create_farsight_dynamic_object_like_cpp` creation/materialization seam only; review `APROBADO`; validation OK; committed locally at `current #428 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:84-133`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:209-225`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:233-239`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::create_farsight_dynamic_object_like_cpp` validates a canonical typed Player caster in this Map and in-world before consuming a map-owned DynamicObject low GUID, rejects invalid destination/map-id/spell-id/cast-time inputs without map mutation as an explicit #428 seam guard, creates `DynamicObject::new(true)` with a world-object GUID using the spell id and generated low guid, binds it to the caster, sets represented CreateDynamicObject fields (entry, scale, caster, type `FarsightFocus`, spell visual, spell id, radius, cast time, duration, active, map/instance/position and inherited phase shift), inserts it through `MapObjectRecord::new_dynamic_object` + `add_map_object_record_to_map_like_cpp`, then calls `apply_dynamic_object_caster_viewpoint_like_cpp(dynamic_guid,true)` and reports that outcome. Complete only for this map-owned creation seam. Not a live Spell handler, not spell target store/SpellInfo runtime, not ObjectAccessor `_RegisterDynObject`, not transport passenger offset, not `UpdatePositionData`/ZoneScript, not aura/update lifecycle, not real SetSeer/visibility fanout, not packets/session mirrors, not DB/install/restart/push.

- [x] **#NEXT.R8.ENTITIES.427** DynamicObject `RemoveFromWorld` caster-viewpoint cleanup during map removal.
  Status: complete for map-owned DynamicObject RemoveFromWorld caster-viewpoint cleanup during `Map::remove_from_map_like_cpp` only; review `APROBADO`; validation OK; committed locally at `current #427 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:63-80`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:218-225`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::remove_from_map_like_cpp` now inspects the canonical `MapObjectRecord` before extraction and, only for typed in-world DynamicObjects with `is_caster_viewpoint=true`, calls `apply_dynamic_object_caster_viewpoint_like_cpp(guid,false)` while Player and DynamicObject are still canonical. The helper resolves only `bound_caster()` to a typed Player, clears Player `FarsightObject` only on matching DynamicObject viewpoint, reports represented SetSeer on successful removal, toggles DynamicObject caster_viewpoint false even when Player removal mismatches, and then existing map-local removal proceeds. Non-viewpoint and not-in-world DynamicObjects skip cleanup and do not mutate Player state. Not full `RemoveAura`, not `UnbindFromCaster`/caster dynobject unregister, not ObjectAccessor store removal identity, not real SetSeer/visibility fanout, not Spell::EffectAddFarsight or aura/cinematic callsite wiring, not full DynamicObject lifecycle, not packets/session mirrors, not DB/install/restart/push.

- [x] **#NEXT.R8.ENTITIES.426** DynamicObject caster `SetViewpoint` map-owned seam.
  Status: complete for bounded map-owned DynamicObject caster-viewpoint helper only; review `APROBADO`; validation OK; committed locally at `current #426 HEAD`; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:209-225; /home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:233-239`, `/home/server/woltk-trinity-legacy/src/server/game/Spells/Auras/SpellAuraEffects.cpp:4983-4996`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/CinematicMgr.cpp:69-75,92-99`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::apply_dynamic_object_caster_viewpoint_like_cpp(dynamic_object_guid,apply)` reads only canonical `Map::map_objects`, validates the typed DynamicObject first, resolves the Player caster from canonical `DynamicObject::bound_caster()` / represented `_caster` only, applies Player::SetViewpoint-like FarsightObject semantics for a DynamicObject target without Unit shared-vision/SetWorldObject, and then toggles DynamicObject `caster_viewpoint` after the Player helper result whenever typed DynamicObject plus bound typed Player caster exist. Existing-viewpoint and mismatch early returns leave Player state/request booleans unchanged but still toggle the DynamicObject flag, matching the C++ caller quirk. Missing DynamicObject, absent bound caster, or bound caster missing/not Player creates no records, does not mutate Player state, and does not toggle the DynamicObject. Not full `Player::SetViewpoint`, not live ObjectAccessor `BindToCaster`, no fallback from `caster_guid`, not live aura/cinematic/DynamicObject lifecycle wiring, not real `SetSeer`/`UpdateVisibilityOf` fanout, not ObjectAccessor/session/packet/DB runtime.


- [x] **#NEXT.R8.ENTITIES.425** `CMSG_FAR_SIGHT` represented `SetSeer`/visibility handler seam.
  Status: complete for the represented CMSG_FAR_SIGHT parser/handler + session `m_seer` GUID seam only; review `APROBADO`; validation OK; committed locally at current #425 HEAD; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/MiscPackets.h:712-720`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/MiscPackets.cpp:537-540`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MiscHandler.cpp:863-880`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h:2432-2438`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25389-25395`.
  Rust targets: `crates/wow-packet/src/packets/misc.rs`, `crates/wow-world/src/handlers/misc.rs`, `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: packet parser reads exactly one `Enable` bit for `CMSG_FAR_SIGHT`; handler is inventory-registered and dispatch-wired, switches only the represented session `m_seer` GUID seam to self or the current canonical `ActivePlayerData::farsight_object` when that target matches C++ `TYPEMASK_SEER` (`Player`, Unit-like `Creature`/`Pet`, or `DynamicObject`), never mutates `farsight_object` or calls `Map::apply_player_set_viewpoint_unit_like_cpp`, and forces the existing visibility refresh wrapper after parsing. Missing player/viewpoint/target or non-`TYPEMASK_SEER` targets keep current seer and still request refresh. Remaining gaps: full SetSeer fanout/grid notifier/ObjectAccessor visibility, aura/cinematic/dynamic-object SetViewpoint callsites, full `Player::SetViewpoint`, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.424** Map-owned `Player::SetViewpoint` Unit-target shared-vision consumption seam.
  Status: complete for map-owned Player::SetViewpoint Unit-target shared-vision consumption seam only; review `APROBADO`; validation OK; committed locally at current #424 HEAD; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:6489-6509`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:910-916`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2557-2594`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::apply_player_set_viewpoint_unit_like_cpp(player_guid,target_guid,apply,vehicle_base_guid)` reads only canonical `Map::map_objects`, mutates typed `MapObjectRecord::Player` FarsightObject and typed Creature/Pet Unit shared-vision state, and immediately consumes emitted Unit SetWorldObject DTOs through the map-owned facade without draining queues. It reports missing Player, missing/untyped target, existing viewpoint, mismatch, and vehicle-base skip as explicit outcomes; `UpdateVisibilityOf` and `SetSeer` are represented only as outcome booleans. Not full `Player::SetViewpoint`, not real `SetSeer`, not visibility fanout/ObjectAccessor/session/aura callsite wiring, not DynamicObject/Cinematic/FarSight handlers, not full AddToWorld/RemoveFromWorld, no install/restart/push.

- [x] **#NEXT.R8.ENTITIES.423** Map bridge for Unit shared-vision `SetWorldObject` requests.
-  Status: complete for the explicit request -> Map facade bridge only; review `APROBADO`; validation OK; committed locally; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:6489-6509`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:910-916`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2557-2572`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2594`.
  Rust targets: `crates/wow-entities/src/lib.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow-entities` publicly reexports `UnitSharedVisionSetWorldObjectRequestLikeCpp`, and `Map::apply_unit_shared_vision_set_world_object_request_like_cpp` consumes that DTO by delegating to `Map::set_world_object_like_cpp(request.unit_guid, request.on)`. Ownership remains map-owned over canonical `map_objects` and `objects_to_switch`; missing/stale, not-in-world, and ignored non-unit outcomes remain the existing facade fallbacks without dummy records or queue creation. Focused wrapper tests cover enqueue+drain, opposite-toggle cancellation, and fallback/no-record creation. Remaining gaps stay live `Player::SetViewpoint` callsites, fanout/visibility/ObjectAccessor/AddToWorld/RemoveFromWorld/scripts/AI, session wiring, and full shared-vision runtime.

- [x] **#NEXT.R8.ENTITIES.422** Unit shared-vision `SetWorldObject` request seam.
  Status: complete for Unit shared-vision SetWorldObject request seam only; review APROBADO; validation OK; committed locally; no push/install/restart.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:6489-6509`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25387`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:910-916`.
  Rust targets: `crates/wow-entities/src/unit.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Unit::add_player_to_vision_like_cpp` / `remove_player_from_vision_like_cpp` update only local shared-vision state plus `WorldObject::is_active` and return an explicit SetWorldObject request for a future map-owned caller. Empty->non-empty add requests `on=true`; non-empty/duplicate adds do not request again; removing while another viewer remains does not request; removing the last or an absent viewer from an already-empty set requests `on=false` like C++ `list::remove` then `empty()`. No `wow-map` dependency or live map enqueue is introduced. Not full `Player::SetViewpoint`, SetSeer/FarsightObject/UpdateVisibilityOf/fanout, live map wiring, or full `WorldObject::SetWorldObject`.

- [x] **#NEXT.R8.ENTITIES.421** Map-owned `WorldObject::SetWorldObject` enqueue facade for canonical records.
  Status: complete for the map-owned facade only; no live callsite/fanout/AddToWorld completion.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:910-916`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2557-2572`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2594`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:918-925`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:723-725`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.h:365`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::set_world_object_like_cpp(guid,on)` reads only the canonical `MapObjectRecord`, returns missing/stale without insertion, returns not-in-world without enqueue/mutation, and delegates in-world records to `add_object_to_switch_list_like_cpp`. Non-unit in-world records surface the existing ignored outcome, opposite toggles cancel through the existing switch-list semantics, and `Creature::is_temp_world_object` still changes only when `remove_all_objects_in_remove_list_like_cpp()` drains the queue. Remaining gaps: live `WorldObject::SetWorldObject` callsites, fanout/visibility/ObjectAccessor/dynamic-tree/scripts/AI, full AddToWorld/RemoveFromWorld and runtime ownership.

- [x] **#NEXT.R8.ENTITIES.420** Map-owned switch-list drain for temporary Unit world-object container toggles.
  Status: complete after review `APROBADO`, focused validation, and local commit. Complete only for map-local queue+drain/container move.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:345-346`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:651-652`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2557-2572`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2594`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:260-305`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:910-925`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:723-724`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.h:365`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-entities/src/creature.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow_map::Map` owns `objects_to_switch`; enqueue validates canonical `MapObjectRecord`, supports Creature/Pet only, cancels opposite toggles, reports duplicate same-direction conflicts, and drain runs before `objects_to_remove`, moving GUIDs between existing loaded-grid Creature buckets and updating `Creature::is_temp_world_object` only on executed switches. Stale/missing and unloaded-grid cases drain without dummy insertion or grid creation. Remaining gaps: live `WorldObject::SetWorldObject` callsites/fanout, full visibility/AddToWorld/RemoveFromWorld/scripts/AI/ObjectAccessor/dynamic-tree/DB persistence/full Unit shared vision runtime; do not claim full `#NEXT.R8.ENTITIES.021`.

- [x] **#NEXT.R8.ENTITIES.419** Map deferred remove-list physical removal seam.
  Status: complete for map-owned deferred remove-list queue + physical map-local drain only.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2547-2555`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2646`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1826-1835`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2034-2055`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:933-951`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow_map::Map` owns `objects_to_remove`; `add_object_to_remove_list_like_cpp` validates canonical `MapObjectRecord`, marks destroyed and runs represented cleanup before enqueue, `remove_all_objects_in_remove_list_like_cpp` drains GUIDs via `remove_from_map_like_cpp(..., true)`, tolerates missing/stale GUIDs without dummy insertion, applies Creature second cleanup, and `despawn_all_by_spawn_id_like_cpp` queues instead of physically removing. Pool safe despawn actions queue through the same seam. Remaining gaps: switch-list, visibility/fanout, ObjectAccessor, dynamic tree, full per-type RemoveFromMap side effects, scripts/AI, corpse ObjectAccessor path, DB/install/restart/push.

- [x] **#NEXT.R8.ENTITIES.418** `Map::UnloadGrid` applies GUID unload actions and purges personal phases.
  Status: complete (represented action application/personal-phase purge only; no physical remove-list/fanout).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1561-1627`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2547-2555`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2646`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:35-63`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:232-245`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:260-267`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/grid_unload.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: live `Map::run_unload_lifecycle` drains/applies represented `GridUnloadAction`s to canonical `Map::map_objects` after evacuator/cleaner/unloader, `MapGridHost::stop_grid_objects` drains stoper actions for Active->Idle, and personal phase trackers are purged before unloader. Fallback lifecycles with no emitted actions remain no-op. This is not full unload/remove-list parity: `DeleteObject` remains represented by flags/requests on entities, and full `RemoveAllObjectsInRemoveList` physical removal/fanout, per-type `RemoveFromMap` side effects, ObjectAccessor/session visibility, dynamic tree, corpse unloader parity and full lifecycle cleanup remain pending.

- [x] **#NEXT.R8.ENTITIES.417** Map-owned GridUnloadEntityStore bridge.
  Status: complete (map-owned bridge only; no live unload/remove-list fanout).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:35-48`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:50-63`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1561-1613`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2551-2554`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2631-2645`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow_map::Map` now implements `GridUnloadEntityStore` directly over canonical `Map::map_objects`/typed `MapObjectRecord` accessors for Creature, GameObject, DynamicObject, Corpse, AreaTrigger, SceneObject and Conversation. `apply_grid_unload_action(s)` can mutate the canonical map-owned records for represented respawn-relocation, cleanup and delete-request actions, returning `MissingEntity` for absent GUIDs or type mismatches without inserting entities, removing `MapObjectRecord`s, touching `WorldSession`, or executing the live remove-list. Complete only for this bridge; live `Map::UnloadGrid`, remove-list execution, cross-session fanout, full lifecycle cleanup and corpse unloader parity remain pending under the map runtime/#021 follow-ups.

- [x] **#NEXT.R8.ENTITIES.416** Canonical Player map-transfer stale-map cleanup seam (transfer/sync cleanup seam only; not full Player transfer lifecycle/AddPlayerToMap).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:1452-1457`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MovementHandler.cpp:76-89`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MovementHandler.cpp:101-123`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:427-464`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:907-930`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::ensure_canonical_world_map_for_current_player_like_cpp()` now syncs the current Player snapshot through a full-`MapManager` helper. Before inserting/replacing into the destination `ManagedMap`, the helper scans existing canonical maps and calls `remove_from_map_like_cpp(guid, false)` on any non-target map that still owns the same typed `MapObjectRecord::Player`, treating `ObjectNotFound` as no-op. The existing destination-map helper still preserves mutable gameplay state for same-target replacement. Focused session tests cover cross-map cleanup while preserving another player and creature in the old map, same-target mutable-state preservation, and rejected non-world maps not creating/removing maps. Complete only for this stale-map cleanup seam; it does not implement full transfer packets, transport copies, phase/fanout/scripts, grid visibility, `AddPlayerToMap`, or full Player lifecycle/runtime.

- [x] **#NEXT.R8.ENTITIES.415** Player logout cleanup removes canonical map Player before ObjectAccessor (cleanup seam only; not full Player lifecycle/runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/CharacterHandler.cpp:1190-1199`, `/home/server/woltk-trinity-legacy/src/server/game/Server/WorldSession.cpp:667-680`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:907-930`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:315-320`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:1539-1578`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:297-309`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::cleanup_shared_runtime_state()` now first asks the existing canonical `wow_map::Map` instance that already owns the typed `MapObjectRecord::Player` to run `remove_from_map_like_cpp(guid, true)` before unregistering `PlayerRegistry`, `ObjectAccessor.players`, and represented inventory state. The helper searches existing maps by current player map id/instance, does not create maps, treats missing manager/map/player as C++ `FindMap()`-style no-op, and never lets `ObjectAccessor` write back into `Map`. Focused session tests cover logout cleanup removal from canonical map plus ObjectAccessor/inventory cleanup, preserving another player and creature, no map creation for missing map, and the disconnect cleanup delegation path. Complete only for this cleanup seam; it does not claim full Player lifecycle, item AddToWorld/RemoveFromWorld, UpdateObject visibility, RemoveUpdateObject, scripts, combat/phase cleanup, SaveToDB complete, cross-map transfers, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.414** WorldSession -> ObjectAccessor typed Player sync (bridge seam only; not full Player lifecycle/runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/CharacterHandler.cpp:1190-1199`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h:93-109`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:227-239`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:252-280`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:297-309`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:1539-1578`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:316-320`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:907-930`, `/home/server/woltk-trinity-legacy/src/server/game/Server/WorldSession.cpp:667-680`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::sync_object_accessor_player()` now builds the represented canonical `Player` snapshot via `canonical_player_entity_snapshot_like_cpp()` and registers it through `ObjectAccessor::add_player_entity_with_inventory_and_items`, so `ObjectAccessor.players` represents C++ `HashMapHolder<Player>` for connected session players. Existing inventory/item snapshots remain registered and legacy `find_connected_player`/type-mask item lookup continue through `AccessorPlayer::object()` on the typed body. Focused tests cover typed `find_connected_player_entity`/`find_player_entity`, name/map/position/phase preservation, legacy view exposure, inventory item lookup, and replacement of position/level/health/phase snapshots on resync. Complete only for this WorldSession -> typed ObjectAccessor bridge seam; it does not complete full Player lifecycle AddToMap/RemoveFromMap, item AddToWorld, SaveToDB, cross-map instance ownership, or real ObjectAccessor pointer identity.

- [x] **#NEXT.R8.ENTITIES.413** ObjectAccessor typed Player HashMapHolder seam (typed global Player registry read API only; not full Player/ObjectAccessor runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h:63-78`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:101-118`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:120-169`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:206-214`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:227-239`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:252-264`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3434-3437`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ObjectAccessor.players` remains the global Player registry and can preserve either legacy `WorldObject` entries or a typed `Player` body. New typed registration and lookup helpers return `&Player` only for typed entries, only when connected/in-world and same-map as required by C++ `HashMapHolder<Player>`/`GetPlayer(Map*, guid)`; typed map-source helper uses source map/instance only as a context guard and never reads `MapObjectRecord::Player`. Existing legacy `WorldObject` player lookup, high-GUID dispatch, `TYPEMASK_PLAYER`, and player-first `GetUnit` paths still resolve from the global registry. Complete only for this seam; it does not implement Player login lifecycle, full ObjectAccessor/AddToWorld/fanout/visibility, inventory/equipment runtime, update fields, auras/threat/motion, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.412** ObjectAccessor typed Pet/Transport map-local seams (typed read API only; not full ObjectAccessor/runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h:63-78`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:101-118`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:120-169`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:176-184`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:206-224`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:241-249`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:390-399`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3454-3470`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds canonical typed `MapObjectRecord` bodies, constructors, typed record accessors, and C++-shaped map-source typed ObjectAccessor read helpers for `Pet` and `Transport`. Helpers read only `ObjectAccessorMapSource::map_object_record(guid)`, validate context/source map and instance, allowed kind, and same-map record object, then return typed bodies only when preserved; generic `WorldObject` fallback remains for existing `&WorldObject` helpers and is rejected by typed APIs. `Transport` preserves its embedded `GameObject` for typed GameObject lookup/type-mask parity while `GetTransport` requires a real typed `Transport` body. Complete only for this typed map-local read seam; it does not complete full Pet/Transport runtime, full ObjectAccessor, live AddToWorld/fanout/scripts/visibility, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.411** ObjectAccessor typed non-Creature/GameObject map-local seams (typed read API only; not full ObjectAccessor/runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h:63-78`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:101-118`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:120-169`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:171-204`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:390-399`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3419-3471`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds canonical typed `MapObjectRecord` bodies and C++-shaped map-source typed ObjectAccessor read helpers for `DynamicObject`, `AreaTrigger`, `Corpse`, `SceneObject`, and `Conversation`. Helpers read only `ObjectAccessorMapSource::map_object_record(guid)` through the shared map/source/kind/same-map validation path and return `None` for generic `MapObjectRecord::new(kind, WorldObject)` fallback records or crossed kinds. `Corpse` remains direct-map accessible but excluded from type-mask lookup. Complete only for this typed map-local read seam; it does not complete Pet/Transport typed bodies, full ObjectAccessor, live AddToWorld/fanout/scripts/visibility, AreaTrigger runtime, corpse load, dynamic object aura-owner runtime, SceneObject/Conversation runtime, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.410** ObjectAccessor typed Creature/GameObject map-local seam (typed read API only; not full ObjectAccessor/runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h:63-78`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:101-118`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:120-169`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:176-193`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:196-219`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:222-249`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp:227-239`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds C++-shaped map-local typed ObjectAccessor read helpers for canonical `MapObjectRecord::Creature` and `MapObjectRecord::GameObject` bodies. The helper validates the context has a map, the source map/instance matches the context, the record kind is allowed, and the record object is on the same map before returning `record.creature()`/`record.game_object()` without clone, downcast, scan, bridge storage, mutation, or sync back into Map. Generic `MapObjectRecord::new(WorldObject)` fallback remains valid for the existing `&WorldObject` helpers but is rejected by the typed APIs. Complete only for this seam; it does not complete full ObjectAccessor, live AddToWorld/fanout/scripts/visibility, Player/Pet/Transport/DynamicObject/Corpse/SceneObject/Conversation typed APIs, map mutation, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.409** GameObject linked-trap loaded-grid pre-add and map-local removal seam (represented; not full GO runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:951-1200`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1221-1232`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:103-128`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/world-server/src/gameobject_loaded_grid.rs`, `crates/world-server/src/main.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: complete only for represented linked-trap `Create`/`AddToMap` pre-add in the DB-backed loaded-grid GameObject seam plus `RemoveFromMap`/`RemoveFromWorld` map-local linked-trap cleanup: owner template linked entries resolve through `get_linked_gameobject_entry_like_cpp`, `world-server` allocates linked trap low GUIDs from `map.generate_low_guid_like_cpp(HighGuid::GameObject)`, linked trap records use spawn_id=0, owner position/rotation/map/instance, anim_progress=255, `GoState::Ready`, dynamic=false, and missing/rejected/pre-add-failed linked traps do not block the primary owner. Linked transport templates are skipped by this represented seam without consuming a GameObject low GUID. `wow_map::Map` remains source-of-truth for `map_objects`; no bidirectional sync/cache ownership was added. Remaining gaps include full GameObject runtime/AddToWorld/ObjectAccessor/fanout/scripts/AI/model/collision/visibility/transport, full `GO_JUST_DEACTIVATED`, PoolMgr live spawn, AreaTrigger runtime, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.408** GameObject loaded-grid DB-backed represented `LoadFromDB`/AddToMap seam for supported intrinsic rows (review `APROBADO`; validation OK; committed locally at `current #408 HEAD`; not full GameObject runtime).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2492-2736`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:7552-7610`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:7770-7854`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:951-1185`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1911-1978`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:843-851`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:998-1010`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:103-128`.
  Rust targets: `crates/wow-data/src/gameobject_template.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-entities/src/lib.rs`, `crates/world-server/src/gameobject_loaded_grid.rs`, `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: DB-backed template/addon and spawn override lifecycle stores plus raw `gameobject` runtime row preservation feed a pure GameObject loaded-grid builder/resolver; live `world-server` `ProcessRespawns -> DoRespawn` closure now returns typed `MapObjectRecord::GameObject` for supported intrinsic rows, with map-owned GameObject/Transport GUID generation, effective post-erase respawn time `0` for due timer respawns, override faction/flags precedence, represented `GetDespawnPossibility`/`IsDespawnAtAction`, quaternion normalization/error handling, non-transport GO state limited to 0/1/2, transport GO state represented only for 24/25, typed unsupported rejection for transport stop frames 26..33, invalid state rejection, and `GAMEOBJECT_TYPE_MAP_OBJ_TRANSPORT` rejection. Complete only for this represented seam; remaining gaps include full `GameObject::Create`/`LoadFromDB`, linked traps, zone scripts, model/collision, phasing runtime, AddToWorld/ObjectAccessor/fanout/scripts/AI, visibility overrides, full transport runtime/stop frames, PoolMgr live spawn, AreaTrigger runtime, install/restart/push.

- [x] **#NEXT.R8.ENTITIES.407** Variable-level live Creature loaded-grid DB-backed respawn loader RNG seam (map-owned RNG; not full LoadFromDB).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1567-1575`, `/home/server/woltk-trinity-legacy/src/common/Utilities/Random.h:25-29`, `/home/server/woltk-trinity-legacy/src/common/Utilities/Random.cpp:35-47`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1813`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2188`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`.
  Rust targets: `crates/wow-map/Cargo.toml`, `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow_map::Map` owns the Creature level RNG seam, exposes inclusive C++-like `urand` plus `select_creature_level_like_cpp`, fixed-level rows do not consume RNG, and live `world-server` loaded-grid Creature DB-backed builder no longer blocks solely because `MinLevel != MaxLevel`; it still returns `None` for missing real dependencies and keeps vehicle/AddToWorld/ObjectAccessor/fanout/scripts/AI/formation/dynamic-tree/GameObject/PoolMgr full runtime out of scope.

- [x] **#NEXT.R8.ENTITIES.406** Review correction for live Creature loaded-grid DB-backed respawn loader wiring (fixed-level cases only; variable-level deferred).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2188`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1813`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2505-2511`.
  Scope: `runtime` wiring correction in `world-server` only; `wow_map::Map` remains source-of-truth for respawn timers, map objects and GUID sequence; `world-server` composes DB/template/difficulty/stats/model caches under `&mut Map` with no async/DB inside the map lock.
  Acceptance: tests pass explicit test-only empty loaded-grid caches; live loader uses real canonical spawn metadata and DB-backed caches, rejects missing difficulty rows and `MinLevel != MaxLevel` with `None` to preserve map-owned timer/order until map RNG ownership exists, and only allows fixed-level supported Creature records through the pure builder/resolver seam. Not full `Creature::LoadFromDB`: gaps remain variable-level RNG/map RNG ownership, vehicle runtime, AddToWorld/ObjectAccessor/fanout/scripts/AI/zonescript/formation/dynamic-tree, GameObject DB-backed loaded-grid loader, AreaTrigger runtime, PoolMgr full live spawn, corpse load, and broader Unit/Player systems.

- [x] **#NEXT.R8.ENTITIES.405** `Creature::SetSpawnHealth` flags5 `NO_HEALTH_REGEN` correction in DB-backed loaded-grid builder (dependency/builder only; not live runtime complete).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1954-1978`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureData.h:188-223`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:940-1040`.
  Rust targets: `crates/world-server/src/creature_loaded_grid.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: pure builder consumes `CreatureDifficultyRecordLikeCpp.static_flags[4]` and preserves initial max health/mana when flags5 `NO_HEALTH_REGEN` is present, matching the C++ early return before `_regenerateHealth` and DB `curhealth`/`curmana` branches. Does not mutate live maps, does not wire `ProcessRespawns`, does not execute AddToMap/fanout/scripts/AI, and does not mark live Creature `LoadFromDB` complete.

- [x] **#NEXT.R8.ENTITIES.404** DB-backed input stores for Creature loaded-grid `LoadFromDB` resolver (dependency/builder only; not live runtime complete).
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:349-400`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:403-482`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:575-617`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:620+`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:491-575`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1567-1630`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1954-1978`.
  Rust targets: `crates/wow-data/src/creature_template.rs`, `crates/wow-data/src/lib.rs`, `crates/world-server/src/spawn_store_loader.rs`, `crates/world-server/src/creature_loaded_grid.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: DB-backed dependency only: C++-shaped creature-template lifecycle store with spells and ordered models, raw `creature` runtime row preservation from the existing spawn load, and a pure builder composing DB/template/spawn/difficulty/base-stats/display/model stores into resolver inputs with typed missing-template/missing-difficulty/missing-model/unsupported-vehicle failures. Does not mutate live maps, does not wire `ProcessRespawns`, does not execute AddToMap/fanout/scripts/AI, and does not mark live Creature `LoadFromDB` complete.

- [x] **#NEXT.R8.ENTITIES.403** Map-owned caller GUID seam for Creature loaded-grid resolver/`DoRespawn` loader closure.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1813`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2188`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:514-526`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2505-2511`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:103-128`.
  Rust targets: `crates/world-server/src/creature_loaded_grid.rs`, `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: the pure `CreatureLoadedGridLifecycleResolverLikeCpp` no longer stores or derives runtime object GUIDs from `ResolvedCreatureSpawnLikeCpp`/`spawn_id`; callers must provide the `map_object_guid` generated by the map-owned low-guid sequence, and the resolver validates high type plus map id before propagating it into `CreatureCreateLifecycleRecord`, `Creature`, and optional `MapObjectRecord::Creature`. Templates with `vehicle_id` now return typed `UnsupportedVehicle` rather than creating a false `HighGuid::Creature`. `Map::process_due_respawns_composite_loaded_grid_respawns_like_cpp` passes `&mut Map` into the loaded-grid record loader so future world-server wiring can generate the low GUID at the C++ `map->GenerateLowGuid<HighGuid::Creature>()` ownership point. Complete only for this GUID ownership/caller seam; it does not wire DB-backed `creature`/template/stat/model loading into live `ProcessRespawns`, does not implement vehicle runtime, and does not complete AddToWorld/ObjectAccessor/fanout/scripts/AI.

- [x] **#NEXT.R8.ENTITIES.402** Map-local GUID sequence seam for loaded-grid Creature/GameObject `LoadFromDB`/`DoRespawn`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:514-526`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:789-791`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:145-166`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2505-2511`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.h:180-227`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.h:396-404`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.cpp:788-796`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow_map::Map` now owns lazy per-`HighGuid` low-guid sequence generators for all map-sequence `HighGuid` values present in both Rust and C++, exposes C++-shaped generate/get-next-after-max-used/set APIs, keeps sequences independent per high-guid and per map instance, starts lazily at 1, allows explicit `Transport` sequence set for future global sync/tests, accepts non-Creature/GameObject map sources such as `AreaTrigger`/`DynamicObject`, and rejects non-map highs such as `Player` with typed `UnsupportedSequenceSource`. Complete only for the map-owned low-guid sequence seam; it does not implement full DB-backed `LoadFromDB`, loaded-grid insertion wiring, `ObjectGridLoader` rewrite, formation/AI/vehicle/script callbacks, AddToWorld/ObjectAccessor/fanout, PoolMgr live execution, or install/restart. Next recommended gap: wire loaded-grid Creature `DoRespawn` caller to use this Map GUID sequence plus the #401 resolver.


- [x] **#NEXT.R8.ENTITIES.401** Creature loaded-grid lifecycle resolver for real Map insertion path.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1770-1813`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:333-350`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:44-78`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:519-542`.
  Rust targets: `crates/world-server/src/creature_loaded_grid.rs`, `crates/world-server/src/main.rs`, `crates/world-server/Cargo.toml`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds pure `CreatureLoadedGridLifecycleResolverLikeCpp` in `world-server` over caller-resolved template/spawn/runtime-selection inputs; it performs no DB/async work and no map mutation, rejects missing spawn/template/stats-selection inputs instead of fabricating dummies, maps canonical fields into `CreatureLoadFromDbLifecycleRecord`, builds `Creature::load_from_db_lifecycle`, and produces a typed `MapObjectRecord::Creature` only when `add_to_map` requests insertion metadata. Complete only for resolver/lifecycle bridge; loaded-grid fanout, ObjectAccessor/AddToWorld side effects, world-server DB loader wiring, PoolMgr live execution, scripts/AI/vehicle/zonescript initialization and actual Map::AddToMap caller plumbing remain pending.


- [x] **#NEXT.R8.ENTITIES.400** Creature template classification/rate wiring for C++-normalized creature difficulty/base-stat runtime stores.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:349-400`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:403-482`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:940-1040`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1675-1695`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:9976-10062`.
  Rust targets: `crates/wow-data/src/creature_template.rs`, `crates/wow-data/src/lib.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow-data` now exposes a pure minimal creature-template classification store and C++ `Creature::GetDamageMod` rate mapping; `world-server` loads template classifications before `creature_template_difficulty`, derives damage modifiers from `WorldConfigSet` rates, then loads `creature_classlevelstats` and logs counts. Complete only for data wiring needed by future live Creature `LoadFromDB`/loaded-grid `DoRespawn`; it does not create `Creature`, `MapObjectRecord`, AddToMap, fanout, PoolMgr runtime, scripts, or full Creature runtime. Missing classification rows use the C++ default elite modifier fallback without inventing a template.
- [x] **#NEXT.R8.ENTITIES.399** Creature base stats/difficulty lifecycle data store dependency for live Creature `LoadFromDB`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:940-1040`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureData.h:481-485`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureData.h:582-608`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:9976-10060`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:87-105`.
  Rust targets: `crates/wow-data/src/creature_template.rs`, `crates/wow-data/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `wow-data` now represents `creature_template_difficulty` and `creature_classlevelstats` as C++-normalized stores and pure helpers: `-1` health scaling maps to current expansion 2, invalid scaling resets to 0, MinLevel/MaxLevel and GoldMin/GoldMax are normalized, classification damage modifier is applied by caller-supplied multiplier, loaded HP zero becomes 1, negative base damage becomes 0, missing `(level,class)` returns the C++ zero fallback, and health/mana/armor/damage helpers are deterministic. Complete only as a data-store/calculation dependency; no live spawn, GUID creation, MapManager mutation, world-server loader wiring, or entity insertion is claimed.

- [x] **#NEXT.R8.ENTITIES.398** GameObject resolved lifecycle record seam for Create/LoadFromDB intrinsic entity state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:951-1185`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1911-1978`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:2840-2843`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:2885-2893`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h:2900-2903`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3486-3510`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: resolved records apply intrinsic Create/LoadFromDB state, reject invalid/unsupported creation inputs, expose no public panic lifecycle API, represent nodespawn flag, preserve caller-pre-normalized respawn time, and represent rotation packing. Complete only for the resolved lifecycle record seam; not full DB-backed `CreateFromDB`/`LoadFromDB`/`AddToMap`. Remaining gaps: ObjectMgr/template lookup, zone script, model/collision, phasing/visible map, AddToMap, map timer due clear/removal ownership, linked trap creation, visibility overrides, transport runtime, GO type-specific values/AI/scripts/fanout, and actual world-server DB loader wiring. `#NEXT.R8.ENTITIES.020` and `.021` remain pending.

- [x] **#NEXT.R8.ENTITIES.397** `Map::DoRespawn` loaded-grid `AddToMap` execution seam via supplied `MapObjectRecord` loader.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2189`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:1815-1923`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1911-1978`, and `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:103-128`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::process_due_respawns_composite_loaded_grid_respawns_like_cpp` executes only the loaded-grid non-pooled `ProcessRespawns -> DoRespawn` seam when `CheckRespawn` allows and an explicit caller loader returns a fully constructed typed `MapObjectRecord`; it removes the map-owned timer before `AddToMap` in C++ order, inserts the typed Creature/GameObject record through the #396 AddToMap seam, preserves timer/order on loader `None`, skips the loader for unloaded grids, and counts AddToMap failure after timer removal without inventing entities. Complete only for the map-owned execution seam; remaining gaps are real DB-backed `Creature::CreateCreatureFromDB`/`GameObject::CreateGameObjectFromDB` record construction, world-server DB loader wiring, fanout/scripts, PoolMgr live spawn execution, and AreaTrigger runtime.

- [x] **#NEXT.R8.ENTITIES.396** Typed `MapObjectRecord` `AddToMap` seam for loaded-grid `LoadFromDB`/respawn work.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:529-577`, `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:91-128`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2189`, and `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:355-379`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::add_to_map_like_cpp(kind, WorldObject)` is now a compatibility wrapper over new `Map::add_map_object_record_to_map_like_cpp(MapObjectRecord)`, and the new seam runs the same C++-shaped AddToMap validation/grid/cell/world-flag lifecycle while preserving the caller-provided typed `MapObjectRecord` body through `insert_map_object_record`. Focused tests prove typed Creature/GameObject records survive AddToMap and populate by-spawn indexes for loaded-grid future `LoadFromDB`/`DoRespawn`/PoolMgr work. Complete only for the typed AddToMap ownership seam; it does not implement DB-backed `Creature::CreateCreatureFromDB`, `GameObject::CreateGameObjectFromDB`, loaded-grid `DoRespawn`, PoolMgr live spawn execution, scripts/fanout, or persistence side effects.

- [x] **#NEXT.R8.ENTITIES.395** PoolMgr `Spawn1Object`/`ReSpawn1Object` loaded-grid load-plan evidence seam.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-350`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:353-363`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:366-379`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:382-407`, and parity relation to `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2356-2395`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `ProcessRespawnsSafeSideEffectsSummaryLikeCpp` now carries explicit `PoolSpawnActionLoadPlanLikeCpp` evidence for loaded-grid Creature/GameObject PoolMgr `Spawn1Object`/`ReSpawn1Object` work that remains blocked: `SpawnOne` records `respawn=false`; `RespawnOne` still performs only the safe map-local `Despawn1Object(..., false, false)` half before recording `respawn=true` for the spawn half. Unloaded grids still increment the skipped counter with no plan; missing spawn metadata and unsupported Pool member kinds preserve existing fallback counters. Complete only for honest map-local planning/evidence; remaining gaps: real `CreateFromDB`/`LoadFromDB`/`AddToMap`, loaded-grid entity creation, recursive live child-pool execution, DB/fanout/scripts, full `DoRespawn`, and full PoolMgr runtime.

- [x] **#NEXT.R8.ENTITIES.394** `Map::SpawnGroupSpawn` safe map-local planning/execution seam.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2315-2401`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::spawn_group_spawn_like_cpp` now validates missing/system groups without mutation, activates valid groups before iteration, walks canonical `SpawnStore` group metadata, removes respawn timers for `ignore_respawn`/`force`, skips active timers/live objects/difficulty mismatches/unloaded grids, and returns explicit loaded-grid Creature/GameObject `LoadFromDB` plans with blocked counters. The represented `UpdateSpawnGroupConditions` Spawn branch now calls this seam, returns `spawn_outcome`, and the world-server regression test asserts the C++ active-state side effect plus the existing condition-failure despawn branch. Complete only for safe map-local planning/state/timer effects; no entity `CreateFromDB`/`LoadFromDB`/`AddToMap`, AreaTrigger creation, DB persistence, fanout, or scripts are complete.

- [x] **#NEXT.R8.ENTITIES.393** `ProcessRespawns` pooled `UpdatePool` safe map-local action execution.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:183-257`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-407`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:907-919`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::process_due_respawns_composite_safe_side_effects_like_cpp` now applies safe map-local actions from successful pooled `UpdatePool` plans before removing the trigger timer: `DespawnOne` removes current Creature/GameObject records through typed by-spawn indexes and `remove_from_map_like_cpp(guid, true)`, `RespawnOne` applies the despawn half without saving/deleting respawn timers and reports the spawn half by grid-loaded state, `RemoveRespawnTime` deletes only map-owned member timers, and `SpawnOne` reports missing metadata/unloaded-grid skip/loaded-grid blocked without creating entities. Complete only for pooled UpdatePool safe map-local action execution; remaining gaps: full `CreateFromDB`/`LoadFromDB`/`AddToMap` for spawn, live `SpawnGroupSpawn`, DB save/delete persistence beyond existing bridge/diff, grid/session fanout, scripts, corpse load, and broader `ObjectAccessor`.

- [x] **#NEXT.R8.ENTITIES.392** `Map::ProcessRespawns` `DoRespawn` unloaded-grid early-return + DB-delete bridge.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-688`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2188`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2305`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3549-3560`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::process_due_respawns_composite_safe_side_effects_like_cpp` now executes only the safe C++ `CheckRespawn == Allowed` + unloaded-grid branch: removes the map-owned respawn timer after pool precedence and represented `CheckRespawn`, counts `processed_unloaded_grid_respawns`, continues later due timers, and lets `world-server` derive `CHAR_DEL_RESPAWN` from the before/after respawn timer diff outside the map lock. Loaded-grid `DoRespawn` still blocks as `blocked_do_respawn_runtime`; no entity creation, `LoadFromDB`, AddToMap/fanout, full ProcessRespawns, PoolMgr live runtime, or DB writes inside `wow-map` are claimed complete.

- [x] **#NEXT.R8.ENTITIES.391** `SpawnGroupDespawn` condition-failure map-local side effects + respawn DB-delete bridge.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2404-2425`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2034-2055`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2574-2646`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2140-2146`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2152-2163`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3549-3560`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::UpdateSpawnGroupConditions` represented path now executes only the safe C++ condition-failure `SpawnGroupDespawn(groupId, true)` side effects for map-local state: group validation, canonical `SpawnStore` metadata iteration, map-owned respawn timer removal, current Creature/GameObject removal via typed by-spawn indexes and `remove_from_map_like_cpp(guid, true)`, counter/error reporting, and inactive toggle. `world-server` derives DB deletes from the before/after respawn timer diff outside the map lock. Complete only for condition-failure map-local despawn + DB-delete bridge; live `SpawnGroupSpawn`, full `Spawn1Object`/`ReSpawn1Object`, entity creation/load/fanout, full cleanup/dynamic-tree/session side effects, AreaTrigger live despawn, and DB writes inside `wow-map` remain gaps.

- [x] **#NEXT.R8.ENTITIES.390** `Map::ProcessRespawns` pooled timer branch + `UpdatePool` deterministic plan seam.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:833-848`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:891-920`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-407`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:353-407`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:39-125`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map::process_due_respawns_composite_safe_side_effects_like_cpp` now consults canonical `PoolMgrLikeCpp::is_part_of_a_pool_like_cpp` before `CheckRespawn`; pooled due timers run deterministic `update_pool_plan_like_cpp` against map-owned `SpawnedPoolDataLikeCpp`, record plan/error evidence, delete the map-owned respawn timer only on successful plan, and continue the due loop. `world-server` passes `canonical_spawn_metadata.pool_mgr_like_cpp()` and queues DB deletes from the existing before/after respawn diff outside map mutation. Complete only for this represented pooled timer -> UpdatePool plan + map-owned mutation/report/DB-delete bridge; live `Spawn1Object`/`ReSpawn1Object`/`DespawnObject`, real RNG parity, entity create/load/add/remove, grid/session fanout, full `DoRespawn`, and full live PoolMgr/ProcessRespawns runtime remain gaps.

- [x] **#NEXT.R8.ENTITIES.389** `PoolMgr::InitPoolsForMap` deterministic autospawn planning + map-owned pool-data mutation/report.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:145-168`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:71-76`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:100-110`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:922-929`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:806-811`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-407`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.h:162-176`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/lib.rs`, `crates/world-server/src/main.rs`, docs/inventory.
  Acceptance: adds `PoolMgrLikeCpp::init_pools_for_map_plan_like_cpp` and `Map::init_pools_for_map_like_cpp`, preserving C++ autospawn vector order and existing top-level `SpawnPool` order Pool -> GameObject -> Creature; mutates only map-owned `SpawnedPoolDataLikeCpp`; reports per-pool errors without panic/truncation; world-server creation hook applies represented autospawn planning before persisted respawn snapshot and before `InitSpawnGroupState`; action records remain report-only. Complete only for deterministic planning + map-owned spawned-pool state mutation/report, not full live PoolMgr runtime.

- [x] **#NEXT.R8.ENTITIES.388** `PoolMgr::LoadFromDB` canonical metadata/planning + autospawn candidate metadata.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:442-774`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:715-764`.
  Rust targets: `crates/world-server/src/spawn_store_loader.rs`, `crates/world-server/src/main.rs`, `crates/wow-database/src/statements/world.rs`, `crates/wow-map/src/pool.rs`, docs/inventory.
  Acceptance: adds prepared statements for pool templates, typed members, and default autospawn candidates; `CanonicalSpawnMetadataLikeCpp` owns `PoolMgrLikeCpp`; DB load report counts templates, member skip reasons, relation removals, empty pools, missing-map after non-empty validation, and autospawn skips; validation/order matches C++ skip order; map propagation and cycle removal are represented; final C++ `ASSERT(templateData.MapId != -1)` is represented without panic; tests cover skip order, map propagation/mismatch/cycle/autospawn. Complete only for canonical DB -> metadata/planning and autospawn candidate-list metadata; remaining gaps are real `PoolMgr` runtime execution, live `SpawnPool`/`DespawnPool` RNG/chance execution, real entity creation / `LoadFromDB`, `AddToMap`/`RemoveFromMap`, `DoRespawn`, DB save/delete for runtime branches, grid/session fanout, and wiring into `Map::ProcessRespawns` pool branch. First reviewer pass returned `CAMBIOS NECESARIOS` only because docs/inventory/handoff were missing; code architecture passed; this docs correction records the slice honestly.

- [x] **#NEXT.R8.ENTITIES.387** `PoolMgr` DespawnPool/DespawnObject deterministic plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:183-218`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:220-264`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:409-422`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:813-830`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds deterministic C++-shaped despawn plans for `PoolMgrLikeCpp`/`PoolGroupLikeCpp` over caller-owned `SpawnedPoolDataLikeCpp`: top-level `DespawnPool` order Creature -> GameObject -> Pool, `DespawnObject` bucket order EqualChanced -> ExplicitlyChanced, missing/empty skips without template lookup, `DespawnOne` and `RemoveRespawnTime` action records for future side-effect owners, map-owned RemoveSpawn mutations only for spawned matching objects, no respawn-time action for Pool, child-pool recursive plans before parent relation removal, safe child-pool id overflow errors, and a Rust-only `ChildPoolCycle` guard for invalid cyclic pool data. Complete only for deterministic planning/state mutation; live `AddObjectToRemoveList`, `RemoveRespawnTime` DB deletes, entity destruction, live recursive PoolMgr execution, ProcessRespawns integration, grid/session fanout, install/restart, and push remain separate gaps.

- [x] **#NEXT.R8.ENTITIES.386** `PoolMgr` SpawnPool/UpdatePool deterministic orchestration plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:776-811`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:891-920`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:850-888`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:833-848`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-407`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds pure C++-shaped `PoolMgrLikeCpp` with separate template/group stores, Creature/GameObject spawn indices, child-pool mother index, insertion validation, typed unsupported/missing-template/wrong-kind/overflow errors, top-level `SpawnPool` plan ordering Pool -> GameObject -> Creature with lazy per-specialization explicit rolls consumed only at the C++ `rand_chance()` point (no roll for missing/empty/equal-only/count<=0), typed `SpawnPool<T>` planning through existing map-owned `SpawnedPoolDataLikeCpp` mutations, `UpdatePool` mother-pool branch precedence, AreaTrigger unsupported update, `IsPartOfAPool` AreaTrigger zero, and `IsEmpty`/`CheckPool` GameObject -> Creature -> Pool order with cycle-safe deep pool checks. Reviewer-found eager explicit-roll consumption divergence was corrected after initial implementation. Complete only for “PoolMgr SpawnPool/UpdatePool deterministic orchestration plan”; remaining gaps: LoadFromDB real, auto-spawn map init, DespawnPool live, Spawn1Object/ReSpawn1Object/Despawn1Object real, entity creation/AddToMap/RemoveFromMap, DB save/delete, ProcessRespawns pool branch, recursive live PoolMgr execution, and grid/session fanout.

- [x] **#NEXT.R8.ENTITIES.385** `PoolGroup` SpawnObject deterministic selection plan + map-owned spawned-pool state mutations.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:288-350`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:353-422`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:776-811`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.h:51-83`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:39-125`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds deterministic `PoolGroupLikeCpp::spawn_object_plan_like_cpp` plus `PoolSpawnObjectPlanLikeCpp`/action records for `SpawnOne`, `RespawnOne`, and `DespawnOne`; it calculates signed missing count from map-owned `SpawnedPoolDataLikeCpp`, preserves trigger increment/respawn/despawn semantics, selects explicit-chanced entries with one required supplied roll representing C++ `rand_chance()` before equal-chanced entries with a deterministic chooser, mutates only map-owned spawned-pool state for normal spawns/fallback trigger removals, and returns plans for side effects that still belong to future owners. Complete only for “PoolGroup SpawnObject deterministic selection plan + map-owned spawned-pool state mutations”; remaining gaps: `PoolMgr::SpawnPool` orchestration, `DespawnPool`, entity `CreateFromDB`/`AddToMap`/`RemoveFromMap`, grid-loaded checks, DB save/delete, `ProcessRespawns` pool branch, recursive live PoolMgr integration, and grid/session fanout.

- [x] **#NEXT.R8.ENTITIES.384** `PoolGroup` pure data/chance foundation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.h:34-45`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.h:86-117`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:139-181`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:266-286`.
  Rust targets: `crates/wow-map/src/pool.rs`, `crates/wow-map/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: adds public C++-shaped `PoolTemplateDataLikeCpp`, `PoolObjectLikeCpp`, `PoolMemberKindLikeCpp`, `PoolGroupLikeCpp` and `PoolRelationRemovalLikeCpp` with pure helpers for `SetPoolId`/getters, `isEmpty`, `isEmptyDeepCheck` normal and Pool-of-Pools closure fallback, `AddEntry` explicit/equal bucket choice, `CheckPool` explicit chance validation, and Pool-only `RemoveOneRelation` removing the first matching explicit and then first matching equal entry. Complete only as in-memory foundation data for future `PoolMgr`; this does not implement live `PoolMgr`, `SpawnPool`/`DespawnPool`, RNG/chance rolling, DB load, entity creation/AddToMap/RemoveFromMap, `Map::pool_data` synchronization, `ProcessRespawns` pool branch, or grid/session fanout.

- [x] **#NEXT.R8.ENTITIES.383** Map-owned `SpawnedPoolData` state + grid-load seam.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:167`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:710-711`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:792`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.h:51-83`, `/home/server/woltk-trinity-legacy/src/server/game/Pools/PoolMgr.cpp:39-125`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2305`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/lib.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` now owns `SpawnedPoolDataLikeCpp`, initialized empty in `with_hooks`, exposing C++-shaped creature/gameobject/subpool membership, pool counters, duplicate-add counter increment, remove-create-zero/saturating decrement semantics, unsupported AreaTrigger typed errors, and spawned Creature/GameObject entries feeding `SpawnGridLoadStateLikeCpp`; grid-load checks now accept/reject pooled Creature/GameObject spawns from map-owned pool state plus existing map-owned respawn/spawn-group state. Complete only for map-owned SpawnedPoolData state and grid-load seam; remaining gaps: no real `PoolMgr::SpawnPool`/`DespawnPool`, no RNG/chance, no `ProcessRespawns` pool branch, no entity creation/AddToMap/RemoveFromMap, no DB persistence/delete, no grid/session fanout.

- [x] **#NEXT.R8.ENTITIES.382** Canonical player display/mount/collision-height sync from represented `WorldSession` shape.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:448-463`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h:875-876`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:7826-7854`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:7859-7868`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:10014-10036`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:13571-13596`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:28128-28144`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1158-1181`.
  Rust targets: `crates/wow-world/src/session.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldSession::canonical_player_entity_snapshot_like_cpp` applies represented player display/native display, mount display id, object scale and collision height into the canonical `Player` before insertion/replacement, and `update_player_collision_height_like_cpp` propagates current mount display id plus collision height to an existing canonical player even when DB2 stores are absent and the represented session height is the fallback. Sync direction remains session -> canonical `Player`; no canonical fields are read back to overwrite represented session state. Complete only for this map-local canonical player shape dependency; broader Maps/ObjectAccessor/LOS ownership and full Player Create/Load/login remain separate gaps.

- [x] **#NEXT.R8.ENTITIES.381** `WorldObject` LOS endpoints: collision-height + hit-sphere parity.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:146`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:496`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:557-560`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h:748-752`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1158-1165`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1167-1185`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1187-1210`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h:1743`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp:13571-13596`.
  Rust targets: `crates/wow-entities/src/world_object.rs`, `crates/wow-entities/src/unit.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `WorldObject` now owns resolved runtime `collision_height_like_cpp` state with base default `0.0`, C++-shaped `GetHitSpherePointFor` endpoint construction, and `IsWithinLOS`/`IsWithinLOSInMap` constructors that apply collision-height and hit-sphere adjustments before delegating to the unchanged `WorldObjectEnvironment::line_of_sight` hook. `Unit` exposes collision-height hydration and keeps `UnitData` combat reach synchronized into the embedded `WorldObject`. Complete only for endpoint construction from hydrated scalars; remaining gaps include DB2/model/mount hydration, real terrain/vmap/dynamic-tree collision, transports, visibility overrides/cinematic/sight runtime, and broader #NEXT.R8.ENTITIES.005 ownership.

- [x] **#NEXT.R8.ENTITIES.380** `Map` como `WorldObjectEnvironment` con hooks explícitos de terreno/LOS.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1167-1185`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1187-1210`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1366-1371`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:1449-1485`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp:3749-3769`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:217`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:287-290`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:456-464`.
  Rust targets: `crates/wow-map/src/map.rs`, `docs/migration/current-session-handoff.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: `Map` implements the existing `WorldObjectEnvironment` trait from `wow-entities` and delegates visibility range to `visible_distance` plus LOS/map-height/floor queries to an explicit terrain/dynamic-tree hook trait. `NoopTerrainGridLoader` is an explicit noop hook returning LOS `true` and `INVALID_HEIGHT` for heights. Focused unit tests prove `WorldObject` helpers can use `Map` as the environment and that query shape reaches a controlled terrain hook. Complete only for the bounded `Map`↔`WorldObjectEnvironment` integration seam; this does not close #NEXT.R8.ENTITIES.005. Remaining gaps include real terrain/vmap/dynamic-tree collision, exact collision-height/hit-sphere endpoint adjustment, transports, visibility overrides/cinematic/sight runtime, and broader map/object ownership.

- [x] **#NEXT.R8.ENTITIES.379** `Map::GetWorldObjectBySpawnId` parity over map-local spawn-id stores.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:400-430`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3473-3508`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.h:251-256`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:748-777`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1954-2023`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2240`.
  Rust targets: `crates/wow-map/src/map.rs`.
  Acceptance: `Map` exposes C++-shaped typed getters for Creature/GameObject by spawn id plus `get_world_object_by_spawn_id_like_cpp(type, spawn_id)` over Creature/GameObject/AreaTrigger. The helpers read only the derived map-local spawn-id GUID indexes, prefer alive Creature / spawned GameObject before deterministic fallback, resolve back through canonical `map_objects`, return `None` for absent/stale/type-mismatched/zero-spawn cases, and do not scan the full object store. Complete only for map-local lookup parity; remaining gaps include AreaTrigger Create/Load/Update runtime, templates/spawns, AI, caster unregister, unit enter/exit, movement/visibility/transport, respawn support for AreaTrigger, PoolMgr/DoRespawn, entity creation/fanout, ObjectAccessor/grid/session ownership, full AddToWorld/RemoveFromWorld side effects, and broader Unit/Player systems.

- [x] **#NEXT.R8.ENTITIES.378** `AreaTrigger` by-spawn store en `Map`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:418-430`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:793-796`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/AreaTrigger/AreaTrigger.cpp:62-76`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/AreaTrigger/AreaTrigger.cpp:78-102`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3501-3508`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `crates/wow-map/src/map.rs`.
  Acceptance: `MapObjectRecord` now has a typed `AreaTrigger` variant while preserving generic `MapObjectRecord::new(AccessorObjectKind::AreaTrigger, WorldObject)` bridge behavior; `Map` owns a derived multimap-like `area_triggers_by_spawn_id` GUID-set index beside the Creature/GameObject indexes, keeps it coherent through insert/remove/replace, skips spawn id zero, resolves helpers back through canonical `map_objects`, and exposes C++-shaped count/GUID/get-by-spawn helpers. Complete only for typed `MapObjectRecord` AreaTrigger plus map-local by-spawn index/store helper; remaining gaps include AreaTrigger Create/Load/Update runtime, templates/spawns, AI, caster unregister, unit enter/exit, movement/visibility/transport, full entity-specific AddToWorld/RemoveFromWorld side effects outside the store, ObjectAccessor/grid/session fanout, PoolMgr/DoRespawn, and broader Unit/Player systems.

- [x] **#NEXT.R8.ENTITIES.377** `Map` spawn-id live-object indexes like Trinity multimap.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:414-493`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:793-796` (foreman spec also cited nearby respawn fields at `Map.h:748-777`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp:330-419`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:899-968`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1966-2002`.
  Rust targets: `crates/wow-map/src/map.rs`.
  Acceptance: `Map` now owns typed multimap-like by-spawn-id indexes for live Creature and GameObject records beside the primary GUID `map_objects` store; insert/remove/replace paths keep indexes coherent, skip spawn id zero, and `Map::check_respawn_live_object_guard_like_cpp` resolves spawn-id index GUIDs back through `map_objects` instead of scanning the full object store. Complete only for map-owned Creature/GameObject by-spawn indexes plus guard usage; remaining gaps include AreaTrigger by-spawn store, real AddToWorld/RemoveFromWorld entity-specific side effects beyond `MapObjectRecord`, PoolMgr, DoRespawn/LoadFromDB, DB save/delete, entity creation/fanout, broader ObjectAccessor ownership, and real escort runtime.

- [x] **#NEXT.R8.ENTITIES.376** `Map::ProcessRespawns` linked future reschedule + `CHAR_REP_RESPAWN` side effect.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:682-688`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2004-2020`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3549-3560`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`.
  Acceptance: when the composite `CheckRespawn` linked-respawn guard mutates a due timer to a future/infinite respawn time, `Map::process_due_respawns_composite_delete_only_like_cpp` now removes the original due timer, reinserts the same map-owned respawn info at the future time, reports the reschedule, and continues processing later due timers; `world-server` queues and executes exact `CharStatements::REP_RESPAWN(type, spawnId, respawnTime, mapId, instanceId)` for world maps outside the `MapManager` lock, with non-world maps skipped like C++ `Instanceable()` and invalid map ids skipped without truncation. This does not implement `PoolMgr`, `DoRespawn`/`LoadFromDB`, corpse load, entity creation/fanout, ObjectAccessor/grid ownership, optimized by-spawn indexes or real dynamic escort runtime.

- [x] **#NEXT.R8.ENTITIES.375** `Map::ProcessRespawns` safe zero-delete DB delete side effect.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:682-688`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2140-2146`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2152-2163`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3549-3560`.
  Rust targets: `crates/world-server/src/main.rs`.
  Acceptance: after `Map::process_due_respawns_composite_delete_only_like_cpp` removes due timers for the already-safe inactive spawn-group/live-object zero-delete branches, `world-server` derives exact removed `(type, spawnId)` keys via per-map before/after snapshots, queues `CharStatements::DEL_RESPAWN(type, spawnId, mapId, instanceId)` only for `ManagedMapKind::World`, skips non-world and invalid `map_id > u16::MAX` without truncation, and executes deletes asynchronously after releasing the `MapManager` lock. This does not implement full `ProcessRespawns`, future `SaveRespawnInfoDB` reschedules, `DoRespawn`/`LoadFromDB`, PoolMgr, linked-respawn persistence, corpse load, entity creation/fanout or ObjectAccessor/grid ownership.

- [x] **#NEXT.R8.ENTITIES.374** Startup `Map::LoadRespawnTimes` into map-owned respawn store.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:71-76`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:100-110`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3516-3546`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3563-3594`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2057-2090`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:748-777`.
  Rust targets: `crates/world-server/src/main.rs`, `crates/wow-database/src/statements/character.rs`.
  Acceptance: world-server reads `characters.respawn` at startup into a snapshot, validates rows through canonical spawn metadata, computes C++ grid ids from spawn points, and applies Creature/GameObject timers to map-owned `RespawnStoreLikeCpp` for `ManagedMapKind::World` before `InitSpawnGroupState`; invalid type, AreaTrigger, missing metadata and non-world maps are counted/ignored without DB writes. This does not implement DB save/delete during tick, `DoRespawn`/`LoadFromDB`, PoolMgr, linked-respawn persistence, entity live creation/fanout or corpse load.

- [x] **#NEXT.R8.ENTITIES.373** `Map::ProcessRespawns` safe composite zero-delete seam.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-688`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1950-2023`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1959-1964`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1966-2002`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2004-2020`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `Map::process_due_respawns_composite_delete_only_like_cpp` uses the composite `check_respawn_like_cpp` over map-owned respawn timers and executes only safe in-memory zero-delete effects for inactive spawn-group and live creature/gameobject blockers; linked-respawn outcomes, PoolMgr, DoRespawn, DB save/delete/persistence, unsupported types, missing metadata, entity creation and fanout remain blocked with counters and preserve the oldest due timer/order. `world-server` calls this seam before `UpdateSpawnGroupConditions` and passes `SpawnStore` plus `LinkedRespawnStoreLikeCpp`; dynamic escort config/runtime remains an explicit false/false bridge until owned.

- [x] **#NEXT.R8.ENTITIES.372** `Map::CheckRespawn` composite helper over represented guards.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1950-2023`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1956-1964`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1966-2002`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2004-2020`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3607-3620`.
  Rust targets: `crates/wow-map/src/map.rs`, `crates/wow-map/src/lib.rs`.
  Acceptance: `Map::check_respawn_like_cpp` composes the represented `CheckRespawn` guards in the required C++ order (spawn-group guard -> live-object guard -> linked-respawn guard -> Allowed), preserving early-return mutations and non-effects. This is complete only for the helper composition over existing represented guards; it is not full live `CheckRespawn`, `ProcessRespawns`, `PoolMgr`, `DoRespawn`, DB save/delete, entity creation, real escort runtime ownership, optimized by-spawn indexes, or grid/session fanout.

- [x] **#NEXT.R8.ENTITIES.371** Linked respawn metadata/load/store + pure `CheckRespawn` linked-time guard dependency.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:1811-1997`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:120-126`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.h:1503-1508`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2004-2020`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:3607-3620`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h:2611-2614`.
  Rust targets: `crates/wow-map/src/spawn.rs`, `crates/wow-map/src/map.rs`, `crates/wow-map/src/lib.rs`, `crates/wow-database/src/statements/world.rs`, `crates/world-server/src/spawn_store_loader.rs`.
  Acceptance: `linked_respawn` rows are loaded via the C++ SQL, validated against canonical creature/gameobject spawn metadata, instanceable map, same-map and difficulty-intersection guards, then stored in a read-only ObjectMgr-like linked respawn store; `Map` can read linked creature/GO respawn timers from its map-owned respawn store and apply only the C++ linked-time guard with caller-supplied jitter. This is not full `CheckRespawn`, `ProcessRespawns`, PoolMgr, `DoRespawn`, DB save/delete, live entity creation or fanout.

- [x] **#NEXT.R8.ENTITIES.001** Create `wow-entities` crate and base `EntityObject`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h`, `Object.cpp`, `ObjectGuid.h`, `Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/lib.rs`, `crates/wow-entities/src/object.rs`, `Cargo.toml`.
  Acceptance: default constructor state matches C++ `Object::Object`; `_Create`, `AddToWorld`, `RemoveFromWorld`, `SetEntry`, `SetObjectScale`, dynamic flag helpers, type id/mask checks, new/destroyed/update state and clear-update-mask behavior are represented and tested; `map_id`/`instance_id`/`in_grid` are explicit Rust bridge state for canonical map ownership, not claimed as C++ `Object` fields.

- [x] **#NEXT.R8.ENTITIES.002** Port `WorldObject` base state and distance helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.h`, `Object.cpp`, `Position.h`.
  Rust targets: `crates/wow-entities/src/world_object.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `WorldLocation` default map id and orientation normalization match C++; `WorldObject` tracks map/instance binding, current cell, world-object flag, active/far-visible flags, zone/area, DB phase and minimal phase shift; distance/range helpers subtract combat reach, clamp visible distance to zero and use C++ strict `< dist²` checks.

- [x] **#NEXT.R8.ENTITIES.004** Port `ObjectAccessor` base lookup API.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.h`, `ObjectAccessor.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp::normalizePlayerName`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: global player holder supports add/remove/find-connected/find-in-world by GUID and normalized name; same-map `GetPlayer` semantics are represented; `GetWorldObject`, `GetObjectByTypeMask`, unit/creature/pet/gameobject/corpse/dynamicobject/areatrigger/sceneobject/conversation dispatch follows C++ high-GUID/type-mask branches, including C++'s `GetObjectByTypeMask` corpse branch returning null; map-local objects are stored through a bridge store until canonical `wow_map::Map` owns real entity containers.

- [x] **#NEXT.R8.ENTITIES.007** Port update-field delta foundation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateMask.h`, `UpdateField.h`, `UpdateFields.h`, `UpdateFields.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp`.
  Rust targets: `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/object.rs`, `crates/wow-packet/src/packets/update.rs`.
  Acceptance: `UpdateMask` block/block-mask behavior is represented; `EntityObject` emits `UF::ObjectData` values update masks using C++ bit positions 0..3; `UpdateObject` can serialize an ObjectData-only VALUES update in the C++ `Object::PrepareValuesUpdateBuffer`/`ObjectData::WriteUpdate` shape; creature health VALUES updates no longer write the create-only `UpdateFieldFlag` byte.

- [x] **#NEXT.R8.ENTITIES.009** Port `Unit` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h`, `Unit.cpp`, `UnitDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/unit.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-entities/src/update_fields.rs`.
  Acceptance: base `Unit` constructor state matches C++ for type id/mask, movement update flag, attack/speed/weapon defaults, death state and unit state; health/max-health clamps follow C++; power setters use a derived power-index bridge and clamp current power to max; display, native display, level, faction, bounding radius and combat reach update `UF::UnitData` bit positions; `UnitValuesUpdate` sets the `TYPEID_UNIT` object-type bit.

- [x] **#NEXT.R8.ENTITIES.011** Port `Player` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/StatSystem.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/unit.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Player` constructor retags `Unit` to `TYPEID_PLAYER`/`TYPEMASK_PLAYER`, stores session bridge, uses C++ hit chance/team/active/control/whisper defaults, exposes race/class/gender/native gender and target selection setters, ports PlayerData flags/loot/spec/bank setters, ports ActivePlayerData money/XP/backpack/inv-slot field masks, and splits `PlayerData` vs `ActivePlayerData` values updates for self vs other receivers.

- [x] **#NEXT.R8.ENTITIES.015** Port `Creature` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.h`, `Creature.cpp`, `CreatureData.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/StatSystem.cpp`, `UnitDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Movement/MovementDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp`.
  Rust targets: `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/unit.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Creature` constructor remains `TYPEID_UNIT`/`TYPEMASK_UNIT`, preserves C++ defaults for respawn/corpse timers, regen, react state, idle movement, assistance flags, spell slots, loot mode, sight/combat distance and temp-world-object state; `Creature::GetPowerIndex` semantics are represented; faction/display setters update `UnitData`, with model dimensions passed explicitly until ObjectMgr template/model stores are canonical.

- [x] **#NEXT.R8.ENTITIES.019** Port `GameObject` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.h`, `GameObject.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `GameObject` constructor matches C++ type id/mask and stationary/rotation create flags; respawn/despawn/restock/cooldown, loot state/unit guid, spawned-by-default, spell/spawn ids, packed rotation, loot mode, respawn compatibility and stationary position defaults are represented; `UF::GameObjectData` bit masks cover display, flags, faction, level, state, type, percent health, art kit and custom param; values update sets `TYPEID_GAMEOBJECT`.

- [x] **#NEXT.R8.ENTITIES.022** Port `Corpse` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Corpse/Corpse.h`, `Corpse.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/corpse.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Corpse` constructor matches C++ type id/mask, `WorldObject(type != CORPSE_BONES)`, stationary create flag and ghost time/type state; `CorpseData` dynamic flags, owner/party/guild, display/race/class/sex/flags/faction/items setters use C++ bit positions; expiry thresholds for bones and resurrectable corpses match C++; values update sets `TYPEID_CORPSE`.

- [x] **#NEXT.R8.ENTITIES.024** Port `DynamicObject` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.h`, `DynamicObject.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/dynamic_object.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `DynamicObject` constructor matches C++ type id/mask, `WorldObject(isWorldObject)`, stationary create flag and duration/aura/caster/viewpoint null state; `DynamicObjectType` enum and `DynamicObjectData` caster/type/spell visual/spell id/radius/cast-time setters use C++ bit positions; non-aura duration ticking follows C++; values update sets `TYPEID_DYNAMICOBJECT`.

- [x] **#NEXT.R8.ENTITIES.026** Port `AreaTrigger` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/AreaTrigger/AreaTrigger.h`, `AreaTrigger.cpp`, `AreaTriggerTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/area_trigger.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `AreaTrigger` constructor matches C++ type id/mask, `WorldObject(false)`, stationary and area-trigger create flags, spawn/target/aura/stationary-position/duration/time/removal/movement/template bridge defaults; permanent duration is sent as zero; runtime duration updates do not mark the update mask; scalar `AreaTriggerData`, simple scale-curve constants and `VisualAnim` use C++ bit positions; values update sets `TYPEID_AREATRIGGER`.

- [x] **#NEXT.R8.ENTITIES.028** Port `SceneObject` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/SceneObject/SceneObject.h`, `SceneObject.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/scene_object.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `SceneObject` constructor matches C++ type id/mask, `WorldObject(false)`, stationary and scene-object create flags, stationary position and created-by-spell-cast bridge; `ShouldBeRemoved` predicate shape is represented; `SceneObjectData` script package, random seed, created-by and scene type setters use C++ bit positions; values update sets `TYPEID_SCENEOBJECT`.

- [x] **#NEXT.R8.ENTITIES.030** Port `Conversation` base state and core setters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Conversation/Conversation.h`, `Conversation.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ConversationDataStore.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/conversation.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Conversation` constructor matches C++ type id/mask, `WorldObject(false)`, stationary and conversation create flags, creator/duration/texture/stationary-position defaults; lines, actors and last-line-end-time use C++ `ConversationData` bits; actor world-object/talking-head variants match C++ field shape; max last-line-end-time plus 10s despawn delay is represented; values update sets `TYPEID_CONVERSATION`.

- [x] **#NEXT.R8.ENTITIES.032** Port `Totem` base state and core rules.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Totem/Totem.h`, `Totem.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/TemporarySummon.h`, `TemporarySummon.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h`.
  Rust targets: `crates/wow-entities/src/totem.rs`, `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Totem` remains a `Creature`/`Unit` shape with `UNIT_MASK_SUMMON|UNIT_MASK_MINION|UNIT_MASK_TOTEM`; owner/summoner bridge, properties slot, totem type and duration defaults match C++; inherited spell slots back `GetSpell(slot)`; passive/active init-summon rules, update duration/owner-alive unsummon shape, delayed unsummon bridge, totem-created packet slot offset and positive/aura immunity special cases are represented.

- [x] **#NEXT.R8.ENTITIES.034** Port `Pet` base state, spell map and stable slot helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Pet/Pet.h`, `Pet.cpp`, `PetDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/TemporarySummon.h`, `TemporarySummon.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/UnitDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/CreatureData.h`.
  Rust targets: `crates/wow-entities/src/pet.rs`, `crates/wow-entities/src/totem.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Pet` constructor matches C++ `Guardian(nullptr, owner, true)` shape with `UNIT_MASK_SUMMON|MINION|GUARDIAN|PET|CONTROLABLE_GUARDIAN` and hunter-pet mask branch; name, pet type, duration, loading, removed, focus regen timer, group update mask and specialization defaults match C++; pet spell map/autospells and autocast toggles follow `PetSpell` field shape; `PetSaveMode` active/stable ranges and `GetLoadPetInfo` priority order are represented; pet XP factor is recorded.

- [x] **#NEXT.R8.ENTITIES.036** Port `Vehicle` base kit and seat helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Vehicle/Vehicle.h`, `Vehicle.cpp`, `VehicleDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`.
  Rust targets: `crates/wow-entities/src/vehicle.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Vehicle` is represented as a kit attached to a unit GUID/type/position, not as an independent object; vehicle id, creature entry, status, usable seat count, seats, passenger info, seat addon, accessory and template structures match C++ shape; passenger add/remove/remove-all and pending join-event seat checks are represented; `TransportBase::CalculatePassengerPosition/Offset` formulas are ported and round-trip tested.

- [x] **#NEXT.R8.ENTITIES.038** Port `Transport` base state and passenger-set helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Transport/Transport.h`, `Transport.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Maps/TransportMgr.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.h`, `GameObject.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h`.
  Rust targets: `crates/wow-entities/src/transport.rs`, `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/object.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Transport` is represented as a `GameObject`/map-object transport with `SERVER_TIME|STATIONARY|ROTATION` create flags; movement state, current path leg, request-stop timestamp, path progress, position-change timer, delayed-add-model flag, event trigger bitset shape and dynamic/static passenger GUID sets match C++ base fields; `GetTransportPeriod`/`SetPeriod` use `GameObjectData::Level`; path-progress-for-client encodes into high dynamic-flag bits; passenger add/remove/cleanup/unload helpers and `TransportBase::CalculatePassengerPosition/Offset` formulas are represented and tested.

- [x] **#NEXT.R8.ENTITIES.040** Port pure `WorldObject`/`Position` geometry helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Position.h`, `Position.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Object.cpp`.
  Rust targets: `crates/wow-entities/src/world_object.rs`.
  Acceptance: absolute/relative angle conversion, `HasInArc`, `isInFront`, `isInBack`, `HasInLine`, rotated `IsWithinBox` and double vertical cylinder checks match C++ pure math semantics and are tested; LOS, terrain height, transport relocation and visibility-range hooks remain pending because they require canonical `Map`/terrain/transport ownership.

- [x] **#NEXT.R8.ENTITIES.041** Port `Item` base state and `UF::ItemData` masks.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`, `Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemDefines.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`, `UpdateFields.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPacketsCommon.h`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`, `crates/wow-constants/src/item.rs`.
  Acceptance: base `Item` is represented as `Object`/`TYPEID_ITEM`/`TYPEMASK_ITEM`, not `WorldObject`; constructor state, slot/container bridge, update queue state, trade/refund/text fields, core `Create` initialization shape, dynamic item flags/flags2, stack/durability/expiration/context/appearance, spell charges, enchantments, item bonus key and `UF::ItemData` 43-bit masks are ported and tested. Template lookup, Bag/Container, DB save/load, item update packet serializers and Player inventory ownership remain pending under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.042** Port `Bag` base state and `UF::ContainerData` masks.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.h`, `Bag.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`, `UpdateFields.cpp`.
  Rust targets: `crates/wow-entities/src/bag.rs`, `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/update_fields.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: base `Bag` retags `Item` to `TYPEID_CONTAINER`/`TYPEMASK_CONTAINER`, preserves `MAX_BAG_SIZE=36`, owns a GUID slot bridge for `m_bagslot`, ports `ContainerData::NumSlots` and `Slots[36]` bit positions, rejects templates with too many slots, stores/removes child `Item` state like C++ `StoreItem`/`RemoveItem`, and emits values updates with `TYPEID_CONTAINER`. Real `Item*` ownership/destruction, DB save/load recursion, Player inventory indexes and packet serializers remain pending under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.043** Port `Player` inventory storage lookup bridge.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `m_items[PLAYER_SLOT_END=141]`, `m_currentBuybackSlot`, storage slot constants, `ForEachItem` search locations, top-level/bag/reagent/bank GUID storage, `GetItemByPos`, packed-pos lookup, `GetBagByPos`, `GetItemByGuid`, buyback slots and `ActivePlayerData::{InvSlots,BuybackPrice,BuybackTimestamp}` masks are represented and tested against C++ lookup rules. Actual `Item*` ownership, item mutation side effects, equip spell/aura/stat application, visible item data, DB persistence and packet serializers remain pending under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.044** Wire `ObjectAccessor` `TYPEMASK_ITEM` lookup branch.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp`, `ObjectAccessor.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `GetObjectByTypeMask` item semantics are represented: item GUID lookup only works when `typemask` contains `TYPEMASK_ITEM` and the context object is a player; the branch delegates to Player inventory storage. Because C++ returns `Object*` and Rust `Item` is not a `WorldObject`, a new `AccessorObjectRef::{WorldObject,Item}` API carries item hits while the legacy `get_object_by_type_mask` remains world-object-only. Real `Player` ownership storage, item object registry and packet serializers remain #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.045** Port `Player` visible item slot state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/Updates/UpdateFields.h`, `UpdateFields.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `UF::VisibleItem` value shape (`ItemID`, `ItemAppearanceModID`, `ItemVisual`), `PlayerData::VisibleItems[19]` array bits (`61`, `62..80`), `SetVisibleItemSlot` clear/set semantics and the equipment-slot branch of `VisualizeItem` are represented and tested. C++ template-dependent `Item::GetVisibleEntry/GetVisibleAppearanceModId/GetVisibleItemVisual`, BoE/BoA binding, real `Item*` ownership side effects and final nested update-field packet serializers remain pending under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.046** Fix `InventoryType` bridge against C++ signed DB2 field and bag slots.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-data/src/item.rs`, `crates/wow-packet/src/packets/item.rs`, `crates/wow-world/src/handlers/character.rs`.
  Acceptance: `ItemEntry::InventoryType` is treated as signed at the DB2 boundary, negative and zero values do not map to equipment slots, `-1` no longer wraps to `INVENTORY_SLOT_BAG_0=255`, and `INVTYPE_BAG=18` maps to C++ equipped bag slots `30..33` instead of an equipment display slot. Existing flat `wow-world` inventory remains a temporary bridge until canonical `Player`/`Item` ownership replaces it under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.047** Port `Item` visible transmog/enchant modifier helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `Item.h`, `ItemDefines.h`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `AppearanceModifierSlotBySpec`, `IllusionModifierSlotBySpec`, `SecondaryAppearanceModifierSlotBySpec`, `GetModifier`/`SetModifier`, `GetVisibleEntry`, `GetVisibleAppearanceModId`, `GetVisibleEnchantmentId`, `GetVisibleItemVisual` and secondary appearance precedence are represented and tested. DB2 store bridges for `SpellItemEnchantment` and `ItemModifiedAppearance` are closed by #NEXT.R8.ENTITIES.114 and #NEXT.R8.ENTITIES.115.

- [x] **#NEXT.R8.ENTITIES.048** Port `Player::VisualizeItem` item mutation side effects.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: `VisualizeItem` binding behavior for `BIND_ON_EQUIP`, `BIND_ON_ACQUIRE` and `BIND_QUEST`, top-level inventory slot storage, item `ContainedIn`/`OwnerGUID`/slot/container reset and `ITEM_CHANGED` transition are represented and tested. Collection appearance registration, equip stat/aura/spell application, canonical item object registry, DB persistence and packet serializers remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.049** Port empty top-level branch of `Player::_StoreItem`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: empty-slot top-level `StoreItem` side effects are represented: `SetCount(count)`, bind-on-store for `BIND_ON_ACQUIRE`, `BIND_QUEST` and `BIND_ON_EQUIP` only when `IsBagPos(pos)`, slot storage, `ContainedIn`/`OwnerGUID`/slot/container reset and `ITEM_CHANGED` transition. Stack merge into an existing `Item*`, clone split storage, bag-contained branch, AddToWorld/update packets, enchant/item durations and obtain spells remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.050** Port empty bag-contained branch of `Player::_StoreItem`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/bag.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: `Player::_StoreItem`'s `pBag->StoreItem(slot, pItem, update)` branch is represented for empty bag slots: Player bag storage and `Bag::m_bagslot` stay in sync, child item count/owner/contained/container/slot/state are updated, `BIND_ON_ACQUIRE`/`BIND_QUEST` bind while `BIND_ON_EQUIP` does not bind for bag-contained positions, and the bag item receives `ITEM_CHANGED`. Existing-stack merge, clone split storage, AddToWorld/update packets, duration hooks, obtain spells and persistence remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.051** Port top-level existing-stack merge branch of `Player::_StoreItem`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: top-level existing stack merge is represented with GUID-slot validation, C++ bind-on-store rules on the existing stack, count increment, existing item `ITEM_CHANGED`, incoming item owner assignment, refundable/BOP-tradeable cleanup and `ITEM_REMOVED` transition. Bag-contained existing-stack merge, clone split semantics, world update packets, duration hooks, trade list removal, collection side effects, obtain spells and persistence remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.052** Port bag-contained existing-stack merge branch of `Player::_StoreItem`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/bag.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: bag-contained existing stack merge is represented with Player-storage and `Bag` slot GUID validation, C++ bind-on-store rules for bag-contained positions, count increment, existing item `ITEM_CHANGED`, incoming item owner assignment, refundable/BOP-tradeable cleanup and `ITEM_REMOVED` transition; the bag item is not marked changed because C++ does not change `m_bagslot` in this branch. Clone split semantics, world update packets, duration hooks, trade list removal, collection side effects, obtain spells and persistence remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.053** Port `Item::CloneItem` field subset and empty-slot clone storage.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/player.rs`.
  Acceptance: clone storage preserves the source item, creates a separate item with caller-provided GUID, copies entry/context/creator/gift creator/expiration and dynamic flags without `REFUNDABLE`/`BOP_TRADEABLE`, applies the requested count, and then stores the clone through the already ported empty top-level/bag `_StoreItem` branches. C++ ObjectMgr GUID generation, template-backed max-stack/max-durability/effect-charge lookup, `NewItemOrBag` subtype selection, split count allocation, world update packets, duration hooks and persistence remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.054** Port `Player::SplitItem` count allocation into empty storage branches.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: split helpers reject zero, all-count and too-large splits like C++ guard paths, preserve source count/state if destination storage fails, and on success clone the requested count into empty top-level/bag storage while decrementing and marking the source item changed. Full `SplitItem` destination resolution through `CanStoreItem`/`CanBankItem`/`CanEquipItem`, equip/bank-specific side effects, world update packets, duration hooks and persistence remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.055** Port representable `Player::SplitItem` loot/trade guards.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: split helpers reject source items with generated loot before count guards, reject all-count/too-large splits before trade checks, and reject source items marked in-trade without mutating source count/state. Full `TradeData::GetTradeSlotForItem` ownership, packet error mapping and destination validation remain under #NEXT.R8.ENTITIES.013/#008.

- [x] **#NEXT.R8.ENTITIES.056** Port static Player inventory position classifiers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `NULL_BAG`, packed item positions, `IsInventoryPos`, `IsEquipmentPos`, `IsBankPos`, `IsBagPos` and `IsChildEquipmentPos` are represented and tested against C++ slot ranges, including equipped bag slots being equipment, bag contents not being `IsBagPos`, keyring/child-equipment inventory handling and `NULL_SLOT` only matching inventory with `INVENTORY_SLOT_BAG_0`. `IsValidPos`, `CanStoreItem`/`CanBankItem`/`CanEquipItem` and destination vector generation remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.057** Port `Player::IsValidPos` inventory/bag rules.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: `Player::is_valid_pos` and packed-pos wrapper match C++ explicit/non-explicit handling for `NULL_BAG`, `NULL_SLOT`, top-level equipment/profession/bag/reagent slots, backpack slots limited by `GetInventorySlotCount`, bank main/bag/keyring ranges, and registered bag contents limited by bag size. Full `CanStoreItem`/`CanBankItem`/`CanEquipItem` destination vector generation and bank-bag purchase gating remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.058** Port `Item::CanBeMergedPartlyWith` guard subset.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/item.rs`.
  Acceptance: `Item::can_be_merged_partly_with(entry, max_stack_size)` returns C++ `InventoryResult` values for generated-loot, entry mismatch, full-stack and mergeable-stack cases. The method takes the template-derived `entry/max_stack_size` values explicitly until canonical `ItemTemplate` lookup is ported under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.059** Port `ItemCanGoIntoBag` family/subclass rules.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `ItemStorageTemplate` carries the template fields needed by storage validation and `item_can_go_into_bag` matches C++ regular container, profession/specialized container, reagent container and quiver/ammo-pouch family rules. Full template loading, `IsCraftingReagent` derivation and `CanStoreItem_InSpecificSlot` integration remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.060** Port `Player::CanStoreItem_InSpecificSlot` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `ItemPosCount`, duplicate destination suppression, empty-slot fit checks, existing-stack merge checks, source non-empty-bag/child-equipment guards and specialized bag-family integration match C++ `CanStoreItem_InSpecificSlot` using explicit `Item`/template bridge inputs. The current C++ keyring family check is preserved as written and tested as unreachable (`KEYRING_SLOT_START + KEYRING_SLOT_START - KEYRING_SLOT_END`); full object-registry lookup, template resolver loading, `_InBag`, `_InInventorySlots` and top-level `CanStoreItem` orchestration remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.061** Port `Player::CanStoreItem_InInventorySlots` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: top-level inventory-slot scanning matches C++ skip-slot, reagent-slot omission, source non-empty-bag rejection, move-source-as-empty handling, merge vs empty-slot modes, mergeability filtering, duplicate destination suppression and early return when requested count reaches zero. Full object-registry lookup, template resolver loading, `_InBag` and top-level `CanStoreItem` orchestration remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.062** Port `Player::CanStoreItem_InBag` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: bag-contained scanning matches C++ skip-bag, missing/self-target bag rejection, source non-empty-bag and child-item rejection, bag-template presence, specialized vs regular bag mode, `ItemCanGoIntoBag`, skip-slot, move-source-as-empty handling, merge vs empty-slot modes, mergeability filtering, duplicate destination suppression and early return when count reaches zero. Full object-registry lookup, template resolver loading and top-level `CanStoreItem` orchestration remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.063** Port `Player::CanStoreItem` representable orchestration.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: top-level storage orchestration matches C++ template-missing, source loot/bound, partial/full similar-item limit, specific-slot first pass, specific-bag/inventory pass, general merge pass, keyring/specialized bag pass, non-empty-bag-in-bag rejection, child-equipment free pass, new regular bag direct-equip search, fallback inventory/bag search and `InventoryResult`/`no_space_count` outcomes using explicit bridge inputs for template lookup, bag templates, slot item refs, `CanTakeMoreSimilarItems` and bound-with-player checks. Canonical template resolver, item object registry, real `CanTakeMoreSimilarItems`, BOA/BOP allowed-owner logic and top-level overloads remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.064** Port `Player::CanTakeMoreSimilarItems` representable max-count checks.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DBCEnums.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: template-missing, generated-loot, no-max, `INT32_MAX` max-count, entry max-count overflow, missing item-limit category entry, `ITEM_LIMIT_CATEGORY_MODE_HAVE` count overflow with offending item id, and `ITEM_LIMIT_CATEGORY_MODE_EQUIP` ignore path match C++ outcomes. `CanStoreItem` now computes similar-item limits through this helper; real inventory/bank/reagent item counting, gem count inclusion and DB2 condition-adjusted `GetItemLimitCategoryQuantity` remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.065** Port representable `GetItemCount` / `GetItemCountWithLimitCategory` counting.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: entry counting matches C++ location filtering for equipment/inventory plus optional bank, skips the source item by GUID and sums stack counts; limit-category counting matches C++ everywhere scan, skips source item and sums counts for templates with the requested limit category. `CanStoreItem` now derives current entry/category counts from explicit stored-item refs. Real object registry traversal, reagent-bank location, socketed gem counting and DB2-backed template lookup remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.066** Port representable `Item::IsBindedNotWith`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/player.rs`.
  Acceptance: soulbound, owner GUID, BOP-tradeable allowed-player and account-bound template cases match C++ `Item::IsBindedNotWith`; `CanStoreItem` now calls the item helper instead of receiving a precomputed bound-with-player boolean. Allowed-GUID ownership is closed by #NEXT.R8.ENTITIES.120; C++ template-flag derivation for `IsBoundAccountWide` is closed by #NEXT.R8.ENTITIES.121.

- [x] **#NEXT.R8.ENTITIES.067** Port `Player::CanBankItem` representable orchestration.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: bank storage orchestration matches C++ null item/template, generated-loot, bound-owner, currency-token, similar-item limit, bank-bag slot `WrongSlot`/`NoBankSlot`/`CanUseItem`, specific-slot first pass, specific bank bag/main-bank pass, general bank merge pass, special/regular bank bag search, main-bank free-slot search and `BankFull` fallback using explicit bridge inputs for source bag/currency/can-use and templates. Real `Item::IsBag`, `Item::IsCurrencyToken`, `CanUseItem`, object registry and template resolver remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.068** Port `Player::FindEquipSlot` representable destination selection.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: equipment destination selection matches C++ inventory-type mapping, explicit requested-slot/swap handling, free-slot search, offhand rejection while a two-hand item is used, swap replacement by lowest equipped item level, bag slot candidates, dual-wield/titan-grip gating and profession tool/gear routing. The C++ primary-profession gear overwrite behavior is preserved and documented. Real `CanEquipItem`, `CanUseItem`, player skill registry, `GetProfessionSlotFor`, item level resolver and equipped item object registry remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.069** Port `ItemTemplate::CanChangeEquipStateInCombat` representable helper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-entities/src/item.rs`.
  Acceptance: combat equip-state allowance matches C++ inventory-type exceptions (`Relic`, `Shield`, `Holdable`) and item-class exceptions (`Weapon`, `Projectile`), with other equipment templates rejected. Full `Player::CanEquipItem` runtime combat/arena/casting checks remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.070** Port `Player::CanEquipItem` representable orchestration.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: equipment validation matches C++ null item/template, generated-loot, bound-owner, similar-item limit, not-loading runtime guards, heirloom-level failure hook, `FindEquipSlot`, `CanUseItem`, occupied destination ordering, unique-equipped ignore-slot calculation, quiver/ammo uniqueness, offhand dual-wield/two-hand rules, two-hand offhand-store fallback and destination packing. Real `CanUseItem`, `CanEquipUniqueItem`, equipped object registry, skill/profession registries, spell state, battleground state, weapon-change timer and DB2 heirloom/content tuning remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.071** Port `Player::CanUnequipItem` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: unequip validation matches C++ equipment/bag-position applicability, empty-slot OK path, missing template, generated-loot, charmed, combat/arena restrictions using `CanChangeEquipStateInCombat`, non-empty bag rejection when not swapping and swap allowance. Real equipped object lookup and runtime player combat/battleground/charm state remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.072** Port `Player::CanUseItem(ItemTemplate const*)` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: template use validation matches C++ null template, internal/faction/class/race gates, required skill value/rank, required spell, optional required-level check, holiday lockout, reputation rank, already-known learning effect guard and artifact specialization mismatch. Real template flag extraction, race/class mask resolver, skill/spell/reputation/holiday/artifact stores and object-level `CanUseItem(Item*)` remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.073** Port `Player::CanUseItem(Item*)` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: item use validation matches C++ null item, dead-player `not_loading` guard, missing template, bound-owner, item required-level, template helper call with required-level skip, skill proficiency and heirloom armor morph allowance for Hunter/Shaman mail and Paladin/Warrior plate. Real item required-level/skill extraction, item quality resolver, player class/skill stores and full template resolver remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.074** Port `Player::CanEquipUniqueItem(ItemTemplate const*)` representable validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: unique-equipped template validation matches C++ unique-equippable entry/gem checks, except-slot exclusion, missing item-limit category, limit-count overflow, equipped item limit-category overflow and socketed gem limit-category overflow. Real item/gem equipped scans, DB2 condition-adjusted `GetItemLimitCategoryQuantity` and object-level socketed-gem wrapper remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.075** Port `Player::CanEquipUniqueItem(Item*)` representable wrapper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: object-level unique-equipped validation matches C++ base-template-first ordering, socketed gem traversal, missing gem-template skip, unequipped source gem limit-category count reuse and equipped source gem limit count of one. Real `sObjectMgr` gem template resolution, direct socket field traversal and DB2-backed source gem category counting remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.076** Port representable `Player::EquipItem` item-storage side effects.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: equip side effects match C++ empty-slot `VisualizeItem` call, item owner/contained/slot/container mutation, visible item update, bind-on-visualize behavior, equipped dynamic flag2, and existing-stack merge path with incoming item refundable/BOP-trade cleanup plus removed/changed states. Enchantment/item durations, set bonuses, item mods, weapon-swap cooldown packet, world add/remove updates, equip cooldown, expertise/rating/titan grip, criteria and average item-level updates remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.077** Port representable `Player::QuickEquipItem` item-storage side effects.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: quick-equip side effects match C++ null-free path through `VisualizeItem`, owner/contained/slot/container mutation, visible item update, item changed state and equipped dynamic flag2. Enchantment/item durations, world add/update packets, titan grip and criteria updates remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.078** Port representable `Player::RemoveItem` storage unlink.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: remove-item unlink matches C++ no-item no-op, top-level storage clear, visible equipment clear, equipped dynamic flag2 removal for top-level inventory/bag slots, bag-contained `Bag::RemoveItem` slot clear, item contained/slot/container unlink, and owner preservation. Enchantment/item duration removal, tradeable removal, set bonus/item mod/aura/enchant/rating/titan-grip/average-ilvl updates, packet sends and update-queue handling remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.079** Port representable `Player::MoveItemFromInventory` unlink wrapper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: move-from-inventory wrapper matches C++ no-item no-op, delegates to `RemoveItem` storage unlink and clears refundable state, refund recipient, paid money and paid extended cost while preserving owner. Quest removed counters, update-queue removal, temporary appearance removal and world destroy/update packets remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.080** Port representable `Player::MoveItemToInventory` post-store finalization.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: move-to-inventory finalization matches C++ original-item-only post-store handling: merged stack results are left untouched, original item owner is corrected to player GUID, and state becomes `ITEM_CHANGED` for character-inventory DB rows or `ITEM_NEW` otherwise. Full `StoreItem(dest, pItem)` multi-destination orchestration, BOP-tradeable registration, quest added counters and obtain criteria remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.081** Port representable `Player::DestroyItem` storage and item cleanup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: destroy-item cleanup matches C++ no-item no-op, top-level storage clear, visible equipment clear, bag-contained `Bag::RemoveItem` slot clear, refundable and BOP-tradeable cleanup, item contained/slot/container unlink, owner preservation and `ITEM_REMOVED` state. The C++ behavior of not explicitly clearing `ITEM_FIELD_FLAG2_EQUIPPED` during destroy is preserved for removed equipped items. Recursive non-empty bag destruction, wrapped gift DB delete, loot-storage cleanup, duration/tradeable/spell/script hooks, quest counters, world packets and average item-level/stat updates remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.082** Port representable `Player::DestroyItemCount(Item*, uint32&)`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: item-pointer destroy-count overload matches C++ null no-op, full-stack path subtracting the item count and delegating to `DestroyItem`, and partial-stack path decrementing item count, zeroing requested count and marking `ITEM_CHANGED`. Quest removed counters and world update packet sends remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.083** Port representable `Player::DestroyItemCount(entry, count)` scan planning.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: destroy-count-by-entry planning matches C++ scan order across inventory, keyring, inventory bag contents, equipment/bag-list, bank, bank bag contents, bank bag-list and child equipment; skips in-trade/nonmatching entries; emits full-stack destroy actions vs partial-stack decrement actions; stops when requested count is reached; and applies `unequip_check` only to full-stack top-level equipment/bag-list and bank-bag-list destroys. Real action execution across object registry, mutable bag refs, quest counters and world packets remains under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.084** Port representable `DestroyZoneLimitedItem` and `DestroyConjuredItems` scan planning.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: filtered destroy planning matches C++ scan order for zone-limited items over inventory, keyring, inventory bag contents and equipment/bag-list, and for conjured items over inventory, inventory bag contents and equipment/bag-list without keyring. Real `IsLimitedToAnotherMapOrZone`, `IsConjuredConsumable`, action execution through `DestroyItem`, map/zone state and world packets remain under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014.

- [x] **#NEXT.R8.ENTITIES.085** Port representable `Player::GetItemByEntry` and `GetItemListByEntry`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: entry lookup and listing use the C++ `ForEachItem` order over equipment, inventory, optional bank and reagent bank; `GetItemByEntry` stops on the first matching entry; `GetItemListByEntry` excludes bank by default and includes it before reagent bank when requested. Real object registry resolution remains bridged by caller-provided `ItemStorageRef` slices until #NEXT.R8.ENTITIES.013 owns canonical `Item*` storage.

- [x] **#NEXT.R8.ENTITIES.086** Port representable `Player::RemoveItemFromBuyBackSlot` item side effects.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: buyback removal now has an object-aware path matching C++ slot clearing, `InvSlots`/buyback price/timestamp reset, current-buyback-slot reuse, item `RemoveFromWorld`, and optional `ITEM_REMOVED` state when `del` is true. Stored-loot removal for `ITEM_FLAG_HAS_LOOT` remains parked until loot storage ownership is canonical.

- [x] **#NEXT.R8.ENTITIES.087** Port representable weapon/titan-grip helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `IsUseEquipedWeapon`, `SetCanTitanGrip`, `IsTwoHandUsed`, `IsUsingTwoHandedWeaponInOneHand` and the decision branch of `CheckTitanGripPenalty` are represented against explicit form/disarm/template/aura inputs. Actual aura lookup, `CastSpell` and `RemoveAurasDueToSpell` remain under spell/aura runtime work.

- [x] **#NEXT.R8.ENTITIES.088** Port representable `Player::SwapItem` preflight guards.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the first `SwapItem` decision block is represented with C++ order: missing source no-op, CHILD item redirect pairs, dead-player error before unequip checks, source unequip check with exact `swap` boolean, bag-in-self and same-slot-from-inside guards, and destination unequip check with exact `swap` boolean. Actual recursive swap execution, validation/store/equip calls, merge/fill, real two-item swaps, bag contents exchange, aura cleanup and loot release remain pending.

- [x] **#NEXT.R8.ENTITIES.089** Port representable `Player::SwapItem` empty-destination move plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the `!pDstItem` branch is represented with C++ destination ordering: inventory calls `CanStoreItem` and moves through `StoreItem` with bank-source quest-added hook, bank calls `CanBankItem` and moves through `BankItem` with quest-removed hook, equipment calls `CanEquipItem` and schedules `AutoUnequipOffhandIfNeed`, and invalid empty destinations return without action. Actual object mutation remains in the existing store/bank/equip/remove primitives until full `SwapItem` orchestration is wired.

- [x] **#NEXT.R8.ENTITIES.090** Port representable `Player::SwapItem` merge/fill plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the occupied non-bag merge/fill attempt is represented with C++ destination validation order, invalid-destination no-op, validation-failure fallthrough to real swap, full-stack merge through inventory/bank/equip destinations, partial fill count math, in-world update flag and refund-info scheduling. Real mutation remains pending in full `SwapItem` orchestration.

- [x] **#NEXT.R8.ENTITIES.091** Port representable `Player::SwapItem` real-swap validation plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: bidirectional real-swap validation is represented in C++ order: source-to-destination checks inventory/bank/equip with `swap=true`, equip destinations chain `CanUnequipItem(eDest,true)`, source-side failures report against source, destination-to-source repeats the same selection and reports against destination, and unclassified positions preserve the C++ no-validation shape. Bag content exchange, remove/store/equip execution, aura cleanup and loot release remain pending.

- [x] **#NEXT.R8.ENTITIES.092** Port representable `Player::SwapItem` bag-content exchange plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the special two-bag exchange branch is represented: it only triggers when both items are bags and exactly one empty bag is outside a bag position, validates every full-bag item against the empty bag, rejects incompatible contents with `BagInBag`, rejects too-small empty bags with `CantSwap`, and plans compacted slot moves in C++ scan order. Actual `Bag::RemoveItem`/`StoreItem` execution remains pending in full `SwapItem` orchestration.

- [x] **#NEXT.R8.ENTITIES.093** Port representable `Player::SwapItem` final real-swap execution plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: final real-swap actions are represented: destination is removed before source with `update=false`, source and destination are re-added to their planned targets, item-dependent aura cleanup is requested when either packed position is a top-level equipment/bag slot, loot release is requested when active loot exists in a moved bag, and `AutoUnequipOffhandIfNeed` is scheduled. Actual object mutation remains pending in full orchestration.

- [x] **#NEXT.R8.ENTITIES.094** Port representable `Player::SwapItem` branch orchestration plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the full `SwapItem` decision order is represented across the already ported phase plans: preflight/no-source/child-redirect/error terminals, empty-destination move terminals, merge/fill terminals, real-swap bidirectional validation, bag-content exchange, and final real-swap execution. Error item ordering matches C++ `SendEquipError` call sites and missing or inconsistent downstream phases are explicit plan results instead of silent success. Actual recursive calls and object mutation remain pending in the full `SwapItem` executor.

- [x] **#NEXT.R8.ENTITIES.095** Port object-aware `Player::AddItemToBuyBackSlot`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/item.rs`.
  Acceptance: buyback insertion now has the C++ object-aware path: selects current/free/oldest slot, removes overwritten buyback item with delete side effects, stores the new item GUID, computes buyback price from template sell price times item count, computes timestamp as `game_time - login_time + 30h`, updates `InvSlots`/buyback masks, and advances `m_currentBuybackSlot` with C++ non-wrapping behavior. Stored-loot removal for overwritten loot containers remains parked until loot storage ownership is canonical.

- [x] **#NEXT.R8.ENTITIES.096** Port representable `Player::SendEquipError` packet shape.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.*`.
  Rust targets: `crates/wow-packet/src/packets/item.rs`.
  Acceptance: `InventoryChangeFailure` now uses canonical `wow-constants::InventoryResult` values instead of the stale local duplicate, preserves C++ bag-result/item GUID/container slot order, writes `Level` for `CantEquipLevelI` and `PurchaseLevelTooLow`, writes `LimitCategory` for the three item-limit-category failures, and writes the auto-equip bind-confirm source/destination context. Player/session convenience wrapper is closed by #NEXT.R8.ENTITIES.112.

- [x] **#NEXT.R8.ENTITIES.097** Port representable `Player::SendBuyError`/`SendSellError` packet shape.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.*`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemDefines.h`.
  Rust targets: `crates/wow-packet/src/packets/misc.rs`, `crates/wow-world/src/handlers/character.rs`.
  Acceptance: `BuyFailed` now carries canonical `BuyResult` values and serializes `Reason` as C++ `uint8`; `SellResponse` now serializes `VendorGUID`, item GUID count, `Reason` as `int32`, then each item GUID, with helper constructors for C++ sell-error responses and current Rust success responses. Existing world buy/sell handlers were updated away from raw reason bytes to canonical error values. Player/session convenience wrappers are closed by #NEXT.R8.ENTITIES.118.

- [x] **#NEXT.R8.ENTITIES.098** Port representable `Player` soulbound tradeable item set.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `Player` now owns the `m_itemSoulboundTradeable` equivalent set, supports C++ `AddTradeableItem`/`RemoveTradeableItem` GUID insertion/removal, and represents `UpdateSoulboundTradeItems` cleanup by removing entries whose item is missing, owned by another player, or whose trade window has expired. The canonical `Item::CheckSoulboundTradeExpire` time calculation is closed by #NEXT.R8.ENTITIES.117.

- [x] **#NEXT.R8.ENTITIES.099** Port representable `Player` item and enchantment duration lists.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `Player` now owns the `m_itemDuration` and `m_enchantDuration` equivalents, ports `AddItemDurations`/`RemoveItemDurations` and `UpdateItemDuration` as C++-ordered actions, ports `AddEnchantmentDurations`, `AddEnchantmentDuration`, `RemoveEnchantmentDurations`, `RemoveEnchantmentDurationsReferences` and `UpdateEnchantTime`, preserves replacement behavior that saves old enchant left-duration back into the item, converts enchant time updates from milliseconds to seconds like C++, and emits explicit actions for expired/missing enchantments. Actual item expiration destruction, script hook dispatch, `ApplyEnchantment` stat effects and packet sending remain pending in canonical runtime wiring.

- [x] **#NEXT.R8.ENTITIES.100** Port representable `Player::RemoveArenaEnchantments`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: arena enchantment cleanup is represented in C++ order: first walks `m_enchantDuration`, keeps duration entries whose enchantment is arena-allowed, clears non-allowed equipped enchantments with stat-removal action, removes stale/zero duration references, then scans backpack slots up to `GetInventorySlotCount()` and equipped inventory bags in slot order to clear non-allowed inventory enchantments. Real `sSpellMgr->IsArenaAllowedEnchancment` store lookup is closed by #NEXT.R8.ENTITIES.114; `ApplyEnchantment`, item mutation and packet/update dispatch remain pending in runtime wiring.

- [x] **#NEXT.R8.ENTITIES.101** Port representable duration resend helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`.
  Acceptance: `SendEnchantmentDurations` and `SendItemDurations` are represented over the canonical player duration lists, preserving list order, duplicate entries, and C++ enchant milliseconds-to-seconds conversion for `SendItemEnchantTimeUpdate`. Session packet delivery is closed by #NEXT.R8.ENTITIES.111.

- [x] **#NEXT.R8.ENTITIES.102** Port representable `Player::ApplyEnchantment` guard/duration shape.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `ApplyEnchantment` now has a C++-ordered plan for missing item, non-equipped item, empty enchantment slot, missing enchantment template, condition failure, player level, required skill, prismatic socket requirements, gem skill requirements, broken-item effect suppression, permanent-enchant visible-item update, and `apply_dur` duration add/remove behavior via the canonical enchant-duration list. Actual enchantment effect execution for damage/stat/rating/spell/totem effects remains pending in follow-up subtasks.

- [x] **#NEXT.R8.ENTITIES.103** Port representable `ApplyEnchantment` effect classification.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: the non-broken enchantment effect loop now has C++-ordered action planning for `None`, deferred combat/use spells, damage/totem `GetAttackBySlot` resolution, equip-spell cast/remove-aura actions, resistance/stat modifier placeholders with original amount/arg, explicit no-op enchantment types and unknown display-type reporting. Detailed stat/rating expansion and actual spell/stat runtime execution remain pending.

- [x] **#NEXT.R8.ENTITIES.104** Expand `ApplyEnchantment` stat/rating effect actions.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `ITEM_ENCHANTMENT_TYPE_STAT` now expands the C++ `ITEM_MOD_*` switch into concrete action lists for mana/health base modifiers, primary stats plus `UpdateStatBuffMod`, individual and combined combat ratings, attack power/ranged attack power, mana regen, armor penetration, spell power, health regen, spell penetration and shield block value. Runtime application to active player fields/unit mods remains pending.

- [x] **#NEXT.R8.ENTITIES.105** Resolve `ApplyEnchantment` random suffix stat/resistance amounts.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `ITEM_ENCHANTMENT_TYPE_RESISTANCE` and `ITEM_ENCHANTMENT_TYPE_STAT` now follow the C++ zero-amount path by matching `abs(RandomPropertiesID)` to an explicit `ItemRandomSuffix` ref, scanning suffix enchantment slots in order, and calculating `AllocationPct[k] * PropertySeed / 10000` before producing effect actions. Real DB2 store lookup wiring is closed by #NEXT.R8.ENTITIES.113.

- [x] **#NEXT.R8.ENTITIES.106** Port representable `Player::UpdateSkillEnchantments`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `UpdateSkillEnchantments` is represented as a C++-ordered plan over top-level `m_items[0..INVENTORY_SLOT_BAG_END)`, each enchantment slot, normal enchantment template lookup with early abort on missing template, required-skill threshold apply/remove transitions, and prismatic socket requirement rechecks for colorless socket gems. Real `ApplyEnchantment` invocation and DB2/socket-color wiring remain pending.

- [x] **#NEXT.R8.ENTITIES.107** Port representable `Player::SendNewItem` packet plan.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `SendNewItem` is represented as a packet plan that preserves the null-item no-op, player/item GUIDs, bag slot and slot-in-bag quantity rule, quest log item id, quantity and quantity-in-inventory, battle pet modifier extraction, pushed/created flags, encounter loot display/dungeon fields, and direct-vs-group broadcast selection with the party loot-log suppression flag. Session/group delivery bridge is closed by #NEXT.R8.ENTITIES.110; concrete call-site replacement remains pending with canonical entity inventory operations.

- [x] **#NEXT.R8.ENTITIES.108** Port `ItemInstance` and `ItemPushResult` packet serialization.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPacketsCommon.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPacketsCommon.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.h`.
  Rust targets: `crates/wow-packet/src/packets/item.rs`.
  Acceptance: `ItemInstance`, `ItemModList`, `ItemMod`, `ItemBonuses` and `ItemPushResult` serialize in C++ field order, using packed GUIDs, the same item-bonus presence bit, 6-bit modification count, item bonus payload order, and `ItemPushResult` pushed/created/display/bonus-roll/encounter-loot bit layout. Conversion from `wow-entities` send-new-item plans to session/group dispatch is closed by #NEXT.R8.ENTITIES.110.

- [x] **#NEXT.R8.ENTITIES.109** Close `SendNewItem` item-instance plan gap.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPacketsCommon.cpp`.
  Rust targets: `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `SendNewItemPlan` now includes the item instance fields C++ gets from `packet.Item.Initialize(item)`: item id, random property seed, random properties id and the non-zero item modifier values with their modifier type ids. This keeps packet conversion lossless for the item-instance data currently represented by Rust entities and feeds the session/group delivery bridge closed by #NEXT.R8.ENTITIES.110.

- [x] **#NEXT.R8.ENTITIES.110** Wire `SendNewItemPlan` conversion and delivery.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.cpp`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/Cargo.toml`.
  Acceptance: `wow-world` can convert a complete `wow-entities::SendNewItemPlan` into the serialized `ItemPushResult` packet, preserving C++ `SendNewItem` fields, item-instance fields and encounter/direct display behavior. Direct delivery sends to the current session; group delivery broadcasts to all registered group members, including the current player, with direct fallback if group state is unavailable. Call-site replacement in concrete loot/quest/vendor flows remains pending where those flows adopt canonical entity inventory operations.

- [x] **#NEXT.R8.ENTITIES.111** Wire item duration update packet delivery.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/ItemHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.h`.
  Rust targets: `crates/wow-packet/src/packets/item.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `ItemTimeUpdate` and `ItemEnchantTimeUpdate` serialize in C++ field order with packed item/owner GUIDs, and `WorldSession` can deliver `PlayerItemTimeUpdate` plus `PlayerEnchantTimeUpdate` plans while preserving C++ list order through slice helpers.

- [x] **#NEXT.R8.ENTITIES.112** Wire `SendEquipError` session helper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.h`.
  Rust targets: `crates/wow-packet/src/packets/item.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `WorldSession::send_equip_error` mirrors C++ `Player::SendEquipError` packet construction for result, optional item GUIDs, level-gated failures and item-limit-category failures. `InventoryChangeFailure` also covers the C++ bind-confirm context fields for callers that need that packet branch.

- [x] **#NEXT.R8.ENTITIES.113** Wire DB2 `ItemRandomSuffix` lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-data/src/item_random_suffix.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `ItemRandomSuffix.db2` loads C++ `ItemRandomSuffixEntry` ID, five enchantment refs and five allocation percentages; world-server shares the store with sessions; `WorldSession::apply_enchantment_random_suffix_ref` resolves `abs(RandomPropertiesID)` into the entity-level suffix ref used by `ApplyEnchantment` amount calculation.

- [x] **#NEXT.R8.ENTITIES.114** Wire DB2 `SpellItemEnchantment` lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-data/src/spell_item_enchantment.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `SpellItemEnchantment.db2` loads C++ enchantment effect args, effect types, min points, flags, condition, level and skill gates; world-server shares the store with sessions; `WorldSession` can resolve arena-allowed enchantments, `ApplyEnchantmentTemplateRef`, and the three C++ effect refs from the DB2 row.

- [x] **#NEXT.R8.ENTITIES.115** Wire DB2 `ItemModifiedAppearance` lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`.
  Rust targets: `crates/wow-data/src/item_modified_appearance.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `ItemModifiedAppearance.db2` loads C++ IDs, item IDs, appearance modifier IDs, appearance IDs, order and source type; the store indexes direct IDs plus C++ `itemId | (appearanceMod << 24)` keys with unmodified fallback; world-server shares the store with sessions for visible entry/appearance-mod closures.

- [x] **#NEXT.R8.ENTITIES.116** Wire DB2 `ItemAppearance` display-info lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`.
  Rust targets: `crates/wow-data/src/item_appearance.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `ItemAppearance.db2` loads C++ display type, item display info, default icon and UI order; world-server shares the store with sessions; `WorldSession::item_display_id` mirrors `DB2Manager::GetItemDisplayId` by combining `ItemModifiedAppearance` item/modifier fallback with `ItemAppearance.ItemDisplayInfoID`.

- [x] **#NEXT.R8.ENTITIES.117** Port canonical `Item` refund and BoP trade expiry timers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/item.rs`, `crates/wow-entities/src/player.rs`, `crates/wow-entities/src/lib.rs`.
  Acceptance: `Item::played_time(now)` mirrors C++ `GetPlayedTime()` using `CreatePlayedTime + elapsed`, refund expiry uses the strict C++ `> 2*HOUR` threshold, and BoP trade expiry uses `CreatePlayedTime + 2*HOUR < owner.GetTotalPlayedTime()` through `SoulboundTradeableItemRef::from_item`. Runtime collection appearance registration and DB deletion from `ClearSoulboundTradeable` remain pending with canonical player/session persistence wiring.

- [x] **#NEXT.R8.ENTITIES.118** Wire `SendBuyError` and `SendSellError` session helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPackets.h`.
  Rust targets: `crates/wow-world/src/session.rs`.
  Acceptance: `WorldSession::send_buy_error` mirrors C++ `Player::SendBuyError` by using the creature GUID when present, `ObjectGuid::EMPTY` when absent, the C++ `Muid` item value and canonical `BuyResult`; `WorldSession::send_sell_error` mirrors C++ `Player::SendSellError` by sending one item GUID, the vendor/empty fallback and canonical `SellResult`.

- [x] **#NEXT.R8.ENTITIES.119** Replace existing vendor error call-sites with session helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/ItemHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/handlers/character.rs`.
  Acceptance: current Rust buy/sell handlers route all existing `BuyFailed` and sell-error responses through `WorldSession::send_buy_error`/`send_sell_error`, matching the C++ handler pattern of delegating error packet construction to `Player::SendBuyError`/`SendSellError`. Full canonical vendor/inventory behavior remains pending with the runtime inventory bridge.

- [x] **#NEXT.R8.ENTITIES.120** Port `Item` BoP trade allowed-GUID ownership.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`.
  Rust targets: `crates/wow-entities/src/item.rs`.
  Acceptance: `Item::set_soulbound_tradeable` stores the allowed GUID set and sets `BOP_TRADEABLE`; object-level bind checks can derive allowed-player status from the owned set; `clear_soulbound_tradeable` clears the flag and allowed set while reporting whether C++ would run collection/persistence side effects. Collection appearance registration and DB deletion remain pending with runtime persistence wiring.

- [x] **#NEXT.R8.ENTITIES.121** Derive account-bound item checks from C++ `ItemFlags`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-entities/src/item.rs`.
  Acceptance: `ItemStorageTemplate` carries C++ template `ItemFlags` instead of a standalone `is_bound_account_wide` boolean, and `Item::is_binded_not_with` uses `ItemFlags::IS_BOUND_TO_ACCOUNT` to mirror `GetTemplate()->HasFlag(ITEM_FLAG_IS_BOUND_TO_ACCOUNT)`. DB2 `ItemSparse` flag extraction is closed by #NEXT.R8.ENTITIES.122; full template resolver population remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.122** Read C++ `ItemSparseEntry::Flags` in the data store.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-data/src/item_stats.rs`.
  Acceptance: `ItemStatsStore` keeps the existing stat-only lookup semantics while also retaining `ItemSparseEntry::Flags[0..4]` for every parsed record; `item_flags(entry)` exposes `Flags[0]` as C++ `ItemFlags`, including `ITEM_FLAG_IS_BOUND_TO_ACCOUNT`. Wiring these flags into a canonical `ItemStorageTemplate` resolver remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.123** Expose `ItemSparse` template flags through `WorldSession`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-data/src/item_stats.rs`.
  Acceptance: `WorldSession::item_template_flags(entry)` resolves C++ `ItemTemplate::ExtendedData->Flags[0]` from the loaded `ItemStatsStore`, and `WorldSession::is_item_bound_account_wide(entry)` mirrors `Item::IsBoundAccountWide` through `ITEM_FLAG_IS_BOUND_TO_ACCOUNT`. Building full `ItemStorageTemplate` values for inventory validation still remains under #NEXT.R8.ENTITIES.013 because the remaining template fields are not yet loaded as one canonical resolver.

- [x] **#NEXT.R8.ENTITIES.124** Read the `ItemSparse` fields needed by `ItemStorageTemplate`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`.
  Rust targets: `crates/wow-data/src/item_stats.rs`.
  Acceptance: `ItemSparseTemplateEntry` preserves C++ `ExtendedData` fields needed by storage validation: `Flags[0..4]`, `BagFamily`, `Stackable`, `MaxCount`, `SellPrice`, `MaxDurability`, `LimitCategory`, `Bonding`, `ContainerSlots` and `InventoryType`; `max_stack_size()` mirrors C++ `ItemTemplate::GetMaxStackSize`, including the `0x7ffffffe` fallback for non-positive or `INT32_MAX` stack sizes. Wiring this together with `Item.db2` class/subclass into a canonical `ItemStorageTemplate` resolver remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.125** Build a canonical session `ItemStorageTemplate` resolver.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-data/src/item.rs`.
  Acceptance: `WorldSession::item_storage_template(entry)` combines C++ `ItemTemplate::BasicData` from `Item.db2` (`ClassID`, `SubclassID`) with `ExtendedData` from `ItemSparse.db2` (`InventoryType`, `Bonding`, `BagFamily`, stack/count/limit/container/sell/flags), derives `IsCraftingReagent` from `Flags[1] & ITEM_FLAG2_USED_IN_A_TRADESKILL`, and intentionally uses sparse `InventoryType` instead of the legacy `ItemStore::inventory_type` helper. Replacing remaining call-sites that still read `Item.db2` inventory type directly remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.126** Route character inventory caches through C++ `ItemTemplate::GetInventoryType`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/ItemHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`.
  Acceptance: `WorldSession::item_template_inventory_type(entry)` wraps the canonical template resolver and excludes `INVTYPE_NON_EQUIP`; character load, vendor buy insertion and auto-equip cache reads use that helper instead of `ItemStore::inventory_type`, preserving only the old equipped-slot fallback for missing DB2 template data.

- [x] **#NEXT.R8.ENTITIES.127** Back `ObjectAccessor` item lookup with registered `Item` objects.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`.
  Acceptance: `AccessorPlayer` owns a GUID-indexed `Item` registry alongside `PlayerInventoryStorage`; `ObjectAccessor::get_object_ref_by_type_mask(..., TYPEMASK_ITEM)` now returns an `AccessorObjectRef::Item(&Item)` only when the GUID is both present in the player's inventory storage and registered as a real item object, matching the C++ `Player::GetItemByGuid` pointer branch. Runtime population from login/store/buy paths remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.128** Populate runtime session item objects for direct inventory paths.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-database/src/statements/character.rs`.
  Acceptance: `WorldSession` keeps a GUID-indexed `Item` object registry in step with the existing direct-inventory cache; login loads `count`, `durability` and `context` from `item_instance`, creates `Item` objects with template max durability and `ITEM_UNCHANGED` state, vendor buy registers a new `Item` object, swap/auto-equip updates item slots, and sell/destroy remove item objects. Wiring this registry into a shared runtime `ObjectAccessor`/canonical `Player` owner remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.129** Wire session-owned item objects into shared `ObjectAccessor`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectAccessor.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-entities/src/object_accessor.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `world-server` owns a shared `ObjectAccessor`; each logged-in `WorldSession` registers a player snapshot with `WorldObject`, `PlayerInventoryStorage` and cloned runtime `Item` objects; direct inventory mutations resync that snapshot; movement/teleport keeps the accessor player position/map current; logout/disconnect unregisters the player and clears runtime item objects. This closes the direct-inventory `ObjectAccessor::GetObjectByTypeMask(TYPEMASK_ITEM)` bridge against C++ `Player::GetItemByGuid`; canonical `Player` entity ownership, bags/bank/buyback and full store/equip side effects remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.130** Add direct-inventory `CanStoreItem` planning bridge.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/session.rs`.
  Acceptance: `WorldSession::plan_store_new_direct_inventory_item(entry, count)` builds a temporary C++-style `Player` storage snapshot from the current direct inventory and runtime `Item` objects, resolves `ItemStorageTemplate` from DB2 stores, and calls the ported `Player::can_store_item` with `NULL_BAG`/`NULL_SLOT` like C++ `CanStoreNewItem`. The covered test proves stackable purchases plan existing-stack fill before empty backpack slots. Executing multi-position store plans in `BuyItem` remains under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.131** Execute direct-inventory vendor buys through multi-position store plans.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/ItemHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, `crates/wow-packet/src/packets/update.rs`, `crates/wow-database/src/statements/character.rs`.
  Acceptance: `handle_buy_item` consumes `WorldSession::plan_store_new_direct_inventory_item` instead of choosing the first empty backpack slot. Direct-inventory purchase execution mirrors C++ `StoreNewItem` for representable bag-0 destinations: existing stacks update `item_instance.count`, runtime `Item::count`, and item VALUES deltas; empty destinations create one item instance/inventory row/runtime `Item` per planned stack, with real stack count and durability in item CREATE blocks; money and inventory DB writes commit atomically. Remaining vendor gaps are price/discount/extended-cost/equipment purchase paths and canonical `Player` ownership.

- [x] **#NEXT.R8.ENTITIES.132** Use C++ vendor stack count for direct vendor buy money price.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-world/src/handlers/character.rs`.
  Acceptance: `SEL_VENDOR_ITEM_PRICE` returns `ItemSparse.VendorStackCount`, and direct vendor buy money price uses `BuyPrice / max(VendorStackCount, 1) * quantity` like C++ `ItemTemplate::GetBuyCount()` and `Player::BuyItemFromVendorSlot`. Reputation discounts, aura price modifiers, overflow clamping and extended costs remain tracked under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.133** Clamp direct vendor buy quantity to C++ max-money price limit.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-world/src/handlers/character.rs`.
  Acceptance: direct vendor buy price calculation uses C++ `MAX_MONEY_AMOUNT = 99999999999` and clamps the requested quantity before planning storage, charging money and sending `BuySucceeded.QuantityBought` when `BuyPrice / BuyCount * quantity` would exceed that cap. Reputation discounts, aura price modifiers and extended costs remain tracked under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.183** Open HAS_LOOT items inside represented carried bags like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/SpellHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Container/Bag.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootItemStorage.cpp`.
  Rust targets: `crates/wow-database/src/statements/character.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `CMSG_OPEN_ITEM` resolves `(bag, slot)` through the Rust equivalent of `Player::GetItemByPos`: `bag == INVENTORY_SLOT_BAG_0` keeps top-level direct inventory with buyback excluded, represented carried bag slots resolve nested runtime items, login loads child rows by joining `character_inventory.bag` to the represented top-level bag item GUID, invalid/missing represented bags are skipped instead of producing `ObjectGuid::EMPTY` containers, nested positions preserve `bag_slot == top-level bag slot` and `slot == inner slot`, and fully looted release removes the nested item without deleting the container bag. Bank/reagent bags, lock/key checks, wrapped gifts, random properties/context, quest/condition/rate loot branches, scripts and collection appearance side effects remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.184** Open HAS_LOOT items inside represented bank and reagent bags like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/SpellHandler.cpp`.
  Rust targets: `crates/wow-database/src/statements/character.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `CMSG_OPEN_ITEM` lookup now mirrors the next C++ `Player::GetItemByPos`/`GetBagByPos` step for represented bags: direct top-level lookup still excludes buyback, nested lookups accept carried bag, bank bag and reagent bag top-level slots only, login loads child `character_inventory` rows whose represented top-level bag row is carried/bank/reagent, missing represented bags are skipped instead of producing empty containers, and fully looted nested release removes the child while preserving the bag. Full Lock.db2/sLockStore lookup, wrapped gifts, random properties/context preservation, quest/condition/rate loot branches, scripts and collection appearance side effects remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.185** Reject locked open-item containers like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/SpellHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`.
  Rust targets: `crates/wow-data/src/item_stats.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `ItemSparseTemplateEntry` now preserves `LockID`, `WorldSession::item_template_lock_id` exposes the represented `ItemTemplate::GetLockID()` value, and `CMSG_OPEN_ITEM` sends `EQUIP_ERR_ITEM_LOCKED` without creating item loot when a `HAS_LOOT` item has `LockID != 0` and the runtime item is still locked. Items with the same locked template but `ItemFieldFlags::UNLOCKED` continue through the existing loot path. `Lock.db2`/`sLockStore` existence lookup is closed by #NEXT.R8.ENTITIES.188; wrapped gifts, random/context preservation, quest/condition/rate loot branches, scripts and collection appearance side effects remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.186** Open wrapped gifts like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/SpellHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp`, `/home/server/woltk-trinity-legacy/src/server/database/Database/Implementation/CharacterDatabase.cpp`.
  Rust targets: `crates/wow-database/src/statements/character.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: `CMSG_OPEN_ITEM` now opens represented wrapped gifts like Trinity: wrapped runtime items pass the open gate even without `HAS_LOOT`, never generate item loot, load `character_gifts(entry, flags)`, revalidate the same still-wrapped runtime item, clear `giftCreatorGuid`, replace entry/flags/durability fields, update top-level inventory metadata when applicable, persist the `item_instance` gift transformation and delete `character_gifts` transactionally when the character DB is available; if `character_gifts` has no row, Rust follows C++ by destroying the stale wrapped item instead of leaving it lootable or intact. `Lock.db2`/`sLockStore` existence lookup is closed by #NEXT.R8.ENTITIES.188; random/context preservation, quest/condition/rate loot branches, scripts and collection appearance side effects remain under #NEXT.R8.ENTITIES.013.

- [x] **#NEXT.R8.ENTITIES.187** Preserve stored open-item loot random properties/context and port CMSG_LOOT_ITEM store-time random generation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h`, `Loot.cpp`, `LootItemStorage.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemEnchantmentMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `crates/wow-data/src/item.rs`, `crates/wow-data/src/item_stats.rs`, `crates/wow-data/src/item_random_properties.rs`, `crates/wow-data/src/item_random_enchantment.rs`, `crates/wow-data/src/rand_prop_points.rs`, `crates/wow-database/src/statements/character.rs`, `crates/wow-database/src/statements/world.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `CMSG_OPEN_ITEM` preserves represented stored `item_loot_items.random_properties_id`, `random_properties_seed` and `context` in `LootEntry`; `LootResponse.item_context` uses the stored context; saving stored loot writes the same metadata. `CMSG_LOOT_ITEM` now follows C++ `Player::StoreLootItem` by generating store-time random properties from `ItemEnchantmentMgr::GenerateRandomProperties(itemid)`: Rust loads `Item.db2` `RandomSelect`/`ItemRandomSuffixGroupID`, `ItemRandomProperties.db2`, `ItemRandomSuffix.db2`, `RandPropPoints.db2` and validated `item_random_enchantment_template` groups, then selects weighted random-property IDs as positive values with seed 0 or random-suffix IDs as negative values with C++ `GetRandomPropertyPoints` seed. Represented `QuestRequired`, `Quest::ItemDrop` and `ItemSparse.StartQuestID` quest-status eligibility are closed by #NEXT.R8.ENTITIES.189; represented `Rate.Drop.*` loot modifiers are closed by #NEXT.R8.ENTITIES.190; `QuestLogItemId` and `ITEM_FLAGS_CU_IGNORE_QUEST_STATUS` are closed by #NEXT.R8.ENTITIES.191; represented loot-row condition linking/evaluation is closed by #NEXT.R8.ENTITIES.192; represented `LootItem` metadata persistence is closed by #NEXT.R8.ENTITIES.193. Group/master/NBG loot behavior and scripts/collection appearance side effects remain pending.

- [x] **#NEXT.R8.ENTITIES.188** Port `CMSG_OPEN_ITEM` `Lock.db2`/`sLockStore` existence gate.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/SpellHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`.
  Rust targets: `crates/wow-data/src/lock.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`.
  Acceptance: `Lock.db2` now loads C++ `LockEntry` ID, Index/Skill/Type/Action arrays; world-server shares `LockStore` with sessions; `CMSG_OPEN_ITEM` mirrors Trinity by rejecting a non-zero template `LockID` when `sLockStore.LookupEntry(lockId)` is missing, and only then checking the runtime item locked flag. This closes the represented `sLockStore` existence gate; real key/spell unlock behavior remains outside `CMSG_OPEN_ITEM`.

- [x] **#NEXT.R8.ENTITIES.189** Port represented `QuestRequired` item loot eligibility for `CMSG_OPEN_ITEM`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootItemStorage.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Quests/QuestDef.h`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-data/src/quest.rs`, `crates/wow-data/src/item_stats.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`.
  Acceptance: item loot generation no longer rejects every `QuestRequired` row. Plain and grouped item_loot_template/reference_loot_template rows now pass when the current session has either an active incomplete quest objective of C++ `QUEST_OBJECTIVE_ITEM` type for the row item or an active incomplete quest with matching `Quest::ItemDrop[]` still below its C++ max allowed count. Rust also loads `ItemSparse.StartQuestID` and applies the represented `AllowedForPlayer` status check for start-quest items when the quest status is non-none. This matches the represented branches of `LootItem::AllowedForPlayer` -> `Player::HasQuestForItem`; rows remain hidden when the player lacks the represented quest objective/drop allowance. Stored item loot loading also no longer discards saved `NeedsQuest` rows, matching Trinity's `LoadStoredLoot` behavior after save-time `AllowedForPlayer` filtering. `ItemTemplate::QuestLogItemId` and `ITEM_FLAGS_CU_IGNORE_QUEST_STATUS` are closed by #NEXT.R8.ENTITIES.191; represented loot-row conditions are closed by #NEXT.R8.ENTITIES.192; represented `LootItem` metadata persistence is closed by #NEXT.R8.ENTITIES.193. Remaining unrepresented subbranches are full canonical `ConditionMgr`, group/master/NBG loot behavior and script/collection side effects. Represented `Rate.Drop.*` modifiers are closed by #NEXT.R8.ENTITIES.190.

- [x] **#NEXT.R8.ENTITIES.190** Port represented `Rate.Drop.*` modifiers for `CMSG_OPEN_ITEM` item loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/World/World.h`, `/home/server/woltk-trinity-legacy/src/server/worldserver/worldserver.conf.dist`.
  Rust targets: `crates/wow-config/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-network/src/lib.rs`, `crates/world-server/src/main.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/cpp-world-config-registry.tsv`, `docs/migration/inventory/cpp-config-keys.tsv`.
  Acceptance: the world config registry now includes `RATE_DROP_ITEM_POOR`, `RATE_DROP_ITEM_NORMAL`, `RATE_DROP_ITEM_UNCOMMON`, `RATE_DROP_ITEM_RARE`, `RATE_DROP_ITEM_EPIC`, `RATE_DROP_ITEM_LEGENDARY`, `RATE_DROP_ITEM_ARTIFACT`, `RATE_DROP_ITEM_REFERENCED`, `RATE_DROP_ITEM_REFERENCED_AMOUNT` and `RATE_DROP_MONEY`; `world-server` reads those keys from `WorldConfigSet` and passes them to each session; represented item money loot multiplies C++ `Loot::generateMoneyLoot` output by `RATE_DROP_MONEY`; plain and grouped item loot chances multiply by the C++ per-quality item rate; reference row chance and reference `MaxCount` multiply by referenced and referenced-amount rates. Remaining gaps: full canonical `ConditionMgr` beyond the represented loot subset, group/master/NBG loot behavior, script/collection side effects and non-`CMSG_OPEN_ITEM` loot stores.

- [x] **#NEXT.R8.ENTITIES.191** Port represented `QuestLogItemId` and `ITEM_FLAGS_CU_IGNORE_QUEST_STATUS` for `CMSG_OPEN_ITEM` item loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`, `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`.
  Acceptance: item loot eligibility now reads represented `item_template_addon.FlagsCu` and `QuestLogItemId` like Trinity's `ObjectMgr::LoadItemTemplateAddon`; `ITEM_FLAGS_CU_IGNORE_QUEST_STATUS` bypasses the `needs_quest`/start-quest status gate exactly like `LootItem::AllowedForPlayer`; `QuestLogItemId` is checked as the alternate `QUEST_OBJECTIVE_ITEM` object id used by C++ `Player::GetQuestObjectiveForItem`; plain and grouped item-loot rows both use the same metadata-aware gate. Represented `ITEM_FLAGS_CU_FOLLOW_LOOT_RULES` metadata persistence is closed by #NEXT.R8.ENTITIES.193; group/master/NBG behavior remains pending because it does not alter the basic `CMSG_OPEN_ITEM` item visibility predicate. Represented loot-row conditions are closed by #NEXT.R8.ENTITIES.192; full canonical `ConditionMgr` remains pending.

- [x] **#NEXT.R8.ENTITIES.192** Port represented loot-template condition linking/evaluation for `CMSG_OPEN_ITEM` item loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h`, `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.h`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`.
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_OPEN_ITEM` item loot now loads `conditions` rows for `CONDITION_SOURCE_TYPE_ITEM_LOOT_TEMPLATE` and `CONDITION_SOURCE_TYPE_REFERENCE_LOOT_TEMPLATE` with `SourceId = 0`, attaches them to the matching `LootStoreItem` analogue by `(SourceGroup = loot entry, SourceEntry = item id)`, and checks them before the quest/template visibility gate like `LootItem::AllowedForPlayer`. The bridge implements C++ `ElseGroup` OR / in-group AND, `NegativeCondition`, level comparisons, item count in represented inventory, team/class/race/gender/spell, rewarded/taken/none/complete quest states, `CONDITION_QUESTSTATE`, and represented objective-progress checks. Unsupported reference conditions, non-player condition targets, script-backed conditions and unsupported condition types fail closed rather than granting loot. This is not the full reusable `ConditionMgr`; canonical conditions for non-item loot, phasing, SmartAI, gossip, vendors and scripts remain pending.

- [x] **#NEXT.R8.ENTITIES.193** Preserve represented C++ `LootItem` metadata for `CMSG_OPEN_ITEM` item loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootItemStorage.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_OPEN_ITEM` item loot now carries the C++ `LootItem` booleans in `LootEntry`: `follow_loot_rules`, `freeforall`, `blocked`, `counted`, `under_threshold` and `needs_quest`. Generated rows compute `freeforall` from `ITEM_FLAG_MULTI_DROP`, compute `follow_loot_rules` like Trinity's constructor as `!needs_quest || ITEM_FLAGS_CU_FOLLOW_LOOT_RULES`, and initialize blocked/counted/under-threshold to false. Stored item loot loading preserves the saved booleans from `item_loot_items`; new stored item loot persists those same booleans instead of writing constants. This closes represented metadata/persistence parity only; actual group/master/NBG roll routing, threshold blocking and per-player FFA partitioning remain pending under the canonical loot work.

- [x] **#NEXT.R8.ENTITIES.194** Apply represented `LootItem` metadata to `FREE_FOR_ALL` loot response item view.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/loot.md`.
  Acceptance: represented `LootResponse` item construction no longer hardcodes every item as UI type 0. For the currently represented `FREE_FOR_ALL` loot method, Rust mirrors `LootItem::GetUiTypeForPlayer` by sending `LOOT_SLOT_TYPE_OWNER = 4`. The first implementation still used an ad-hoc row layout for extra metadata bits; #NEXT.R8.ENTITIES.213 supersedes that packet shape with the real C++ 3.4.3 `LootItemData` layout. This covers `CMSG_OPEN_ITEM` item loot and the current creature-loot bridge response; non-FFA group/master/NBG routing, roll-winner visibility and per-player FFA item partitioning remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.195** Guard represented `CMSG_LOOT_ITEM` by the player's active loot view and blocked item state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`.
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ITEM` no longer accepts arbitrary loot GUIDs from the session-global loot table. Until Rust has real `AELootView`, the non-AE bridge only accepts the current `active_loot_guid`, mirroring Trinity's lookup through the player's loot view; inactive GUID requests now send a loot release and do not mark entries taken. Items with `LootEntryFlags.blocked` now follow `Player::StoreLootItem` by releasing the active loot view without storing or marking the item taken. Represented allowed-looter and roll-winner guards are closed by #NEXT.R8.ENTITIES.196. Full AE loot, per-player FFA state and inventory-side criteria/news remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.196** Guard represented `CMSG_LOOT_ITEM` by `allowedGUIDs` and `rollWinnerGUID`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/loot.md`.
  Acceptance: `LootEntry` now carries represented C++ `allowedGUIDs` and `rollWinnerGUID` metadata. Opening represented loot marks visible entries as allowed for the current player; `CMSG_LOOT_ITEM` checks `HasAllowedLooter(player)` before blocked/store logic and releases the active loot view when the player is not allowed. It also mirrors the C++ protected-roll branch by releasing without storing when `rollWinnerGUID` is non-empty and not the current player. This is still metadata-level parity; full roll creation, winner assignment, group eligibility broadcasts and per-player FFA state remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.197** Filter represented `LootResponse` item visibility by `allowedGUIDs`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootItem::GetUiTypeForPlayer`, `Loot::BuildLootResponse`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LootItem::allowedGUIDs`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `LootResponse` item construction now mirrors the first C++ `LootItem::GetUiTypeForPlayer` visibility gate by hiding untaken item rows whose `allowedGUIDs` does not contain the current player. This applies to the current `CMSG_LOOT_UNIT` bridge and represented `CMSG_OPEN_ITEM` item-loot bridge before mapping visible rows to `LootItemData`. Full per-player FFA partitioning through `GetPlayerFFAItems`, non-FFA group/master/NBG UI routing and roll visibility remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.198** Represent FFA loot removal as per-player state instead of global `taken`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::StoreLootItem` FFA branch), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::LootItemInSlot`, `Loot::AutoStore`, `Loot::FillNotNormalLootFor`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`NotNormalLootItem`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: `LootEntry` now carries represented per-player FFA removal state (`ffa_looted_by`) beside the existing global `taken` bit. `CMSG_LOOT_ITEM` lookup uses the C++ `LootItemInSlot` distinction: non-FFA items remain globally taken, while FFA rows are hidden/removed only for the player that looted them. Represented full-looted checks treat FFA rows as complete only when every represented allowed looter has looted that row. This does not implement the real shared `PlayerFFAItems` map, group member fill, broadcasts or cross-session loot ownership; those remain under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.199** Remove stale represented `CMSG_LOOT_UNIT` magic failure code.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LootError`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::isAllowedToLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::hasItemForAll`, `Loot::hasItemFor`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: Rust no longer sends the stale magic `failure_reason = 2` from the earlier bridge. The first implementation of this item mapped live/empty represented `CMSG_LOOT_UNIT` to `LootError` failures, but that was re-checked against C++ and corrected by #NEXT.R8.ENTITIES.206/#NEXT.R8.ENTITIES.207: invalid or fully looted loot-unit targets return silently. Full `LootError` coverage remains pending for canonical loot work and represented `CMSG_LOOT_ITEM` refusal branches.

- [x] **#NEXT.R8.ENTITIES.200** Port represented `CMSG_LOOT_MONEY` coin-removal notification order.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMoneyOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`NotifyMoneyRemoved`, `LootMoney`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`SendNotifyLootMoneyRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`/`.h` (`CoinRemoved`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_MONEY` now sends `SMSG_COIN_REMOVED` for the active loot object before consuming the coins and then sends `SMSG_LOOT_MONEY_NOTIFY`, matching the C++ single-looter packet order. Item-container money still deletes the stored money row and releases/destroys represented item containers only when the loot is fully empty. Group split, `PlayersLooting` fanout, aura modifiers, criteria and script hooks remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.201** Port represented `CMSG_LOOT_ITEM` creature-distance refusal.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleAutostoreLootItemOpcode`, `AELootCreatureCheck::LootDistance = 30.0f`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LOOT_ERROR_TOO_FAR = 4`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ITEM` now checks active creature/vehicle loot requests against the player position and the represented creature position; if the player is beyond 30 yards, Rust sends `LOOT_ERROR_TOO_FAR = 4` and leaves the loot row untouched. Exact C++ combat-reach/map/phase distance behavior, gameobject/fishing distance exceptions, AE loot fanout and full `LootError` coverage remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.202** Port represented `CMSG_LOOT_ITEM` missing-creature refusal.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleAutostoreLootItemOpcode` creature branch), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LOOT_ERROR_NO_LOOT = 17`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ITEM` now explicitly covers the C++ branch where an active creature/vehicle loot owner no longer resolves: Rust sends `LOOT_ERROR_NO_LOOT = 17` and leaves the loot row untouched. Canonical ObjectAccessor/Map ownership, gameobject owner-missing release behavior and broader `LootError` coverage remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.203** Port represented `CMSG_LOOT_ITEM` missing-gameobject release.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleAutostoreLootItemOpcode` gameobject branch).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ITEM` now explicitly covers the C++ branch where an active gameobject loot owner no longer resolves: Rust sends `SMSG_LOOT_RELEASE` with that gameobject GUID and leaves the loot row untouched. The current representation uses `visible_gameobjects` as the available GO-resolution guard; canonical `GameObject` storage, owned fishing-bobber/fishing-hole exceptions, exact GO distance checks and gameobject loot generation remain pending.

- [x] **#NEXT.R8.ENTITIES.204** Port represented `CMSG_LOOT_RELEASE` anti-spoof active-view guard.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootReleaseOpcode`, `DoLootRelease`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_RELEASE` now ignores release GUIDs outside the current non-AE active loot view and ignores stale active GUIDs with no represented loot entry, matching C++'s `GetLootByWorldObjectGUID(packet.Unit)` guard. It sends no `SMSG_LOOT_RELEASE` in that case and does not mutate unrelated loot. Real `AELootView` membership, `Loot::RemoveLooter`/`PlayersLooting` fanout, round-robin reset and full gameobject/corpse/item release side effects remain pending.

- [x] **#NEXT.R8.ENTITIES.205** Port represented `CMSG_LOOT_RELEASE` unlooted-object retention.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`DoLootRelease` creature and gameobject branches).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_RELEASE` now sends `SMSG_LOOT_RELEASE` and clears the active loot view but keeps the session loot row when the represented object is not fully looted, matching C++'s behavior of removing the player from the loot view without deleting the `Loot` contents. Fully looted represented objects still remove their session loot row and trigger the existing represented corpse/item cleanup. Real `Loot::RemoveLooter`, round-robin reset, GO state transitions and shared cross-session loot storage remain pending.

- [x] **#NEXT.R8.ENTITIES.206** Correct represented `CMSG_LOOT_UNIT` invalid-target/empty-loot behavior against C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootOpcode`, `AELootCreatureCheck::IsValidLootTarget`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::BuildLootResponse`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now returns silently for alive creatures and creatures beyond `AELootCreatureCheck::LootDistance = 30.0`, matching C++'s invalid-target guard. Fully looted/empty represented loot rows and allowed-looter filtering are covered by #NEXT.R8.ENTITIES.207. Player alive-state is covered by #NEXT.R8.ENTITIES.218; AE loot target scan is covered by #NEXT.R8.ENTITIES.217. Cross-session loot ownership remains pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.207** Port represented `CMSG_LOOT_UNIT` `Player::isAllowedToLoot` consumed/allowed-looter guards.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::isAllowedToLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`Loot::isLooted`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::hasItemForAll`, `Loot::hasItemFor`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now returns silently when the represented loot row is already consumed/empty or when the current player has no represented lootable item/money view. Existing item `allowed_looters` are preserved instead of being broadened on open; generated/empty-allowed represented rows still add the current player as the allowed looter. Full loot-level `_allowedLooters`, round-robin/master/group permissions and cross-session loot ownership remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.208** Port represented `CMSG_LOOT_UNIT` creature/vehicle GUID type guard.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootOpcode`: `packet.Unit.IsCreatureOrVehicle()`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now returns silently before any represented creature lookup/generation when the client sends a non-creature/non-vehicle GUID, matching C++'s anti-cheat prefilter. Player alive-state is covered by #NEXT.R8.ENTITIES.218; ObjectAccessor/map ownership remains pending under canonical entity work.

- [x] **#NEXT.R8.ENTITIES.209** Port represented `CMSG_LOOT_MONEY` stale-view no-op behavior.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMoneyOpcode`: iterates `Player::GetAELootView()`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`GetLootByWorldObjectGUID` for view ownership semantics).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_MONEY` now returns silently and does not clear the represented active loot GUID when there is no represented loot row for that active view, matching C++'s no-op when the player has no loot in `m_AELootView`. Full AE loot-view iteration and group money split remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.210** Port represented `CMSG_LOOT_MONEY` zero-money notification behavior.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMoneyOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`NotifyMoneyRemoved`, `LootMoney`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_MONEY` no longer returns early when represented coins are zero. Like C++, it still emits `SMSG_COIN_REMOVED`, sends `SMSG_LOOT_MONEY_NOTIFY` with zero money/modifier, keeps the active loot view open unless item-container release cleanup applies, and marks represented coins as consumed. Full group split, aura money modifiers, criteria and script hooks remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.211** Port `SMSG_LOOT_RELEASE` payload order and owner semantics.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLootRelease`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp` (`LootReleaseResponse::Write`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: `SLootRelease` now writes C++ order `LootObj` then `Owner`, and represented loot release callsites send the current player GUID as owner instead of repeating the loot GUID. Fully looted represented item-container release also clears the active loot view like C++ `DoLootRelease`. Full AE release-all and shared `Loot::RemoveLooter` fanout remain pending under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.212** Port represented loot window owner GUID semantics for response/removal packets.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`, `SendNotifyLootItemRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::NotifyItemRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp` (`LootResponse::Write`, `LootRemoved::Write`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `LootResponse` and `LootRemoved` callsites now use the represented loot owner GUID instead of the player GUID, matching C++ `Owner = loot.GetOwnerGUID()`. Separate represented `HighGuid::LootObject` routing for creature loot is closed by #NEXT.R8.ENTITIES.214; item-container loot still uses the item GUID as the represented object bridge until canonical loot objects/AELootView are ported.

- [x] **#NEXT.R8.ENTITIES.213** Port `LootItemData` server-packet shape to C++ 3.4.3.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp` (`operator<<(LootItemData const&)`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h` (`LootItemData`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/ItemPacketsCommon.cpp` (`ItemInstance::Initialize(::LootItem const&)`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: `LootItemData` now serializes C++ fields in order: `Type` bits, `UIType` bits, `CanTradeToTapList`, `ItemInstance`, `Quantity`, `LootItemType`, `LootListID`. Represented loot responses populate `ItemInstance.item_id` like C++ `ItemInstance::Initialize(LootItem)`. Stored random/context metadata is still preserved for inventory store-time behavior, but it is no longer serialized through the loot-window item row because C++ does not do that for `LootItem`.

- [x] **#NEXT.R8.ENTITIES.214** Port represented creature loot object GUID semantics.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::Loot`, `NotifyMoneyRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`GetGUID`, `GetOwnerGUID`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`, `GetLootByWorldObjectGUID`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleAutostoreLootItemOpcode`, `HandleLootMoneyOpcode`, `HandleLootReleaseOpcode`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented creature loot now separates `Loot::GetOwnerGUID()` from `Loot::GetGUID()`: the session loot table remains keyed by represented owner GUID, but generated creature loot rows carry a represented `HighGuid::LootObject` `loot_guid`. `CMSG_LOOT_UNIT` sends `LootResponse.Owner = owner` and `LootResponse.LootObj = loot object`; `CMSG_LOOT_ITEM` resolves the requested loot object through the active loot view before owner validation/store/removal; `SMSG_LOOT_REMOVED` keeps owner and loot object distinct; `CMSG_LOOT_MONEY` sends `SMSG_COIN_REMOVED` with the loot object like `Loot::NotifyMoneyRemoved`. This is still a deterministic represented loot-object GUID, not the real map `GenerateLowGuid<HighGuid::LootObject>()`; represented multi-entry `m_AELootView` iteration is closed by #NEXT.R8.ENTITIES.215, while cross-session ownership and canonical loot-object lifetime remain pending.

- [x] **#NEXT.R8.ENTITIES.215** Port represented multi-entry `m_AELootView` iteration for loot item/money.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleAutostoreLootItemOpcode`, `HandleLootMoneyOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h` (`m_AELootView`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`, `GetLootByWorldObjectGUID`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: `WorldSession` now keeps a represented active loot-view owner set beside the legacy `active_loot_guid` bridge. Existing single-target paths still call `set_active_loot_guid`, while represented AE-style tests can add secondary active loot owners. `CMSG_LOOT_ITEM` can resolve a requested secondary `LootObject` back to its active owner, and `CMSG_LOOT_MONEY` iterates all represented active loot views, sending `SMSG_COIN_REMOVED` and `SMSG_LOOT_MONEY_NOTIFY` per loot before zeroing coins. Server-driven represented AE target fanout is closed by #NEXT.R8.ENTITIES.217; group money split/fanout, aura money modifiers, canonical grid/shared loot and cross-session shared loot storage remain pending.

- [x] **#NEXT.R8.ENTITIES.216** Port represented secondary active-owner release lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootReleaseOpcode`, `DoLootRelease`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`GetLootByWorldObjectGUID`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_RELEASE` no longer requires `packet.Unit` to equal the legacy single `active_loot_guid`; it accepts any owner present in the represented active loot view, sends the C++-shaped `SMSG_LOOT_RELEASE`, removes only that owner from the represented view, and keeps other active views intact. Canonical `Loot::RemoveLooter`, `SMSG_LOOT_RELEASE_ALL`, unit flag cleanup, GO/fishing/corpse side effects and cross-session loot ownership remain pending.

- [x] **#NEXT.R8.ENTITIES.217** Port represented AE loot target packets and `CMSG_LOOT_UNIT` fanout.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`AELootCreatureCheck`, `HandleLootOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp` (`AELootTargets::Write`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h` (`AELootTargets`, `AELootTargetsAck`), `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp` (`CONFIG_ENABLE_AE_LOOT`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/loot.md`.
  Acceptance: `SMSG_AE_LOOT_TARGETS` now serializes the C++ `uint32 Count` payload and `SMSG_AE_LOOT_TARGET_ACK` serializes an empty payload. `world-server` reads `EnableAELoot` through `CONFIG_ENABLE_AE_LOOT` and passes it to `WorldSession`. When enabled, represented `CMSG_LOOT_UNIT` finds additional dead in-range represented creatures, sends `SMSG_AE_LOOT_TARGETS`, streams the main `LootResponse` with `AELooting=false`, sends an ack, then streams each secondary `LootResponse` with `AELooting=true` and an ack while adding secondaries to the represented active loot view. This intentionally remains represented/session-local: canonical `Cell::VisitGridObjects`, full `Player::isAllowedToLoot`, shared loot ownership, group/fanout/criteria/script hooks, `SMSG_LOOT_RELEASE_ALL` and config hot reload remain pending.

- [x] **#NEXT.R8.ENTITIES.218** Port represented `CMSG_LOOT_UNIT` player-alive prefilter.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootOpcode`: `!GetPlayer()->IsAlive()`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/loot.md`.
  Acceptance: `WorldSession` now carries a represented `Player::IsAlive()` switch and `CMSG_LOOT_UNIT` returns silently before represented creature lookup/generation when the current player is dead, matching the first C++ anti-cheat prefilter. This is still a represented session flag until canonical player health/death state is owned by the runtime player entity; cast interruption is covered by #NEXT.R8.ENTITIES.219 and aura removal on successful looting remains pending.

- [x] **#NEXT.R8.ENTITIES.219** Port represented successful-loot active cast interruption.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootOpcode`: `IsNonMeleeSpellCast(false)` then `InterruptNonMeleeSpells(false)` after valid target checks).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.tsv`, `docs/migration/inventory/r3-opcodes-registry.md`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now clears `WorldSession::active_spell_cast` only after the same represented target guards that C++ performs before interrupting casts. Invalid/dead-player loot requests return before this interruption, while a valid dead in-range creature loot opens and cancels the active delayed cast. Looting aura interruption is covered by #NEXT.R8.ENTITIES.220.

- [x] **#NEXT.R8.ENTITIES.220** Port represented successful-loot looting-aura interruption.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootOpcode` calls `RemoveAurasWithInterruptFlags(SpellAuraInterruptFlags::Looting)`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp` (`RemoveAurasWithInterruptFlags`), `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellDefines.h` (`Looting = 0x00000800`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented visible auras now carry a Trinity `SpellAuraInterruptFlags` bitmask. A successful represented `CMSG_LOOT_UNIT` removes visible auras with the `Looting` bit only after the same successful-target guards and after non-melee cast interruption, matching the C++ handler order. Invalid/dead-player loot requests preserve those auras. Full canonical aura ownership, source-ignore checks and `AURA_REMOVE_BY_INTERRUPT` side effects remain under aura runtime work.

- [x] **#NEXT.R8.ENTITIES.221** Port represented `DoLootReleaseAll` before opening a new main loot view.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot` calls `m_session->DoLootReleaseAll()` when `GetLootGUID()` is set and the new view is not AE), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`DoLootReleaseAll`, `DoLootRelease`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now releases existing non-item main active loot views before installing the new main loot view, sends the same C++-shaped `SMSG_LOOT_RELEASE` per active owner, preserves unlooted represented loot rows, and reuses the same represented `DoLootRelease` helper as `CMSG_LOOT_RELEASE`. Full `SMSG_LOOT_RELEASE_ALL` packet callsites, `Loot::RemoveLooter`, unit-flag updates and canonical object-specific release side effects remain under canonical loot work.

- [x] **#NEXT.R8.ENTITIES.222** Port `SMSG_LOOT_RELEASE_ALL` empty packet serializer.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h` (`LootReleaseAll::Write` returns an empty packet), `/home/server/woltk-trinity-legacy/src/server/game/Server/Protocol/Opcodes.cpp` (`SMSG_LOOT_RELEASE_ALL`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `wow-packet` now has `LootReleaseAll` with opcode `SMSG_LOOT_RELEASE_ALL` and an empty payload matching C++ 3.4.3. Represented `Player::StoreLootItem` refusal callsites are covered by #NEXT.R8.ENTITIES.223; logout `DoLootReleaseAll` cleanup is covered by #NEXT.R8.ENTITIES.224. Other runtime callsites remain pending where canonical item movement, teleport and full loot-view cleanup require bulk client close behavior.

- [x] **#NEXT.R8.ENTITIES.223** Port represented `Player::StoreLootItem` release-all refusal branches.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::StoreLootItem`: disallowed looter, blocked item, non-winning roll branches call `SendLootReleaseAll()`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ITEM` now sends the C++ empty `SMSG_LOOT_RELEASE_ALL` packet when the current loot row fails `HasAllowedLooter`, is blocked, or has a different `rollWinnerGUID`. These branches no longer fabricate `SMSG_LOOT_RELEASE` or clear the represented active loot view server-side, matching `Player::StoreLootItem`'s client-close-only behavior. Canonical group/master/NBG roll routing and broader release-all callsites remain pending.

- [x] **#NEXT.R8.ENTITIES.224** Port represented logout `DoLootReleaseAll` cleanup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::RemoveFromWorld` calls `m_session->DoLootReleaseAll()` before clearing loot rolls and removing the player from world).
  Rust targets: `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOGOUT_REQUEST` now calls the shared represented `DoLootReleaseAll` helper before clearing player/session state, sending C++-shaped `SMSG_LOOT_RELEASE` packets for active loot views and preserving unlooted represented loot rows. This replaces the previous direct active-loot GUID clear during logout. Full `m_lootRolls` cleanup, pet/charm/totem removal and canonical world removal sequencing remain pending.

- [x] **#NEXT.R8.ENTITIES.225** Port represented logout-request `SendLootReleaseAll` client close.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/MiscHandler.cpp` (`WorldSession::HandleLogoutRequestOpcode`: if `GetLootGUID()` is non-empty, call `Player::SendLootReleaseAll()` before sending `LogoutResponse`).
  Rust targets: `crates/wow-world/src/handlers/character.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOGOUT_REQUEST` now sends the empty `SMSG_LOOT_RELEASE_ALL` packet before `SMSG_LOGOUT_RESPONSE` when a represented active loot GUID exists, then still performs the represented `DoLootReleaseAll` cleanup from #NEXT.R8.ENTITIES.224 during immediate logout completion. Combat/duel/falling/rested delayed logout parity remains pending.

- [x] **#NEXT.R8.ENTITIES.226** Port represented disconnect `LogoutPlayer` loot cleanup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/WorldSession.cpp` (`WorldSession::LogoutPlayer`: if `_player->GetLootGUID()` is non-empty, call `DoLootReleaseAll()`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: world-server now uses an async disconnect cleanup path that calls the represented `DoLootReleaseAll` helper before unregistering/clearing shared runtime state when a represented active loot GUID exists. This preserves unlooted represented loot rows and clears the active loot view like the C++ session logout path. Full death/ghost, save, transfer, combat and delayed logout sequencing remains pending.

- [x] **#NEXT.R8.ENTITIES.227** Port represented `SwapItem` moved-bag active item-loot cleanup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SwapItem`: after item moves, if a moved bag contains an item present in `GetAELootView()`/`GetLootByWorldObjectGUID`, call `m_session->DoLootReleaseAll()`).
  Rust targets: `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_SWAP_INV_ITEM` now checks source/destination bag-storage slots after the direct swap update; if the moved represented bag contains a child item with an active represented loot row in the current loot view, Rust calls the shared represented `DoLootReleaseAll` helper like C++. Full canonical `Player::SwapItem` orchestration, recursive `CMSG_SWAP_ITEM`, bag-content exchange execution, aura cleanup and can-store/can-equip validation remain pending.

- [x] **#NEXT.R8.ENTITIES.228** Port group/master loot roll packet shapes.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp` (`LootRoll::Read`, `MasterLootItem::Read`, `StartLootRoll::Write`, `LootRollBroadcast::Write`, `LootRollWon::Write`, `LootAllPassed::Write`, `LootRollsComplete::Write`, `MasterLootCandidateList::Write`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `wow-packet` now parses `CMSG_LOOT_ROLL` and `CMSG_MASTER_LOOT_ITEM`, and serializes `SMSG_START_LOOT_ROLL`, `SMSG_LOOT_ROLL`, `SMSG_LOOT_ROLL_WON`, `SMSG_LOOT_ALL_PASSED`, `SMSG_LOOT_ROLLS_COMPLETE` and `SMSG_MASTER_LOOT_CANDIDATE_LIST` in the C++ field order. This intentionally closes only the protocol layer; canonical `LootRoll`, group/master/NBG rules, timers, candidate eligibility and dispatch handlers remain pending.

- [x] **#NEXT.R8.ENTITIES.229** Port represented group/master loot dispatch guards.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`WorldSession::HandleLootRoll`, `WorldSession::HandleLootMasterGiveOpcode`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `CMSG_LOOT_ROLL` is now registered and dispatched; when no canonical Rust `LootRoll` state exists, the represented handler returns silently like C++ `GetLootRoll(...) == nullptr`. `CMSG_MASTER_LOOT_ITEM` is registered and dispatched with the C++ first permission guard: players without represented master-looter permission receive `LOOT_ERROR_DIDNT_KILL` using an empty owner/object pair. Full canonical `LootRoll`, master-looter GUID state, candidate eligibility, target validation, inventory storage, vote timers and winner/assignment fanout remain pending; represented master-looter GUID state is covered by #NEXT.R8.ENTITIES.230.

- [x] **#NEXT.R8.ENTITIES.230** Port represented group master-looter GUID state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Groups/Group.cpp` (`Group::SetMasterLooterGuid`, `Group::GetMasterLooterGuid`, `Group::SendUpdate` loot settings), `/home/server/woltk-trinity-legacy/src/server/game/Groups/Group.h` (`m_masterLooterGuid`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode` permission guard).
  Rust targets: `crates/wow-network/src/group_registry.rs`, `crates/wow-world/src/handlers/group.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: shared Rust `GroupInfo` now stores a represented `master_looter_guid`. `PartyUpdate` writes that GUID only when `loot_method == MASTER_LOOT`, matching C++ `LootSettings->LootMaster = m_lootMethod == MASTER_LOOT ? m_masterLooterGuid : ObjectGuid::Empty`. The represented `CMSG_MASTER_LOOT_ITEM` guard now checks `loot_method == MASTER_LOOT` and `master_looter_guid == player_guid`, matching C++ `GetMasterLooterGuid() != _player->GetGUID()` instead of assuming the leader is master looter. Full `CMSG_SET_LOOT_METHOD`, persisted group loot settings, member validation, candidate list, target validation, inventory assignment and roll/assignment fanout remain pending.

- [x] **#NEXT.R8.ENTITIES.231** Port represented `CMSG_SET_LOOT_METHOD` no-op branch.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/GroupHandler.cpp` (`WorldSession::HandleSetLootMethodOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/PartyPackets.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/PartyPackets.cpp` (`SetLootMethod::Read`).
  Rust targets: `crates/wow-packet/src/packets/party.rs`, `crates/wow-world/src/handlers/group.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `wow-packet` now parses `CMSG_SET_LOOT_METHOD` in the C++ order: `hasPartyIndex` bit, `LootMethod`, `LootMasterGUID`, `LootThreshold`, optional `PartyIndex`. Rust registers and dispatches `CMSG_SET_LOOT_METHOD`, but preserves this Trinity branch's runtime behavior as a no-op because the entire mutation block is commented out under `// not allowed to change`. The represented handler parses valid packets, leaves group loot method/master/threshold unchanged and sends no failure packet. A future configurable loot-method implementation must be explicitly based on a different C++ branch or an intentional behavior change.

- [x] **#NEXT.R8.ENTITIES.232** Port represented `CMSG_OPT_OUT_OF_LOOT` pass-on-group-loot state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/GroupHandler.cpp` (`WorldSession::HandleOptOutOfLootOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/PartyPackets.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/PartyPackets.cpp` (`OptOutOfLoot::Read`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h` (`SetPassOnGroupLoot`, `GetPassOnGroupLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`GetPassOnGroupLoot` roll initialization).
  Rust targets: `crates/wow-packet/src/packets/party.rs`, `crates/wow-world/src/handlers/group.rs`, `crates/wow-world/src/session.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `wow-packet` now parses `CMSG_OPT_OUT_OF_LOOT` as the C++ single `PassOnLoot` bit. Rust registers and dispatches the opcode as `PROCESS_INPLACE` parity, ignores it when no player is loaded, and otherwise stores represented `pass_on_group_loot` on `WorldSession` with default `false`, matching C++ `m_bPassOnGroupLoot = false` and `SetPassOnGroupLoot(packet.PassOnLoot)`. Full integration with canonical `LootRoll` vote initialization remains pending until the roll engine exists.

- [x] **#NEXT.R8.ENTITIES.233** Represent `SMSG_LOOT_LIST` packet shape and correct the nonexistent `CMSG_LOOT_LIST` plan gap.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Protocol/Opcodes.cpp`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `wow-packet` now serializes `SMSG_LOOT_LIST` in the exact C++ order: `Owner`, `LootObj`, `Master.has_value` bit, `RoundRobinWinner.has_value` bit, flushed bits, optional `Master`, optional `RoundRobinWinner`. The migration docs now record that contrasted Trinity 3.4.3 has no `CMSG_LOOT_LIST`/`HandleLootList`; the remaining gap is the runtime `Loot::NotifyLootList` fanout from canonical group/master/round-robin loot state.

- [x] **#NEXT.R8.ENTITIES.234** Represent corpse-money group split/fanout for `CMSG_LOOT_MONEY`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/World/World.cpp`.
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_MONEY` now keeps item/non-corpse money as sole-looter, but for represented creature/corpse loot with an active group it picks allowed near members from the group/player registries, uses Trinity's default `MaxGroupXPDistance = 74.0`, divides money with C++ integer division, sends `SMSG_LOOT_MONEY_NOTIFY` to each represented recipient with `SoleLooter = recipients.len() <= 1`, and credits the current session only for its share. Full `ModifyMoney` persistence for other live sessions, aura money modifiers, criteria updates and script hooks remain pending until shared canonical session/player state exists.

- [x] **#NEXT.R8.ENTITIES.235** Represent early `CMSG_MASTER_LOOT_ITEM` target validation errors.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_MASTER_LOOT_ITEM` now preserves the C++ error sequence beyond the master-looter guard: missing target on this map sends `LOOT_ERROR_PLAYER_NOT_FOUND`, and a represented target outside the group or not allowed for the represented item sends `LOOT_ERROR_MASTER_OTHER` with the C++ `LootResponse` owner/loot-object payload. Loot lookup/index misses still return silently like the C++ early returns. Full target `CanStoreNewItem`, inventory assignment, item removal and group fanout remain pending until target inventory/session state is canonical.

- [x] **#NEXT.R8.ENTITIES.236** Port `SMSG_LOOT_RESPONSE` currency entry wire shape.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h`.
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `LootResponse` now carries and serializes `LootCurrencyData` entries after item rows in the C++ order: `CurrencyID`, `Quantity`, `LootListID`, then 3-bit `UIType` with a bit flush per entry. The response header writes the real currency count instead of hardcoding zero, while current represented runtime callsites pass an empty currency vector until canonical currency loot generation/storage exists.

- [x] **#NEXT.R8.ENTITIES.237** Port represented `CMSG_SET_LOOT_SPECIALIZATION`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.h`.
  Rust targets: `crates/wow-constants/src/opcodes.rs`, `crates/wow-packet/src/packets/loot.rs`, `crates/wow-data/src/chr_specialization.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-network/src/accept.rs`, `crates/world-server/src/main.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: `CMSG_SET_LOOT_SPECIALIZATION` now has the C++ opcode, parses a single `uint32 SpecID`, is registered/dispatched as `STATUS_LOGGEDIN`/`PROCESS_THREADUNSAFE`, loads `ChrSpecialization.db2`, and updates represented `loot_specialization_id` only when `SpecID == 0` or the DB2 row exists with `ClassID == player_class`, matching `HandleSetLootSpecialization`. Full `ActivePlayerData::LootSpecID` update-field propagation, DB load/save and all consumers such as `ItemTemplate::IsUsableByLootSpecialization` remain pending under canonical Player/data integration.

- [x] **#NEXT.R8.ENTITIES.238** Represent self-target master-loot inventory preflight errors.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode` target `CanStoreNewItem` branch), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`CanStoreNewItem`), `/home/server/woltk-trinity-legacy/src/server/game/Server/Packets/LootPackets.h` (`LootError` values).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r8-entities-miniphase.{tsv,md}`, `docs/migration/loot.md`.
  Acceptance: represented `CMSG_MASTER_LOOT_ITEM` now invokes the existing direct-inventory `CanStoreItem` planner when the master-loot target is the current session's player, matching the C++ pre-assignment validation order for the representable self-target case. `InventoryResult::ItemMaxCount` maps to `LOOT_ERROR_MASTER_UNIQUE_ITEM`, `InventoryResult::InvFull` maps to `LOOT_ERROR_MASTER_INV_FULL`, and other non-OK results map to `LOOT_ERROR_MASTER_OTHER`. Remote target inventory preflight, actual item assignment/removal and group fanout remain pending until Rust has canonical shared target session/player inventory state.

- [x] **#NEXT.R8.ENTITIES.239** Represent `Loot::GetLootMethod() == MASTER_LOOT` guard for master-loot give.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode`: `!loot || loot->GetLootMethod() != MASTER_LOOT` silent return), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`Loot::GetLootMethod`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `CreatureLoot` now carries the loot method, `LootResponse` uses that stored method instead of a hardcoded `0`, and `CMSG_MASTER_LOOT_ITEM` returns silently unless the active loot view is `MASTER_LOOT`, matching the C++ early guard. Generated/default represented loot remains method `0`; tests that exercise master-loot target/preflight branches explicitly mark the loot view as `MASTER_LOOT`. Full canonical loot-method selection during `Loot` creation and group/master assignment fanout remain pending.

- [x] **#NEXT.R8.ENTITIES.240** Represent loot-level allowed-looter guard for master-loot give.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode`: `!loot->HasAllowedLooter(Target)`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::HasAllowedLooter`, `FillNotNormalLootFor`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`_allowedLooters`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `CreatureLoot` now carries loot-level `allowed_looters`, represented loot opening adds the looter to that set like C++ `FillNotNormalLootFor`, represented item-container loot initializes the set for the opener, and `CMSG_MASTER_LOOT_ITEM` sends `LOOT_ERROR_MASTER_OTHER` when the target is not allowed for the loot as a whole before slot/item validation. Full canonical `_allowedLooters` maintenance across group membership, map objects and persistent loot state remains pending with the canonical loot engine.

- [x] **#NEXT.R8.ENTITIES.241** Represent current-session master-loot give assignment/removal.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode`: `StoreNewItem`, mark `count = 0`/`is_looted = true`, `NotifyItemRemoved`, `--unlootedCount`, `SendNewItem`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: after the represented master-loot guards and self-target `CanStoreItem` preflight pass, current-session self-target gives now call the existing direct inventory store path; only a successful store marks the represented loot entry quantity as zero/taken and emits C++-shaped `SMSG_LOOT_REMOVED` using the loot owner and loot object. Remote target assignment remains pending because the current Rust shared player registry exposes send channels/snapshots, not a mutable target session/inventory transaction equivalent to C++ `ObjectAccessor::FindConnectedPlayer`.

- [x] **#NEXT.R8.ENTITIES.242** Route represented remote master-loot gives through the target session.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`ObjectAccessor::FindConnectedPlayer(Target)`, target `CanStoreNewItem`, target `StoreNewItem`, master `NotifyItemRemoved`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-network/src/lib.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: `PlayerRegistry` now exposes a bounded per-session command channel in addition to packet fanout. Represented remote `CMSG_MASTER_LOOT_ITEM` sends a `MasterLootGive` command to the connected target session; the target session runs its own allowed-looter, `CanStoreItem` and `StoreNewItem` logic, returns C++ master-loot error codes to the master on refusal, and the master marks/removes the represented loot slot only on a successful target-session store. Unit coverage verifies the remote target session reports `LOOT_ERROR_MASTER_UNIQUE_ITEM` from its own inventory state. Live successful remote assignment still needs DB-backed integration validation.

- [x] **#NEXT.R8.ENTITIES.243** Keep represented remote master-loot command waits finite.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`ObjectAccessor::GetPlayer` returns immediately, then target `CanStoreNewItem`/`StoreNewItem` run synchronously).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented remote master-loot command delivery now uses a non-blocking send plus a bounded result wait; if the target command channel is unavailable or no result arrives, the master maps the situation to the existing C++ target-missing path (`LOOT_ERROR_PLAYER_NOT_FOUND`) instead of hanging the session handler. Unit coverage verifies an unavailable connected-target command channel returns the empty-owner/object `LOOT_ERROR_PLAYER_NOT_FOUND` packet and leaves the loot entry untouched.

- [x] **#NEXT.R8.ENTITIES.244** Represent master-loot candidate list on first loot open.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot` response-before-`OnLootOpened` order), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::OnLootOpened` master-loot candidate list branch).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_UNIT` now mirrors the C++ packet order for master loot by sending `SMSG_LOOT_RESPONSE` first and then `SMSG_MASTER_LOOT_CANDIDATE_LIST` only when the first opener is the represented master looter. The existing represented loot open flag now tracks C++ `_wasOpened`, so later opens do not resend the candidate list. Unit coverage verifies packet order, candidate GUID payload and first-open-only behavior. Full canonical shared `Loot` ownership, `Loot::NotifyLootList`, roll creation and cross-session candidate/fanout state remain pending.

- [x] **#NEXT.R8.ENTITIES.245** Represent `Loot::NotifyLootList` fanout from current represented loot state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::NotifyLootList`, `Loot::hasOverThresholdItem`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp` (`loot->NotifyLootList(creature->GetMap())` after group creature-loot creation).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented loot now builds and fans out `SMSG_LOOT_LIST` to connected allowed looters on the same map, using the represented owner/loot-object identity, adding `Master` only for `MASTER_LOOT` views with an unlooted over-threshold non-FFA item, and leaving `RoundRobinWinner` absent because Rust has no canonical `roundRobinPlayer` yet. Unit coverage verifies local and remote allowed-looter delivery plus the optional master GUID. This is intentionally hooked to represented open-time state until the canonical creature-death/group loot creation hook from C++ `Unit.cpp` exists.

- [x] **#NEXT.R8.ENTITIES.246** Represent first-open group-loot roll start prompts.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::OnLootOpened`, `LootRoll::TryToStart`, `LootRoll::SendStartRoll`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LOOT_ROLL_TIMEOUT`, roll masks and slot types).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented first-open handling now mirrors the C++ `_wasOpened` split: after `SMSG_LOOT_RESPONSE` and represented `SMSG_LOOT_LIST`, `MASTER_LOOT` sends the existing candidate list path while `GROUP_LOOT`/`NEED_BEFORE_GREED` starts represented `SMSG_START_LOOT_ROLL` prompts for connected allowed looters when a blocked item has more than one connected candidate. The prompt uses the C++ 60s timeout, represented no-disenchant roll mask, roll-ongoing loot slot UI type, loot object, map and loot method; if only one connected candidate exists, the item is marked under-threshold and unblocked like `LootRoll::TryToStart`. Current-session `pass_on_group_loot` suppresses the local prompt while still counting the player for roll start eligibility. Canonical persisted `LootRoll` state, timers, votes, winner resolution, need/disenchant eligibility and completion fanout remain pending.

- [x] **#NEXT.R8.ENTITIES.247** Represent immediate `LootRoll::PlayerVote` broadcast for current-session group rolls.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootRoll`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::PlayerVote`, `LootRoll::SendRoll`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, `docs/migration/inventory/r3-opcodes-registry.{tsv,md}`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `CMSG_LOOT_ROLL` now resolves the active represented loot object, requires a blocked `GROUP_LOOT`/`NEED_BEFORE_GREED` item where the current player is an allowed looter, ignores current-session `pass_on_group_loot` like C++ players without an attached roll, and broadcasts the immediate `SMSG_LOOT_ROLL` packet to connected allowed looters on the same map. The represented immediate C++ roll values are preserved: Pass/Greed/Disenchant send `Roll=-1`, Need sends `Roll=0`, and unsupported roll types return silently. Unit coverage verifies a Need vote broadcast reaches both the voter and a connected allowed looter. Persistent shared `LootRoll` state, random 1..100 stored rolls, all-voted/timer finish, winner selection, `SMSG_LOOT_ROLL_WON`, `SMSG_LOOT_ALL_PASSED`, item storage and cross-session remote voting remain pending.

- [x] **#NEXT.R8.ENTITIES.248** Represent all-voted group-roll winner finish for current-session shared state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::AllPlayerVoted`, `LootRoll::Finish`, `LootRoll::SendLootRollWon`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`PlayerRollVote`, `RollVoteMap`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `WorldSession` now keeps pending group/NBG roll state keyed by `(LootObj, LootListID)` when first-open roll prompts are started. Valid current-session votes update that state, assign a represented 1..100 roll number for Need/Greed/Disenchant, and when every represented voter has voted, the same C++ winner priority is applied: Need beats Greed/Disenchant, otherwise highest Greed/Disenchant roll wins, Pass cannot win. Finishing unblocks the loot item, stores `roll_winner`, removes the pending roll, and sends `SMSG_LOOT_ROLL_WON` with locked UI to non-winners and allow-loot UI to the winner. Unit coverage simulates two voters over one represented shared state and verifies Need beats Greed, the item is unblocked, winner GUID is stored and both packet variants are emitted. Remaining gaps: real cross-session shared roll ownership, command routing for remote `CMSG_LOOT_ROLL`, C++ `urand(1,100)` source, timer expiry, all-passed exact branch, criteria updates and winner item storage.

- [x] **#NEXT.R8.ENTITIES.249** Match C++ all-passed group-roll finish fanout for represented valid voters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::SendAllPassed`, `LootRoll::Finish`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented all-pass finish now follows the contrasted C++ branch instead of intuitive broadcast semantics: when all valid represented voters choose Pass, the roll item is unblocked, no `roll_winner` is set, pending roll state is removed, and `SMSG_LOOT_ALL_PASSED` is sent only to voters with `RollVote::NotValid` in the represented roll map. The common all-valid-voters-pass case therefore emits only the per-player `SMSG_LOOT_ROLL` pass broadcasts and no extra all-passed packet, matching this Trinity branch. Unit coverage verifies two valid represented voters passing closes the roll without any `SMSG_LOOT_ALL_PASSED` fanout. Remaining gaps: modelling invalid voters from disconnected/ineligible allowed looters, timer expiry, cross-session vote ownership and winner item storage.

- [x] **#NEXT.R8.ENTITIES.250** Add represented owner-session command path for remote group-roll votes.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootRoll`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::PlayerVote`, `LootRoll::SendRoll`, `LootRoll::AllPlayerVoted`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-network/src/lib.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented cross-session roll mutation now has the same ownership direction as C++ `Player::GetLootRoll(...)->PlayerVote(...)`: `SessionCommand::LootRollVote` carries voter GUID, loot object, loot-list id, roll type and the voter's pass-on-group-loot flag to the owner session, and the owner session applies it to its pending represented roll state using the same immediate `SMSG_LOOT_ROLL` fanout/update path as local votes. Unit coverage verifies a connected remote greed vote command updates the owner session's roll state and broadcasts the C++ immediate roll packet to both local and remote allowed looters. Remaining gaps: actual remote `CMSG_LOOT_ROLL` discovery/routing to the owner session, canonical shared `Loot`/`LootRoll` ownership, timer expiry, C++ RNG source and winner item storage.

- [x] **#NEXT.R8.ENTITIES.251** Route represented remote `CMSG_LOOT_ROLL` to the owner session.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootRoll`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::PlayerVote`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: the represented owner session now publishes pending `(LootObj, LootListID)` keys through `PlayerBroadcastInfo::active_loot_rolls` whenever first-open roll state is created or finished. If a remote session receives `CMSG_LOOT_ROLL` and has no local roll state, it searches same-map registered sessions for the pending key and sends `SessionCommand::LootRollVote` to that owner's command channel, preserving the voter's pass-on-group-loot flag. Unit coverage simulates two sessions and verifies the remote handler routes a Greed vote to the owner session, then the owner emits the C++ immediate `SMSG_LOOT_ROLL` fanout and records the remote vote. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, timer expiry, C++ RNG source, disconnected/invalid voter modelling and winner item storage.

- [x] **#NEXT.R8.ENTITIES.252** Represent `LootRoll::UpdateRoll` timeout finish.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::UpdateRoll`, `LootRoll::AllPlayerVoted`, `LootRoll::Finish`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`m_endTime`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented pending roll state now stores an `end_time` matching the C++ 60s `LOOT_ROLL_TIMEOUT`; the session update loop ticks represented rolls and, when a roll expires, finishes it using the current Need-over-Greed winner without requiring every voter to have voted, mirroring `UpdateRoll`'s `AllPlayerVoted(winnerItr) || m_endTime <= GameTime::Now()` branch. Expiry unblocks the item, stores `roll_winner` when a winner exists, removes the pending roll and republishes owner-session roll keys. Unit coverage forces an expired roll with one Greed vote and one pending voter and verifies the current Greed voter wins. Remaining gaps: C++ `SendLootRollWon`'s final per-voter `SMSG_LOOT_ROLL` value replay before `SMSG_LOOT_ROLL_WON`, canonical shared `Loot`/`LootRoll` ownership, C++ RNG source, disconnected/invalid voter modelling and winner item storage.

- [x] **#NEXT.R8.ENTITIES.253** Replay final `LootRoll::SendLootRollWon` vote values.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::SendLootRollWon`, `LootRoll::SendRoll`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented winner finish now mirrors the C++ pre-winner replay loop: before `SMSG_LOOT_ROLL_WON`, each non-pass vote sends a final `SMSG_LOOT_ROLL` with the stored roll number, and `NotEmittedYet`/`NotValid` votes send a pass roll value of `0`; recipients with `NotValid` are skipped and the winner receives the allow-loot UI variant, matching `SendRoll(..., rollWinner)`. Unit coverage verifies both all-voted and timeout winner paths now include final replay packets before the winner packet. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, C++ RNG source, disconnected/invalid voter modelling, criteria updates and winner item storage.

- [x] **#NEXT.R8.ENTITIES.254** Replace represented stored roll numbers with the C++ RNG source.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::PlayerVote`, `urand(1, 100)`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `LootRoll` vote storage now assigns an actual inclusive `1..100` random roll via `rand::thread_rng().gen_range(1..=100)` for Need/Greed/Disenchant votes, matching C++ `urand(1, 100)`, instead of deriving deterministic values from player GUID, loot object, loot list id and vote type. Existing all-voted and timeout coverage verifies the stored roll is consumed by final winner/final replay packets as an in-range value. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, disconnected/invalid voter modelling, criteria updates and winner item storage.

- [x] **#NEXT.R8.ENTITIES.255** Add represented `LootRoll` criteria hook points.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::PlayerVote`, `LootRoll::Finish`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `LootRoll` now calls explicit C++ criteria hook points for vote-time `RollAnyNeed` and `RollAnyGreed` and winner-time `RollNeed`, `RollGreed`, and `CastSpell(13262)` for disenchant wins. Because the Rust achievements/criteria backend and `Criteria.db2`/`CriteriaTree.db2` stores are still not ported, these hooks record events only in tests today; this avoids inventing fake achievement behavior while fixing the integration points. Existing all-voted and timeout tests verify the hook sequence for Need and Greed winner paths. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, real achievements/criteria backend integration, disconnected/invalid voter modelling and winner item storage.

- [x] **#NEXT.R8.ENTITIES.256** Model represented `LootRoll` startup `NotValid`/auto-pass voters.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::TryToStart`, `LootRoll::SendStartRoll`, `LootRoll::SendRoll`, `LootRoll::SendAllPassed`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented `LootRoll` startup now builds the vote map from every allowed looter like C++: connected same-map voters become `NotEmittedYet` or `Pass` based on `pass_on_group_loot`, while disconnected/off-map allowed looters become `NotValid`. `SMSG_START_LOOT_ROLL` is only sent to `NotEmittedYet` voters, and `Pass` voters emit the C++ auto-pass `SMSG_LOOT_ROLL` fanout. Unit coverage verifies a disconnected allowed looter is retained as `NotValid` and pass-on-loot suppresses the prompt while broadcasting the pass roll. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, real `ConditionMgr`/`AllowedForPlayer` invalid-voter checks, real achievements/criteria backend integration and winner item storage.

- [x] **#NEXT.R8.ENTITIES.257** Store represented non-disenchant `LootRoll` winner items.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::Finish`, `Player::StoreLootItem`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-network/src/lib.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented winner finish now mirrors the C++ non-disenchant `LootRoll::Finish` tail after `SendLootRollWon` and criteria updates. A current-session winner calls the existing direct represented `StoreLootItem` bridge and removes the represented slot only after a successful store. A connected remote winner is routed through a bounded `SessionCommand::LootRollStoreWinner` so the target session runs its own allowed-looter, roll-winner and `CanStoreItem`/store checks before the owner marks/removes the slot. Timer expiry was moved from the sync update loop into async `process_pending` so this finish path can await DB/target-session work. Remaining gaps: canonical shared `Loot`/`LootRoll` ownership, live DB-backed successful remote assignment validation, disenchant winner `AutoStore`/retrieval-mail fallback, real `ConditionMgr`/`AllowedForPlayer` invalid-voter checks and real achievements/criteria backend integration.

- [x] **#NEXT.R8.ENTITIES.258** Apply C++ greed-only item flag to represented `SMSG_START_LOOT_ROLL`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::TryToStart`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`RollMask`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h` (`ITEM_FLAG2_CAN_ONLY_ROLL_GREED`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs in this miniphase and `docs/migration/loot.md`.
  Acceptance: represented roll startup now starts from the current no-disenchant mask and clears `ROLL_FLAG_TYPE_NEED` when the item template's `ItemSparse.Flags[1]` contains `CanOnlyRollGreed`, matching the C++ `m_voteMask = m_voteMask & ~ROLL_FLAG_TYPE_NEED` branch. This intentionally does not reject spoofed `CMSG_LOOT_ROLL` Need votes because the contrasted C++ `PlayerVote` path does not re-check the mask. Unit coverage verifies `SMSG_START_LOOT_ROLL.ValidRolls` is `Pass | Greed` for a can-only-roll-greed item. Remaining gaps: real `ItemDisenchantLoot.db2`/`disenchant_loot_template` skill-based disenchant mask calculation, canonical shared `LootRoll` ownership, real `ConditionMgr`/`AllowedForPlayer` invalid-voter checks and full disenchant winner storage.

- [x] **#NEXT.R8.ENTITIES.259** Add the contrasted `ItemDisenchantLoot.db2` store foundation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sItemDisenchantLootStore`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ItemDisenchantLootLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ItemDisenchantLootEntry`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetDisenchantLoot`).
  Rust targets: `crates/wow-data/src/item_disenchant_loot.rs`, `crates/wow-data/src/lib.rs`, database registry docs and `docs/migration/loot.md`.
  Acceptance: `wow-data` now exposes `ItemDisenchantLootEntry` and `ItemDisenchantLootStore` with the C++ field order (`ID`, `Subclass`, `Quality`, `MinLevel`, `MaxLevel`, `SkillRequired`, `ExpansionID`, `Class`), ID lookup and ordered iteration so first-match behavior can match the C++ store traversal. A reusable `find_for_item_like_cpp` helper mirrors the DB2 filter portion of `Item::GetDisenchantLoot`, including this branch's existing subclass guard shape, while keeping item-template gates outside the DB2 store. Unit coverage verifies ID indexing, stable order and C++-shaped class/quality/level/expansion/subclass filtering. Remaining gaps: Rust `ItemSparseTemplateEntry` still lacks the area/map/required-expansion fields used by `Item::GetDisenchantLoot`; `ItemCurrencyCost`, sell-price gating, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.260** Wire `ItemDisenchantLoot.db2` into world-server session resources.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sItemDisenchantLootStore` global load), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetDisenchantLoot` consumers).
  Rust targets: `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, registry docs and `docs/migration/loot.md`.
  Acceptance: `world-server` now loads `ItemDisenchantLoot.db2` at startup with the same required-resource behavior as the other DB2 stores, stores it in shared `SessionResources`, and injects it into each `WorldSession` through `set_item_disenchant_loot_store`. This closes the resource-availability step needed before the represented roll mask can consume the C++ `Item::GetDisenchantLoot` filters. Remaining gaps: hotfix SQL overlay, the full item-template gates (`area`, `map`, required expansion and currency/sell-price checks), `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.261** Preserve `ItemSparse` fields needed by C++ disenchant gates.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ItemSparseEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ItemSparseLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h` (`GetArea`, `GetMap`, `GetRequiredExpansion`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetDisenchantLoot`).
  Rust targets: `crates/wow-data/src/item_stats.rs`, test initializers in `crates/wow-world/src/session.rs` and `crates/wow-world/src/handlers/loot.rs`, plus docs.
  Acceptance: `ItemSparseTemplateEntry` now preserves `ZoneBound[2]`, `InstanceBound` and `ExpansionID` alongside the existing storage fields. The manual WDC4 offset reader tracks the same C++ `ItemSparseEntry` field positions, and unit coverage verifies the sparse-template helper subset returns those values. Remaining gaps: `ItemCurrencyCost`, sell-price/currency-cost gate, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.262** Add and wire `ItemCurrencyCost.db2` for the C++ disenchant sell-price gate.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ItemCurrencyCostEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ItemCurrencyCostLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sItemCurrencyCostStore`, `_itemsWithCurrencyCost`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.h` (`HasItemCurrencyCost`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetDisenchantLoot`).
  Rust targets: `crates/wow-data/src/item_currency_cost.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, registry docs and `docs/migration/loot.md`.
  Acceptance: `wow-data` now exposes `ItemCurrencyCostEntry` and `ItemCurrencyCostStore` for `ItemCurrencyCost.db2`, building the same item-id set C++ builds in `DB2Manager::_itemsWithCurrencyCost`. `world-server` loads it as a required DB2 resource, injects it through `SessionResources`, and `WorldSession::has_item_currency_cost_like_cpp` mirrors `sDB2Manager.HasItemCurrencyCost(itemId)`. Unit coverage verifies entry lookup and item-id set semantics, including ignoring negative item ids. Remaining gaps: hotfix SQL overlay, final `Item::GetDisenchantLoot` gate integration, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.263** Add `ItemClass.db2` and preserve `ItemSparse` price fields before porting C++ item pricing.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ItemClassEntry`, `ItemSparseEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ItemClassLoadInfo`, `ItemSparseLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sItemClassStore`, `_itemClassByOldEnum`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.h` (`GetItemClassByOldEnum`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetBuyPrice`, `Item::GetSellPrice`, `Item::GetDisenchantLoot`).
  Rust targets: `crates/wow-data/src/item_class.rs`, `crates/wow-data/src/item_stats.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/world-server/src/main.rs`, registry docs and `docs/migration/loot.md`.
  Acceptance: `wow-data` now exposes `ItemClassEntry`/`ItemClassStore`, loads `ItemClass.db2` fields `ID`, `ClassID`, `PriceModifier` and `Flags`, and builds the same old-enum class index used by C++ `DB2Manager::GetItemClassByOldEnum`. `world-server` loads it as a required DB2 resource and injects it into sessions; `WorldSession::item_class_price_modifier_like_cpp` exposes the C++ price modifier lookup without pretending full price calculation is complete. `ItemSparseTemplateEntry` now also preserves `BuyPrice`, `VendorStackCount`, `PriceVariance` and `PriceRandomValue`, with fixtures updated and unit coverage for the helper-facing subset. Remaining gaps: hotfix SQL overlay, `ImportPrice*` DB2 stores, full `Item::GetBuyPrice`/`Item::GetSellPrice` parity, final `Item::GetDisenchantLoot` gate integration, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.264** Add and wire the C++ `ImportPrice*.db2` stores used by item buy-price calculation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ImportPriceArmorEntry`, `ImportPriceQualityEntry`, `ImportPriceShieldEntry`, `ImportPriceWeaponEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ImportPrice*LoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sImportPrice*Store`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetBuyPrice`).
  Rust targets: `crates/wow-data/src/import_price.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, registry docs and `docs/migration/loot.md`.
  Acceptance: `wow-data` now exposes `ImportPriceArmorStore`, `ImportPriceQualityStore`, `ImportPriceShieldStore`, `ImportPriceWeaponStore` and an aggregate `ImportPriceStores`, preserving the C++ fields used by `Item::GetBuyPrice`. The WDC4 loader accepts both observed layout shapes where `ID` is either a readable field or only the record id list. `world-server` loads all four DB2 files as required startup resources, injects the aggregate through `SessionResources`, and `WorldSession` exposes the first C++ lookup helper (`quality + 1`) without yet claiming full price parity. Remaining gaps: `ItemPriceBase.db2`, hotfix SQL overlay, full `Item::GetBuyPrice`/`Item::GetSellPrice` parity, final `Item::GetDisenchantLoot` gate integration, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.265** Add and wire `ItemPriceBase.db2` for C++ item buy-price base factors.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`ItemPriceBaseEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`ItemPriceBaseLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sItemPriceBaseStore`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetBuyPrice`).
  Rust targets: `crates/wow-data/src/item_price_base.rs`, `crates/wow-data/src/lib.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, registry docs and `docs/migration/loot.md`.
  Acceptance: `wow-data` now exposes `ItemPriceBaseEntry`/`ItemPriceBaseStore` with the C++ fields `ID`, `ItemLevel`, `Armor` and `Weapon`, and indexes records by `ID` for the C++ `sItemPriceBaseStore.LookupEntry(itemLevel)` callsite. `world-server` loads `ItemPriceBase.db2`, injects it through `SessionResources`, and `WorldSession::item_price_base_like_cpp` exposes the armor/weapon base factors needed by `Item::GetBuyPrice`. Remaining gaps: hotfix SQL overlay, full `Item::GetBuyPrice`/`Item::GetSellPrice` parity, final `Item::GetDisenchantLoot` gate integration, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.266** Add represented C++ item buy/sell-price helpers.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetBuyPrice`, `Item::GetSellPrice`).
  Rust targets: `crates/wow-world/src/session.rs`, docs.
  Acceptance: `WorldSession::item_buy_price_like_cpp` now calculates the represented C++ buy-price path from `ItemSparse` price variance/random value, `ImportPriceQuality`, `ItemPriceBase`, inventory type, armor/shield/weapon modifiers, robe-to-chest conversion and artifact relic gem override. `WorldSession::item_sell_price_like_cpp` mirrors the contrasted branch shape: override-gold-cost returns sparse sell price, otherwise it calls buy price and because this C++ branch leaves `standardPrice` false, returns sparse `SellPrice` rather than inventing the standard-price sell calculation. Unit coverage verifies armor and gem-artifact-relic paths plus the standardPrice=false sell fallback. Remaining gaps: hotfix SQL overlay, deciding whether the C++ `standardPrice=false` behavior is an upstream bug to fix in both C++ and Rust, final `Item::GetDisenchantLoot` gate integration, `disenchant_loot_template` generation and grouped-roll `SkillRequired` mask integration remain pending.

- [x] **#NEXT.R8.ENTITIES.267** Add represented C++ `Item::GetDisenchantLoot` gate helper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/Item.cpp` (`Item::GetDisenchantLoot`).
  Rust targets: `crates/wow-world/src/session.rs`, docs.
  Acceptance: `WorldSession::item_disenchant_loot_like_cpp` now applies the contrasted C++ gate sequence over the represented stores: explicit `BonusData::CanDisenchant` parameter, `ITEM_FLAG_CONJURED`, `ITEM_FLAG_NO_DISENCHANT`, `BIND_QUEST`, `GetArea(0/1)`, `GetMap`, `GetMaxStackSize`, represented `GetSellPrice` plus `HasItemCurrencyCost`, then ordered `ItemDisenchantLoot.db2` lookup by class/subclass/quality/item-level/required-expansion. The helper returns `(DisenchantID, SkillRequired)` so the grouped-roll mask can consume it next without re-querying. Unit coverage exercises the positive path and `CanDisenchant=false` refusal. Remaining gaps: canonical `BonusData::CanDisenchant` storage, `disenchant_loot_template` generation, grouped-roll `SkillRequired` mask integration and disenchant winner `AutoStore`/mail fallback remain pending.

- [x] **#NEXT.R8.ENTITIES.268** Wire C++ `LootRoll::TryToStart` disenchant `ValidRolls` mask.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::TryToStart`, `SendStartRoll`, `GetItemDisenchantLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`RollMask`).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-data/src/item_stats.rs`, docs.
  Acceptance: represented group/NBG roll startup now starts from the C++ `ROLL_ALL_TYPE_MASK = 0x0F`, clears Need for `ITEM_FLAG2_CAN_ONLY_ROLL_GREED`, resolves the represented `Item::GetDisenchantLoot` skill requirement from the item template quality/level, computes the max represented connected looter enchanting skill, and clears Disenchant when the DB2 gate has no match or `SkillRequired` is too high. `PlayerBroadcastInfo` now has a represented `enchanting_skill` hook so later canonical `Player::GetSkillValue(SKILL_ENCHANTING)` wiring can feed this path without reshaping loot roll startup. Unit coverage verifies a connected looter with skill 175 receives a `SMSG_START_LOOT_ROLL.ValidRolls` mask containing Disenchant. Remaining gaps: canonical per-player skill state, canonical `BonusData::CanDisenchant` storage, `disenchant_loot_template` generation and disenchant winner `AutoStore`/mail fallback remain pending.

- [x] **#NEXT.R8.ENTITIES.269** Add represented C++ `ROLL_DISENCHANT` winner storage path.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::Finish`, `Loot::FillLoot`, `Loot::AutoStore`, `NotifyItemRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplates_Disenchant`, `LoadLootTemplates_Disenchant`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`AutoStoreLoot`, `StoreLootItem`).
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: when represented group loot finishes with a `ROLL_DISENCHANT` winner, Rust now re-resolves the contrasted `Item::GetDisenchantLoot` ID, loads `disenchant_loot_template` rows and `reference_loot_template` expansions, applies C++-shaped default-loot-mode, chance, min/max count and item-exists guards, expands stack counts, and stores generated items through the existing represented current-session or remote-session `StoreLootItem` bridge. The original rolled item is removed/notified only after represented storage succeeds, matching the C++ `AutoStore(...)=true` success branch. Unit coverage verifies the represented disenchant loot-template guard shape. Remaining gaps: canonical `LootStore`/`LootTemplate` globals, loot-template conditions for disenchant rows, grouped disenchant rows, full all-or-mail fallback semantics, canonical per-player skill state and real `BonusData::CanDisenchant`.

- [x] **#NEXT.R8.ENTITIES.270** Start canonical `wow-loot` `LootStoreItem`/`LootTemplate` foundation and wire disenchant `GroupId`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.h` (`LootStoreItem`, `LootTemplate`, private `LootGroup`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootStoreItem::IsValid`, `LootTemplate::AddEntry`, `LootGroup::Roll`, `LootTemplate::Process`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/Cargo.toml`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow-loot` is no longer an empty crate; it now exposes the contrasted `LootStoreItem`, `LootTemplate`, and `LootGroup` foundation with C++-shaped validity guards, grouped-vs-ungrouped `AddEntry` splitting, explicit-chance-before-equal-chance group roll order, and focused unit coverage. The represented disenchant winner generator now loads `GroupId`, builds a `LootTemplate` frame from rows, processes non-group entries, grouped rows, and reference rows that request a specific group like `Referenced->Process(..., item->groupid, ...)`. Remaining gaps: canonical 12 `LootStore` globals/loaders, full reusable `Loot::FillLoot` materialization, `ConditionMgr`-backed loot conditions, full all-or-mail fallback semantics, canonical per-player skill state and real `BonusData::CanDisenchant`.

- [x] **#NEXT.R8.ENTITIES.271** Add canonical `LootStore` definitions and row-loader foundation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.h` (`LootStore`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplates_*`, `LootStore::LoadLootTable`, `LoadAndCollectLootIds`, `HaveLootFor`, `GetLootFor`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, docs.
  Acceptance: `wow-loot` now has `LootStoreKind` definitions for the 12 C++ `LootTemplates_*` globals, `LootStoreDefinition` table/entry/rates metadata, and a pure `LootStore` map with `clear_like_cpp`, `have_loot_for`, `get_loot_for`, `collect_loot_ids_like_cpp`, and `load_rows_like_cpp`. The loader mirrors the contrasted C++ shape by clearing first, rejecting `GroupId >= 128`, validating each row through `LootStoreItem::IsValid`, grouping accepted rows by `Entry` into `LootTemplate`, and returning the accepted row count. `wow-database` now exposes one-row-by-entry prepared statements for the remaining C++ loot-template tables: creature, fishing, gameobject, mail, milling, pickpocketing, prospecting, skinning and spell, in addition to item/reference/disenchant. Remaining gaps: async startup wiring that fills the 12 stores, `LootTemplates_Reference` cross-store reference verification, `ConditionMgr` fill/linking, full `Loot::FillLoot` materialization, canonical `LootItem`/`Loot`/`LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.272** Wire startup loading for the 12 C++ `LootTemplates_*` stores.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootStore::LoadLootTable`, `LoadAndCollectLootIds`, `LootTemplates_*`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.h` (`LootStore` globals).
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, `crates/world-server/src/main.rs`, docs.
  Acceptance: `world-server` now loads all 12 C++ loot-template store foundations at startup using full-table statements with the contrasted column shape `Entry, Item, Reference, Chance, QuestRequired, LootMode, GroupId, MinCount, MaxCount`. The loader validates rows through `wow-loot::LootStore::load_rows_like_cpp` using `Item.db2` existence checks for non-reference items, logs accepted rows/template IDs for each table, stores the `LootStores` map in `SessionResources`, and injects it into each `WorldSession`. Remaining gaps: `LootTemplates_Reference` cross-store reference verification, `ConditionMgr` fill/linking, full `Loot::FillLoot` materialization, canonical `LootItem`/`Loot`/`LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.273** Port C++ `LootTemplates_Reference` cross-store verification.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LoadLootTemplates_Reference`, `LootStore::LoadAndCollectLootIds`, `LootStore::CheckLootRefs`, `LootTemplate::CheckLootRefs`, `LootGroup::CheckLootRefs`, `LootStore::ReportUnusedIds`, `LootStore::ReportNonExistingId`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/world-server/src/main.rs`, docs.
  Acceptance: `wow-loot::check_loot_references_like_cpp` now mirrors the contrasted C++ reference pass: it seeds the unused-reference set from `reference_loot_template`, checks the C++ store order (`Creature`, `Fishing`, `Gameobject`, `Item`, `Milling`, `Pickpocketing`, `Skinning`, `Disenchant`, `Prospecting`, `Mail`, `Reference`), intentionally preserves the C++ omission of `Spell`, records missing reference targets, erases used reference IDs, and returns sorted unused IDs for deterministic diagnostics. `world-server` runs the report after loading the 12 stores and logs missing/unused references non-fatally like TrinityCore. Remaining gaps: `ConditionMgr` fill/linking, full `Loot::FillLoot` materialization, canonical `LootItem`/`Loot`/`LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.274** Port structural C++ loot-condition source/link validation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`isSourceTypeValid` loot-template branches, `addToLootTemplate`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h` (`ConditionSourceType`, `ConditionId`, `ConditionsReference`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplate::LinkConditions`, `LootTemplate::isReference`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/world-server/src/main.rs`, docs.
  Acceptance: `wow-loot::check_loot_condition_links_like_cpp` now mirrors the structural part of C++ `ConditionMgr` for loot sources: source types `1..12` map to the 12 `LootStoreKind` values, `SourceGroup` must exist in the matching store, `SourceEntry` must be either an existing `Item.db2` row or an entry-level reference in the target `LootTemplate`, and the link target lookup follows `LootTemplate::LinkConditions` order over entries, explicitly-chanced group rows and equal-chanced group rows. `world-server` now loads distinct loot condition IDs from `conditions` with `SourceId=0` and logs missing templates/items non-fatally after loot-store load. Remaining gaps: full reusable `ConditionMgr` storage/evaluator, actual weak-reference condition attachment to `LootStoreItem`, full `Loot::FillLoot` materialization, canonical `LootItem`/`Loot`/`LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.275** Add pure C++ `Loot::FillLoot` / `LootTemplate::Process` foundation in `wow-loot`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootStoreItem::Roll`, `LootTemplate::LootGroup::Roll`, `LootTemplate::LootGroup::Process`, `LootTemplate::Process`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::FillLoot`, `Loot::AddItem`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`MAX_NR_LOOT_ITEMS`).
  Rust targets: `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: `wow-loot` now exposes a pure, testable `LootStore::fill_loot_like_cpp` foundation over the already-loaded `LootStores`: missing loot IDs return an explicit no-template error, non-group entries honor `lootMode`, chance rolls and a caller-supplied C++ rate callback, reference entries recurse through `LootStoreKind::Reference` using `referenced_amount_rate` and the row `GroupId`, grouped rows roll explicitly-chanced entries before equal-chanced entries, and generated rows split by caller-supplied max stack while preserving sequential `LootListId` and the C++ `MAX_NR_LOOT_ITEMS = 18` cap. Remaining gaps: full `LootItem` metadata/random-property/context, reusable `ConditionMgr` evaluation, Player/Group access-right marking, creature/gameobject ownership, canonical `LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.276** Add C++ `LootItem` constructor metadata to pure generated loot rows.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootItem::LootItem`, `Loot::AddItem`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LootItem` fields).
  Rust targets: `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: `GeneratedLootItem` now preserves the contrasted `LootItem` constructor/AddItem metadata in the pure `wow-loot` foundation: `random_properties_id` is supplied by a caller callback equivalent to `ItemEnchantmentMgr::GenerateRandomProperties`, `context` is copied from `LootFillOptions` like `_itemContext`, `free_for_all` comes from item metadata equivalent to `ITEM_FLAG_MULTI_DROP`, `follow_loot_rules` mirrors `!needs_quest || ITEM_FLAGS_CU_FOLLOW_LOOT_RULES`, `needs_quest` is copied from the `LootStoreItem`, and the initial C++ state bits `is_looted`, `is_blocked`, `is_under_threshold` and `is_counted` start false. Remaining gaps: real `ItemRandomProperties` object shape, `allowedGUIDs`/`rollWinnerGUID`, reusable `ConditionMgr` evaluation, Player/Group access-right marking, creature/gameobject ownership, canonical `LootRoll` runtime and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.277** Wire represented creature corpse loot to `LootTemplates_Creature`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp` (`loot->FillLoot(creature->GetLootId(), LootTemplates_Creature, ...)`, `generateMoneyLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Creature/Creature.cpp` (`Creature::GetLootId`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::FillLoot`, `LootItem::LootItem`, `Loot::AddItem`).
  Rust targets: `crates/wow-ai/src/lib.rs`, `crates/wow-database/src/statements/world.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: represented creature spawn state now carries the resolved C++ difficulty `LootID`, `GoldMin` and `GoldMax`; lazy `CMSG_LOOT_UNIT`/AE represented corpse generation uses `LootTemplates_Creature` through `wow-loot::fill_loot_like_cpp` rather than assuming creature `entry == LootID`; `GeneratedLootItem` now carries random properties id plus seed and converts into represented `LootEntry` rows with context and flags; and represented corpse money uses the C++ gold bounds when available. Remaining gaps: full `ConditionMgr::AllowedForPlayer`, group loot threshold/method marking, canonical shared `Loot` ownership, gameobject/player-corpse runtime wiring and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.278** Apply `ITEM_FLAGS_CU_FOLLOW_LOOT_RULES` to represented creature generated loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootItem::LootItem`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp` (`LoadItemTemplateAddon`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Item/ItemTemplate.h` (`ITEM_FLAGS_CU_FOLLOW_LOOT_RULES`).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented creature corpse loot now keeps `wow-loot` pure and synchronous, then loads `item_template_addon.FlagsCu` for the generated item IDs in `wow-world`; converted `LootEntry` rows set `follow_loot_rules` exactly as C++ `LootItem::LootItem`: `!needs_quest || (FlagsCu & ITEM_FLAGS_CU_FOLLOW_LOOT_RULES) != 0`. A focused unit test covers quest-required generated loot with and without the custom flag while preserving random properties and context. Remaining gaps: full `ConditionMgr::AllowedForPlayer`, group loot threshold/method marking, canonical shared `Loot` ownership, gameobject/player-corpse runtime wiring and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.279** Apply represented `LootItem::AllowedForPlayer` gates during creature corpse generation.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootItem::AllowedForPlayer`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplate::Process`, `LootGroupInvalidSelector`, `LootTemplate::LinkConditions`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`addToLootTemplate`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow-loot` now offers a context-aware fill path that tells runtime callbacks the exact C++ condition source tuple (`LootStoreKind`, `SourceGroup`, `SourceEntry`) for direct and referenced rows while preserving the old callback API. Represented creature corpse generation preloads candidate condition rows and `item_template_addon` metadata, then filters candidates before materialization with the represented `AllowedForPlayer` subset: DB conditions with ElseGroup/NegativeCondition, represented inventory/team/class/race/gender/spell/quest/level/objective conditions, faction flags, item template existence, `needs_quest`/start-quest status, `QuestLogItemId` and `ITEM_FLAGS_CU_IGNORE_QUEST_STATUS`. Remaining gaps: reusable canonical `ConditionMgr` storage/evaluator, script-backed/non-player-target conditions, strict usability/profession recipe checks, canonical shared `Loot` ownership, gameobject/player-corpse runtime wiring and all-or-mail fallback semantics.

- [x] **#NEXT.R8.ENTITIES.280** Centralize represented loot-condition row/evaluator helpers in `wow-loot`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/common/Utilities/Util.h` (`ComparisionType`, `CompareValues`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`Condition::Meets`, `LoadConditions`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h` (`Condition` rows).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow-loot` now owns the reusable represented loot-condition row shape plus the C++ ElseGroup OR / in-group AND / NegativeCondition evaluator used by both item-loot and creature-loot bridges. The shared `condition_compare_values_like_cpp` follows the contrasted C++ `ComparisionType` order (`0 ==`, `1 >`, `2 <`, `3 >=`, `4 <=`) and returns `None` for unsupported values, fixing the creature-loot level-comparison mapping introduced in the local bridge. Remaining gaps: full canonical `ConditionMgr` object/source storage, condition-reference expansion, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.281** Expand represented loot condition-reference templates like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`LoadConditions`, `IsObjectMeetToConditionList`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h` (`CONDITION_SOURCE_TYPE_REFERENCE_CONDITION`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented loot conditions now preserve the C++ negative-reference semantics: `ConditionTypeOrReference < 0` is treated as a reference ID, loaded from `conditions` rows whose `SourceTypeOrReferenceId` is `-reference_id` with `SourceGroup = 0` and `SourceEntry = 0`, evaluated recursively with ElseGroup OR / in-group AND behavior, and ignored when missing like the C++ "checked at loading, should never happen" branch. The item-loot bridge and represented creature corpse bridge both preload nested condition-reference templates for direct and referenced loot rows. Remaining gaps: full canonical `ConditionMgr` object/source storage, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.282** Validate loot condition-reference templates during startup diagnostics.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`LoadConditions`, missing-reference runtime branch comment).
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-loot/src/lib.rs`, `crates/world-server/src/main.rs`, docs.
  Acceptance: startup loot-condition diagnostics now load every negative `ConditionTypeOrReference` use from loot-source rows and nested reference-template rows, load the available negative `SourceTypeOrReferenceId` reference templates, and log missing referenced templates non-fatally in the same structural diagnostics pass as missing loot template/item links. This makes the represented runtime's missing-reference no-op match the C++ assumption that missing references were already caught during loading. Remaining gaps: full canonical `ConditionMgr` object/source storage, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.283** Skip self-referencing condition-reference rows like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`LoadConditions` self-reference guard).
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented loot-condition loaders now drop rows where both `SourceTypeOrReferenceId` and `ConditionTypeOrReference` are the same negative reference ID, matching C++'s "self referencing, skipped" branch. Startup reference-use diagnostics also exclude those skipped rows, so self-references do not become false missing-template warnings and cannot turn into runtime recursion. Remaining gaps: full canonical `ConditionMgr` object/source storage, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.284** Make represented loot `ElseGroup` evaluation order-independent like C++.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`IsObjectMeetToConditionList`).
  Rust targets: `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: the shared represented loot-condition evaluator now mirrors C++'s `elseGroupStore` map semantics instead of relying on contiguous rows with the same `ElseGroup`. Conditions in the same ElseGroup are still ANDed, different ElseGroups are still ORed, failed groups still short-circuit later rows in that group, and a focused test covers interleaved ElseGroup rows. Remaining gaps: full canonical `ConditionMgr` object/source storage, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.285** Skip deterministic invalid represented loot-condition rows like C++ load.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`LoadConditions`, `isConditionTypeValid`, `Condition::GetMaxAvailableConditionTargets`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h` (`ConditionTypes`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented item-loot and creature-loot condition loaders now drop deterministic invalid rows that C++ would never store: normal condition types outside `CONDITION_MAX`, deprecated `CONDITION_SPAWNMASK`, non-reference loot rows with unavailable `ConditionTarget`, `CONDITION_ITEM` count 0, invalid team values, invalid gender values, invalid `CONDITION_LEVEL` comparison types and invalid `CONDITION_QUESTSTATE` masks. Negative condition-reference rows remain loadable even with useless target/value fields, matching C++'s reference-row logging-but-load behavior. Remaining gaps: validation requiring global stores (`SpellMgr`, `ObjectMgr`, DB2 stores), full canonical `ConditionMgr` object/source storage, script-backed/non-player-target conditions and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.286** Apply C++ playable class/race mask rules to represented loot conditions.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`isConditionTypeValid`, `Condition::Meets`), `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h` (`CLASSMASK_ALL_PLAYABLE`), `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/RaceMask.h` (`RaceMask::GetRaceBit`, `RACEMASK_ALL_PLAYABLE`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented loot-condition loading now skips `CONDITION_CLASS` masks with non-playable class bits and `CONDITION_RACE` masks with non-playable race bits like C++ `isConditionTypeValid`. The represented runtime race-mask helper now mirrors C++'s `RaceMask::GetRaceBit` remapping for Dark Iron Dwarf, Vulpera, Mag'har Orc, Mechagnome and Dracthyr Horde/Alliance instead of assuming `race_id - 1` for every race. Remaining gaps: full canonical `ConditionMgr` object/source storage, external-store validations and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.287** Skip deterministic comparison/target invalid represented loot-condition rows like C++ load.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`isConditionTypeValid`, `Condition::GetMaxAvailableConditionTargets`).
  Rust targets: `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: represented loot-condition loading now applies the C++ deterministic load skips for invalid `CONDITION_DRUNKENSTATE`, all `CONDITION_DISTANCE_TO` rows on loot sources (loot sources expose only target 0, making the distance target either self or unavailable), invalid `CONDITION_HP_VAL` comparison type, and invalid `CONDITION_HP_PCT` percent/comparison values. Runtime evaluation for valid HP/drunkenstate conditions remains intentionally unsupported until canonical player/unit state is available. Remaining gaps: full canonical `ConditionMgr` object/source storage, external-store validations and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.288** Skip remaining deterministic target/mask invalid loot-condition rows like C++ load.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`isConditionTypeValid`, `Condition::GetMaxAvailableConditionTargets`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.h` (`TYPEMASK_*`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/UnitDefines.h` (`UNIT_STAND_STATE_SUBMERGED`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Pet/PetDefines.h` (`MAX_PET_TYPE`).
  Rust targets: `crates/wow-loot/src/lib.rs`, docs.
  Acceptance: represented loot-condition loading now also mirrors C++ deterministic skips for invalid `CONDITION_TYPE_MASK`, all `CONDITION_RELATION_TO`/`CONDITION_REACTION_TO` rows on loot sources (only target 0 exists, so relation target is self or unavailable), invalid `CONDITION_STAND_STATE`, invalid `CONDITION_PET_TYPE`, and unsupported `CONDITION_INSTANCE_INFO` `INSTANCE_INFO_GUID_DATA`. Runtime evaluation for valid versions of these still remains unsupported until canonical player/unit/map state exists. Remaining gaps: full canonical `ConditionMgr` object/source storage, external-store validations and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.289** Normalize legacy object/type-mask loot conditions and evaluate represented player object checks.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp` (`CONDITION_OBJECT_ENTRY_GUID_LEGACY`, `CONDITION_TYPE_MASK_LEGACY`, `CONDITION_OBJECT_ENTRY_GUID`, `CONDITION_TYPE_MASK`, `Condition::Meets`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Object/ObjectGuid.h` (`TypeID`, `TypeMask`, `Trinity::Legacy::ConvertLegacyTypeID`, `ConvertLegacyTypeMask`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented loot-condition loading now mirrors the C++ legacy mutation from `CONDITION_OBJECT_ENTRY_GUID_LEGACY` to `CONDITION_OBJECT_ENTRY_GUID` and `CONDITION_TYPE_MASK_LEGACY` to `CONDITION_TYPE_MASK`, then applies the converted `TypeID`/`TypeMask` validation before rows enter runtime evaluation. Invalid object `TypeID` rows are skipped like C++. Represented item-loot and creature-loot condition evaluation now handles `CONDITION_OBJECT_ENTRY_GUID` for the available player target and `CONDITION_TYPE_MASK` against the player object/unit/player type mask. Remaining gaps: external ObjectMgr template/GUID validations for unit/gameobject object-entry conditions, full canonical `ConditionMgr` object/source storage and non-loot condition consumers.

- [x] **#NEXT.R8.ENTITIES.290** Stabilize dirty-tree baseline before further port work.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp`, `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp`.
  Rust targets: full workspace plus `crates/wow-data/src/currency.rs`, `docs/migration/loot.md`, `docs/migration/inventory/r8-entities-miniphase.md`, `docs/migration/inventory/r8-entities-miniphase.tsv`.
  Acceptance: dirty tree was inventoried before continuing (111 modified files and 10 untracked `wow-data` DB2 modules at checkpoint time). Base validation is green: `cargo check --workspace`, `cargo test --workspace`, package-level smoke tests for touched crates, `cargo fmt --check`, and `git diff --check` all pass. One brittle Rust-only test assumption was fixed after C++ contrast: `CurrencyTypesStore` now tests lookup semantics against a real loaded DB2 ID instead of requiring ID `395`, because C++ global stores require correct `HasRecord`/`LookupEntry` behavior over available records, not that a specific local client-data row exists. This checkpoint does not certify full port parity; it only establishes a build/test-clean baseline for the next C++-contrasted gap closures.

- [x] **#NEXT.R8.ENTITIES.291** Centralize C++ `Loot::generateMoneyLoot` money generation in `wow-loot`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::generateMoneyLoot`), `/home/server/woltk-trinity-legacy/src/server/game/World/World.h` (`RATE_DROP_MONEY`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow_loot::generate_money_loot_with_rate_like_cpp` now owns the contrasted C++ branch shape: `maxAmount == 0` returns zero for a fresh Rust loot value, `maxAmount <= minAmount` uses max, small ranges roll inclusive `min..=max`, large ranges roll shifted `min >> 8..=max >> 8` and shift back, and all nonzero branches multiply by `Rate.Drop.Money`. Represented `CMSG_OPEN_ITEM` item money loot now consumes the shared helper instead of a private duplicate, and represented creature corpse money now applies `Rate.Drop.Money` to DB gold bounds instead of ignoring the configured rate. Remaining gaps: full canonical `Loot` ownership/state, money generation stored on canonical `Loot`, and removal of the represented legacy fallback for creatures without DB gold bounds.

- [x] **#NEXT.R8.ENTITIES.292** Port the C++ `LootItem::GetUiTypeForPlayer` decision tree into a shared helper.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootItem::GetUiTypeForPlayer`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LootMethod`, `LootSlotType`).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow_loot::loot_item_ui_type_for_player_like_cpp` now mirrors the C++ slot-visibility/UI decision order for looted rows, `allowedGUIDs`, per-player FFA rows, quest-only rows, free-for-all, round-robin, master-loot, group/need-before-greed rolls and personal loot. Represented `LootResponse` item construction now consumes that helper instead of the previous free-for-all-only visibility/UI shortcut. Remaining gaps: represented `CreatureLoot` still lacks canonical `roundRobinPlayer`, `_lootMaster` and `Loot::GetPlayerFFAItems()` ownership state, so those arguments are bridged from currently available represented fields until canonical `Loot` ownership replaces the session bridge.

- [x] **#NEXT.R8.ENTITIES.293** Carry C++ loot method, master and round-robin owner on represented creature loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::Loot`, `Loot::FillLoot`, `Loot::NotifyLootList`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`DoLootRelease` round-robin clear).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, docs.
  Acceptance: represented `CreatureLoot` now stores `loot_master` and `round_robin_player` alongside `loot_method`. Represented creature loot generation copies the current group loot method and master-looter GUID like C++ `Loot::Loot`, assigns the first represented corpse loot owner as `roundRobinPlayer` like C++ `FillLoot` for group corpse loot, feeds both fields into `LootItem::GetUiTypeForPlayer` parity, includes them in `SMSG_LOOT_LIST` fanout like `NotifyLootList`, and clears/broadcasts round-robin ownership on release like `DoLootRelease`. Remaining gaps: this is still represented/session-backed state, not canonical `Creature::m_loot`/shared `Loot`, and per-player `Loot::GetPlayerFFAItems()` remains pending.

- [x] **#NEXT.R8.ENTITIES.294** Carry represented per-player C++ `Loot::GetPlayerFFAItems()` state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`NotNormalLootItem`, `NotNormalLootItemMap`, `Loot::GetPlayerFFAItems`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::FillNotNormalLootFor`, `LootItem::GetUiTypeForPlayer`, `Loot::LootItemInSlot`, `Loot::AutoStore`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented `CreatureLoot` now carries a per-player FFA item list matching C++ `NotNormalLootItem { LootListId, is_looted }`. Opening represented loot fills one FFA row per eligible player instead of treating FFA visibility as global, `LootResponse` and represented `CMSG_LOOT_ITEM` slot lookup consume that per-player row like `GetPlayerFFAItems()`, and FFA pickup marks only that player's row looted while preserving the item for other eligible players. Remaining gaps: this is still a represented/session bridge; canonical shared `Loot`, full `LootItem::AllowedForPlayer`, `_allowedLooters` ownership and cross-session loot views remain pending.

- [x] **#NEXT.R8.ENTITIES.295** Split represented C++ `PlayersLooting` from `_allowedLooters`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`, `Player::StoreLootItem` FFA/non-FFA removal notification split), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::OnLootOpened`, `Loot::NotifyItemRemoved`, `Loot::NotifyMoneyRemoved`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`Loot::RemoveLooter`, `PlayersLooting`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented `CreatureLoot` now tracks active `players_looting` separately from eligible `allowed_looters`. Opening represented loot adds the player to `players_looting` like C++ `OnLootOpened`, release removes only that active viewer like `RemoveLooter`, non-FFA item removal and coin removal notify active viewers rather than every eligible player, and FFA item removal keeps the C++ one-player notification behavior. Remaining gaps: this is still represented/session-backed state; canonical shared `Loot`, exact disconnected-player pruning and full cross-session ownership remain pending.

- [x] **#NEXT.R8.ENTITIES.296** Carry represented C++ `Loot::unlootedCount` / `LootItem::is_counted` semantics.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`Loot::unlootedCount`, `Loot::isLooted`, `LootItem::is_counted`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::FillNotNormalLootFor`, `Loot::AutoStore`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootItemStorage.cpp` (`LoadStoredLoot` unlooted count).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, tests and docs.
  Acceptance: represented `CreatureLoot` now stores `unlooted_count`, `loot_is_looted_like_cpp` mirrors C++ `gold == 0 && unlootedCount == 0`, represented open-time `FillNotNormalLootFor` increments one shared non-FFA count via `flags.counted` and one FFA count per eligible player, pickup decrements exactly on unlooted-to-looted transitions, and stored/opened item-container loot initializes the count from remaining item rows like `LootItemStorage`. Remaining gaps: this is still represented/session-backed state; canonical `Loot` ownership, roll-loss/master-loot decrement paths and cross-session persistence remain under full loot closure.

- [x] **#NEXT.R8.ENTITIES.297** Apply represented C++ master-loot/roll-winner `unlootedCount` decrement.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMasterGiveOpcode`: mark item looted, `NotifyItemRemoved`, `--unlootedCount`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::Finish` winner routes through store/removal).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented master-loot and represented loot-roll winner removal now decrement `CreatureLoot.unlooted_count` only when the target transition was still unlooted, matching the C++ removal branch after a successful store. Unit coverage asserts self-target master-loot removal clears both item state and the represented unlooted counter. Remaining gaps: canonical shared `Loot`, full DB-backed remote success validation, roll-loss/mail fallback semantics and real criteria/news/script side effects remain pending.

- [x] **#NEXT.R8.ENTITIES.298** Carry represented C++ `LootType`/`GetLootTypeForClient` into `LootResponse.AcquireReason`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LootType`, `GetLootTypeForClient`, `Loot::_dungeonEncounterId`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendLoot`, `Player::StoreLootItem`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMoneyOpcode`, `HandleLootReleaseOpcode`).
  Rust targets: `crates/wow-packet/src/packets/loot.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/spell.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/session.rs`, docs.
  Acceptance: represented `CreatureLoot` now carries `loot_type` and `dungeon_encounter_id` fields like C++ `Loot`; represented creature corpse generation initializes `LOOT_CORPSE`, represented item-container opening initializes `LOOT_ITEM`, and `LootResponse.AcquireReason` now uses the C++ `GetLootTypeForClient` mapping (`PROSPECTING`/`MILLING` -> `DISENCHANTING`, `INSIGNIA` -> `SKINNING`, `FISHINGHOLE`/`FISHING_JUNK` -> `FISHING`) instead of a hardcoded zero. Unit coverage asserts both the alias mapping and the represented response field. Remaining gaps: `dungeon_encounter_id` is carried but not yet fed into canonical `SendNewItem`/criteria paths, and non-corpse loot sources such as skinning, pickpocketing, fishing, prospecting and milling still need canonical runtime wiring.

- [x] **#NEXT.R8.ENTITIES.299** Propagate represented C++ `Loot::_dungeonEncounterId` through roll and item-push packets.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`GetDungeonEncounterId`, `SetDungeonEncounterId`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`LootRoll::TryToStart`, `LootRoll::SendAllPassed`, `LootRoll::SendRoll`, `LootRoll::SendLootRollWon`, `Loot::AutoStore`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`Player::SendNewItem`, `Player::StoreLootItem`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootItemOpcode`, `HandleLootMasterGiveOpcode` AE/master/roll winner paths).
  Rust targets: `crates/wow-network/src/player_registry.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented roll packets now use the owner `CreatureLoot.dungeon_encounter_id` for `SMSG_START_LOOT_ROLL`, immediate/final `SMSG_LOOT_ROLL`, `SMSG_LOOT_ALL_PASSED` and `SMSG_LOOT_ROLL_WON` instead of a hardcoded zero. Represented direct loot pickup, master-loot self/remote target and loot-roll winner self/remote target storage pass the same ID into the C++-shaped `SendNewItem` bridge; nonzero IDs set `SMSG_ITEM_PUSH_RESULT.DungeonEncounterID`, `EncounterLoot` display text and `IsEncounterLoot` like `Player::SendNewItem`. Remote master/roll winner session commands now carry the ID to the target session. Unit coverage asserts the start-roll packet field and direct item-push encounter fields, and focused loot roll/master/open/pickup tests pass. Remaining gaps: real nonzero assignment still depends on canonical dungeon encounter/gameobject/unit generation (`SetDungeonEncounterId` callsites), AE loot result aggregation/criteria delivery is still represented, and canonical shared `Loot` ownership remains pending.

- [x] **#NEXT.R8.ENTITIES.300** Carry represented creature dungeon encounter loot source into generated `CreatureLoot`.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Unit/Unit.cpp` (`InstanceScript::GetBossDungeonEncounter`, `Loot::SetDungeonEncounterId`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootMgr::GenerateDungeonEncounterPersonalLoot`, `Loot::SetDungeonEncounterId`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GameObjectTemplate::Chest::DungeonEncounter`).
  Rust targets: `crates/wow-ai/src/lib.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented `CreatureAI` now carries a `dungeon_encounter_id` source field, `WorldSession::register_world_creature` accepts it, pending respawns preserve it, and represented creature loot generation copies it into `CreatureLoot.dungeon_encounter_id` instead of resetting to zero. This closes the bridge-side data loss found after #NEXT.R8.ENTITIES.299, so any represented creature source that resolves a nonzero C++ encounter ID can feed loot response, roll and item-push paths. Unit coverage asserts nonzero represented creature generation. Remaining gaps: Rust spawn/visibility registration still passes `0` because canonical `InstanceScript::GetBossDungeonEncounter` / dungeon encounter DB2 resolution is not ported, GameObject chest `DungeonEncounter` loot generation is still not wired, and personal dungeon encounter loot plus criteria/script delivery remain pending under canonical loot/runtime work.

- [x] **#NEXT.R8.ENTITIES.301** Port minimal C++ `sDungeonEncounterStore` DB2 reader.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Structure.h` (`DungeonEncounterEntry`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2LoadInfo.h` (`DungeonEncounterLoadInfo`), `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`sDungeonEncounterStore`), `/home/server/woltk-trinity-legacy/src/server/database/Database/Implementation/HotfixDatabase.cpp` (`HOTFIX_SEL_DUNGEON_ENCOUNTER`).
  Rust targets: `crates/wow-data/src/dungeon_encounter.rs`, `crates/wow-data/src/lib.rs`, `docs/migration/inventory/r3-database-registry.tsv`, docs.
  Acceptance: `wow-data` now exposes `DungeonEncounterEntry`/`DungeonEncounterStore` with the C++ field layout needed by encounter lock/loot parity: `ID`, `MapID`, `DifficultyID`, `OrderIndex`, `Bit`, `Flags` and `Faction`. The localized `Name` column remains intentionally skipped because no current runtime parity path consumes it. Unit coverage asserts C++ store-style lookup by ID, and the fixture-backed test loads `DungeonEncounter.db2` when present. Remaining gaps: startup/session injection, hotfix SQL overlay, script-provided boss-to-encounter arrays, `InstanceScript::GetBossDungeonEncounter`, player lock checks and criteria/LFG delivery remain pending.

- [x] **#NEXT.R8.ENTITIES.302** Wire `DungeonEncounterStore` into world startup and sessions.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/DataStores/DB2Stores.cpp` (`LOAD_DB2(sDungeonEncounterStore)`), `/home/server/woltk-trinity-legacy/src/server/game/Instances/InstanceScript.cpp` (`sDungeonEncounterStore.LookupEntry`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`IsLockedToDungeonEncounter` lookup).
  Rust targets: `crates/world-server/src/main.rs`, `crates/wow-network/src/accept.rs`, `crates/wow-world/src/session.rs`, docs.
  Acceptance: `world-server` now loads `DungeonEncounter.db2` at startup beside the other DB2 stores, carries the shared store through `SessionResources`, and exposes it on `WorldSession` through setter/getter plumbing. This prepares canonical `InstanceScript` and player lock/criteria code to consume a real DB2 store instead of hardcoded encounter metadata. Remaining gaps: the store is not yet used by represented creature registration, boss script arrays or lockout checks; hotfix SQL overlay is still pending.

- [x] **#NEXT.R8.ENTITIES.303** Start `wow-instances` with C++ `BossInfo` dungeon encounter lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Instances/InstanceScript.h` (`MAX_DUNGEON_ENCOUNTERS_PER_BOSS`, `DungeonEncounterData`, `BossInfo`), `/home/server/woltk-trinity-legacy/src/server/game/Instances/InstanceScript.cpp` (`BossInfo::GetDungeonEncounterForDifficulty`, `InstanceScript::LoadDungeonEncounterData`, `InstanceScript::GetBossDungeonEncounter(uint32)`).
  Rust targets: `Cargo.toml`, `Cargo.lock`, `crates/wow-instances/Cargo.toml`, `crates/wow-instances/src/lib.rs`, docs.
  Acceptance: the workspace now has a `wow-instances` crate instead of a missing declared target. It ports the pure C++ encounter metadata path for `InstanceScript`: `EncounterState`, `DungeonEncounterData`, `BossInfo`, `InstanceScriptBase::load_dungeon_encounter_data`, bulk row loading and `boss_dungeon_encounter` difficulty selection. Tests assert the C++ first-match semantics where `DifficultyID == 0` matches any difficulty, nonmatching rows are skipped, invalid boss IDs no-op and missing DB2 rows produce no stored encounter pointer. Remaining gaps: `GetBossDungeonEncounter(Creature const*)` still needs BossAI/Creature AI ownership, and full `InstanceScript` map state, boss transitions, persistence, doors/minions, criteria and LFG delivery remain pending.

- [x] **#NEXT.R8.ENTITIES.304** Represent C++ `BossAI::GetBossId()` for encounter lookup.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/AI/ScriptedAI/ScriptedCreature.h` (`BossAI`, `GetBossId`, `_bossId`), `/home/server/woltk-trinity-legacy/src/server/game/AI/ScriptedAI/ScriptedCreature.cpp` (`BossAI::BossAI`), `/home/server/woltk-trinity-legacy/src/server/game/Instances/InstanceScript.cpp` (`GetBossDungeonEncounter(Creature const*)` dynamic_cast branch).
  Rust targets: `crates/wow-instances/src/lib.rs`, docs.
  Acceptance: `wow-instances` now exposes a minimal `BossAiLikeCpp` contract and `BossAiRef` value for future script/AI adapters, and `InstanceScriptBase::boss_dungeon_encounter_for_boss_ai` mirrors the C++ creature overload after `dynamic_cast<BossAI const*>`: `None` returns no encounter, and a boss-AI source resolves by `boss_id()` through the already-ported boss/difficulty lookup. Tests cover both success and failed-cast branches. Remaining gaps: real `CreatureAI`/script instances do not yet implement this contract, and no runtime creature registration consumes it yet.

- [x] **#NEXT.R8.ENTITIES.305** Preserve represented C++ `BossAI::_bossId` on creature AI state.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/AI/ScriptedAI/ScriptedCreature.h` (`BossAI::_bossId`, `GetBossId`), `/home/server/woltk-trinity-legacy/src/server/game/AI/ScriptedAI/ScriptedCreature.cpp` (`BossAI::BossAI`), `/home/server/woltk-trinity-legacy/src/server/game/Instances/InstanceScript.cpp` (`GetBossDungeonEncounter(Creature const*)`).
  Rust targets: `crates/wow-ai/src/lib.rs`, `crates/wow-ai/Cargo.toml`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/character.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented `CreatureAI` now carries `boss_id: Option<u32>` separately from `dungeon_encounter_id`, exposes `boss_ai_like_cpp()` only when that script-provided boss identity exists, and returns `None` for plain creatures to match the failed C++ `dynamic_cast<BossAI const*>` branch. `WorldSession::register_world_creature` accepts the optional boss id and pending respawns preserve it. Current DB-spawn/visibility callsites pass `None` because C++ obtains this value from script constructors, not from creature spawn rows. Unit coverage asserts both the plain-creature and boss-creature branches. Remaining gaps: real scripts still do not instantiate BossAI-backed Rust creatures, and no runtime instance script yet uses `boss_ai_like_cpp()` to derive `dungeon_encounter_id`.

- [x] **#NEXT.R8.ENTITIES.306** Contrast and represent C++ GameObject chest loot source fields.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`GAMEOBJECT_TYPE_CHEST`, `GetLootId`, `chestLoot`, `usegrouplootrules`, `DungeonEncounter`, `chestPersonalLoot`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GAMEOBJECT_TYPE_CHEST` use/loot branch), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`CheckLootRefs` gameobject template validation).
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/lib.rs`, docs.
  Acceptance: `wow-entities` now has C++-indexed constants for the gameobject template data fields consumed by chest loot (`Data1` loot id, `Data15` group-loot rules, `Data25` dungeon encounter, `Data30` personal loot), a `GameObjectTemplateData::get_loot_id_like_cpp` switch for chest/fishing-hole/gathering-node parity, and `chest_loot_source_like_cpp` that exposes the C++ chest loot source without depending on DB/session state. Unit coverage asserts the C++ switch and exact chest field extraction. Remaining gaps: `CMSG_GAME_OBJ_USE` is still a stub, represented gameobject loot generation is not wired into `LootTemplates_Gameobject`, personal dungeon encounter loot is not ported, and runtime GO template/addon ownership remains under #NEXT.R8.ENTITIES.020/#NEXT.R8.ENTITIES.021.

- [x] **#NEXT.R8.ENTITIES.307** Wire represented `CMSG_GAME_OBJ_USE` chest loot opening.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GameObject::Use`, `GAMEOBJECT_TYPE_CHEST`, `Loot(..., LOOT_CHEST, ...)`, `FillLoot(..., LootTemplates_Gameobject, ...)`, `SetDungeonEncounterId`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h` (`LOOT_CHEST`, `GetLootTypeForClient`).
  Rust targets: `crates/wow-world/src/handlers/misc.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-packet/src/packets/loot.rs`, docs.
  Acceptance: `CMSG_GAME_OBJ_USE` now reads the packed gameobject GUID, requires a visible `HighGuid::GameObject`, loads `gameobject_template`, extracts chest source fields through the #NEXT.R8.ENTITIES.306 helper, and opens represented chest loot through `LootTemplates_Gameobject` with `LOOT_CHEST`, group-loot rule gating, carried `DungeonEncounterID`, existing represented loot response/open hooks, and represented condition/quest/random-property filtering. Unit coverage asserts represented chest metadata, including that personal-loot fallback still uses `LOOT_CHEST` like C++. Remaining gaps: full `GameObject::Use` side effects (events, linked traps, unique-use, restock/despawn/state changes), template addon money, dungeon encounter personal lock fanout/tappers, push loot and non-chest GO types remain pending under #NEXT.R8.ENTITIES.020/#NEXT.R8.ENTITIES.021 and canonical loot runtime.

- [x] **#NEXT.R8.ENTITIES.308** Add represented `gameobject_template_addon` money for chest loot.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`GameObjectTemplateAddon::Mingold`, `Maxgold`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp` (`LoadGameObjectTemplateAddons` SQL), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`loot->generateMoneyLoot(addon->Mingold, addon->Maxgold)`).
  Rust targets: `crates/wow-database/src/statements/world.rs`, `crates/wow-database/src/statements/mod.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `WorldStatements` now exposes `SEL_GAMEOBJECT_TEMPLATE_ADDON_MONEY_LOOT` for `gameobject_template_addon.entry -> mingold/maxgold` with C++-safe min/max normalization, represented chest generation loads those bounds by gameobject entry and uses the shared C++ `generateMoneyLoot` helper plus configured money drop rate. Focused DB statement and represented chest tests pass. Remaining gaps: startup `GameObjectTemplateAddon` object ownership, art kits/faction/flags/world-effect fields, full loot mode by map difficulty, restock/state/despawn side effects and dungeon encounter personal lock fanout remain pending.

- [x] **#NEXT.R8.ENTITIES.309** Add represented chest `chestPushLoot` autostore source.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`chestPushLoot` Data33), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`if (!m_unique_users.count(player) && !info->GetLootId())` push-loot branch), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::AutoStore`).
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: the represented chest source now captures Data33, distinguishes open loot (`chestLoot`/`chestPersonalLoot`) from push-only loot, and for the C++ `!GetLootId()` branch generates `LootTemplates_Gameobject` push loot with `LOOT_CHEST` semantics and stores eligible rows directly through the existing represented direct-inventory bridge without creating a loot window or money. A session-local unique-use guard prevents repeated represented push autostore for the same gameobject. Remaining gaps: canonical `GameObject::m_unique_users`, triggered events, linked traps, loot state transitions, restock/despawn and canonical shared `GameObject::m_loot` ownership are still pending.

- [x] **#NEXT.R8.ENTITIES.310** Add represented fishing-hole and gathering-node loot opens.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GAMEOBJECT_TYPE_FISHINGHOLE`, `GAMEOBJECT_TYPE_GATHERING_NODE`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`GetLootId` switch).
  Rust targets: `crates/wow-world/src/handlers/misc.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `CMSG_GAME_OBJ_USE` now routes non-chest `GetLootId` gameobjects for fishing holes and gathering nodes: fishing holes generate personal `LootTemplates_Gameobject` with `LOOT_FISHINGHOLE`, while gathering nodes generate per-session personal `LootTemplates_Gameobject` with `LOOT_CHEST` like C++. Both use the represented loot response/open path and avoid claiming side effects not yet ported. Fishing holes now also record the C++ `CatchFishInFishingHole` criteria update hook with the gameobject entry after a represented open. Remaining gaps: canonical criteria manager mutation, gathering triggered events, linked traps, XP reward, spell cast, max-loot/despawn/dynamic-flag transitions and canonical per-player `m_personalLoot` ownership remain pending.

- [x] **#NEXT.R8.ENTITIES.311** Add represented gathering-node XP reward.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`gatheringNode.xpDifficulty` branch), `/home/server/woltk-trinity-legacy/src/server/game/Quests/QuestDef.cpp` (`Quest::RoundXPValue`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`gatheringNode` Data6/Data7/Data13/Data14/Data18/Data20).
  Rust targets: `crates/wow-data/src/quest_xp.rs`, `crates/wow-entities/src/game_object.rs`, `crates/wow-world/src/handlers/loot.rs`, `crates/wow-world/src/handlers/misc.rs`, docs.
  Acceptance: `QuestXpStore` now exposes the exact C++ player-level row + difficulty lookup used by gathering nodes and fixes `RoundXPValue` threshold rounding to match C++; represented gathering-node use captures C++ side-effect fields and grants non-kill XP once per represented session/gameobject when `xpDifficulty` is 1..9 and QuestXP data exists. Remaining gaps: canonical per-player `m_personalLoot`, criteria, triggered events, linked traps, spell cast, max-loot/despawn/dynamic flags and script hooks remain pending.

- [x] **#NEXT.R8.ENTITIES.312** Record represented gameobject use events and linked traps.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GameEvents::Trigger`, `TriggeringLinkedGameObject` in chest/gathering branches), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h` (`chest.triggeredEvent` Data6, `chest.linkedTrap` Data7, `gatheringNode.triggeredEvent` Data7, `gatheringNode.linkedTrap` Data20).
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented chest and gathering-node use now records the C++ hook points for nonzero triggered events and linked traps. Chest records them once when `m_loot` would be created for a nonzero `GetLootId`, and once in the C++ unique-use branch when there is no `GetLootId`; gathering records them once per represented session/gameobject first use. This intentionally records hooks rather than executing real events/traps because canonical `GameEvents` and linked-trap runtime are not ported yet. Remaining gaps: actual GameEvents execution, real `TriggeringLinkedGameObject`, canonical `m_unique_users`/`m_personalLoot`, gathering spell cast, max-loot/despawn/dynamic flags and script hooks remain pending.

- [x] **#NEXT.R8.ENTITIES.313** Record represented gameobject use state transitions.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`SetLootState(GO_ACTIVATED)`, gathering `MaxNumberofLoots`, `SetGoState(GO_STATE_ACTIVE)`, `SetDynamicFlag(GO_DYNFLAG_LO_NO_INTERACT)`, `DespawnOrUnsummon(ObjectDespawnDelay)`, post-switch spell handling), `/home/server/woltk-trinity-legacy/src/server/game/Miscellaneous/SharedDefines.h` (`GO_DYNFLAG_LO_NO_INTERACT`).
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented gameobject use now keeps session-local GO use state until canonical GO ownership lands. Chest use records `LootState::Activated` with the activating player, including no-loot/unique-use branches. Gathering use increments represented personal-loot use count once, applies C++ max-loot depletion state (`GoState::Active` plus `GO_DYNFLAG_LO_NO_INTERACT`), records first-activation despawn delay, and records the post-switch GameObject spell-cast hook without pretending to execute the real spell runtime. Remaining gaps: actual UpdateObject state fanout, canonical `GameObject::m_loot`/`m_personalLoot` ownership, real despawn scheduler, real spell cast validation/execution, GameEvents/trap execution and script hooks remain pending.

- [x] **#NEXT.R8.ENTITIES.314** Represent gameobject personal encounter tap-list selection.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`chestPersonalLoot`, `DungeonEncounter`, `GetTapList`, current-player fallback), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.h` (`m_tapList`), `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootMgr::GenerateDungeonEncounterPersonalLoot` tapper loop).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented chest personal encounter loot now has a session-local `GameObject::m_tapList` mirror. For `chestPersonalLoot` with nonzero `DungeonEncounter`, represented loot selection filters player GUID tappers, dedupes them in stable GUID order and marks those tappers as allowed looters; when the tap list is empty or missing it falls back to the current player like C++. Focused tests cover the fallback and tap-list branches. Remaining gaps: real `GameObject::m_tapList` ownership/setters from scripts, actual `GenerateDungeonEncounterPersonalLoot` per-player loot map, dungeon encounter lockout checks, per-player money/personal loot instances, map difficulty loot mode and canonical `m_personalLoot`.

- [x] **#NEXT.R8.ENTITIES.315** Apply represented personal encounter lockout/open gating.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`GenerateDungeonEncounterPersonalLoot`, `Player::IsLockedToDungeonEncounter` skip), `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp` (`IsLockedToDungeonEncounter`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GetLootForPlayer` before `SendLoot`).
  Rust targets: `crates/wow-entities/src/game_object.rs`, `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `GameObjectLootSource` now exposes the C++ personal-encounter branch predicate, represented session state can mark `(player, dungeonEncounterId)` lockouts, represented personal encounter generation skips locked tappers like `GenerateDungeonEncounterPersonalLoot`, and `open_represented_gameobject_chest_like_cpp` no longer auto-adds the current player for personal encounter loot. A non-tapper current player therefore receives no represented loot window, matching `GetLootForPlayer == nullptr`. Remaining gaps: real `InstanceLockMgr`/completed-encounter mask derivation, `DungeonEncounterStore` bit lookup in runtime lock checks, actual per-player `Loot` objects/money instances, `ProcessPersonalLoot`, `FillNotNormalLootFor` on canonical personal loot and canonical `GameObject::m_personalLoot`.

- [x] **#NEXT.R8.ENTITIES.316** Represent personal encounter per-player money.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`GenerateDungeonEncounterPersonalLoot`, per-tapper `Loot::generateMoneyLoot`, `ProcessPersonalLoot`, `FillNotNormalLootFor`), `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp` (`HandleLootMoneyOpcode`), `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp` (`GetLootForPlayer`).
  Rust targets: `crates/wow-world/src/session.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented personal encounter chest generation now treats the shared `CreatureLoot.coins` as zero and records session-local money by `(gameobject, player)` for each eligible tapper, matching the C++ shape where each tapper gets its own `Loot`. `LootResponse` and `CMSG_LOOT_MONEY` read the current player's personal money, and money pickup zeroes only that player's represented personal-money entry while preserving other tappers' entries. Remaining gaps: canonical per-player `Loot` objects/items, `LootTemplate::ProcessPersonalLoot`, `FillNotNormalLootFor`, map-difficulty loot mode, persistence and canonical `GameObject::m_personalLoot`.

- [x] **#NEXT.R8.ENTITIES.317** Represent personal encounter item ownership.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplate::ProcessPersonalLoot` chooses one looter for each rolled entry/group), `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.cpp` (`Loot::FillNotNormalLootFor` allowed-looter/counting rules).
  Rust targets: `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: represented personal encounter chest items are no longer marked lootable by every tapper. After represented generation, each generated item is assigned to one eligible tapper, FFA state is rebuilt for that tapper, and `unlooted_count` follows the C++ `FillNotNormalLootFor` counting split between normal and FFA items. Remaining gaps: exact `ProcessPersonalLoot` reference/group looter-selection loops in `wow-loot`, per-tapper condition evaluation for non-current players, canonical per-player `Loot` objects and canonical `GameObject::m_personalLoot`.

- [x] **#NEXT.R8.ENTITIES.318** Port reusable personal loot template processing.
  C++ refs: `/home/server/woltk-trinity-legacy/src/server/game/Loot/LootMgr.cpp` (`LootTemplate::ProcessPersonalLoot`, `LootTemplate::HasDropForPlayer`, `LootGroup::HasDropForPlayer`, reference got-loot cycling).
  Rust targets: `crates/wow-loot/src/lib.rs`, `crates/wow-world/src/handlers/loot.rs`, docs.
  Acceptance: `wow-loot` now exposes a pure `fill_personal_loot_with_context_like_cpp` path that rolls non-grouped entries, references and groups using the C++ personal-loot looter-selection shape, including reference max-count cycling across eligible looters before repeating. Represented personal encounter gameobject loot uses that path instead of generating shared gameobject loot and assigning afterwards. Remaining gaps: per-tapper condition evaluation still uses the represented current-session subset until canonical Player/ConditionMgr data for remote tappers exists; canonical per-player `Loot` objects and canonical `GameObject::m_personalLoot` remain pending.

## Follow-Up Work Items

- [ ] **#NEXT.R8.ENTITIES.003** Bind `wow-map` grid unload actions to real entity methods once Creature/GameObject/Corpse exist.
  Rust targets: `crates/wow-entities/src/creature.rs`, `crates/wow-entities/src/game_object.rs`, `crates/wow-entities/src/corpse.rs`, `crates/wow-map/src/grid_unload.rs`.
  Acceptance: `wow-map` keeps the GUID-only unload collector unchanged and adds a `GridUnloadEntityStore` applier for real `Creature`/`GameObject`/`Corpse` instances; `SetDestroyedObject` now calls entity-level destroyed-object methods, `CleanupsBeforeDelete` records represented cleanup calls, `DeleteObject` records represented delete requests and clears grid/cell state, creature `CombatStop` clears the represented attack target, creature/gameobject respawn relocation requests are recorded, and currently unrepresented creature dyn-object/area-trigger ownership is an explicit no-op. #NEXT.R8.ENTITIES.417 connects that trait to canonical `Map::map_objects`, but only as a map-owned bridge; live grid unload, remove-list execution, cross-session fanout and full lifecycle cleanup remain pending under #NEXT.R8.ENTITIES.006/#NEXT.R8.ENTITIES.017/#NEXT.R8.ENTITIES.021/#NEXT.R8.ENTITIES.023 and the map runtime work.
- [ ] **#NEXT.R8.ENTITIES.005** Port `WorldObject` LOS, terrain-height, transport relocation and visibility-range helpers that require Map/Terrain/Transport integration.
  Note: foundation progress recorded only as a bridge-level port. `IsWithinLOSInMap` now matches the represented C++ prefilter (`IsInMap`) and exposes source/target endpoint metadata, but canonical collision-height and `GetHitSpherePointFor(...)` endpoint adjustment remain TODO until RustyCore has shared collision/hit-sphere infrastructure.
- [ ] **#NEXT.R8.ENTITIES.006** Complete remaining `ObjectAccessor` integration: real `SaveAllPlayers` and wiring to canonical `wow_map::Map` containers instead of bridge storage.
  Note: foundation progress recorded at the minimal canonical-storage bridge level. `ObjectAccessor::save_all_players_with` now executes a real save callback per registered player and reports the failing player GUID; `wow-map::Map` owns a validated `MapObjectRecord` store and implements `ObjectAccessorMapSource` so ObjectAccessor lookups can read Map-backed records. Full typed `Map` containers/ownership for `Creature`, `GameObject`, etc. remain deferred to the entity lifecycle tasks.
- [ ] **#NEXT.R8.ENTITIES.008** Complete generated update-field sections beyond `ObjectData`: `UnitData`, `PlayerData`, `ActivePlayerData`, `GameObjectData`, `ItemData`, `CorpseData`, `DynamicObjectData`, `AreaTriggerData`, `SceneObjectData`, `ConversationData`, including visibility flag filters and dynamic/optional fields.
  Note: foundation progress recorded as a `wow-entities` bridge/foundation. `update_fields.rs` now exposes central section metadata (bit/block/block-mask counts and client type bits), Trinity visibility filters for `ItemData`/`UnitData`/`PlayerData`, all-bits truncated masks for unfiltered sections, and descriptor metadata for generated dynamic/optional/nested/array fields. Full binary serialization of every nested/dynamic field remains scoped to packet/update serializer tasks where not already handled by entity modules.
- [ ] **#NEXT.R8.ENTITIES.010** Complete `Unit` subsystems beyond base fields: aura hooks, threat/combat manager, SpellHistory, MotionMaster/move spline, charm/minion ownership, vehicle hooks, AI references and runtime power-index implementations for Player/Creature/Pet.
  Note: foundation progress recorded as a bridge/foundation only. `UnitSubsystems` now provides state handles/hooks for aura refs, current spells/SpellHistory, threat/combat, MotionMaster/move spline, charm/control/minion ownership, vehicle state and AI references; Player now has a runtime `PlayerPowerIndexResolver`, Creature keeps the C++ display-power/combo-points mapping, and Pet delegates power-index lookup. Full aura engine behavior, combat manager semantics, movement generators, AI execution and vehicle transition logic remain deferred to specific follow-up tasks.
- [ ] **#NEXT.R8.ENTITIES.012** Complete `Player` create/load/login lifecycle: `Player::Create`, `LoadFromDB`, login packet sequencing, world insertion, visibility bootstrap, stats initialization and DB2-backed `GetPowerIndexByClass`.
  Note: foundation progress recorded as a foundation/bridge, not as full gameplay login. `wow-entities::Player` now has explicit create/load lifecycle records for the representable `characters` fields, applies GUID/name/race/class/gender/location/level/xp/money/bag slots/flags/health/powers/display/faction metadata as a clean loaded baseline, and exposes a testable Trinity-ordered login plan for before-add, map/ObjectAccessor insertion, after-add, visibility/worldstate bootstrap and online marking. `wow-data` adds a record-backed `Db2PlayerPowerIndexResolver` bridge for class/power index rows while keeping DB2 globals out of entities. Real packet serialization, full DB `LoadFromDB` subsystem loads, starter spells/items/actions/skills, quests, guild/social, auras, reputation, currencies, achievements and runtime map insertion behavior remain deferred under #NEXT.R8.ENTITIES.013/#NEXT.R8.ENTITIES.014 and the relevant packet/map tasks.
- [ ] **#NEXT.R8.ENTITIES.013** Complete `Player` inventory/equipment bridge: build on base `Item`/#NEXT.R8.ENTITIES.041, `Bag`/#NEXT.R8.ENTITIES.042, Player storage lookup/#NEXT.R8.ENTITIES.043, `ObjectAccessor` item branch/#NEXT.R8.ENTITIES.044, visible item state/#NEXT.R8.ENTITIES.045, item visible modifier helpers/#NEXT.R8.ENTITIES.047, `VisualizeItem` mutation side effects/#NEXT.R8.ENTITIES.048, empty top-level `_StoreItem`/#NEXT.R8.ENTITIES.049, empty bag-contained `_StoreItem`/#NEXT.R8.ENTITIES.050, top-level existing-stack merge/#NEXT.R8.ENTITIES.051, bag-contained existing-stack merge/#NEXT.R8.ENTITIES.052, clone storage/#NEXT.R8.ENTITIES.053, split count allocation/#NEXT.R8.ENTITIES.054, split guards/#NEXT.R8.ENTITIES.055, position classifiers/#NEXT.R8.ENTITIES.056, valid-position checks/#NEXT.R8.ENTITIES.057, mergeability guard/#NEXT.R8.ENTITIES.058, bag family fit/#NEXT.R8.ENTITIES.059, specific-slot storage validation/#NEXT.R8.ENTITIES.060, inventory-slot scanning/#NEXT.R8.ENTITIES.061, bag scanning/#NEXT.R8.ENTITIES.062, representable `CanStoreItem` orchestration/#NEXT.R8.ENTITIES.063, similar-item max-count validation/#NEXT.R8.ENTITIES.064, item counting/#NEXT.R8.ENTITIES.065, bound-owner validation/#NEXT.R8.ENTITIES.066, bank storage validation/#NEXT.R8.ENTITIES.067, equip slot selection/#NEXT.R8.ENTITIES.068, combat equip-state helper/#NEXT.R8.ENTITIES.069, representable equip validation/#NEXT.R8.ENTITIES.070, unequip validation/#NEXT.R8.ENTITIES.071, template use validation/#NEXT.R8.ENTITIES.072, item use validation/#NEXT.R8.ENTITIES.073, unique-equip template validation/#NEXT.R8.ENTITIES.074, unique-equip object wrapper/#NEXT.R8.ENTITIES.075, equip-item storage side effects/#NEXT.R8.ENTITIES.076, quick-equip storage side effects/#NEXT.R8.ENTITIES.077, remove-item unlink/#NEXT.R8.ENTITIES.078, move-from-inventory unlink/#NEXT.R8.ENTITIES.079, move-to-inventory finalization/#NEXT.R8.ENTITIES.080, destroy-item cleanup/#NEXT.R8.ENTITIES.081, item-pointer destroy-count/#NEXT.R8.ENTITIES.082, entry destroy-count planning/#NEXT.R8.ENTITIES.083, filtered destroy planning/#NEXT.R8.ENTITIES.084, entry lookup/listing/#NEXT.R8.ENTITIES.085, buyback removal object side effects/#NEXT.R8.ENTITIES.086, weapon/titan-grip helpers/#NEXT.R8.ENTITIES.087, swap preflight guards/#NEXT.R8.ENTITIES.088, swap empty-destination move planning/#NEXT.R8.ENTITIES.089, swap merge/fill planning/#NEXT.R8.ENTITIES.090, swap real-swap validation/#NEXT.R8.ENTITIES.091, swap bag-content exchange planning/#NEXT.R8.ENTITIES.092, swap final execution planning/#NEXT.R8.ENTITIES.093, swap branch orchestration/#NEXT.R8.ENTITIES.094, buyback insertion object side effects/#NEXT.R8.ENTITIES.095, equip-error packet shape/#NEXT.R8.ENTITIES.096, buy/sell error packet shape/#NEXT.R8.ENTITIES.097, soulbound tradeable item set/#NEXT.R8.ENTITIES.098, item/enchantment duration lists/#NEXT.R8.ENTITIES.099, arena enchantment cleanup/#NEXT.R8.ENTITIES.100, duration resend helpers/#NEXT.R8.ENTITIES.101, ApplyEnchantment guard/duration shape/#NEXT.R8.ENTITIES.102, ApplyEnchantment effect classification/#NEXT.R8.ENTITIES.103, ApplyEnchantment stat/rating expansion/#NEXT.R8.ENTITIES.104, ApplyEnchantment random suffix amount resolution/#NEXT.R8.ENTITIES.105, UpdateSkillEnchantments planning/#NEXT.R8.ENTITIES.106, SendNewItem packet planning/#NEXT.R8.ENTITIES.107, ItemPushResult serialization/#NEXT.R8.ENTITIES.108, SendNewItem item-instance plan gap/#NEXT.R8.ENTITIES.109, SendNewItem plan delivery bridge/#NEXT.R8.ENTITIES.110, duration update packet delivery/#NEXT.R8.ENTITIES.111, SendEquipError session helper/#NEXT.R8.ENTITIES.112, DB2 ItemRandomSuffix lookup/#NEXT.R8.ENTITIES.113, DB2 SpellItemEnchantment lookup/#NEXT.R8.ENTITIES.114, DB2 ItemModifiedAppearance lookup/#NEXT.R8.ENTITIES.115, DB2 ItemAppearance display-info lookup/#NEXT.R8.ENTITIES.116, item refund/BoP trade expiry timers/#NEXT.R8.ENTITIES.117, SendBuyError/SendSellError session helpers/#NEXT.R8.ENTITIES.118, vendor error call-site replacement/#NEXT.R8.ENTITIES.119, item BoP trade allowed-GUID ownership/#NEXT.R8.ENTITIES.120, account-bound flag derivation/#NEXT.R8.ENTITIES.121, ItemSparse template flags/#NEXT.R8.ENTITIES.122, session template-flag bridge/#NEXT.R8.ENTITIES.123, ItemSparse storage-template fields/#NEXT.R8.ENTITIES.124, canonical ItemStorageTemplate resolver/#NEXT.R8.ENTITIES.125, character inventory-type call-site wiring/#NEXT.R8.ENTITIES.126, ObjectAccessor item object registry/#NEXT.R8.ENTITIES.127, session runtime item object population/#NEXT.R8.ENTITIES.128, direct-inventory ObjectAccessor wiring/#NEXT.R8.ENTITIES.129, direct-inventory CanStoreItem planning/#NEXT.R8.ENTITIES.130, direct-inventory vendor buy multi-position execution/#NEXT.R8.ENTITIES.131, direct vendor buy-count price/#NEXT.R8.ENTITIES.132 and direct vendor max-money count clamp/#NEXT.R8.ENTITIES.133 to port canonical `Player` entity ownership, bag/bank/buyback containers, full store/equip destination validation, collection appearance registration, equip stat/aura/spell side effects and save/load persistence.
  Note: foundation progress recorded as an inventory/equipment bridge/foundation rollup over #NEXT.R8.ENTITIES.041..#NEXT.R8.ENTITIES.133. `wow-entities` now carries the canonical `Item`, `Bag`, `PlayerInventoryStorage` and `ObjectAccessor` item-reference shapes plus representable store/split/can-store/can-bank/equip/use/unique/swap/buyback/duration/enchantment helpers; `wow-world` wires the direct-inventory session bridge through runtime item-object population, shared `ObjectAccessor` snapshots, `ItemStorageTemplate` DB2 resolution and vendor-buy `CanStoreItem` planning/execution. This is not a full runtime inventory system yet: real packet serializers/delivery beyond the represented bridges, complete character/item persistence and recursive bag/bank/buyback save/load, map/world ownership integration, collection appearance registration, full stat/aura/spell application and remaining runtime side effects stay deferred to follow-up packet/persistence/world/gameplay tasks.

- [ ] **#NEXT.R8.ENTITIES.014** Complete `Player` gameplay state: quests, skills, spells/actions, taxi, social, mail, group/guild, battleground/arena queues, reputation, achievements, cooldowns and rest state.
  Note: foundation progress recorded as an entity bridge/foundation. `wow-entities::Player` now owns an explicit `PlayerGameplayState` with buckets for quests/objectives/reward cadence, skills, spells/talents/action buttons, taxi masks/destinations, social/mail/group/guild, battleground/random-BG state, reputation, achievements/criteria, currencies, cooldowns/charges and rest bonus/logout state, plus a Trinity-ordered `PlayerGameplayLoadPlan` for the represented `Player::LoadFromDB` sequence. Real DB loaders, packet serializers/delivery, full AchievementMgr/ReputationMgr/SocialMgr/Mail managers, spell/aura runtime, group/guild/BG queue behavior and world/session integration remain deferred.
- [ ] **#NEXT.R8.ENTITIES.016** Complete `Creature` create/load/template lifecycle: `Creature::Create`, `CreateCreatureFromDB`, `LoadFromDB`, creature template/difficulty/model refs, spawn data, equipment, level/stat selection and map insertion.
  Note: foundation progress recorded as a Creature create/load/template lifecycle bridge/foundation. `wow-entities::Creature` now has explicit template/spawn/create/load lifecycle records, lifecycle metadata, a Trinity-ordered lifecycle plan, and clean baseline create/load application for represented GUID/entry/map/position/template class/race-none/faction/display/model dimensions/spells/equipment/level/health/mana/respawn/default movement/string-id/add-to-map request state. Real ObjectMgr/DB loaders, real Map insertion/duplicate store, phase/terrain/collision, addon/equipment DB details, AI/threat runtime, respawn group/pool semantics and full update packet/runtime behavior remain deferred to #NEXT.R8.ENTITIES.017 or other follow-up tasks.
- [ ] **#NEXT.R8.ENTITIES.017** Complete `Creature` runtime lifecycle: update loop, death/corpse/respawn transitions, forced despawn, evade/combat cleanup, loot owner/tap list, reputation, pickpocket and grid unload bindings.
  Note: foundation progress recorded as Creature runtime lifecycle bridge/foundation; real AI execution, ThreatManager, Map linked respawn/spawn group/pool, loot object contents, reputation reward formulas, pickpocket loot refill DB, packet visibility delivery, and full world-session runtime integration remain deferred.
- [ ] **#NEXT.R8.ENTITIES.018** Move real AI ownership from `wow-ai`/`wow-world::WorldCreature` bridge into canonical `Creature`/Map refs without mixing entity state into session.
  Note: foundation progress recorded as an ownership foundation. `wow-entities::Creature` now owns the represented AI runtime/ownership state (state machine, home/move target/timers, combat target, swing/aggro/wander/respawn/corpse metadata, display/faction/npc/unit flags and damage bounds) without depending on `wow-ai`/`wow-world`; `wow-world::WorldCreature` wraps canonical `Creature` as its source of truth and MapManager add/get/visibility reads derive GUID/position/health/runtime from it. `WorldSession` spawn/respawn registration can insert canonical map creatures when a shared MapManager exists, but it intentionally keeps the legacy per-session `CreatureAI` fallback/cache until the broader session migration lands. Deferred: real AI scripting, ThreatManager, complete packet movement/health updates, DB respawn persistence, grid relocation across cells and full removal of the session legacy `HashMap`.
- [ ] **#NEXT.R8.ENTITIES.020** Complete `GameObject` create/load/template lifecycle: `GameObject::Create`, `CreateGameObjectFromDB`, template/addon refs, rotations, model/collision creation, spawn data, map insertion and respawn compatibility.
- [ ] **#NEXT.R8.ENTITIES.021** Complete `GameObject` runtime lifecycle: update loop, loot/use state machine, door/button/trap/chest/fishing/destructible behavior, cooldown/restock, despawn/respawn persistence and grid unload bindings.
  Note: foundation progress recorded for the `CMSG_GAME_OBJ_USE` remote-control guard. `GameObjectTemplateData::is_usable_mounted_like_cpp` now mirrors C++ `GameObjectTemplate::IsUsableMounted`, and represented gameobject use rejects a remote-controlled mover unless the player is on a vehicle, mounted, or the target template allows mounted use. Remaining gaps: full `GameObject::Use` dispatch, real vehicle passenger ownership, GameObjectAI/script hooks, ReportUse `OnReportUse`, persistent criteria manager and canonical GO runtime ownership.
  Note: foundation progress recorded for the `CMSG_GAME_OBJ_REPORT_USE` AI gate. Represented report-use now records the C++ `go->AI()->OnReportUse(_player)` hook and suppresses represented `UseGameobject` criteria when the represented AI result is `true`, matching the C++ early return without pretending to execute SmartAI scripts. Remaining gaps: real `GameObjectAI` ownership, SmartAI `SMART_EVENT_GOSSIP_HELLO` execution, persistent criteria manager and canonical GO runtime ownership.
  Note: foundation progress recorded for the player preamble of `GameObject::Use`. `GameObjectTemplateData::get_no_damage_immune_like_cpp` now mirrors C++ `GetNoDamageImmune`, represented use rejects immune players when required, removes represented mounted state when the template is not usable mounted, records `PlayerTalkClass->ClearMenus`, and gates dispatch through represented `AI()->OnGossipHello`. Remaining gaps: real aura removal by type across canonical aura ownership, real PlayerTalkClass/GossipMenu state, SmartAI gossip execution, cooldown persistence and the remaining per-type `GameObject::Use` branches.
  Note: foundation progress recorded for the `GameObject::Use` template cooldown gate. `GameObjectTemplateData::get_cooldown_like_cpp` now mirrors the C++ `GetCooldown` switch for trap/goober, represented use starts/rejects session-local cooldowns before type dispatch like C++, and represented trap use applies the C++ 4-second fallback when template cooldown is zero. Remaining gaps: canonical `GameObject::m_cooldownTime`, wall-clock persistence across map ownership and full trap/goober spell execution.
  Note: foundation progress recorded for the door/button `GameObject::Use` branch. `GameObjectTemplateData::get_auto_close_time_like_cpp` now mirrors the representable C++ `GetAutoCloseTime` subset, and represented door/button use applies the C++ ready-state gate, toggles represented GO state, sets `GO_FLAG_IN_USE`, sets `GO_ACTIVATED` with the user GUID, stores the restore cooldown and wires the represented update tick into session pending processing for activated door/button objects, clearing `GO_FLAG_IN_USE`, restoring the previous GO state and moving to `GO_JUST_DEACTIVATED` when the cooldown expires. Remaining gaps: real UpdateObject fanout, collision/model updates and canonical `GameObject::Update` ownership.
  Note: foundation progress recorded for the trap `GameObject::Use` branch. `GameObjectTemplateData::trap_use_source_like_cpp` now captures C++ trap radius/spell/charges/cooldown/startDelay/IgnoreTotems/Checkallunits fields, represented trap use records the spell cast, applies the C++ cooldown fallback of 4 seconds when template cooldown is zero, rejects active cooldowns, and moves one-charge traps to `GO_JUST_DEACTIVATED`. Session pending processing also represents the C++ trap `GO_NOT_READY` path: bomb traps (`trap.charges == 2`) arm to `GO_READY` with the hardcoded 10s cooldown, non-bomb traps move to `GO_READY`, and non-bomb traps with represented owner-in-combat state arm `trap.startDelay`; expired bomb `GO_READY` moves to `GO_ACTIVATED`, and `GO_ACTIVATED` records the null-target bomb spell hook before `GO_JUST_DEACTIVATED`. Non-bomb traps now also mirror the C++ `GO_READY` target activation and `GO_ACTIVATED` trigger path: environmental traps use radius/2 against the current represented player, owner/check-all-units traps can consume an explicit represented target GUID, target GUID is stored as `lootStateUnitGUID`, activated traps record target spell casts with `OriginalCaster`, arm template-or-4s cooldown and apply the C++ charges split. Remaining gaps: real `GameObject::CastSpell`, spell validation/outdoor-PvP fallback, canonical cooldown ownership, real ObjectAccessor target search/hostility filtering, canonical owner combat state, LOS/totem filtering and real UpdateObject/despawn processing.
  Note: foundation progress recorded for the spell-focus `GameObject::Use` branch. `GameObjectTemplateData::spell_focus_linked_trap_like_cpp` now mirrors C++ `spellFocus.linkedTrap` (`Data2`), and represented spell-focus use records the `TriggeringLinkedGameObject` hook only when the linked trap entry is nonzero. Remaining gaps: real linked-trap lookup/spawn/cast semantics and canonical `GameObject::Use` script/runtime ownership.
  Note: foundation progress recorded for the camera `GameObject::Use` branch. `GameObjectTemplateData::camera_use_source_like_cpp` now mirrors C++ `camera.camera` (`Data1`) and `camera.eventID` (`Data2`), sends the C++ `SMSG_TRIGGER_CINEMATIC` packet with an empty conversation GUID, and represented camera use records the cinematic-start and `GameEvents::Trigger` hook points when nonzero. Remaining gaps: real cinematic manager state, GameEvents execution and canonical script/runtime ownership.
  Note: foundation progress recorded for the first goober `GameObject::Use` sub-branch. `GameObjectTemplateData::goober_use_source_like_cpp` now captures the C++ goober data indices for quest/event/page/spell/linked-trap/gossip/multi-interact/player-cast, represented goober use sends the C++ `SMSG_PAGE_TEXT` gameobject GUID packet when `goober.pageID` is nonzero, `CMSG_QUERY_PAGE_TEXT` reads the C++ page-id/raw-item-guid layout and responds with the C++ `SMSG_QUERY_PAGE_TEXT_RESPONSE` page chain from `page_text`/`page_text_locale`, and represented goober use records page/gossip, event, quest-incomplete gating, group-distance `KillCreditGO`, and linked-trap hook points in C++ order. Remaining gaps: real gossip menu packet population, quest objective mutation and canonical GameEvents/trap execution.
  Note: foundation progress recorded for the second goober `GameObject::Use` sub-branch. Represented goober use now applies the C++ `AllowMultiInteract` split, including per-player despawn for consumable multi-interact and per-player active state otherwise; the normal branch records `GO_FLAG_IN_USE`, `GO_ACTIVATED`, sends the C++ `SMSG_GAME_OBJECT_CUSTOM_ANIM` packet using represented `GetGoAnimProgress()` when `goober.customAnim` is set, applies `GO_STATE_ACTIVE` otherwise, records autoclose timing and preserves post-switch spell caster selection from `playerCast`. Remaining gaps: real nearby-set fanout for custom animation/per-player update packets, real spell validation/execution and canonical `m_cooldownTime`/flag fanout.
  Note: foundation progress recorded for represented goober `GO_JUST_DEACTIVATED` cleanup. Represented cleanup now records linked-trap despawn, casts the goober spell to represented `m_unique_users`, clears unique users/use state, removes `GO_FLAG_IN_USE`, resets `GO_STATE_READY` when C++ would (`lockId || autoClose`), and returns non-consumable goobers to `GO_READY` while consumable goobers stay `GO_NOT_READY`. Session pending processing now also mirrors the two-step C++ update path for normal activated goobers: expired autoclose first removes `GO_FLAG_IN_USE` and moves to `GO_JUST_DEACTIVATED`, then the next represented update applies the cleanup source. Remaining gaps: real linked-trap object despawn, real `ObjectAccessor` player lookup for unique users, real spell execution, `ClearLoot` ownership and visibility/respawn/delete fanout.
  Note: foundation progress recorded for generic represented `GO_JUST_DEACTIVATED` cleanup. Non-goober represented gameobjects now clear represented loot, reset `lootStateUnitGUID` and `use_count`, move to `GO_NOT_READY`, and owner/summoned represented objects delete through represented despawn/destroy packets like the C++ `Delete()` branch. Represented `respawnDelayTime` now follows the C++ spawned-by-default split: spawned objects leave represented visibility and return to `GO_READY`/`GO_STATE_READY` when the timer expires, while non-spawned represented objects delete instead of scheduling respawn. Remaining gaps: real linked-trap lookup/despawn, GameObjectOverride flag restoration, `IsDespawnAtAction`/`goAnimProgress` despawn visuals, dynamic respawn scaling, `SaveRespawnTime`, respawnCompatibilityMode visibility destroy, map remove-list ownership and canonical map removal.
  Note: #NEXT.R8.ENTITIES.347 implementation complete for represented linked trap despawn hook in non-goober `GO_JUST_DEACTIVATED`. C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1579`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3909-3912`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:1049-1059`. Rust now records source/template `linked_trap_entry` in represented `WorldSession` GO state for chest and gathering-node sources and emits `GameObjectLinkedTrapDespawn` before represented clear/delete-respawn cleanup when known. This is not canonical map/ObjectAccessor despawn; residual TODO: real `m_linkedTrap` GUID ownership, `ObjectAccessor::GetGameObject`, canonical `DespawnOrUnsummon`, map removal and cross-session fanout remain pending. Fallback without known `linked_trap_entry` emits no represented effect.
  Note: #NEXT.R8.ENTITIES.348 implementation complete for a canonical GameObject-owned loot-state dependency inside `wow-entities::GameObject`. C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3711-3733` and `/home/server/woltk-trinity-legacy/src/server/game/Loot/Loot.h:283-306`. Rust now owns shared loot (`m_loot`), personal loot (`m_personalLoot`), unique users (`m_unique_users`) and use-times state in the entity, with `Loot::isLooted` represented exactly as `gold == 0 && unlooted_count == 0`, plus `clear_loot_like_cpp` and `is_fully_looted_like_cpp` unit coverage for shared/personal/empty cases. This is only the autocontained canonical helper/storage slice; `WorldSession::DoLootRelease`, `OnLootRelease`, loot generation, map/ObjectAccessor lookup and cross-session packet fanout remain pending, and no represented fallback hook was added.
  Note: #NEXT.R8.ENTITIES.349 implementation complete for the represented-to-canonical GameObject owned-loot bridge in `WorldSession`. C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Handlers/LootHandler.cpp:262-320`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:3711-3768`, and `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1705-1708`. Represented GameObject chest/fishing/gathering/personal loot opening and release now synchronize available session-local shared/personal loot counts and money into canonical `wow-entities::GameObject` when the canonical object exists, release decisions prefer canonical `is_fully_looted_like_cpp`, and represented `AddUse`/`AddUniqueUse` branches update canonical counters without changing fallback behavior when no canonical object is present. Remaining gaps: full canonical `GameObject::m_loot` generation/ownership, remote per-player personal loot not represented in this session, cross-session fanout, real ObjectAccessor/Map lifecycle, dynamic flag packet rebuilds and complete GameObject runtime ownership.
  Note: #NEXT.R8.ENTITIES.350 implementation complete for represented `GO_JUST_DEACTIVATED` visual despawn ordering after C++ early returns. C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObjectData.h:843-850`, `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1575-1679`, and `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1766-1771`. Represented session pending processing now keeps `IsDespawnAtAction()` separate from `GetGoAnimProgress()`, so non-consumable CHEST and GOOBER objects without represented owner/summon take the C++ early-return path and do not send `SMSG_GAME_OBJECT_DESPAWN` even when `go_anim_progress > 0`; non-consumable CHEST restock sets represented `GO_NOT_READY` with `chest_restock_until`, otherwise returns to `GO_READY`, while generic objects reaching the later C++ branch still send the despawn visual on `go_animProgress > 0` and consumable CHEST keeps the despawn-at-action packet. Remaining gaps: canonical `GameObject::m_loot` ownership/generation, GameObjectOverride flag reset/restore, exact owner/spell-created provenance beyond represented `owner_guid`, `SaveRespawnTime`/map removal and cross-session fanout.
  Note: foundation progress recorded for the chair `GameObject::Use` branch. `GameObjectTemplateData::chair_use_source_like_cpp` now mirrors C++ chair slots/height/triggered-event fields, and represented chair use initializes slots, computes the nearest free slot along the orthogonal chair line using the DB template `size`, applies the represented player position and chair stand-state mutation, records the teleport/stand-state effect and triggers the optional event. Remaining gaps: real `NearTeleportTo` packet/movement flow, live occupant validation through `ObjectAccessor` and packet fanout.
  Note: foundation progress recorded for the questgiver `GameObject::Use` branch. `GameObjectTemplateData::questgiver_use_source_like_cpp` now mirrors C++ `questgiver.gossipID` (`Data3`), and represented questgiver use records the prepared-gossip delivery hook for player use. Remaining gaps: real `PrepareGossipMenu(..., true)`, real `SendPreparedGossip`, quest menu population and non-player caller rejection outside the current player-only represented handler.
  Note: foundation progress recorded for the spellcaster `GameObject::Use` branch. `GameObjectTemplateData::spellcaster_use_source_like_cpp` now mirrors C++ spell/charges/party-only fields, and represented spellcaster use validates `partyOnly` owner/raid state from typed canonical `GameObjectData::CreatedBy` when available before falling back to represented owner state, records the unconditional mounted-aura removal, `AddUse` counter increment and common post-switch spell hook. Represented session pending processing now mirrors the C++ `GameObject::Update` `GetCharges()` branch for spellcasters, resetting use count and moving to `GO_JUST_DEACTIVATED` when represented use count reaches the template charge count. Remaining gaps: live `GetOwner()` Unit object type validation beyond GUID/raid membership, real spell execution and packet/runtime fanout.
  Note: foundation progress recorded for the barber-chair `GameObject::Use` branch. `GameObjectTemplateData::barber_chair_use_source_like_cpp` now mirrors C++ chair-height, sit-anim-kit and customization-scope fields, sends the C++ `SMSG_ENABLE_BARBER_SHOP` customization-scope packet, applies the represented player position and chair stand-state mutation, and records the fallback teleport and stand-state effects. Remaining gaps: real `TeleportTo` flags, sit-anim-kit application to canonical unit fields and packet fanout.
  Note: foundation progress recorded for the UI-link `GameObject::Use` branch. `GameObjectTemplateData::ui_link_use_source_like_cpp` now mirrors C++ `UILink.UILinkType` (`Data0`), maps the C++ `PlayerInteractionType` values including invalid/default `None`, and sends `SMSG_GAME_OBJECT_INTERACTION` with the raw gameobject GUID and interaction type. Remaining gaps: packet fanout beyond the current session send path.
  Note: foundation progress recorded for the item-forge `GameObject::Use` branch. `GameObjectTemplateData::item_forge_use_source_like_cpp` now mirrors C++ `itemForge.conditionID1` and forge type fields, and represented item-forge use records the player-condition-gated no-op branch after the existing global represented condition check. Remaining gaps: real player-condition store lookup provenance, future forge UI side effects if the legacy branch grows them, and packet/runtime fanout if needed.
  Note: foundation progress recorded for the capture-point `GameObject::Use`/`GameObject::Update` branches. `GameObjectTemplateData::capture_point_use_source_like_cpp` now mirrors the C++ `GAMEOBJECT_TYPE_CAPTURE_POINT` template fields needed by `AssaultCapturePoint`, `UpdateCapturePoint` and the capture-point `GameObject::Update` branch: capture time, assault/capture/defended broadcasts, world state, contested/capture/defended events and `SpellVisual1..5`. Represented capture-point use applies the C++ `CanInteractWithCapturePoint` team/state gate, records the `AI()->OnCapturePointAssaulted` hook before the battleground requirement, requires represented battleground membership, then mutates represented state/timer like `AssaultCapturePoint`: assaults move to contested state and arm `CaptureTime`, defenses by the last owning team capture instantly, and represented pending processing expires contested timers into captured states while updating `LastTeamCapture`. It records the C++ `UpdateCapturePoint` customAnim/spell-visual/world-state/broadcast/event hook decisions, and `wow-packet` now serializes C++ `SMSG_UPDATE_CAPTURE_POINT` (`BattlegroundCapturePointInfo`: GUID, XY position, state, and contested-only capture times) for the current represented session when GO position is known. Remaining gaps: battleground-wide `SendPacketToAll` fanout, `UpdateWorldState` delivery, dynamic-flag nearby updates, spawn-group condition refresh, GameEvents execution, `AI()->OnCapturePointUpdated` override and canonical capture-point map ownership.
  Note: foundation progress recorded for the flagstand/flagdrop `GameObject::Use` branches. `GameObjectTemplateData::flag_stand_use_source_like_cpp` and `flag_drop_use_source_like_cpp` mirror the C++ data indices for pickup/return spells, return aura, event and expiry fields; represented use records the C++ `CanUseBattlegroundObject` guard for faction, damage immunity, recently-dropped-flag auras and alive state, requires represented battleground membership before vehicle/aura/click/delete side effects, rejects represented vehicle users before BG click/delete like C++, removes represented stealth/invisibility auras by type before the battleground click/delete path, records the battleground flag click hook, flagdrop event trigger and flagdrop delete hook, and sends the C++ `SMSG_GAME_OBJECT_DESPAWN` GUID packet plus session-local destroy `UpdateObject` on the delete path. Flagdrop click routing preserves the C++ entry plus battleground-type split for Warsong Gulch (`179785`/`179786` only in `BATTLEGROUND_WS`) and Eye of the Storm (`184142` only in `BATTLEGROUND_EY`). Remaining gaps: real `Battleground::EventPlayerClickedOnFlag`, GameEvents execution, full cross-session/object-map deletion fanout and canonical battleground runtime ownership.
  Note: foundation progress recorded for the new-flag/new-flag-drop `GameObject::Use` branches. Rust now has `GAMEOBJECT_TYPE_NEW_FLAG_DROP`, C++-indexed lock/interact-radius helpers for both new flag types, `new_flag_use_source_like_cpp` for pickup/expiry/respawn/drop/category/world-state/defender-return fields and `new_flag_drop_use_source_like_cpp` for the drop vignette field. Represented use records the C++ `CanUseBattlegroundObject` guard for faction, damage immunity, recently-dropped-flag auras and alive state, requires represented `InBase` state before new-flag pickup, records pickup-spell request, drop interaction, represented owner `GAMEOBJECT_TYPE_NEW_FLAG` lookup/state validation, owner/friendly return-vs-take decision, owner-cast pickup spell result gating for `Taken`, owner-state transition (`InBase`/`Taken`) and sends the C++ `SMSG_GAME_OBJECT_DESPAWN` GUID packet plus session-local destroy `UpdateObject` on the delete path. Represented `SetNewFlagState` now mirrors the C++ `NewFlag::SetState` data mutation for state, carrier GUID, taken-from-base game time and respawn timer. Remaining gaps: live canonical `GameObjectType::NewFlag` object lookup, ZoneScript visibility/callback fanout, full cross-session/object-map delete fanout and canonical battleground runtime ownership.
  Note: foundation progress recorded for the ritual/meeting-stone `GameObject::Use` branches. `GameObjectTemplateData::ritual_use_source_like_cpp` mirrors ritual caster/spell/persistent/target/group fields and represented use now preserves the C++ distinction between summoned gameobject owner and non-summoned ritual owner, rejects summoned-owner self-use, enforces represented same-raid gating when `castersGrouped` is set, rejects known missing owner channeling before `AddUniqueUse`, tracks session-local unique users until the C++ caster threshold is reached, preserves the final ritual caster GUID, casts the animation spell hook after validation, maps the broken `62330` ritual spell to `61993`, records caster-target spell hooks by selecting represented random targets from `m_unique_users`, records summoned-owner channeled-spell finish on completion and sets non-persistent rituals to `GO_JUST_DEACTIVATED` while persistent rituals clear only ritual-owner/unique-use state. `meeting_stone_use_source_like_cpp` mirrors area/friendly-target fields and represented use validates the selected target is present, not self and same-raid, checks represented `ContentTuningId` max-level gates through `ContentTuning.db2`, then records the C++ entry-based spell split (`194097` -> `61994`, otherwise `59782`). Remaining gaps: live owner/channeling object lookup, raid/group target checks beyond represented registry state, actual spell execution/cost semantics and canonical runtime fanout.
  Note: foundation progress recorded for the common post-switch spell path. Represented meeting-stone use routes the selected spell through a reusable C++-shaped post-use helper that records the C++ `OutdoorPvPMgr::HandleCustomSpell` hook, records the map difficulty id that C++ passes to `SpellMgr::GetSpellInfo(spellId, GetMap()->GetDifficultyID())`, records missing `SpellInfo` when a `SpellStore` is available and otherwise records the final cast hook with the C++ triggered flag and concrete caster GUID. Represented new-flag use runs the pickup spell through the same helper with the C++ gameobject-caster path and records the `Taken` state request after a represented successful cast. Completed rituals route their final mapped spell through the helper with the represented ritual-owner caster GUID. `SPELLCASTER`, `GOOBER` and `GATHERING_NODE` now also use the helper, preserving the C++ caster split (`user` vs gameobject caster) and the C++ `AddUse`/state-before-post-cast ordering. Remaining gaps: real OutdoorPvP manager execution/handled result, real `Spell::CastSpell` execution/result propagation, real difficulty-indexed `SpellMgr` lookup data, full ritual owner/channeling validation, and canonical gameobject spell-caster result fanout.
  Note: foundation progress recorded for represented `ObjectDespawnDelay`. Gathering-node first activation now arms a real represented despawn timer from the C++ `gatheringNode.ObjectDespawnDelay` field; pending processing mirrors the C++ delayed `DespawnOrUnsummon`/`Delete` shape by moving the object to `GO_NOT_READY`, restoring `GO_STATE_READY` for non-transports, clearing represented loot/visibility and sending represented despawn/destroy packets. Remaining gaps: real `SaveRespawnTime`, spawn-data persistence, map remove-list ownership, pool refresh and cross-session fanout.
  Note: foundation progress recorded for the fishing-bobber `GAMEOBJECT_TYPE_FISHINGNODE` use branch. Represented use now respects a known owner GUID, treats missing represented loot state as `GO_READY`, moves ready bobbers to `GO_ACTIVATED`/`GO_STATE_ACTIVE`, replaces flags with C++ `GO_FLAG_IN_MULTI_USE`, records the represented `UpdateFishingSkill` hook plus C++ chance formula from `skill_fishing_base_level` through the same AreaTable parent fallback as `ObjectMgr::GetFishingBaseSkillLevel` when the bobber area is known, resolves `GetProfessionSkillForExp(SKILL_FISHING, 0)` through loaded `SkillLine.db2` before reading represented `character_skills`, records the fish-vs-junk loot type decision, searches represented nearby fishing holes with the C++ `20.0f + CONTACT_DISTANCE` range plus `fishingHole.radius` gate and delegates to the nearest one when present, opens represented fish/junk loot from `LootTemplates_Fishing` using the bobber area rather than the player area and with the C++ area-parent/default-zone `1` fallback, syncs represented owner GUIDs into canonical `GameObjectData::CreatedBy` when a typed gameobject exists, reads that canonical owner for owned-gameobject loot distance exceptions, and mirrors the successful-catch C++ `SetOwnerGUID(player); SetSpellId(0)` handoff on typed canonical bobbers without applying it to junk/wrong-owner paths. It also sends the empty C++ `FishNotHooked` packet for non-ready states and always records the channeled-spell finish hook on handled paths. Represented pending processing now mirrors the C++ bobber-ready `GO_NOT_READY` branch by turning bobbers ready once their represented ready timestamp expires and recording the owner splash-animation hook. Remaining gaps: terrain-derived bobber zone/area calculation, spell-created bobber owner/channel/respawn-time provenance, real `UpdateFishingSkill` mutation/persistence, canonical `GameObject::m_loot` ownership, real `SendCustomAnim` fanout and actual channeled spell mutation.
  Note: foundation progress recorded for represented `GameObject::Update` per-player state expiry. Goober `AllowMultiInteract` now records both C++ local branches: `DespawnForPlayer` stores the seer GUID and expiry, while `SetGoStateFor(GO_STATE_ACTIVE)` stores a per-player state override until the represented respawn delay. Chest personal-loot release also stores the seer GUID for its C++ `DespawnForPlayer` path. `process_pending` now clears expired represented per-player despawn/state overrides and records whether C++ would call `UpdateVisibilityOf` or send a local state values update. Remaining gaps: real ObjectAccessor seer lookup, direct packet rebuild, cross-session fanout and canonical `GameObject::m_perPlayerState` ownership.
  Note: foundation progress recorded for the remaining C++ `GameObjectTemplate::GetCharges` branch. Rust now has `GAMEOBJECT_TYPE_GUARDPOST`, a C++-indexed guardpost source helper for creature id, charges and LOS preference, and represented pending processing depletes guardpost `use_count >= guardPost.charges` through the same C++ `m_usetimes = 0; SetLootState(GO_JUST_DEACTIVATED)` path used by spellcasters. Remaining gaps: full guardpost summon/creature AI behavior and canonical GameObject runtime ownership.
  Note: foundation progress recorded for the C++ `GO_NOT_READY` default update branch. Represented pending processing now moves known non-special gameobject types from `GO_NOT_READY` to `GO_READY`, while leaving trap, fishing bobber and chest to their dedicated C++ NotReady branches. Represented shared chest restock now follows the C++ partial/full-loot split: partial non-consumable chests stay `GO_ACTIVATED` with a restock timer, fully looted shared non-consumable chests move to `GO_NOT_READY`, and expired restock returns the chest to `GO_READY` while clearing represented loot contents. Fishing bobbers now also leave `GO_NOT_READY` at their represented ready timestamp and record the C++ owner splash-animation hook. Remaining gaps: fishing bobber escape/despawn timers, canonical `GameObject::m_loot` ownership, dynamic flag/visibility fanout and canonical GameObject update ownership.
- [ ] **#NEXT.R8.ENTITIES.023** Complete `Corpse` create/load/persistence lifecycle: player-owned corpse creation, DB save/load/delete, character cache invalidation, phasing, loot object and map registration.
- [ ] **#NEXT.R8.ENTITIES.025** Complete `DynamicObject` create/add-to-map/update runtime: caster map inheritance, GUID creation, phase inheritance, caster registration, Aura ownership/removal, SpellInfo lookup, farsight viewpoint, transport passenger offset and map relocation.
- [ ] **#NEXT.R8.ENTITIES.027** Complete `AreaTrigger` create/load/update runtime: AreaTriggerDataStore templates/spawns, GUID creation, phase inheritance, static spawn store, AI selection, shape search, unit enter/exit actions, splines/orbit/attached movement, server-side visibility, transport offset and map relocation.
- [ ] **#NEXT.R8.ENTITIES.029** Complete `SceneObject` create/map/update runtime: SceneTemplate lookup, GUID creation, private object owner, phase inheritance, random seed time source, map insertion, creator/aura lookup and removal scheduling.
- [ ] **#NEXT.R8.ENTITIES.031** Complete `Conversation` create/start/update runtime: ConversationDataStore templates, conditions, actor fill visitor, line locale timings, private owner locale, script hooks, map insertion, actor unit/creature lookup and removal scheduling.
- [ ] **#NEXT.R8.ENTITIES.033** Complete `TempSummon`/`Minion`/`Totem` runtime: SummonProperties, owner slots, usable totem slot selection, model lookup by spell/race, `SMSG_TOTEM_CREATED`, spell casting, CombatStop, aura removal from owner/group, cooldown event and map removal scheduling.
- [ ] **#NEXT.R8.ENTITIES.035** Complete `Pet` create/load/save/update runtime: pet GUID/create from DB/tamed creature, stable persistence, action bar, XP/level sync, stats, auras/cooldowns/charges, specialization/talents/passives, PetAI/charm info, group updates and map/object-store insertion.
- [ ] **#NEXT.R8.ENTITIES.037** Complete `Vehicle` runtime: DB2 vehicle/seat lookup, npc flags, install/uninstall/reset scripts, accessories, control auras, immunities, passenger relocation/exit, pending join events, despawn delay and integration with Unit movement/transport state.
- [ ] **#NEXT.R8.ENTITIES.039** Complete `Transport` runtime: `TransportMgr` template loading/path generation, `Transport::Create`, path update/event triggering, map transition teleport/hide behavior, static creature/GameObject passenger spawning, summon passenger path, passenger movement-info transport offsets, script hooks, model/collision update and map/grid integration.

### #NEXT.R8.ENTITIES.351 — GameObjectOverride/template-addon flags/faction source + restore hook

Status: complete (represented dependency only).

C++ anchors: `GameObjectOverride` / `GameObjectTemplateAddon` shape in `GameObjectData.h:1317-1332`, `ObjectMgr::LoadGameObjectTemplateAddons` / `LoadGameObjectOverrides` in `ObjectMgr.cpp:7770-7892`, and `GameObject::Update` / `Delete` restore hooks in `GameObject.cpp:1641-1648,1684-1693,1751-1757`.

Implemented Rust dependency: `SEL_GAMEOBJECTS_IN_RANGE` now appends effective flags/faction selected with C++ priority (`gameobject_overrides.spawnId` over `gameobject_template_addon.entry` over zero), visibility spawn paths propagate them into `GameObjectCreateData` and represented session state, and represented `GO_JUST_DEACTIVATED`/delete cleanup restores known override/template-addon flags after visual despawn instead of preserving contaminated mutable flags such as `GO_FLAG_IN_USE`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`; canonical ObjectMgr/map ownership, cross-session values update/fanout, full template-addon artkit/worldEffect/AIAnimKit validation/consumption, canonical map removal, and complete UpdateObject values delta remain open.

### #NEXT.R8.ENTITIES.352 — Map dynamic respawn scaling helper dependency

Status: complete (helper dependency only).

C++ anchors: `GameObject::Update` spawned-by-default respawn branch in `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:1665-1672`, `Map::ApplyDynamicModeRespawnScaling` in `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2242-2284`, declaration in `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:657-660`, and spawn-group dynamic-rate flag source in `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h` / Rust `SpawnGroupFlags::DYNAMIC_SPAWN_RATE`.

Implemented Rust dependency: `wow-map::map::apply_dynamic_mode_respawn_scaling_like_cpp` is a pure helper for `SpawnObjectType::GameObject` and `SpawnObjectType::Creature`. It preserves the C++ early returns for BG/arena maps, unsupported spawn types, missing spawn metadata, missing `DYNAMIC_SPAWN_RATE`, missing or zero zone-player count, `adjustFactor >= 1.0`, and `respawnDelay <= minimum`. Rust mode `0` and unsupported modes are safe no-ops instead of runtime panics. Successful scaling uses `ceil(respawnDelay * rate / playerCount)` clamped to the type-specific minimum, with separate creature vs gameobject rate/minimum config inputs.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` and does not close GameObject respawn runtime. Still missing: wiring from canonical `GameObject::Update` / represented fallback into the helper, real `SaveRespawnTime`, canonical spawn metadata and zone-player-count ownership, map remove-list/cross-session ownership, visibility fanout, and persistence.

### #NEXT.R8.ENTITIES.353 — Spawn group member metadata parity dependency

Status: complete (metadata dependency only; no DB/world-server/runtime wiring).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:32-39` (`SpawnObjectType`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:51-71` (`SpawnGroupFlags` / `SpawnGroupTemplateData`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:82-99` (`SpawnMetadata::spawnGroupData`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2798-2862` (`ObjectMgr::LoadSpawnGroups`), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2313` (`ShouldBeSpawnedOnGridLoad` / `GetSpawnGroupData`).

Implemented Rust dependency: `wow-map::spawn::SpawnData` now carries explicit spawn-group metadata with default group `0` behaving as the C++ system/unassigned group, `SPAWNGROUP_MAP_UNSET` is represented as `0xffff_ffff`, and `SpawnStore::apply_spawn_groups_like_cpp` applies in-memory `spawn_group` rows plus templates using the C++ early-return order for invalid type, missing spawn, duplicate nonzero group, missing template, unset map assignment, non-system cross-map mismatch, assignment, by-map indexing and non-system group-member indexing. Focused tests cover Creature/GameObject/AreaTrigger assignments, flag preservation, unset-map first assignment, system cross-map allowance, non-system mismatch skip, duplicate skip, invalid/missing reports, and group `0` unassigned semantics.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. Still missing: DB/world-server wiring for `spawn_group` and `spawn_group_template`, active spawn-group state, pool data checks, map respawn runtime, and dynamic respawn helper consumption from canonical spawn metadata.
### #NEXT.R8.ENTITIES.354 — Canonical SpawnStore DB loader + spawn_group application dependency

Status: complete (metadata/index dependency only; no live entity spawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2138-2165` (`ObjectMgr::ParseSpawnDifficulties`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2167-2242` (`ObjectMgr::LoadCreatures` fields and default/legacy spawn group), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2413-2485` (game-event grid gate and `AddSpawnDataToGrid`/`AddCreatureToGrid`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2492-2618` (`ObjectMgr::LoadGameObjects` fields/difficulties/event/pool), `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2676-2736` (script/string, validation tail and `AddGameobjectToGrid`), `/home/server/woltk-trinity-legacy/src/server/game/Globals/AreaTriggerDataStore.cpp:312-419` (`LoadAreaTriggerSpawns` query/parse/index/default legacy group), and `/home/server/rustycore/crates/wow-database/src/statements/world.rs:467-529` (existing Rust statements).

Files touched: `crates/world-server/src/main.rs`, `crates/world-server/src/spawn_store_loader.rs`, `crates/wow-map/src/spawn.rs`, `crates/wow-data/src/spawn_group.rs`, `docs/migration/inventory/r8-entities-miniphase.md`.

Implemented Rust dependency: `world-server` now builds a canonical in-memory `wow_map::SpawnStore` during startup after `spawn_group_template`, `Map.db2`, and `MapDifficulty.db2` are loaded. The loader reads `SEL_CREATURE_SPAWNS`, `SEL_GAMEOBJECT_SPAWNS`, `SEL_AREATRIGGER_SPAWNS`, and `SEL_SPAWN_GROUP_MEMBERS`; parses C++-shaped spawn difficulties; filters missing maps, invalid coordinates, and empty difficulty rows; reports represented/skipped validations; preserves game-event-managed Creature/GameObject rows as canonical `SpawnStore::spawns` metadata with `SpawnStore::insert_spawn_metadata_like_cpp` while omitting only their grid indexes, matching C++ `_creatureDataStore`/`_gameObjectDataStore` plus `gameEvent == 0` `AddCreatureToGrid`/`AddGameobjectToGrid`; indexes non-game-event Creature/GameObject rows with `SpawnStore::add_object_spawn`; indexes AreaTrigger rows with `add_area_trigger_spawn`; converts spawn-group templates through a minimal neutral iterator from `wow-data`; and applies member rows through `SpawnStore::apply_spawn_groups_like_cpp` rather than duplicating the assignment semantics in `world-server`. The startup log records row/index/skip counts and spawn-group assignment issues without wiring live grid/entity activation.

Remaining gaps: `ShouldBeSpawnedOnGridLoad` respawn/pool/group-active filter, live `ObjectGridLoader` wiring, canonical Creature/GameObject/AreaTrigger creation, respawn persistence, pool runtime, active spawn group state, canonical transport-map store for exact legacy default-group classification, full phase DB2 personal-phase lookup, VMAP invalid-position correction, AreaTrigger createProperties/spell validation, script-id resolution, and cross-session/runtime fanout remain pending.


### #NEXT.R8.ENTITIES.355 — estado activo de spawn groups y filtro puro ShouldBeSpawnedOnGridLoad

Status: complete (pure runtime-state/filter helper dependency only; no live entity spawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2305` (`Map::ShouldBeSpawnedOnGridLoad`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive` / `IsSpawnGroupActive`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:51-63` (`SpawnGroupFlags`), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2468` (`InitSpawnGroupState` condition-driven caller shape, not implemented here).

Implemented Rust dependency: `wow-map::spawn::SpawnGroupRuntimeState` now represents C++ `_toggledSpawnGroupIds` without mutating `SpawnStore` metadata; `set_spawn_group_active_like_cpp` rejects missing/system groups and toggles/clears according to `state != !(flags & MANUAL_SPAWN)`; `is_spawn_group_active_like_cpp` returns missing=false, system=true, and non-system active state using the C++ toggled/default formula. `SpawnGridLoadStateLikeCpp` provides an explicit-input `ShouldBeSpawnedOnGridLoad` helper over `SpawnStore`, active group state, respawn-timer keys, and pool-selected keys. Its decision order matches C++: respawn timer false first, missing metadata false safe instead of runtime panic, inactive non-system group false, pooled spawn requires explicit pool selection only when `pool_id != 0`, otherwise true. `ObjectGridLoader::with_filter` can consume the helper through `GridSpawnLoadFilter`, but the default `LoadAllGridSpawns` and live `SpawnGridLifecycle` remain unchanged.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` or live grid-loader runtime. Still missing: wiring in `SpawnGridLifecycle`/live map state, real respawn timer store/persistence, real `PoolData`, conditions/`InitSpawnGroupState`, `SpawnGroupSpawn`/`SpawnGroupDespawn` runtime, live entity creation and cross-session fanout.

### #NEXT.R8.ENTITIES.356 — SpawnGridLifecycle consume filtro ShouldBeSpawnedOnGridLoad

Status: complete (dependencia API/lifecycle-filter únicamente; no live entity spawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:103-111` (`LoadHelper` consulta `map->ShouldBeSpawnedOnGridLoad<T>(guid)` antes de crear/cargar), `/home/server/woltk-trinity-legacy/src/server/game/Grids/ObjectGridLoader.cpp:131-135` (`ObjectGridLoader::Visit(GameObjectMapType&)` obtiene GUIDs de celda y delega en `LoadHelper`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2305` (`Map::ShouldBeSpawnedOnGridLoad` respawn/group/pool early returns), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive` / `IsSpawnGroupActive` source state shape from #355).

Implemented Rust dependency: `wow-map::object_grid_loader::SpawnGridLifecycle` is now generic over `Filter = LoadAllGridSpawns`, stores the filter, keeps `SpawnGridLifecycle::new` as the legacy load-all constructor, and adds `SpawnGridLifecycle::with_filter` for callers that provide a `GridSpawnLoadFilter`. Its `GridLifecycle::load_grid_objects` path now constructs `ObjectGridLoader::with_filter` using the stored filter, so the lifecycle can apply the already-represented C++-shaped `SpawnGridLoadStateLikeCpp` decision before inserting spawn GUIDs into loaded grid cells, matching the C++ direction where `LoadHelper` checks `ShouldBeSpawnedOnGridLoad` before object creation/load.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` or live grid/entity runtime. Still missing: wiring real state ownership in `Map`/`SpawnGridLifecycle` owner, persistent respawn timers, real `PoolData`, conditions/`InitSpawnGroupState`, `SpawnGroupSpawn`/`SpawnGroupDespawn` runtime, live entity creation and cross-session fanout.

### #NEXT.R8.ENTITIES.357 — Map-owned spawn-group active state bridge

Status: complete (Map-owned state bridge only; no live spawn/despawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:780-781` (`SetSpawnGroupActive` and `_toggledSpawnGroupIds` are `Map` members), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2439` (`Map::SetSpawnGroupActive` blocks missing/system groups and toggles against manual default), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2441-2453` (`Map::IsSpawnGroupActive` missing=false, system=true, otherwise toggled/default formula), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2468` (`InitSpawnGroupState` skips system groups and applies condition result through `SetSpawnGroupActive`), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502` (`UpdateSpawnGroupConditions` action shape only; live actions not implemented in this slice).

Implemented Rust dependency: `wow_map::Map` now owns `spawn_group_state: SpawnGroupRuntimeState`, initialized by `Map::with_hooks`, and exposes read-only `spawn_group_state()`. `Map::set_spawn_group_active_like_cpp`, `set_spawn_group_inactive_like_cpp`, and `is_spawn_group_active_like_cpp` delegate to the map-owned runtime state while preserving the C++ missing/system/manual/default toggle semantics. `Map::spawn_grid_load_state_like_cpp` is an honest bridge for `ShouldBeSpawnedOnGridLoad`: map-owned toggles are canonical, but spawn metadata remains caller-supplied until ObjectMgr/SpawnStore ownership is wired into `Map`. `Map::init_spawn_group_state_like_cpp` is a pure pre-resolved-template bridge for the C++ init loop; it skips system groups and applies condition results through the same map-owned `SetSpawnGroupActive` semantics.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` and does not implement `SpawnGroupSpawn`, `SpawnGroupDespawn`, real condition manager wiring, pool runtime, respawn persistence, live entity creation, grid fanout, or `UpdateSpawnGroupConditions` side effects. Next gap is wiring canonical ObjectMgr/SpawnStore/conditions owners into `Map` without allowing stale external state to write over `Map` toggles.

### #NEXT.R8.ENTITIES.358 — Map::UpdateSpawnGroupConditions action planner

Status: complete (action-planner dependency only; no live spawn/despawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502` (`Map::UpdateSpawnGroupConditions` group loop, manual-only despawn branch, automatic spawn/despawn/set-inactive branches), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive` / `IsSpawnGroupActive` map-owned toggle semantics), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/SpawnData.h:51-63` (`SPAWNGROUP_FLAG_MANUAL_SPAWN` and `SPAWNGROUP_FLAG_DESPAWN_ON_CONDITION_FAILURE`).

Implemented Rust dependency: `wow_map::Map::plan_update_spawn_group_conditions_like_cpp` now plans the C++ `UpdateSpawnGroupConditions` branches over pre-resolved `SpawnGroupTemplateData` in input order. It uses map-owned `Map::spawn_group_state` through `Map::is_spawn_group_active_like_cpp(Some(group))` as the active-state source of truth, accepts a condition closure that only replaces `ConditionMgr::IsMapMeetingNotGroupedConditions`, and returns explicit `SpawnGroupConditionActionLikeCpp` decisions: `Noop`, default `Spawn { ignore_respawn: false, force: false }`, condition-failure `Despawn { delete_respawn_times: true }`, or `SetInactive`. The planner is intentionally pure and does not mutate toggles even for `SetInactive`; callers that eventually execute this action must apply C++ `SetSpawnGroupInactive` semantics in the live runtime slice.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map spawn_group`; `cargo test -p wow-map update_spawn_group_conditions`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. Still missing: live execution of `SpawnGroupSpawn`/`SpawnGroupDespawn`, real condition manager wiring, pool runtime, respawn persistence, live entity creation, grid/session fanout, and DB/runtime side effects.

### #NEXT.R8.ENTITIES.359 — ConditionMgr map-condition helper for spawn groups

Status: complete (ConditionMgr helper dependency only; no live spawn/despawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp:1121-1146` (`IsObjectMeetingNotGroupedConditions` and `IsMapMeetingNotGroupedConditions`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp:1095-1113` (`CONDITION_SOURCE_TYPE_SPAWN_GROUP` allowed condition types), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.h:187-191` (`CONDITION_SOURCE_TYPE_SPAWN_GROUP = 33` and reference/max boundaries), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2502` (`InitSpawnGroupState` / `UpdateSpawnGroupConditions` caller shape).

Implemented Rust dependency: `wow-world::conditions::is_spawn_group_meeting_map_conditions_like_cpp` now builds `ConditionSourceInfo::from_map`, installs optional `ConditionMapStateSnapshot` and realm achievement ids, resolves the C++ not-grouped key `{ source_group = 0, source_entry = spawnGroupId, source_id = 0 }` through the existing `ConditionEntriesByTypeStore`, evaluates with the existing C++-shaped condition list/reference expansion path, preserves the C++ fallback where a missing bucket returns true, and treats unsupported/map-only-unavailable condition types as false without panic. Focused tests cover missing buckets, `MAPID`, `WORLD_STATE`, `REALM_ACHIEVEMENT`, unsupported `AURA`, and `REFERENCE_CONDITION` expansion.

Checks expected: `cargo fmt --check`; `cargo test -p wow-world spawn_group_meeting_map_conditions`; `git diff --check`.

Remaining gaps: this does NOT complete `#NEXT.R8.ENTITIES.021` or live `UpdateSpawnGroupConditions`. Missing work remains: executing `SpawnGroupSpawn`/`SpawnGroupDespawn`, pool/runtime integration, respawn persistence, live entity creation, grid/session fanout, DB/runtime side effects, and wiring a future `world-server`/`wow-world` caller that resolves these bools and passes them into the `wow-map` planner without making `wow-map` depend on `wow-world`.

### #NEXT.R8.ENTITIES.360 — MapManager InitSpawnGroupState creation hook

Status: complete (MapManager ownership-hook dependency only; no live condition/spawn/despawn wiring).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:71-80` (`MapManager::CreateWorldMap` creates `Map`, loads respawns/corpses, calls `map->InitSpawnGroupState()`, then optional `LoadAllCells`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:84-113` (`MapManager::CreateInstance` creates `InstanceMap`, loads respawns/corpses, instance data/scenario, calls `map->InitSpawnGroupState()`, then optional `LoadAllCells`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:118-129` (`MapManager::CreateBattleground` creates `BattlegroundMap`, BG setup, calls `map->InitSpawnGroupState()`, then optional `LoadAllCells`), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2468` (`Map::InitSpawnGroupState` loops map spawn groups, skips system groups, and writes active state from `ConditionMgr`).

Implemented Rust dependency: `wow_map::MapManager` now owns an optional C++-shaped spawn-group initializer hook (`SpawnGroupInitializerLikeCpp`) configurable through `set_spawn_group_initializer_like_cpp` and removable through `clear_spawn_group_initializer_like_cpp`. `create_map_entry` now detects new `ManagedMap` insertions with the `BTreeMap` entry API, builds the `ManagedMap`, invokes the hook immediately on `&mut ManagedMap`, and only then inserts/returns it. Existing map lookups return the existing `ManagedMap` without invoking the hook. `create_world_map` inherits the behavior by delegating to `create_map_entry`. Default/new managers keep the previous no-hook behavior.

Files touched: `crates/wow-map/src/manager.rs`; `docs/migration/inventory/r8-entities-miniphase.md`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map init_spawn_group_state`; `cargo test -p wow-map map_manager`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. The hook does not execute `SpawnGroupSpawn`/`SpawnGroupDespawn`, load grids, do I/O, call `ConditionMgr`, or duplicate active-state ownership. Still pending: world-server/ConditionMgr wiring that resolves conditions and calls `ManagedMap::map_mut().init_spawn_group_state_like_cpp(...)`, live `SpawnGroupSpawn`/`SpawnGroupDespawn` runtime, pool/respawn integration, live entity creation, and grid/session fanout.

### #NEXT.R8.ENTITIES.361 — Preserve applied spawn-group template map data for future InitSpawnGroupState wiring

Status: complete (data dependency only; no InitSpawnGroupState live hook).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Globals/ObjectMgr.cpp:2798-2862` (`ObjectMgr::LoadSpawnGroups` validates member type/spawn/template, resolves unset template map from first spawn, rejects non-system cross-map rows, writes spawn metadata, and indexes `_spawnGroupsByMap` / `_spawnGroupMapStore` for non-system groups), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2468` (`Map::InitSpawnGroupState` reads `GetSpawnGroupsForMap(GetId())`, resolves `GetSpawnGroupData`, skips system groups, checks conditions, and toggles map-owned state), and `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp:1142-1145` (future map-condition consumer entry point).

Implemented Rust dependency: `world-server` now preserves the post-`apply_spawn_groups_like_cpp` `BTreeMap<u32, SpawnGroupTemplateData>` together with the `SpawnStore` in `CanonicalSpawnMetadataLikeCpp`. The wrapper exposes read-only accessors and a C++-shaped per-map helper that follows `SpawnStore::spawn_group_ids_by_map(map_id)` order and filters through `SpawnStore::spawn_group_template_for_map(...)`, treating missing maps/templates as empty rather than panicking. `main.rs` keeps the data alive under an honest `_canonical_spawn_metadata` `Arc` but does not wire `MapManager::set_spawn_group_initializer_like_cpp` yet.

Files touched: `crates/world-server/src/spawn_store_loader.rs`; `crates/world-server/src/main.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p world-server spawn_group`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`, does not execute live `InitSpawnGroupState`, and does not implement live `SpawnGroupSpawn`/`SpawnGroupDespawn`, condition evaluation, pool/runtime integration, respawn persistence, live entity creation, or grid/session fanout.

### #NEXT.R8.ENTITIES.362 — Wire world-server InitSpawnGroupState hook to canonical spawn metadata and ConditionMgr

Status: complete (world-server InitSpawnGroupState wiring only; no live spawn/despawn/runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:71-80` (`CreateWorldMap` calls `map->InitSpawnGroupState()` during map creation), `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:84-113` (`CreateInstance` calls `map->InitSpawnGroupState()` after instance setup), `/home/server/woltk-trinity-legacy/src/server/game/Maps/MapManager.cpp:118-129` (`CreateBattleground` calls `map->InitSpawnGroupState()`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2455-2468` (`Map::InitSpawnGroupState` resolves groups for map, skips system groups, checks map conditions, and calls `SetSpawnGroupActive`), `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp:1142-1145` (`IsMapMeetingNotGroupedConditions` builds `ConditionSourceInfo(map)`), and `/home/server/woltk-trinity-legacy/src/server/game/Conditions/ConditionMgr.cpp:1095-1113` (allowed spawn-group condition types).

Implemented Rust wiring: `world-server` now keeps `canonical_spawn_metadata` as a live `Arc<CanonicalSpawnMetadataLikeCpp>` and installs a `wow_map::MapManager::set_spawn_group_initializer_like_cpp` hook on the canonical map manager before `register_loaded_instance_ids`. The hook is creation-only, resolves `CanonicalSpawnMetadataLikeCpp::spawn_group_templates_for_map_like_cpp(map_id)` in wrapper/C++ order, builds `ConditionMapRef::new(map_id, instance_id)`, passes an explicit `ConditionMapStateSnapshot` using the map spawn-mode difficulty and empty runtime-owned arrays, evaluates `CONDITION_SOURCE_TYPE_SPAWN_GROUP` via `wow_world::conditions::is_spawn_group_meeting_map_conditions_like_cpp`, and feeds those bools into `managed_map.map_mut().init_spawn_group_state_like_cpp(...)`. It only mutates map-owned spawn-group toggles during new map creation; it does not mutate metadata, perform I/O, load grids, touch the legacy map manager, or run spawn/despawn.

Files touched: `crates/world-server/src/main.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p world-server canonical_spawn_group_initializer`; `cargo check -p world-server`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. Still missing: live `SpawnGroupSpawn`/`SpawnGroupDespawn`, pools, respawn persistence, live entity creation, grid/session fanout, DB/runtime side effects, and runtime map-state sources beyond the explicitly empty arrays used by this wiring slice.

### #NEXT.R8.ENTITIES.363 — Apply only the SetSpawnGroupInactive branch of UpdateSpawnGroupConditions

Status: complete for the `SetSpawnGroupInactive` branch of `Map::UpdateSpawnGroupConditions` only; planned `SpawnGroupSpawn` / `SpawnGroupDespawn` actions remain explicit runtime gaps.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502` (`Map::UpdateSpawnGroupConditions`: returns when no map groups exist; resolves each template, reads `IsSpawnGroupActive`, evaluates map conditions; manual groups only plan/despawn on active + failed condition + `SPAWNGROUP_FLAG_DESPAWN_ON_CONDITION_FAILURE`; `active == should` is a no-op; live `SpawnGroupSpawn` / `SpawnGroupDespawn` branches are separate runtime side effects; automatic active groups failing conditions without the despawn-on-failure flag call `SetSpawnGroupInactive`), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive`, `SetSpawnGroupInactive`, and `IsSpawnGroupActive` are map-owned `_toggledSpawnGroupIds` semantics already represented in Rust).

Implemented Rust bridge: `wow-map` now exposes `apply_update_spawn_group_conditions_set_inactive_like_cpp(...)`, which accepts the same pre-resolved spawn-group templates and condition closure as the existing planner, records the planned action per group, and applies only `SpawnGroupConditionActionLikeCpp::SetInactive` through `Map::set_spawn_group_inactive_like_cpp(Some(group))`. The returned `SpawnGroupConditionUpdateOutcomeLikeCpp` carries `group_id`, planned `action`, and `Option<SpawnGroupActiveChange>` evidence only when the inactive branch was actually applied. `Noop`, `Spawn { .. }`, and `Despawn { .. }` are returned as planned actions without mutating toggles or pretending live runtime exists.

Files touched: `crates/wow-map/src/map.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map update_spawn_group_conditions`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. `SpawnGroupSpawn`, `SpawnGroupDespawn`, pools, respawn persistence, live entity creation, grid/session fanout, DB/runtime side effects, and scheduler/respawn timer wiring remain pending.

### #NEXT.R8.ENTITIES.364 — world-server helper for SetSpawnGroupInactive-only UpdateSpawnGroupConditions composition

Status: complete (world-server condition-update set-inactive composition helper only; no live scheduler/spawn/despawn runtime activation).

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-688` (`Map::Update` calls `ProcessRespawns` and `UpdateSpawnGroupConditions` when `_respawnCheckTimer` passes), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502` (`Map::UpdateSpawnGroupConditions` resolves groups by map, reads `GetSpawnGroupData`, `IsSpawnGroupActive`, and `ConditionMgr`; manual groups only plan condition-failure despawn when already active; automatic `active == should` is a no-op; `should == true` calls `SpawnGroupSpawn`; `should == false` plus despawn flag calls `SpawnGroupDespawn`; otherwise it calls `SetSpawnGroupInactive`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive` / `IsSpawnGroupActive` map-owned `_toggledSpawnGroupIds`), and `/home/server/woltk-trinity-legacy/src/server/game/Entities/GameObject/GameObject.cpp:772-779` plus `4256-4277` (capture point event/update paths call `GetMap()->UpdateSpawnGroupConditions`).

Implemented Rust helper: `world-server` now has private `apply_canonical_spawn_group_condition_update_set_inactive_like_cpp(...)` near the existing InitSpawnGroupState hook. The helper takes `map_id`, `instance_id`, and `difficulty_id` from the existing `ManagedMap`, resolves per-map groups from read-only `CanonicalSpawnMetadataLikeCpp`, returns an empty vector/no-op when no groups exist, builds the same `ConditionMapRef` and explicit empty-runtime `ConditionMapStateSnapshot` arrays as the InitSpawnGroupState hook, evaluates `CONDITION_SOURCE_TYPE_SPAWN_GROUP` through `is_spawn_group_meeting_map_conditions_like_cpp`, and delegates the only allowed map-state mutation to `ManagedMap::map_mut().apply_update_spawn_group_conditions_set_inactive_like_cpp(...)`. It logs evaluated groups/outcomes plus applied `SetInactive`, planned spawn, and planned despawn counts; it does not execute or fake `SpawnGroupSpawn` / `SpawnGroupDespawn`, write metadata, or let ConditionMgr overwrite map-owned toggles directly.

Files touched: `crates/world-server/src/main.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check` or `cargo fmt`; `cargo test -p world-server spawn_group_condition_update_set_inactive`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` and does not wire the live `_respawnCheckTimer` scheduler. `SpawnGroupSpawn`, `SpawnGroupDespawn`, pools, respawn persistence, live entity creation, grid/session fanout, DB/runtime side effects, and runtime sources for event/world-state/instance/boss arrays remain pending; those arrays are intentionally empty in this helper just like the current InitSpawnGroupState hook.

### #NEXT.R8.ENTITIES.365 — Scheduler C++-like for UpdateSpawnGroupConditions SetInactive-only

Status: complete for `_respawnCheckTimer`-anchored scheduler/wiring of the already-safe `SetSpawnGroupInactive` branch only; `ProcessRespawns`, live spawn/despawn and wider runtime side effects remain explicit gaps.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-688` (`Map::Update` runs session updates, then when `_respawnCheckTimer <= t_diff` calls `ProcessRespawns(); UpdateSpawnGroupConditions();` and resets `_respawnCheckTimer` to `CONFIG_RESPAWN_MINCHECKINTERVALMS`, otherwise subtracts `t_diff`), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2471-2502` (`UpdateSpawnGroupConditions` branch order; only the final automatic failed-condition branch without `SPAWNGROUP_FLAG_DESPAWN_ON_CONDITION_FAILURE` may call `SetSpawnGroupInactive` in this slice), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2427-2453` (`SetSpawnGroupActive`/`SetSpawnGroupInactive`/`IsSpawnGroupActive` are map-owned toggle semantics).

Implemented Rust wiring: `wow-map::MapManager` now exposes `do_for_all_maps_mut` to iterate existing managed maps in `BTreeMap` order without depending on `world-server`, and `MapManager::update(diff_ms)` returns `None` when its internal interval has not passed or `Some(effective_diff_ms)` with the accumulated timer current when maps were actually updated. `world-server` now builds a private `CanonicalRespawnConditionSchedulerLikeCpp` from `CONFIG_RESPAWN_MINCHECKINTERVALMS` (fallback `DEFAULT_RESPAWN_MIN_CHECK_INTERVAL_MS = 5000`), passes `Arc<CanonicalSpawnMetadataLikeCpp>` and `Arc<ConditionEntriesByTypeStore>` into `spawn_canonical_map_update_loop`, keeps the previous `diff_ms == 0` skip, and advances the respawn-condition scheduler only after an effective canonical map update, using the effective accumulated diff returned by `MapManager::update` rather than partial outer-loop ticks. When the scheduler fires, `canonical_map_update_tick_set_inactive_like_cpp` iterates all canonical maps and delegates each map to `apply_canonical_spawn_group_condition_update_set_inactive_like_cpp`, aggregating debug counters for maps evaluated, outcomes, applied set-inactive, planned spawn and planned despawn. The hook documents that `ProcessRespawns` is not implemented and does not simulate it.

Files touched: `crates/wow-map/src/manager.rs`; `crates/world-server/src/main.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Acceptance: scheduler does not fire before the configured interval, fires and resets when accumulated diff reaches/exceeds the interval, subtracts `diff_ms` otherwise, and the one-tick helper proves a failing active automatic group remains active before the scheduler fires and becomes inactive only when it fires.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map do_for_all_maps_mut`; `cargo test -p world-server spawn_group_condition_update`; `cargo check -p world-server`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. Still missing: real `ProcessRespawns`, live `SpawnGroupSpawn`/`SpawnGroupDespawn`, pools, respawn persistence, live entity creation/fanout, DB/runtime side effects, and runtime event/world-state/instance/boss arrays beyond the explicitly empty map-state arrays consumed by the current ConditionMgr bridge.

### #NEXT.R8.ENTITIES.366 — Map-owned respawn timer store + ProcessRespawns planner dependency

Status: complete for the `wow-map` map-owned respawn timer store plus planned `ProcessRespawns` dependency only. This does not wire live scheduler execution or perform runtime respawn side effects.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:161-180` (`RespawnInfo` fields and `CompareRespawnInfo` priority by `respawnTime`, spawn-id tie handling, duplicate-type assert), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:472-480` (`GetRespawnTime` returns 0 for missing/no map), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:748-777` (Map owns `_respawnTimes`, `_creatureRespawnTimesBySpawnId`, `_gameObjectRespawnTimesBySpawnId`; AreaTrigger has no respawn map; unsupported types abort in C++), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2057-2090` (`AddRespawnInfo`: reject spawnId 0/no map, replace when new time <= existing, reject later duplicate, insert heap+map), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2107-2150` (`GetRespawnInfo`, `UnloadAllRespawnInfos`, `DeleteRespawnInfo` keep heap/map coherent; DB delete is external), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240` (`ProcessRespawns`: process only due top; pool branch before `CheckRespawn`; then DoRespawn, delete, or reschedule/save with future-time assert), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1950-2023` (`CheckRespawn` contract: allowed, delete via zero, or reschedule strictly future).

Implemented Rust dependency: `wow-map::RespawnStoreLikeCpp` owns separate creature/gameobject respawn maps and an ordered queue of `RespawnInfoLikeCpp`. `add_respawn_info_like_cpp` rejects zero spawn ids and AreaTrigger/no-map types, replaces an existing timer when the new `respawn_time <= existing.respawn_time`, rejects later duplicates, and keeps map/queue keys coherent. `get_respawn_time_like_cpp`, `get_respawn_info_like_cpp`, `remove_respawn_time_like_cpp`, `unload_all_respawn_infos_like_cpp`, and `respawn_timer_keys_like_cpp` expose the C++-shaped store semantics for future map-owned callers and future `SpawnGridLoadStateLikeCpp` consumption.

Planner dependency: `process_due_respawns_like_cpp(now, is_part_of_pool, check_respawn)` returns explicit actions instead of executing side effects. It stops at the first future timer, deletes the entry before returning `UpdatePool`, deletes before returning `DoRespawn`, deletes on `check_respawn` false + zero, reinserts and returns `RescheduleAndSave` on false + future, and returns `InvalidRescheduleNotFuture` plus reinserts/breaks when a closure violates the C++ future-time assertion. `PoolMgr::UpdatePool`, `DoRespawn`, `SaveRespawnInfoDB`, `DeleteRespawnInfoFromDB`, linked-respawn checks, real by-spawn entity stores and LoadFromDB are intentionally not faked.

Files touched: `crates/wow-map/src/spawn.rs`; `crates/wow-map/src/lib.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map respawn_info`; `cargo test -p wow-map process_respawns`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`. Still missing: live `ProcessRespawns` scheduler execution, real `PoolMgr`, real `DoRespawn` entity creation/`LoadFromDB`, DB persistence/delete, linked-respawn `CheckRespawn` implementation, real creature/gameobject by-spawn stores, grid/session fanout, and connection from session/world-server fallback state into the map-owned canonical store.

### #NEXT.R8.ENTITIES.367 — Integrar RespawnStoreLikeCpp como estado canónico de Map

Status: complete for the Map-owned respawn store integration dependency only. This does not wire live scheduler execution or perform runtime respawn side effects.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:472-480` (`Map::GetRespawnTime` reads the map-owned respawn map and returns `0` when missing/no map), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:748-777` (`Map` owns `_respawnTimes`, `_creatureRespawnTimesBySpawnId`, `_gameObjectRespawnTimesBySpawnId`; AreaTrigger returns nullptr; invalid types abort), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2057-2090` (`AddRespawnInfo` rejects spawnId 0/no map, replaces earlier/equal timers, rejects later duplicates, inserts heap+map), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2107-2150` (`GetRespawnInfo`, `UnloadAllRespawnInfos`, `DeleteRespawnInfo` maintain heap/map coherency; DB delete is external), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240` (`ProcessRespawns` consumes the map-owned store while PoolMgr/DoRespawn/DB side effects remain external), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2286-2305` (`ShouldBeSpawnedOnGridLoad` checks `GetRespawnTime(type, spawnId) != 0` before group/pool checks).

Implemented Rust dependency: `wow-map::Map` now owns a private `RespawnStoreLikeCpp` next to map-owned `SpawnGroupRuntimeState`, initializes it in `Map::with_hooks`, and exposes C++-shaped map wrappers for read-only/mutable store access, add/get time/get info/remove/unload/timer-key iteration, and planned due-respawn processing. `Map::spawn_grid_load_state_like_cpp(&SpawnStore)` now feeds `SpawnGridLoadStateLikeCpp` from the map-owned respawn timer keys plus map-owned spawn-group state while preserving caller-supplied `SpawnStore` metadata as the allowed bridge until ObjectMgr/SpawnStore ownership moves into `Map`.

Files touched: `crates/wow-map/src/map.rs`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo test -p wow-map map_owned_respawn`; `cargo fmt --check`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021` or live `ProcessRespawns`. Still missing: live scheduler execution, real `PoolMgr`, real `DoRespawn` entity creation/`LoadFromDB`, DB persistence/delete, linked-respawn `CheckRespawn`, real creature/gameobject by-spawn stores, ObjectAccessor/map-local entity ownership, and grid/session fanout.

### #NEXT.R8.ENTITIES.368 — Representar primera guarda de Map::CheckRespawn por spawn group inactivo

Status: partial/represented dependency only. This implements the first C++ `Map::CheckRespawn` guard and deliberately does not claim full `CheckRespawn` or live `ProcessRespawns` completion.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1956-1957` resolves `SpawnData` and asserts if missing; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1959-1964` clears `info->respawnTime = 0` and returns false if the spawn group is inactive; `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.h:663-668` documents that false means delete when respawn time is zero or reschedule when it is future.

Implemented Rust dependency: `wow-map::Map::check_respawn_spawn_group_guard_like_cpp(&mut RespawnInfoLikeCpp, &SpawnStore)` reads caller-supplied `SpawnStore` metadata, consults map-owned `SpawnGroupRuntimeState`, clears `respawn_time` for inactive groups, preserves timers for active/system groups, and returns `MissingSpawnData` without mutation as a defensive temporary fallback where C++ would assert while ObjectMgr/SpawnStore ownership is still bridged.

Files touched: `crates/wow-map/src/spawn.rs`; `crates/wow-map/src/lib.rs`; `crates/wow-map/src/map.rs`; `docs/migration/current-session-handoff.md`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map check_respawn_spawn_group_guard`; `cargo test -p wow-map process_respawns`; `cargo check -p world-server`; `git diff --check`.

Remaining gaps: this does not complete `#NEXT.R8.ENTITIES.021`, full `Map::CheckRespawn`, or live `ProcessRespawns`. Still missing: live scheduler execution, by-spawn live existence checks, escort dynamic exceptions, linked respawn including random 5-15 reschedule, real `PoolMgr`, real `DoRespawn` entity creation/`LoadFromDB`, DB persistence/delete, real creature/gameobject by-spawn stores, ObjectAccessor/map-local entity ownership, grid/session fanout, and session/world-server fallback-state migration into the map-owned canonical store.


### #NEXT.R8.ENTITIES.369 — ProcessRespawns delete-only inactive spawn-group branch over map-owned timers

Status: complete only for the safe in-memory `ProcessRespawns` delete branch where the represented `CheckRespawn` spawn-group guard clears `respawn_time` because the spawn group is inactive. This does not complete full live `ProcessRespawns`, `DoRespawn`, `PoolMgr`, DB persistence/delete, linked respawn, by-spawn live stores, or fanout.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:666-688` (`Map::Update` calls `ProcessRespawns(); UpdateSpawnGroupConditions();` when `_respawnCheckTimer` fires), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1950-2023` (`CheckRespawn` contract; first guard clears `respawnTime` for inactive spawn groups), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2191-2240` (`ProcessRespawns` due-timer order, PoolMgr branch, DoRespawn branch, zero-respawn-time delete branch, future reschedule/save branch), `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2165-2188` (`DoRespawn` entity creation/LoadFromDB out of scope), and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:2152-2163` (`DeleteRespawnInfoFromDB` DB delete out of scope).

Implemented Rust slice: `wow-map::Map::process_due_respawns_spawn_group_delete_only_like_cpp(now, &SpawnStore)` iterates map-owned respawn timer keys in C++ order, stops at future timers, resolves metadata through the explicit `SpawnStore` bridge, blocks and preserves the oldest due timer for missing metadata, `pool_id != 0`, or active/allowed `DoRespawn` cases, and only removes the real map-owned timer when `check_respawn_spawn_group_guard_like_cpp` returns inactive-group deletion with copied `respawn_time == 0`. `world-server` now calls this delete-only seam before the existing SetInactive-only `UpdateSpawnGroupConditions` seam when the C++-style scheduler fires, and logs explicit counters for deleted/blocked respawn branches.

Files touched: `crates/wow-map/src/map.rs`; `crates/world-server/src/main.rs`; `docs/migration/current-session-handoff.md`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map process_respawns`; `cargo test -p world-server spawn_group_condition_update`; `cargo check -p world-server`; `git diff --check`.

Remaining gaps: full live `ProcessRespawns` remains partial. Pool timers, missing metadata, active/allowed `DoRespawn`, DB save/delete, linked-respawn reschedule, by-spawn live existence, escort exceptions, real entity creation/LoadFromDB, ObjectAccessor/map-local entity ownership, grid/session fanout and persistence are still blocked/pending and are not faked by this slice.

### #NEXT.R8.ENTITIES.370 — Map::CheckRespawn live by-spawn blocker over map_objects

Status: complete only for the `Map::CheckRespawn` live-object existence guard from `Map.cpp:1966-2002`. This ports the branch that clears `info->respawnTime = 0` when an already-live creature with the same spawn id, or any gameobject with the same spawn id, blocks respawn. This does not complete full `CheckRespawn` or live `ProcessRespawns` integration.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1950-2023` (`CheckRespawn` contract) and `/home/server/woltk-trinity-legacy/src/server/game/Maps/Map.cpp:1966-2002` (already-existing live creature/gameobject guard, dynamic escort exception, zeroing respawn time on block, default abort).

Implemented Rust slice: `wow-map::Map::check_respawn_live_object_guard_like_cpp(&mut RespawnInfoLikeCpp, &SpawnStore, respawn_dynamic_escortnpc, is_creature_escorted)` resolves `SpawnData` through the explicit `SpawnStore` bridge, uses canonical map-owned `map_objects` as the local runtime source of truth, blocks only alive matching creatures unless the explicit dynamic-escort flag and caller-provided escort predicate allow the live creature, blocks gameobjects by any matching `MapObjectRecord::game_object()` spawn id, preserves `respawn_time` for missing metadata and unsupported AreaTrigger, and returns explicit outcomes instead of panicking.

Files touched: `crates/wow-map/src/map.rs`; `docs/migration/current-session-handoff.md`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Checks expected: `cargo fmt --check`; `cargo test -p wow-map check_respawn_live_object_guard`; `git diff --check`; `git status --short --branch`.

Remaining gaps: integrate this helper into full `CheckRespawn`/`ProcessRespawns`, linked respawn, PoolMgr, DoRespawn/LoadFromDB, DB save/delete, optimized map-local by-spawn indexes, real escort runtime feeding the closure, entity creation/ownership, grid/session fanout, and world-server scheduler wiring beyond the existing safe delete-only seam.


### #NEXT.R8.ENTITIES.495 — represented typed DynamicObject create visibility

Status: represented only. Typed canonical DynamicObject records are consumed by live `WorldSession::update_visibility()` for first-appearance create packets and client-visible GUID tracking; this is not full visibility/farsight runtime.

C++ anchors: `/home/server/woltk-trinity-legacy/src/server/game/Entities/Player/Player.cpp:25344-25364`; `/home/server/woltk-trinity-legacy/src/server/game/Spells/SpellEffects.cpp:2237-2261`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:34-41`; `/home/server/woltk-trinity-legacy/src/server/game/Entities/DynamicObject/DynamicObject.cpp:254-263`; `/home/server/woltk-trinity-legacy/src/server/game/Grids/Notifiers/GridNotifiers.cpp:41-56`.

Implemented Rust slice: `DynamicObjectCreateData` and `UpdateBlock::CreateDynamicObject` serialize C++-shaped DynamicObject create values; session visibility reads typed `MapObjectRecord::DynamicObject` records from canonical `wow_map::Map::map_objects`, filters by represented effective visibility source/map/distance, sends create or OOR packets, and updates `client_visible_guids_like_cpp`.

Files touched: `crates/wow-packet/src/packets/update.rs`; `crates/wow-world/src/session.rs`; `crates/wow-world/src/handlers/character.rs`; `docs/migration/current-session-handoff.md`; `docs/migration/inventory/r8-entities-miniphase.md`; `docs/migration/inventory/r8-entities-miniphase.tsv`.

Review/validation: reviewer `APROBADO`; CI `CI_OK`; `cargo fmt --check` OK; `cargo test -p wow-packet dynamic_object_create_block_serializes_stationary_create_values_like_cpp -- --nocapture` OK (1 passed, 273 filtered out); `cargo test -p wow-world dynamic_object_visibility -- --nocapture` OK as a command but 0 tests executed (not behavioral coverage); `PROTOC=/home/cdmonio/.local/protoc/bin/protoc cargo check -p world-server` OK with existing warnings; `git diff --check` OK.

Remaining gaps: no full `UpdateVisibilityOf`, full `SetSeer`, ObjectAccessor/session fanout, dynamic tree/collision, scripts, aura runtime, exact grid traversal, or manual-test-ready farsight.
